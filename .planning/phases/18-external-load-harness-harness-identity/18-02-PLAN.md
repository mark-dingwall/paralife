---
phase: 18-external-load-harness-harness-identity
plan: 02
type: execute
wave: 2
depends_on: ["18-01", "18-03"]
files_modified:
  - src/main/java/com/paralife/websocket/WorldWebSocketHandler.java
  - src/test/java/com/paralife/websocket/WorldWebSocketHandlerHandshakeHeaderTest.java
  - src/test/java/com/paralife/websocket/HarnessLogMarkerTest.java
autonomous: true
requirements: [SCALE-04]
tags: [websocket, server, handshake-headers, log-markers]
must_haves:
  truths:
    - "Server reads X-Paralife-Source and X-Paralife-Harness from session.getHandshakeHeaders() at afterConnectionEstablished"
    - "Server stashes ATTR_SOURCE and ATTR_HARNESS on session attributes; ATTR_HARNESS only when source=harness"
    - "Source values not in SOURCE_TAXONOMY fold to 'unknown' (defensive — wire input is untrusted)"
    - "Server emits 'HARNESS connected tick=… session=… harness=… source=… active=…' on connection-establish"
    - "Server emits 'HARNESS disconnected tick=… session=… harness=… source=… reason=token|graceful' on close"
    - "ADMISSION rejected log markers (in AdmissionGate) gain 'source=<v>[ harness=<id>]' field"
    - "BACKPRESSURE log markers (in WorldWebSocketHandler.markStalled / wireCrossBeanCallbacks / afterConnectionClosed) gain 'source=<v>[ harness=<id>]' field"
    - "TICK-HEALTH log markers (TickHealthMonitor) gain 'source=server' field (server-global; no harness slot)"
  artifacts:
    - path: "src/main/java/com/paralife/websocket/WorldWebSocketHandler.java"
      provides: "Header read + ATTR stash + HARNESS log markers + log-marker extensions"
      contains: "X-Paralife-Source"
    - path: "src/test/java/com/paralife/websocket/WorldWebSocketHandlerHandshakeHeaderTest.java"
      provides: "ATTR_SOURCE/ATTR_HARNESS stashing test"
      contains: "ATTR_SOURCE"
    - path: "src/test/java/com/paralife/websocket/HarnessLogMarkerTest.java"
      provides: "HARNESS connected/disconnected log marker structure verified"
      contains: "HARNESS connected"
  key_links:
    - from: "afterConnectionEstablished"
      to: "session.getAttributes()"
      via: "stash ATTR_SOURCE/ATTR_HARNESS"
      pattern: "ATTR_SOURCE"
    - from: "afterConnectionEstablished"
      to: "log.info HARNESS connected"
      via: "structured log marker"
      pattern: "HARNESS connected tick="
---

<objective>
Server-side counterpart to Plan 01: read the WS handshake headers in `WorldWebSocketHandler.afterConnectionEstablished`, stash bounded-taxonomy `ATTR_SOURCE` and `ATTR_HARNESS` on the session, and emit `HARNESS connected` / `HARNESS disconnected` log markers (D-14). Extend the existing `ADMISSION`, `BACKPRESSURE`, and `TICK-HEALTH` log markers with `source=<v>[ harness=<id>]` fields (D-13).

Purpose: Identity ingress. Without this, Plan 03's tagged metrics have no per-session attribution to draw from.
Output: Every connected session is tagged in attributes; every operator-visible log marker carries the source/harness fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-external-load-harness-harness-identity/18-CONTEXT.md
@.planning/phases/18-external-load-harness-harness-identity/18-RESEARCH.md
@.planning/phases/17-durable-admission-control-backpressure/17-ADMISSION.md
@src/main/java/com/paralife/websocket/WorldWebSocketHandler.java
@src/main/java/com/paralife/admission/AdmissionGate.java

<interfaces>
<!-- Existing afterConnectionEstablished (line ~215 of WorldWebSocketHandler.java) -->

```java
@Override
public void afterConnectionEstablished(WebSocketSession session) {
    sessionRegistry.register(session);
    if (outboundSender != null) {
        outboundSender.attachSession(session, admissionConfig.backpressure().outboundQueueSize());
    }
    log.info("Client connected: {} (total: {})", session.getId(), sessionRegistry.getSessionCount());
}
```

<!-- afterConnectionClosed (line ~224) — must emit HARNESS disconnected with reason -->

<!-- Spring HttpHeaders is case-insensitive (RESEARCH.md A1; verified) -->
```java
HttpHeaders headers = session.getHandshakeHeaders();  // org.springframework.http.HttpHeaders
String source = headers.getFirst("X-Paralife-Source");      // null if absent
String harnessId = headers.getFirst("X-Paralife-Harness");  // null if absent
```

<!-- Existing AdmissionGate log marker (line 167) — to be extended -->
```java
log.info("ADMISSION rejected tick={} session={} reason={} active={}/{}", ...);
// Plan 02 extends to: ... reason={} source={}[ harness={}] active={}/{}
```

<!-- AttributionTagger is provided by Plan 03 (this plan depends on it) -->
```java
// src/main/java/com/paralife/admission/AttributionTagger.java (Plan 03)
public final class AttributionTagger {
    public static final String ATTR_SOURCE  = "source";
    public static final String ATTR_HARNESS = "harness";
    public static final Set<String> SOURCE_TAXONOMY = BotIdentity.SOURCE_TAXONOMY;
    public static String sourceOf(WebSocketSession s) { ... }     // returns "unknown" when absent
    public static String harnessOf(WebSocketSession s) { ... }    // returns null when absent
    public static String formatLogFields(WebSocketSession s) { ... }  // returns "source=harness harness=harness-A" or "source=operator"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server reads handshake headers + stashes session attributes + emits HARNESS log markers</name>
  <files>src/main/java/com/paralife/websocket/WorldWebSocketHandler.java, src/test/java/com/paralife/websocket/WorldWebSocketHandlerHandshakeHeaderTest.java, src/test/java/com/paralife/websocket/HarnessLogMarkerTest.java</files>
  <read_first>
    - src/main/java/com/paralife/websocket/WorldWebSocketHandler.java (full file — ATTR_* keys at lines 75-81; afterConnectionEstablished at 215; afterConnectionClosed at 224)
    - .planning/phases/18-external-load-harness-harness-identity/18-CONTEXT.md (D-06, D-08, D-13, D-14)
    - .planning/phases/18-external-load-harness-harness-identity/18-RESEARCH.md §"Architecture Patterns" Pattern 2 (handshake-header read code example)
    - .planning/phases/18-external-load-harness-harness-identity/18-RESEARCH.md §"Anti-Patterns" (raw Jetty getHeaders is case-sensitive — issue #12429; Spring's HttpHeaders is case-insensitive)
    - src/main/java/com/paralife/admission/AttributionTagger.java (read after Plan 03 lands; this is the helper for the log-formatting line)
    - src/main/java/com/paralife/bot/BotIdentity.java (read SOURCE_TAXONOMY constant — must not duplicate)
  </read_first>
  <behavior>
    - Test (`WorldWebSocketHandlerHandshakeHeaderTest`): when a WS upgrade carries `X-Paralife-Source: harness` and `X-Paralife-Harness: harness-A`, the resulting `WebSocketSession.getAttributes()` contains `ATTR_SOURCE = "harness"` and `ATTR_HARNESS = "harness-A"`
    - Test: when the upgrade carries `X-Paralife-Source: operator` (no harness header), `ATTR_SOURCE = "operator"` is set; `ATTR_HARNESS` is absent
    - Test: when the upgrade carries no identity headers (e.g., wscat probe), `ATTR_SOURCE = "unknown"`; `ATTR_HARNESS` is absent (D-08)
    - Test: when the upgrade carries `X-Paralife-Source: admin` (not in SOURCE_TAXONOMY), `ATTR_SOURCE` folds to `"unknown"` (defensive — wire input is untrusted, RESEARCH.md Pitfall 4)
    - Test: when the upgrade carries `X-paralife-source: harness` (mixed-case), the lookup still resolves (Spring's HttpHeaders is case-insensitive — RESEARCH.md A1)
    - Test (`HarnessLogMarkerTest`): on connect, server emits log line matching regex `HARNESS connected tick=\\d+ session=\\S+ harness=(harness-A|-) source=(harness|operator|unknown) active=\\d+`
    - Test: on close, server emits log line matching regex `HARNESS disconnected tick=\\d+ session=\\S+ harness=\\S+ source=\\S+ reason=(token|graceful|stalled-held)`
  </behavior>
  <action>
    Step 1 — Edit `src/main/java/com/paralife/websocket/WorldWebSocketHandler.java`:

    Add session-attribute key constants (next to ATTR_ENTITY_ID block, line ~75). Use exactly the names from `AttributionTagger.ATTR_SOURCE` / `ATTR_HARNESS` (Plan 03 owns the strings — import them, do not duplicate):

    ```java
    // Imports
    import com.paralife.admission.AttributionTagger;
    import org.springframework.http.HttpHeaders;
    import com.paralife.bot.BotIdentity;
    ```

    Replace the existing `afterConnectionEstablished` body with:

    ```java
    @Override
    public void afterConnectionEstablished(WebSocketSession session) {
        sessionRegistry.register(session);
        if (outboundSender != null) {
            outboundSender.attachSession(session, admissionConfig.backpressure().outboundQueueSize());
        }

        // Phase 18 D-06: read handshake identity. Spring's HttpHeaders is case-insensitive
        // (RESEARCH.md A1, sidesteps Jetty issue #12429).
        HttpHeaders headers = session.getHandshakeHeaders();
        String rawSource = headers.getFirst("X-Paralife-Source");
        String harnessId = headers.getFirst("X-Paralife-Harness");

        // Defensive: bounded-taxonomy check on the wire input (T-18-01).
        // Trim, then fold any value not in SOURCE_TAXONOMY to "unknown".
        String source = (rawSource == null || rawSource.isBlank())
                ? "unknown"
                : rawSource.trim();
        if (!BotIdentity.SOURCE_TAXONOMY.contains(source)) {
            source = "unknown";
        }
        session.getAttributes().put(AttributionTagger.ATTR_SOURCE, source);

        // Truncate to 32 chars defense-in-depth (matches BotIdentity.MAX_HARNESS_ID_LENGTH).
        // ATTR_HARNESS only stored when source=harness AND id present.
        if ("harness".equals(source) && harnessId != null && !harnessId.isBlank()) {
            String trimmed = harnessId.trim();
            if (trimmed.length() > BotIdentity.MAX_HARNESS_ID_LENGTH) {
                trimmed = trimmed.substring(0, BotIdentity.MAX_HARNESS_ID_LENGTH);
            }
            session.getAttributes().put(AttributionTagger.ATTR_HARNESS, trimmed);
        }

        long currentTick = tickEngine.currentTick();
        Object harnessAttr = session.getAttributes().get(AttributionTagger.ATTR_HARNESS);
        log.info("HARNESS connected tick={} session={} harness={} source={} active={}",
                currentTick, session.getId(),
                harnessAttr != null ? harnessAttr : "-", source,
                sessionRegistry.getSessionCount());
        log.info("Client connected: {} (total: {})", session.getId(), sessionRegistry.getSessionCount());
    }
    ```

    In `afterConnectionClosed` (line 224), AFTER computing `wasStalled` and BEFORE the `if (wasStalled)` branch, capture identity for log markers; emit a `HARNESS disconnected` line on EVERY exit path (stalled-held, normal-disconnect). Add immediately after `boolean wasStalled = isStalled(session.getAttributes());`:

    ```java
    // Phase 18 D-14: HARNESS disconnected marker. Emitted on every close path with the
    // reason field reflecting which branch we took.
    long closeTick = tickEngine.currentTick();
    String closeSource = (String) session.getAttributes().getOrDefault(
            AttributionTagger.ATTR_SOURCE, "unknown");
    Object closeHarness = session.getAttributes().get(AttributionTagger.ATTR_HARNESS);
    String closeReason = wasStalled ? "stalled-held" : "graceful";
    log.info("HARNESS disconnected tick={} session={} harness={} source={} reason={}",
            closeTick, session.getId(),
            closeHarness != null ? closeHarness : "-", closeSource, closeReason);
    ```

    In `markStalled` (line ~470), extend the closing 408 emission log line. The existing `BACKPRESSURE stalled` log line in the overflow callback (`wireCrossBeanCallbacks`, line ~198) gains the source/harness fields. Use `AttributionTagger.formatLogFields(session)` to produce the suffix:

    ```java
    // In wireCrossBeanCallbacks overflow callback (line ~198):
    log.info("BACKPRESSURE stalled tick={} session={} queue-depth={} limit={} {}",
            currentTick, sessionId, depth,
            admissionConfig.backpressure().outboundQueueSize(),
            AttributionTagger.formatLogFields(s));

    // In afterConnectionClosed wasStalled branch (line ~239):
    log.info("BACKPRESSURE held-on-close tick={} session={} status={} entity={} {}",
            tickEngine.currentTick(), sessionId, status,
            session.getAttributes().get(ATTR_ENTITY_ID),
            AttributionTagger.formatLogFields(session));

    // In handleRegister rebind branch (line ~338):
    log.info("BACKPRESSURE resumed tick={} session={} entity={} respawnCountRestored={} {}",
            currentTick, session.getId(), rebind.entityId(), snapshot,
            AttributionTagger.formatLogFields(session));
    ```

    Step 2 — Create `src/test/java/com/paralife/websocket/WorldWebSocketHandlerHandshakeHeaderTest.java`:
    - `@SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT)`. Use Jetty's WebSocketClient with a `ClientUpgradeRequest` carrying the test header. Connect; wait for `afterConnectionEstablished`; resolve the `WebSocketSession` from `SessionRegistry`; assert `session.getAttributes().get(AttributionTagger.ATTR_SOURCE)` matches expectation.
    - Five tests, one per behavior bullet. Use a polling helper (`Awaitility.await().atMost(2s)`) to wait for the session to register.

    Step 3 — Create `src/test/java/com/paralife/websocket/HarnessLogMarkerTest.java`:
    - Use Logback's `ListAppender` attached to `WorldWebSocketHandler`'s logger to capture log events (existing pattern in the project — search the test tree for prior examples; if none exists, attach via `(ch.qos.logback.classic.Logger) LoggerFactory.getLogger(WorldWebSocketHandler.class)`).
    - Connect a client with `X-Paralife-Source: harness` and `X-Paralife-Harness: harness-A`; assert one captured log line matches the regex from the behavior bullets above.
    - Disconnect cleanly; assert one captured log line matches the disconnected-graceful regex.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.paralife.websocket.WorldWebSocketHandlerHandshakeHeaderTest" --tests "com.paralife.websocket.HarnessLogMarkerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getHandshakeHeaders' src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` >= 1
    - `grep -c 'AttributionTagger.ATTR_SOURCE' src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` >= 2
    - `grep -c 'AttributionTagger.ATTR_HARNESS' src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` >= 2
    - `grep -c 'HARNESS connected tick=' src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` == 1
    - `grep -c 'HARNESS disconnected tick=' src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` == 1
    - `grep -c 'BotIdentity.SOURCE_TAXONOMY' src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` == 1
    - `grep -c 'AttributionTagger.formatLogFields' src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` >= 3 (overflow, held-on-close, rebind-resumed)
    - `./gradlew test --tests "com.paralife.websocket.WorldWebSocketHandlerHandshakeHeaderTest"` exits 0
    - `./gradlew test --tests "com.paralife.websocket.HarnessLogMarkerTest"` exits 0
    - `./gradlew test` exits 0 (no regression)
  </acceptance_criteria>
  <done>Handshake-header read + ATTR_SOURCE/ATTR_HARNESS stash + HARNESS log markers + BACKPRESSURE marker source/harness extensions all in place; tests green; existing 17-VERIFICATION must-haves still hold (`./gradlew test`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ADMISSION log marker with source/harness; verify TICK-HEALTH stays scalar</name>
  <files>src/main/java/com/paralife/admission/AdmissionGate.java, src/main/java/com/paralife/websocket/WorldWebSocketHandler.java, src/test/java/com/paralife/admission/AdmissionLogMarkerTest.java</files>
  <read_first>
    - src/main/java/com/paralife/admission/AdmissionGate.java (current ADMISSION rejected log line at line 167)
    - .planning/phases/18-external-load-harness-harness-identity/18-CONTEXT.md (D-12 — `paralife.admission.maintenance` and `paralife.tick.health.work-time-ms` STAY scalar; D-13 log marker extension)
    - src/main/java/com/paralife/admission/AttributionTagger.java (provided by Plan 03)
  </read_first>
  <behavior>
    - Test: when `AdmissionGate.evaluate()` rejects a request from a session with `ATTR_SOURCE=harness, ATTR_HARNESS=harness-A`, the captured log line matches `ADMISSION rejected tick=\\d+ session=\\S+ reason=\\S+ source=harness harness=harness-A active=\\d+/\\d+`
    - Test: when the rejection comes from a session with `ATTR_SOURCE=unknown` (no harness), the captured log matches `ADMISSION rejected tick=\\d+ session=\\S+ reason=\\S+ source=unknown active=\\d+/\\d+` (no `harness=` field)
    - Test: TICK-HEALTH log marker (in TickHealthMonitor) does NOT carry per-source fields (D-12 — server-global). If the existing log format is `TICK-HEALTH gate=open mean=...`, leave it untouched. Verify with grep no `source=` was added there.
    - Test: ADMISSION maintenance state-change log line stays scalar (D-12)
  </behavior>
  <action>
    Step 1 — Edit `src/main/java/com/paralife/admission/AdmissionGate.java`:

    `AdmissionGate.evaluate(req)` currently has no access to the WS session because `AdmissionRequest` is a session-id record. The cheapest, most correct fix: add an optional `WebSocketSession` parameter to a new overload, and have `WorldWebSocketHandler.handleRegister` pass it through. Do NOT widen `AdmissionRequest` — keep that record stable.

    - Add a new method:
      ```java
      public AdmissionResult evaluate(AdmissionRequest req, WebSocketSession session) {
          AdmissionResult result = evaluate(req);
          if (result instanceof AdmissionResult.Reject reject) {
              // Re-emit the log marker with source/harness fields appended.
              // The first emission inside reject() already incremented metrics — do not double-increment.
          }
          return result;
      }
      ```

      Cleaner: refactor the private `reject(req, code, token)` helper to accept an optional `WebSocketSession session` and use it when present. New signature:

      ```java
      private AdmissionResult.Reject reject(AdmissionRequest req, WebSocketSession session, int code, String token) {
          metrics.incRejected(token, session);   // Plan 03's extended signature
          String attribution = AttributionTagger.formatLogFields(session);  // "source=… [harness=…]"
          log.info("ADMISSION rejected tick={} session={} reason={} {} active={}/{}",
                  req.tickNumber(), req.sessionId(), token, attribution,
                  worldGrid.livingEntityCount(), admissionConfig.cap());
          return new AdmissionResult.Reject(code, token);
      }
      ```

      Update every call to `reject(req, code, token)` inside `evaluate` to `reject(req, session, code, token)`. Add a no-session overload of `evaluate` that defaults `session` to `null` for back-compat with any test that constructs the AdmissionRequest directly.

    - Update `WorldWebSocketHandler.handleRegister` (line ~315) to call `admissionGate.evaluate(req, session)` instead of `admissionGate.evaluate(req)`.

    Step 2 — Verify (do NOT edit) `src/main/java/com/paralife/admission/AdmissionMetrics.java`:

    Plan 03 (Wave 1) owns every edit to `AdmissionMetrics.java` — it shipped before this plan in Wave 2 and added the `incRejected(String reason, WebSocketSession session)` overload that Step 1 above calls into. This task does NOT modify AdmissionMetrics.java; it only verifies the existing scalar-metric ADMISSION-maintenance log line (in `WorldWebSocketHandler.wireCrossBeanCallbacks`, line ~208 `log.info("ADMISSION maintenance state=on")`) is untouched. Confirm via grep:

    - `grep -c 'ADMISSION maintenance state=' src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` >= 1 (the maintenance state-change log line still exists)
    - `grep -E 'ADMISSION maintenance.*source=' src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` returns nothing (the maintenance log line has not been extended with source/harness — D-12 scalar invariant)

    Do NOT add `AdmissionMetrics.java` to this task's files list — Plan 03 owns it. Plan 02 only consumes the surface that Plan 03 ships.

    Step 3 — Verify TICK-HEALTH stays scalar:

    Search `src/main/java/com/paralife/admission/TickHealthMonitor.java` for any `TICK-HEALTH` log line. If present, leave it untouched and document the choice in the test (see Step 4). If no such marker exists yet, this is a no-op — D-12 just requires the scalar gauge `paralife.tick.health.work-time-ms` to remain untagged, which Plan 03 enforces.

    Step 4 — Create `src/test/java/com/paralife/admission/AdmissionLogMarkerTest.java`:
    - Use Logback `ListAppender` on `AdmissionGate`'s logger.
    - Drive a rejection by exceeding the cap (or use a maintenance-flag setup); spin a Spring context with `paralife.admission.maintenance=true` and one connected harness session.
    - Connect a session with handshake `X-Paralife-Source: harness, X-Paralife-Harness: harness-A`; send `r|C`; capture the log line; assert regex match.
    - Repeat with `X-Paralife-Source: unknown` (no harness) and assert the line has `source=unknown` and contains NO `harness=`.
    - Add a test that reads `TickHealthMonitor.class` source via `getResource("/com/paralife/admission/TickHealthMonitor.class")` is sufficient — easier: just `grep -L 'source=' src/main/java/com/paralife/admission/TickHealthMonitor.java` is a build-time check, not a test. Replace with assertion: `assertThat(loggingMessages).noneMatch(line -> line.startsWith("TICK-HEALTH") && line.contains("source="))` after running a workload.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.paralife.admission.AdmissionLogMarkerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AttributionTagger.formatLogFields' src/main/java/com/paralife/admission/AdmissionGate.java` == 1
    - `grep -c 'evaluate(AdmissionRequest req, WebSocketSession session)' src/main/java/com/paralife/admission/AdmissionGate.java` == 1
    - `grep -c 'admissionGate.evaluate(req, session)' src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` == 1
    - `grep -E 'TICK-HEALTH.*source=' src/main/java/com/paralife/admission/TickHealthMonitor.java` returns nothing (D-12 scalar invariant)
    - `./gradlew test --tests "com.paralife.admission.AdmissionLogMarkerTest"` exits 0
    - `./gradlew test` exits 0 (no regression in 162-test suite)
  </acceptance_criteria>
  <done>ADMISSION log marker carries source/harness; TICK-HEALTH stays scalar (D-12); admission-maintenance log unchanged; AdmissionGate has clean session-overload of evaluate. AdmissionMetrics.java is owned by Plan 03 and is not touched here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS upgrade headers → server session attributes | Wire input from any client. Server cannot authenticate. |
| ATTR_HARNESS → log markers / metrics | Once stashed, treated as bounded-taxonomy + truncated input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering (header spoofing) | WorldWebSocketHandler.afterConnectionEstablished | mitigate | Source value folded to "unknown" if not in SOURCE_TAXONOMY (validated against `BotIdentity.SOURCE_TAXONOMY` constant). Harness id truncated to 32 chars. |
| T-18-02 | Denial-of-Service (cardinality) | ATTR_HARNESS → metric tags downstream | accept (mitigation in Plan 03) | The 32-char truncate here is defense-in-depth only; the real cap is the Micrometer MeterFilter registered in Plan 03. |
| T-18-04 | Information Disclosure (silent attribution loss after STALLED rebind) | Server side of rebind | mitigate | New session populates ATTR_SOURCE/ATTR_HARNESS from the reconnect's handshake headers BEFORE handleRegister fires (`afterConnectionEstablished` runs first per Spring contract). Locked by `AttributionRebindTest` in Plan 06. |
</threat_model>

<verification>
- `./gradlew test --tests "com.paralife.websocket.*HandshakeHeader*" --tests "com.paralife.websocket.HarnessLogMarker*" --tests "com.paralife.admission.AdmissionLogMarkerTest"` exits 0
- `./gradlew test` exits 0 (full suite green)
- `grep -E 'TICK-HEALTH.*source=' src/main/java/com/paralife/admission/TickHealthMonitor.java` returns no matches (D-12 scalar invariant)
</verification>

<success_criteria>
- Server reads handshake headers, applies bounded-taxonomy filter, truncates harness id to 32 chars
- ATTR_SOURCE / ATTR_HARNESS session attributes populated; defaults to "unknown" / absent
- HARNESS connected / disconnected log markers emitted on every connection lifecycle
- ADMISSION rejected log marker carries source/harness; BACKPRESSURE markers extended via AttributionTagger.formatLogFields
- TICK-HEALTH and ADMISSION-maintenance markers remain scalar (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/18-external-load-harness-harness-identity/18-02-SUMMARY.md`
</output>
