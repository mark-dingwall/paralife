---
phase: 19.1
triage_of: 19.1-REVIEWS.md
triaged_at: 2026-05-04
verdict: in-place edits, no rewrite
---

# Phase 19.1 Review Triage

## Bottom line

**Do NOT re-run `/gsd-plan-phase 19.1 --reviews`.** All 5 plans' architecture, sequencing, and goals are agreed-good across 3 reviewers. Every HIGH/MEDIUM finding is a **pinpoint correction** to specific pseudo-code, field references, or file paths inside the plans — not a structural defect. ~12 surgical edits across the 5 PLAN.md files, no PLAN.md exceeds 30 lines of churn.

A re-plan would discard validated post-mitigation amendments (D-15 promotion, calibrated tick-bound, public-surface DR-pin, Option A ToxinPathGenerator) and re-introduce the noise the user warned about.

## Verification pass

Verified the load-bearing factual claims in code before triage:

| Claim | Source | Verified |
|---|---|---|
| `PendingGrant::buffType` doesn't exist; record is `(entityId, initialTicks, capturedOccupant, position)` | codex | TRUE — `EnvCleanupHooksBean.java:69` |
| `PerceptionCodecTest.java` doesn't exist; only `PerceptionCodecRoundTripTest` + `PerceptionCodecErrorTest` | codex | TRUE — `src/test/java/com/paralife/codec/` |
| `TickEngine.tickOnce()` doesn't exist | claude/codex | TRUE — no match in `TickEngine.java` |
| `ActionResolver.queueAction` is sessionId-keyed; `drainActions()` private | codex | TRUE — `:351`, `:408` |
| `PerceptionBroadcaster` class doesn't exist (only Javadoc references) | opencode | TRUE — comments only |
| `ToxinPathGeneratorTest.java` uses both ctors and Option A would break it | claude | TRUE — `:41,53,61,75`. **Also `LightningTest.java:78` — collateral not flagged** |
| `OutboundSender` ctor change ripples to test fixtures | claude | **FALSE** — `OutboundSender(AdmissionMetrics)` already exists at `:97`; `OutboundSenderTest.java:36` already uses it. No ctor change needed. |

## Severity reclassification

Codex's HIGH-everything verdict over-weights factual pseudo-code errors. None of these are HIGH risk in the gating sense — they're trivial to fix once pinned. Use **landed-as-written risk** (will the as-written plan compile/pass?) vs **post-edit risk** (after the corrections below, will it work?).

| Item | Codex severity | Reclassified |
|---|---|---|
| `PerceptionCodecTest.java` missing | HIGH | LOW (file-path edit) |
| `PendingGrant::buffType` missing | HIGH | LOW (one comparator-arg edit; replace with `PendingGrant::initialTicks` then `PendingGrant::position` for total order) |
| `tickOnce()` missing | HIGH | MEDIUM (test driver substitution, real choice required) |
| Plan 05 Task 05b sessionId vs entityId | HIGH | LOW (insert one `botRegistry.getSessionByEntity(...)` lookup) |
| Plan 05 Task 05a invalid payloads | HIGH | LOW (swap to `"6"`/`"4"`/`"684"`) |
| Plan 05 Task 05c DrainObserver private + seam-free fallback | HIGH | MEDIUM (drop fallback; add `ActionResolver.java` to `files_modified`; name seam) |
| `sendOutOfBand` swallows IOException | HIGH | LOW (adjust catch to unchecked-only) |
| Capture sender thread BEFORE detach | HIGH | LOW (one assertion-order fix) |
| `OutboundSender` ctor ripple | MEDIUM | **REJECTED** — ctor already takes metrics |

## Edit list per plan

### Plan 01 (3 edits, ~10 lines)

1. **Task 01a target file** — replace `PerceptionCodecTest.java` with `PerceptionCodecErrorTest.java` (or add new test method to `PerceptionCodecRoundTripTest`).
2. **`Event.ALL_CODES` shape** — make it `String` not `char[]` to prevent external mutation. Iterate via `.toCharArray()` in test loops.
3. **F1 test mode** — note `updateBotRegistryForFormation` is private; specify event-driven test (publish formation event via Spring) not direct call.

### Plan 02 (4 edits, ~15 lines)

1. **D-04 tiebreaker** — `.thenComparing(PendingGrant::buffType)` → `.thenComparing(PendingGrant::initialTicks).thenComparing(g -> g.position().x()).thenComparing(g -> g.position().y())`. Total order via existing fields.
2. **`files_modified`** — add `src/test/java/com/paralife/engine/ToxinPathGeneratorTest.java` AND `src/test/java/com/paralife/engine/LightningTest.java` (use ToxinPathGenerator no-arg ctor at `:78`). Document Option A migration: drop ctor calls, switch to `ToxinPathGenerator.generatePath(..., seed)` static.
3. **D-06 `forceSpawnToxinForTest` seed** — pin literally to `private static final long TEST_TOXIN_SEED = 0xDEADBEEFL` per call (or tick-derived if test-fixture diversity matters — explicit either way).
4. **Test placement** — relocate composite-RNG determinism check from `EnvironmentDeterminismTest` (env-only contract) to a new `CompositeEnergyDistributorTest` or `CompositeRegistryDeterminismTest`. Drop the "3+ simultaneous toxin spawns" sub-test (single-active-toxin guard prevents it); replace with sequential `generatePath(seed)` direct calls.

### Plan 03 (5 edits, ~20 lines)

1. **`sendOutOfBand` catch** — note it swallows `IOException` internally; in `markStalled` catch only unchecked (or change `sendOutOfBand` to return boolean — pick one explicitly).
2. **Task 03c driver** — substitute `tickEngine.tickOnce()` with `applicationEventPublisher.publishEvent(new TickEvent(tickNumber))` from the test. Note `tickOnce()` does not exist.
3. **Capture sender thread BEFORE detach** — `Thread t = sender.senderThread(id); detachSession(...); assertThat(t.join(Duration.ofMillis(bound))).isTrue()`. Post-detach lookup is `null`.
4. **Add cross-thread latch** — `markStalled` is invoked from one thread, observation runs on test thread; add `CountDownLatch markStalledStarted` and `markStalledFinished` to make the race deterministic.
5. **Drop `OutboundSender` ctor concern** — strike the "constructor injection" wording where it implies a new parameter; `OutboundSender(AdmissionMetrics)` already exists. Just reference the existing field.

### Plan 04 (2 edits, ~10 lines)

1. **BL probe simplification** — replace `git worktree add ../paralife-probe` workflow with `@Disabled("baseline — confirms BL leak; flip after fix")` test in main worktree, flipped to `assertThat(...).isEmpty()` post-fix in same commit. Equivalent TDD discipline, no worktree CI surface.
2. **A4.3 caller enumeration** — add `<read_first>` step: `grep -rn "cleanupByEntityId" src/main src/test` to confirm no caller depends on env-state preservation on the `sessionId == null` early-return branch.

### Plan 05 (5 edits, ~20 lines)

1. **Task 05b entityId→sessionId** — `actionResolver.queueAction(P1.id(), ...)` becomes `actionResolver.queueAction(botRegistry.getSessionByEntity(P1.id()).orElseThrow(), ...)`.
2. **Task 05a payloads** — replace `"0"`/`"1"` placeholder with valid numpad chars (`"6"`/`"4"` for movement) and 3-char numpad ballot (`"684"`) for verb V. Add post-drive assertion: actions processed (not silently dropped) — e.g., assert `actionResolver` digest count > 0 or expose a processed-count seam.
3. **Task 05c DrainObserver** — (a) add `src/main/java/com/paralife/engine/ActionResolver.java` to `files_modified`; (b) drop the "prefer seam-free stress test" fallback; (c) require `CountDownLatch` deterministic interleave; (d) name the seam class `DrainObserverForD20Pin` so removal is a visible refactor.
4. **Task 05a baseline gate** — promote "first-run baseline requires manual operator commit" into plan-level `must_haves.truths`; add a small probe assertion ("scenario produced ≥N processed actions") that runs BEFORE digest write so a bug-baked digest can't be committed silently.
5. **ES hoist call count** — keep awk-scoped check (already pinned in A5.3); reviewer disagreement (codex 3, claude/opencode 2) resolves via the awk command at execution time. No edit needed; just flag the disagreement in `<read_first>`.

### Cross-plan (1 edit total)

- **`depends_on` chains** — claude argues over-serialised; opencode says fine. Since the 5-plan phase has minimal wall-time pressure and line-number drift is a real concern (plans pin `:888`, `:944` etc. against pre-edit HEAD), keep the chains as conservative drift-mitigation. **No edit.** Acceptable cost.

## What I'd push back on

1. **Codex MEDIUM "`OutboundSender` ctor change ripples"** — false alarm. Reject.
2. **Claude LOW "buildMinimalEventFrame may not exist; both tasks duplicate the helper"** — micro-concern; if it doesn't exist, the executor authors it once during Task 01a and Task 01d references it. Trivial. **Reject as actionable feedback** but flag in plan via "single helper, define in 01a".
3. **Opencode LOW "Event.ALL_CODES future-contributor insertion changes ordering"** — premature. Iteration-order discipline is the test's job; sort if it matters. **Reject** — over-engineering.
4. **Codex MEDIUM "tests must fail pre-fix is hard to guarantee with same-seed runs"** — partially valid but the determinism falsifier is "byte-equal across same-seed runs", which IS deterministic. Codex is right that the multi-composite fixture conflicts with the env-only test class — already covered by edit Plan 02.4. The "hard to guarantee" framing is overstated.
5. **Codex LOW "CLAUDE.md `@Order` set-equality command too broad"** — A5.6 already scopes to the tick-pipeline section per the amendment. Already addressed; reject.
6. **Claude LOW "test count baseline 166 → ≥180"** — trivial, fold into Plan 05's whole-suite gate when writing.

## Effort estimate

- Edits per plan: ~15-20 lines averaged
- Total: ~75-100 lines across 5 PLAN.md files
- One commit per plan, mirroring the existing A2.x/A3.x amendment pattern (e.g., commit msg `docs(19.1): apply pass-4 review triage to Plan NN (Bnn.1, Bnn.2, ...)`)
- One pass of `/gsd-review --phase 19.1 --all` after edits to confirm closure (NOT `/gsd-plan-phase --reviews`)

## Decision

**In-place amendment pattern, not rewrite.** The post-mitigation A1.x/A2.x/A3.x/A4.x/A5.x amendment scheme already works for this — extend it with B-prefixed amendments for this round. Each B-amendment cites the reviewer + finding.
