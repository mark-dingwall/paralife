---
task: code
mode: inline
reviewers_succeeded: ["claude", "gemini", "codex", "opencode"]
reviewers_failed: []
reviewed_at: 2026-05-05T05:35:56Z
files: ["src/main/java/com/paralife/admission/TickHealthMonitor.java", "src/main/java/com/paralife/codec/Event.java", "src/main/java/com/paralife/codec/PerceptionCodec.java", "src/main/java/com/paralife/engine/ActionResolver.java", "src/main/java/com/paralife/engine/CompositeRegistry.java", "src/main/java/com/paralife/engine/EntityIds.java", "src/main/java/com/paralife/engine/EnvPostActionReconciler.java", "src/main/java/com/paralife/engine/EnvironmentEngine.java", "src/main/java/com/paralife/engine/SimulationEngine.java", "src/main/java/com/paralife/websocket/WorldWebSocketHandler.java", "src/test/java/com/paralife/codec/PerceptionCodecRoundTripTest.java", "src/test/java/com/paralife/engine/EnvPostActionReconcilerTest.java", "src/test/java/com/paralife/engine/EnvironmentFullStackSmokeTest.java", "src/test/java/com/paralife/engine/EnvironmentPhaseGateIntegrationTest.java", "src/test/java/com/paralife/engine/GoldenTraceEquivalenceTest.java", "src/test/java/com/paralife/engine/MetabolismIntegrationTest.java", "src/test/resources/golden-trace-phase19.json"]
usage:
  claude: { input: 37, output: 397, cached: 1525389, tool_calls: 7, elapsed_s: 180.3 }
  gemini: { input: 630384, output: 1265, cached: 216526, tool_calls: 7, elapsed_s: 164.0 }
  codex: { input: 1953813, output: 8060, cached: 1796352, tool_calls: 26, elapsed_s: 197.1 }
  opencode: { input: 0, output: 0, cached: 0, tool_calls: 14, elapsed_s: 158.3 }
synthesizer: claude
synthesized_at: 2026-05-05T05:35:56Z
---

# Cross-AI Review

## Claude Review

**SHIP**

| # | Pass-1 finding | Status |
|---|---|---|
| T1 | PerceptionBroadcaster + `@Order(100)` doc/comment cluster | CONFIRMED |
| T2 | `processDeaths` panic-zone snapshot+sort before `simRng.nextDouble()` | CONFIRMED |
| T3 | `Event.MAG_CODES` + static-init invariant + driven `eventHasMagnitude` | CONFIRMED WITH CONCERNS |
| T4 | Golden-trace baseline 2-key surgical refresh | CONFIRMED |

**T1 — sweep complete**
- `grep -r PerceptionBroadcaster src` → zero. `grep -r '@Order(100)' src` → zero. `CLAUDE.md` clean. Residual hits live only in `.planning/` historical docs (out of scope for D-16).

**T2 — sort total-order**
- `SimulationEngine.processDeaths:1135-1156` snapshots `compositeRegistry.getAll()` into ArrayList then `sort(Comparator.comparing(CompositeState::getCompositeId))`. Composite ids minted as `"composite-" + compositeIdCounter.incrementAndGet()` — monotonic AtomicLong → unique → total order under string comparator (lexicographic ordering deterministic, doesn't need numeric).
- All `simRng.nextDouble()` consumers in this method now bounded by deterministic iteration:
  - Phase 3b `handleMemberDeath` rolls 97/3 dissolve via `simRng` — iteration order is `entitySnapshot()` (row-major), independent of CHM. Already deterministic pre-fix.
  - Phase 3c `checkPanicZone` rolls progressive-shatter `simRng.nextDouble()` — now ordered by sorted `sortedComposites`. Fixed.

**T3 — invariant gate**
- `Event.java:46-56` static block uses `throw new AssertionError(...)` (not `assert`) — fires on every JVM regardless of `-ea`. Stronger than spec.
- Check is `MAG_CODES ⊆ ALL_CODES` direction. Catches: dropping a code from ALL_CODES, adding a code to MAG_CODES that's not in ALL_CODES.
- `PerceptionCodec.eventHasMagnitude` reads `Event.MAG_CODES.indexOf(code) >= 0` (single source).
- `PerceptionCodecRoundTripTest.everyEventCodeRoundTrips` `@MethodSource` driven from `Event.ALL_CODES`; `buildMinimalEventFrame` magnitude-branch driven from `Event.MAG_CODES` — both branches exercised every run.
- **Concern (LOW residual):** asymmetric — if a future contributor adds a code to `ALL_CODES` that semantically requires magnitude but forgets to add it to `MAG_CODES`, neither static-init nor round-trip notices (helper agrees with codec because both consult `MAG_CODES`). Documented as contributor responsibility in `Event.java` Javadoc — acceptable trade-off, but worth a line in 19.1 SUMMARY.

**T4 — baseline diff**
- `git show f6da129 -- golden-trace-phase19.json` confirms exactly 2 keys changed: `trace-sess-9` and `trace-sess-21`. Other 24 untouched. Surgical as planned.

**Regression surface**
- LOW: `processDeaths` panic-zone now allocates `new ArrayList<>(compositeRegistry.getAll())` + sort each tick. Cheap (typical N≤dozens; sort O(N log N) negligible) but unconditional even when no composites exist. Smallest fix: `if (sortedComposites.isEmpty()) return;` short-circuit before the second loop. Not blocking — sub-microsecond at typical N.

**Other findings (new)**
- INFO `EnvironmentEngine.tickBuffsAndInfections:732-756` defensive-copies `infections.entrySet()` into ArrayList before iteration. CHM iteration order non-deterministic, but inline comment correctly notes per-entity DoT is commutative. Verified — no determinism risk. Not actionable.
- INFO `Event.java` MAG_CODES `"EAHTMRL"` matches SCHEMA §8.4. ALL_CODES `"EAHTMRLNSDB"` correct. Confirmed.
- INFO `markStalled` D-07 close-aware detach + best-effort OOB 408 — `sendOutOfBand` carries `isOpen()` guard; close-then-OOB ordering correct. CLAUDE.md paragraph accurately reflects code.

Pass-1 DO-NOT-SHIP closed; T1-T4 all land cleanly. Ready to ship pending optional T2 short-circuit micro-opt and T3 residual-gap note in SUMMARY.

---

## Gemini Review

DO NOT SHIP

| # | Finding (pass-1) | Status |
|---|---|---|
| T1 | `PerceptionBroadcaster` doc/comment refs and stale ` @Order(100)` references | CONFIRMED WITH CONCERNS |
| T2 | `SimulationEngine.processDeaths` panic-zone total order | CONFIRMED |
| T3 | `Event.eventHasMagnitude(byte)` hard-coded list silent foot-gun | CONFIRMED |
| T4 | Golden trace baseline updates | CONFIRMED |

## Other findings

- `src/main/java/com/paralife/admission/TickHealthMonitor.java:14` (and 10+ other files): Botched Javadoc Find-and-Replace (DO-NOT-SHIP)
  - **Finding:** The pass-1 sweep for T1 mistakenly performed a global search-and-replace that corrupted `{@code ...}` and `{ @code ...}` Javadoc tags across at least 11 source files, replacing the word `code` with the file path `src/test/java/com/paralife/codec/PerceptionCodecRoundTripTest.java`. This affects `TickHealthMonitor`, `PerceptionCodec`, `ActionResolver`, `EnvironmentEngine`, `SimulationEngine`, `WorldWebSocketHandler`, `Event`, and others. 
  - **Smallest fix:** Execute a workspace-wide find-and-replace to restore the `code` keyword by matching `src/test/java/com/paralife/codec/PerceptionCodecRoundTripTest.java` inside Javadoc brackets (e.g., replace `{@src/test/java/com/paralife/codec/PerceptionCodecRoundTripTest.java ` and `{ @src/test/java/com/paralife/codec/PerceptionCodecRoundTripTest.java ` with `{@code ` and `{ @code ` respectively).

- `src/main/java/com/paralife/engine/SimulationEngine.java` (in `processDeaths` Phase 3c): T2 Regression Surface
  - **Finding:** The `processDeaths` total-order sort introduces a `new ArrayList<>(compositeRegistry.getAll())` allocation and `.sort()` every tick on the main simulation thread. While functionally correct and achieving the required determinism, this adds a per-tick `O(N log N)` sort cost + `O(N)` allocation where N is the number of active composites. 
  - **Smallest fix:** Acceptable for a small registry and does not warrant an immediate revert, but this should be explicitly tracked for the Phase 21 performance rework if the composite registry size scales up. No immediate code change required.

- `src/main/java/com/paralife/codec/Event.java:34`: T3 Implementation Deviation
  - **Finding:** `Event.MAG_CODES` was implemented as a `String` rather than the explicitly requested `byte[]` constant from the pass-1 review.
  - **Smallest fix:** None required. The `String` implementation combined with `.indexOf(code) >= 0` in `PerceptionCodec.eventHasMagnitude` is functionally identical, idiomatic in Java, and fully protected by the static-init `AssertionError` block crosscheck.

---

## Codex Review

SHIP WITH FOLLOW-UPS

| Task | Status | Notes |
|---|---|---|
| T1 | CONFIRMED | `CLAUDE.md` + `src` grep is clean for `PerceptionBroadcaster` and TickBroadcaster `@Order(100)` refs. |
| T2 | CONFIRMED | Panic-zone path snapshots and sorts `compositeRegistry.getAll()` before `checkPanicZone`; `compositeId` is unique through `CompositeRegistry.register(...)` map keying. |
| T3 | CONFIRMED WITH CONCERNS | `Event.MAG_CODES` is used by `PerceptionCodec.eventHasMagnitude`, and the static initializer throws `AssertionError` at class load if `MAG_CODES` contains a char outside `ALL_CODES`. Round-trip test now exercises the magnitude branch via `Event.MAG_CODES`. |
| T4 | CONFIRMED | Diff changes only `trace-sess-9` and `trace-sess-21`; 26 total digest keys remain. |

**Other Findings**

- [Event.java:58](/home/mark/kramtime/paralife/src/main/java/com/paralife/codec/Event.java:58) allows shape-invalid events, e.g. MAG code without magnitude or no-MAG code with magnitude, so `PerceptionCodec.encode(new Event(...))` can emit frames its own decoder rejects. Smallest fix: enforce `magnitude.isPresent() == (MAG_CODES.indexOf(code) >= 0)` in the compact constructor.

- [SimulationEngine.java:343](/home/mark/kramtime/paralife/src/main/java/com/paralife/engine/SimulationEngine.java:343) still says TickBroadcaster has default order `Integer.MAX_VALUE`; actual `TickBroadcaster` is `@Order(50)`. Smallest fix: update the comment.

- [EnvPostActionReconciler.java:25](/home/mark/kramtime/paralife/src/main/java/com/paralife/engine/EnvPostActionReconciler.java:25) and [EnvPostActionReconcilerTest.java:27](/home/mark/kramtime/paralife/src/test/java/com/paralife/engine/EnvPostActionReconcilerTest.java:27) still describe a no-arg `drainPostActionGrants` path, but code/tests use `drainPostActionGrants(long)`. Smallest fix: delete/update those stale sentences.

Verification note: I could not run focused Gradle tests in this sandbox. `./gradlew` failed trying to write a lock under read-only `/home/mark/.gradle`, and invoking the installed Gradle directly failed native-service initialization.

---

## Opencode Review

Now let me check the specific reviewer concerns more precisely:**SHIP**

| # | Task | Verdict | Evidence |
|---|------|---------|----------|
| T1 | `PerceptionBroadcaster` / `@Order(100)` sweep | **CONFIRMED** | Zero `PerceptionBroadcaster` in `src/main/**/*.java` or `src/test/**/*.java`. Zero `@Order(100)` in `src/main/java/**/*.java`. TickHealthMonitor Javadoc:16 correctly says `@Order(50)`. |
| T2 | Panic-zone sort total-order | **CONFIRMED** | `compositeId` from `AtomicLong.incrementAndGet()` at `SimulationEngine.java:788` — monotonically unique, no ties. Sort at `:1135-1136` before the `checkPanicZone` loop at `:1144-1147`, which calls `simRng.nextDouble()` at `:1406`. All `simRng` consumption inside `checkPanicZone` is behind the sort. |
| T3 | MAG_CODES invariant gate | **CONFIRMED** | Static block at `Event.java:46-56` throws `AssertionError` on `MAG_CODES ⊄ CODE_SET`. `PerceptionCodec.eventHasMagnitude:690-691` consults `Event.MAG_CODES`. `PerceptionCodecRoundTripTest.buildMinimalEventFrame` uses `Event.MAG_CODES.indexOf(code) >= 0` to route magnitude vs no-magnitude frames for all 10 codes. |
| T4 | Baseline golden trace | **CONFIRMED** | Diff shows exactly 2 keys changed (`trace-sess-9`, `trace-sess-21`); 24 untouched. No digest drift. |

No other findings. The `new ArrayList<>(compositeRegistry.getAll())` + sort cost is O(composites), typically single-digit — negligible on the tick hot path.

---

## Consensus Summary

### Agreed Strengths
- **T1 sweep clean** (all 4): zero `PerceptionBroadcaster` and zero stale `@Order(100)` references in `src/`; CLAUDE.md correct.
- **T2 panic-zone determinism** (all 4): `processDeaths` snapshots+sorts `compositeRegistry.getAll()` by unique `compositeId` (monotonic `AtomicLong`) before `simRng.nextDouble()` consumers — total order achieved.
- **T3 MAG_CODES invariant gate** (all 4): static-init `AssertionError` (not `assert`) fires on every JVM regardless of `-ea`; `PerceptionCodec.eventHasMagnitude` and `PerceptionCodecRoundTripTest.buildMinimalEventFrame` both driven from `Event.MAG_CODES` — single source of truth.
- **T4 surgical baseline diff** (all 4): exactly 2 keys changed (`trace-sess-9`, `trace-sess-21`); 24 untouched. No digest drift.

### Agreed Concerns
- **T3 residual asymmetric gap** (claude + codex, LOW): nothing prevents a contributor adding a code to `ALL_CODES` that semantically requires magnitude but forgetting to add it to `MAG_CODES` — helper agrees with codec because both consult `MAG_CODES`. claude proposes documenting in 19.1 SUMMARY; codex proposes harder fix — enforce `magnitude.isPresent() == (MAG_CODES.indexOf(code) >= 0)` in `Event` compact constructor at `Event.java:58`.
- **T2 per-tick sort allocation** (claude + gemini, LOW/INFO): `new ArrayList<>(compositeRegistry.getAll())` + `O(N log N)` sort unconditional. Both agree negligible at typical N. claude suggests `if (sortedComposites.isEmpty()) return;` short-circuit; gemini parks for Phase 21 perf rework. Not blocking.

### Divergent Views
- **Overall verdict split**: claude SHIP, opencode SHIP, codex SHIP WITH FOLLOW-UPS, **gemini DO NOT SHIP**. Sole blocker is gemini's Javadoc-corruption finding below — needs verification before accepting verdict split.
- **gemini DO-NOT-SHIP — Javadoc corruption** (single reviewer): claims a botched global find-and-replace replaced `code` with file path `src/test/java/com/paralife/codec/PerceptionCodecRoundTripTest.java` inside `{@code …}` tags across ≥11 files (`TickHealthMonitor`, `PerceptionCodec`, `ActionResolver`, `EnvironmentEngine`, `SimulationEngine`, `WorldWebSocketHandler`, `Event`, others). The other 3 reviewers cite intact `{@code …}` Javadoc — opencode quotes `TickHealthMonitor` Javadoc cleanly. Plausible gemini hallucinated from the test-file path appearing in its tool harness, but **must be confirmed by direct grep before shipping**: `grep -rn "src/test/java/com/paralife/codec" src/main` should return zero hits.
- **codex stale-doc nits** (single reviewer): `SimulationEngine.java:343` comment claims TickBroadcaster default order `Integer.MAX_VALUE` (actual `@Order(50)`); `EnvPostActionReconciler.java:25` + test:27 describe no-arg `drainPostActionGrants` (signature is `drainPostActionGrants(long)`). Other reviewers did not flag — small but worth a one-line fix while addressing T3 doc note.
- **T3 implementation form** (gemini noted, others silent): `MAG_CODES` shipped as `String` not `byte[]`. gemini explicitly accepts — functionally identical, idiomatic, fully gated by static-init. No action.
