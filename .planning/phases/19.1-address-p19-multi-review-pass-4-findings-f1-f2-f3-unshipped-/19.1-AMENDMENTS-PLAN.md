---
phase: 19.1
parent: 19.1-REVIEWS-TRIAGE.md
type: amendments-plan
amendment_prefix: B
sequence: per-plan, sequential commits
verification: /gsd-review --phase 19.1 --all (post-edit, NOT /gsd-plan-phase --reviews)
---

# Phase 19.1 — B-Amendments Plan

Triage decided: in-place edits, no rewrite. This plan sequences the B-amendment commits, one per PLAN.md, mirroring the existing A1.x/A2.x/A3.x/A4.x/A5.x scheme.

## Conventions

- **Amendment IDs:** `B<plan-number>.<n>` (e.g., `B2.1`, `B3.4`).
- **Plan edit pattern:** append a new `### Amendment Bn.x — <one-line>` block under the existing "## Amendments" section of each PLAN.md, citing `(reviewer: claude/codex/opencode, finding: <quoted phrase>)`. Update affected pseudo-code/YAML inline; do NOT delete prior A-amendments.
- **Commit pattern:** `docs(19.1): apply pass-4 review triage to Plan NN (BN.1, BN.2, ...)`.
- **One commit per plan**, in order: Plan 01 → 02 → 03 → 04 → 05. Sequential because amendments touch different files but reviewers should see one plan's amendments stabilise before the next.

## Pre-flight (run once before any edit)

1. Confirm working tree clean — current branch `main`, last commit `86b64ca`.
2. Skim `19.1-REVIEWS-TRIAGE.md` once for context.
3. No code changes in this pass — only `.planning/phases/19.1-.../19.1-NN-PLAN.md` files.

## Plan 01 — B-amendments (3 edits)

| ID | Source | Edit |
|---|---|---|
| B1.1 | codex HIGH | Task 01a target file: change `PerceptionCodecTest.java` → `PerceptionCodecErrorTest.java`. Add new `@ParameterizedTest` method to that file. F2 round-trip gate (Task 01d) goes into `PerceptionCodecRoundTripTest.java`. |
| B1.2 | codex MEDIUM | `Event.ALL_CODES` shape: change from `public static final char[]` to `public static final String`. Iterate via `.toCharArray()` in tests. Compact-ctor logic uses `ALL_CODES.indexOf(code) >= 0` or `ALL_CODES.chars()`. |
| B1.3 | codex MEDIUM | Task 01b F1 test mode: note `updateBotRegistryForFormation` is private at `SimulationEngine.java:881`. Specify event-driven test (publish a composite-formation `TickEvent` via `ApplicationEventPublisher`, observe registry state) — NOT direct private-method invocation. |

Plan 01 NON-edits (rejected/already addressed): `buildMinimalEventFrame` micro-concern (define once in 01a, reference from 01d — already implicit); ALL_CODES future-ordering (premature).

## Plan 02 — B-amendments (4 edits)

| ID | Source | Edit |
|---|---|---|
| B2.1 | codex HIGH | D-04 tiebreaker: replace `.thenComparing(PendingGrant::buffType)` with `.thenComparing(PendingGrant::initialTicks).thenComparing(g -> g.position().x()).thenComparing(g -> g.position().y())`. Total order via verified record fields at `EnvCleanupHooksBean.java:69`. |
| B2.2 | claude MEDIUM | `files_modified` frontmatter: add `src/test/java/com/paralife/engine/ToxinPathGeneratorTest.java` AND `src/test/java/com/paralife/engine/LightningTest.java` (uses no-arg `new ToxinPathGenerator()` at `:78` — collateral). Document Option A migration: drop ctor calls site-by-site, switch to static `ToxinPathGenerator.generatePath(..., seed)`. |
| B2.3 | claude/opencode MEDIUM | D-06 `forceSpawnToxinForTest` seed: pin to `private static final long TEST_TOXIN_SEED = 0xDEADBEEFL` per call. State explicitly: not `0L` (avoids collapsed path diversity). |
| B2.4 | codex HIGH | Test placement: relocate composite-RNG determinism check OUT of `EnvironmentDeterminismTest` (env-only contract) into a new `CompositeEnergyDistributorDeterminismTest` (or extend `CompositeEnergyDistributorTest`). Drop the "3+ simultaneous toxin spawns" sub-test (single-active-toxin guard prevents it); replace with sequential `generatePath(seed)` direct calls with reset between. |

## Plan 03 — B-amendments (5 edits)

| ID | Source | Edit |
|---|---|---|
| B3.1 | codex HIGH | `sendOutOfBand` swallows `IOException` internally — note this in plan; in `markStalled`, catch only unchecked (`IllegalStateException`) at the OOB call site. Alternative considered: change `sendOutOfBand` to return `boolean` and rethrow IOException; reject because current callers depend on the swallow contract. |
| B3.2 | claude/codex/opencode HIGH | Task 03c test driver: `tickEngine.tickOnce()` does NOT exist (verified — no match in `TickEngine.java`). Substitute `applicationEventPublisher.publishEvent(new TickEvent(tickNumber))` and inject `ApplicationEventPublisher` into the test. Update Task 03c pseudo-code accordingly. |
| B3.3 | codex HIGH | Capture sender thread BEFORE detach: `Thread t = sender.senderThread(id); detachSession(...); assertThat(t.join(Duration.ofMillis(bound))).isTrue()`. Post-detach `senderThread(id)` returns `null` because the map entry is removed first. |
| B3.4 | claude MEDIUM | Cross-thread coordination in fake-session test: add `CountDownLatch markStalledStarted = new CountDownLatch(1)` and `markStalledFinished = new CountDownLatch(1)`. Fake `sendMessage` blocks on `markStalledStarted.await()` until close releases it. Test thread asserts via `markStalledFinished.await(bound, MILLIS)`. |
| B3.5 | claude→push-back | Strike Task 03a wording that implies a NEW `OutboundSender` ctor parameter for metrics. Verified: `OutboundSender(AdmissionMetrics)` already exists at `:97` and tests use it (`OutboundSenderTest:36`). Just reference the existing field; no DI change. |

## Plan 04 — B-amendments (2 edits)

| ID | Source | Edit |
|---|---|---|
| B4.1 | claude/codex MEDIUM | Replace BL probe `git worktree` workflow with `@Disabled("baseline — confirms BL leak; flip to assertEmpty after fix")` test in main worktree. Same commit flips the annotation off and rewrites assertion to `assertThat(...).isEmpty()`. Equivalent TDD discipline; removes worktree CI surface. |
| B4.2 | claude MEDIUM | A4.3 caller enumeration: add `<read_first>` step in Task 04b: `grep -rn "cleanupByEntityId" src/main src/test` to confirm no caller depends on env-state preservation on the `sessionId == null` early-return branch (notably reconnect-after-mark-dead within grace window). If any does, escalate. |

Plan 04 NON-edit: counter-invariance claim in idempotency Test 3 (opencode) — fold into Plan 04's existing test sketch as "enumerate which `admissionMetrics` counters are expected to stay invariant; assert those specifically" but treat as `<inline>` clarification within B4.1 commit (no separate amendment ID).

## Plan 05 — B-amendments (5 edits)

| ID | Source | Edit |
|---|---|---|
| B5.1 | claude/codex HIGH | Task 05b: `actionResolver.queueAction(P1.id(), ...)` → `actionResolver.queueAction(botRegistry.getSessionByEntity(P1.id()).orElseThrow(), ...)`. `queueAction` is sessionId-keyed (verified at `ActionResolver.java:351`). |
| B5.2 | codex/opencode HIGH | Task 05a payloads: replace `"0"`/`"1"` placeholder with valid numpad chars (e.g., `"6"` east, `"4"` west) and 3-char numpad ballot `"684"` for verb V. Add a post-drive assertion that proves actions were processed (not silently dropped) — e.g., expose `ActionResolver.processedCountForTick(t)` as a package-private accessor or assert a digest invariant that depends on processing. |
| B5.3 | codex/opencode HIGH | Task 05c DrainObserver: (a) add `src/main/java/com/paralife/engine/ActionResolver.java` to `files_modified`; (b) DROP the "prefer seam-free stress test" fallback wording — replace with hard requirement of deterministic `CountDownLatch` interleave; (c) name the seam class `DrainObserverForD20Pin` (file `DrainObserverForD20Pin.java`) so removal is a visible refactor, not a quiet deletion. |
| B5.4 | claude/opencode MEDIUM | Task 05a baseline gate: promote "first-run baseline requires manual operator commit" into plan-level `must_haves.truths`. Add probe assertion ("scenario produced ≥N processed actions, ≥M deaths, ≥K nutrient consumes") that runs BEFORE digest write so a bug-baked digest cannot be committed silently. |
| B5.5 | claude LOW | Whole-suite gate count: change `>= 166` to `>= 180` after all plans land (Plans 01–05 collectively add ~15-20 new tests). Single-line change in Plan 05 frontmatter or per-plan acceptance. |

Plan 05 NON-edit: ES hoist call count disagreement (codex 3, claude/opencode 2) — already resolved by A5.3's awk-scoped check; reviewer disagreement surfaces at execution time, no edit needed. Add a one-line note in Task 05f's `<read_first>` flagging the disagreement so executor doesn't ignore the awk command.

## Wave ordering

Edits sequential, one commit per plan:

```
01 → 02 → 03 → 04 → 05  (commits B1..B5)
```

Justification: amendments don't depend on each other technically (different PLAN.md files), but sequential commits give a clean per-plan diff for the next review pass.

Estimated wall time: 30–45 min for all 5 commits.

## Verification

After all 5 commits:

1. `git log --oneline | head -10` — confirm 5 new `docs(19.1): apply pass-4 review triage...` commits.
2. Re-run `/gsd-review --phase 19.1 --all` — confirm previous HIGH/MEDIUM concerns now closed.
3. Do **NOT** run `/gsd-plan-phase 19.1 --reviews` — this is in-place amendment, not re-plan.
4. If new HIGH findings emerge in the post-amendment review, repeat triage cycle (write `19.1-REVIEWS-pass5-TRIAGE.md`).

## Out of scope

- Code changes — this pass edits PLAN.md files only.
- Dependency-chain restructuring (`depends_on`) — claude flagged over-serialisation but opencode disagreed; treat as conservative drift-mitigation, leave as-is.
- Rewriting any plan's "## Amendments" history — only append new B-amendments.

## Rollback

If a B-amendment introduces inconsistency, revert that single commit. Each plan is isolated in its own commit.

## C-amendments (post pass-2)

Applied 2026-05-04 from pass-2 cross-AI review (`19.1-REVIEWS.md`). Triage decision: ~14 actionable findings against Plans 01–05 are **C-amendments**, not a re-plan — plan structure, dependency ordering, and task breakdown are sound. Findings are factually-anchored (wrong call-counts, ghost API references, missing CLAUDE.md rows, underspecified test seams). Mirror B-amendment cadence: one commit per plan.

### Plan 01 (commit `dbb68c2`)

| ID | Source | Edit |
|---|---|---|
| C1.1 | claude C8 (HIGH) | Spec `buildMinimalEventFrame(char code)` concretely in Task 01d (helper copies one row from `vectors()` MethodSource at `PerceptionCodecRoundTripTest.java:32`, substitutes the event-code char). Acceptance: `grep buildMinimalEventFrame ≥ 2 hits`. |
| C1.2 | claude (LOW reverse-drift gate) | Add reverse-drift gate to Task 01d acceptance: iterate 0..127, count chars for which `validateEventCode` does NOT throw, assert equality with `Event.ALL_CODES.length()`. Catches "codec accepts char not in ALL_CODES" inverse drift. |
| C1.3 | gemini (LOW F1 pollution) | Add `<read_first>` directive on Task 01b (F1 event-driven test): verify `SimpleApplicationEventMulticaster` is synchronous (no `TaskExecutor`) so `publishEvent(new TickEvent(...))` is observed within timing window. |

### Plan 02 (commit `48a44a7`)

| ID | Source | Edit |
|---|---|---|
| C2.1 | claude C11 (MEDIUM) | Pin `ToxinPathGenerator` Option A end-state to fully-static (no ctor): `generatePath` becomes `public static`; both ctors deleted; callers (`EnvironmentEngine.java:405,1615`, `LightningTest.java:78`, `ToxinPathGeneratorTest.java:41,42,53,61,75`) switch to static-call form; the autowired `toxinPathGenerator` field on `EnvironmentEngine` is removed. |
| C2.2 | claude C6 (MEDIUM) | Add comparator-totality justification on B2.1: `(entityId, initialTicks, position.x, position.y)` is total iff no two `PendingGrant` instances share all four fields, justified via `PendingGrant` ctor invariant (one grant per `(entity, buff-source-event, tick)`; distinct buff sources occupy distinct cells or distinct durations). Fallback: append `.thenComparing(System::identityHashCode)` if invariant later weakened. |
| C2.3 | opencode (LOW falsifier wording) | Replace stale falsifier "Two same-seed runs produce different toxin-path placements" with "Same-seed `ToxinPathGenerator.generatePath(w, h, 0xDEADBEEFL)` produces byte-identical `List<Position>` across two calls in same JVM." Aligned with C2.1 static-stateless contract. |

### Plan 03 (commit `a39cd4b`)

| ID | Source | Edit |
|---|---|---|
| C3.1 | claude C7 (MEDIUM) | Pin Task 03c fake-session injection seam concretely. Read `OutboundSender.java` to identify setter/registration entry point or ADD package-private seam in this commit. Rule out `BlockingWebSocketClient` — verified at `src/test/java/com/paralife/websocket/BlockingWebSocketClient.java`, it stalls INBOUND receive thread via latch, NOT outbound `sendMessage`. Add fake-session shape pseudo-code (sendMessage awaits `closeLatch` then throws `IOException`). Tag `human_needed: confirm injection seam shape before execution`. |
| C3.2 | gemini (LOW event multicaster) | Same as C1.3: confirm `SimpleApplicationEventMulticaster` is synchronous in test config so `publishEvent(new TickEvent(...))` is observed in timing window. |
| C3.3 | claude (LOW calibration floor) | Document calibration-floor rationale on B3.4: when `baselineMaxMs == 0`, bound = 20ms; multiplier irrelevant. 20ms adequate for order-of-magnitude deadlock detection (stuck-VT scenario blocks for seconds). Out-of-scope: sub-20ms perf drift. |

### Plan 04 (commit `a55849f`)

| ID | Source | Edit |
|---|---|---|
| C4.1 | claude C5 (MEDIUM) | Promote B4.2 caller audit from advisory to **HARD STOP** in Task 04b `<read_first>`. If any caller of `cleanupByEntityId` relies on env-state preservation across the `sessionId == null` branch (notably `markStalled` grace-window reconnect), do NOT proceed with A4.3 placement change — open a 19.2 follow-up entry and defer. The audit is a gate, not a checkpoint. |
| C4.2 | opencode (LOW visibility doc) | Correct `EnvironmentEngine.fleeing` visibility doc: verified `private`, not package-private as PATTERNS.md / plan-text claimed. Helper accesses field from inside same class so correctness unchanged — only the plan-text wording was wrong. |

### Plan 05 (commit `5861bef`)

| ID | Source | Edit |
|---|---|---|
| C5.1 | claude C1 (HIGH) | Correct entitySnapshot count in `processInteractions` from 2 to 3 (codex pass-4 was right). Verified at relative offsets +5, +130, +213 (method body closes at +440); absolute lines may drift. Updated `<interfaces>`, Task 05f `<read_first>`, action body, acceptance criteria. File-total grep: was 8, becomes 6 post-hoist. |
| C5.2 | claude C2 (HIGH) | Re-author Task 05d (CLAUDE.md tick pipeline). Verified end-state chain: 9 entries from `@Order(1) ResumeTokenRegistry` to `@Order(MAX_VALUE) TickHealthMonitor` (CLAUDE.md was wrong on TickBroadcaster's `@Order`, missing CompositeEnergyDistributor row, fictional PerceptionBroadcaster row). Stragglers at lines 54, 63, 72, 187 reconciled. Out-of-scope: stale `@Order` references in source comments at `ActionResolver.java:394`, `EnvPostActionReconciler.java:14`, `TickHealthMonitor.java:16` — added to plan frontmatter `out_of_scope` list. |
| C5.3 | claude C3 (HIGH) | Replace all 8 `tickEngine.tickOnce()` pseudo-code occurrences with `applicationEventPublisher.publishEvent(new TickEvent(t))` + `awaitAllSessionQueuesDrained()` per `GoldenTraceEquivalenceTest:309-312` canonical pattern. `TickEvent.java:9-13` convenience overload `(long tickNumber)` cited. Add `@Autowired ApplicationEventPublisher` to test class fields. Task 05a `<read_first>` adds C5.3 directive. |
| C5.4 | claude C4 (HIGH) | Spec `DrainObserverForD20Pin` seam concretely on Task 05c. Package-private interface + null-safe setter + null-guard in `drainPendingActions`. `AtomicReference<Map<String, ActionFrame>>` capture pattern in test replaces prior `installDrainObserverForD20Pin(...)` / `capturedActionsForLastTick(sessionId)` helper sketches. |
| C5.5 | claude C10 (MEDIUM) | Tighten 50% observable-footprint floor: removed. Replaced with two-part gate — per verb in {M,E,R,V} >= 1 processed AND total >= 90% of queued. `must_haves.truths` baseline-commit-gate updated to cite C5.5 floors. |
| C5.6 | opencode (LOW A5.4 wording) | Reconcile A5.4 wording on `entitySnapshot(` file-total counts: "3 in-method calls collapse to 1 hoisted; 5 in OTHER methods unchanged". File-total grep: 8 → 6 post-hoist. Aligned with C5.1's count correction. |

## C-amendments — verification

After all 6 commits land:

1. `git log --oneline | head -10` — 6 new C-amendment commits (`dbb68c2`, `48a44a7`, `a39cd4b`, `a55849f`, `5861bef`, this commit).
2. `grep -n "tickOnce" .planning/phases/19.1-*/19.1-05-PLAN.md` — only C5.3 explanatory references (no pseudo-code calls).
3. `grep -n "buildMinimalEventFrame" .planning/phases/19.1-*/19.1-01-PLAN.md` returns ≥ 1.
4. `grep -n "DrainObserverForD20Pin" .planning/phases/19.1-*/19.1-05-PLAN.md` shows the interface signature, not just bare references.
5. `grep -E "verified 2|verified 3" .planning/phases/19.1-*/19.1-05-PLAN.md` — only "verified 3" appears.
6. `19.1-AMENDMENTS-PLAN.md` contains this `## C-amendments (post pass-2)` section.

**No third review pass.** Go straight to `/gsd-execute-phase 19.1`.

If during execution the executor surfaces new factual drift (e.g. a caller dependency in C4.1's audit, or an injection-seam blocker in C3.1), it goes to `19.1-AMENDMENTS-PLAN.md` as a D-amendment — not a re-plan.

## C-amendments — skipped (push-backs, not amending)

- **C9 (varied insertion-order fixtures):** LOW. Comparator (C2.2) closes the structural risk; varied-order fixtures hedge against a class of bug already prevented. Future-hardening note only.
- **C12 (line-number drift preflight):** LOW. Acceptance criteria are grep-by-text, not offset-anchored. `<read_first>` directives that cite line offsets are advisory. Adding preflight overhead per task wastes more time than it saves.

## Deferred (push-back register)

- **C9 — varied insertion-order fixtures for D-04 comparator-totality.** Documented as future hardening if a same-entity multi-grant scenario is later observed in production. Not blocking.

## D-Amendment Pass (review pass-3)

Applied 2026-05-04 from pass-3 cross-AI review (`19.1-REVIEWS.md` + verified findings in `19.1-REVIEWS-pass3-TRIAGE.md`). Triage decision: 6 real HIGHs + 5 real MEDIUMs are **D-amendments**, not a re-plan — same in-place edit pattern as B and C blocks. False positives FP-1, FP-2, FP-3 confirmed; no edit. Unverified U-1, U-2 left as accept-and-monitor.

### Plan 01

| ID | Source | Edit |
|---|---|---|
| D3-M4 | codex (down-rated MEDIUM) | Task 01d `buildMinimalEventFrame(char)` helper expanded into a two-branch builder. Codes that REQUIRE magnitude (E,A,H,T,M,R,L) emit a `v6H3`-shape row; codes that FORBID magnitude (N,S,D) emit a `vS`-shape row. Branch via `Set.of('E','A','H','T','M','R','L').contains(code)`. Substituting a no-magnitude code into a magnitude row (or vice versa) was the original ambiguity. Pre-flight added to Task 01a `<action>`: verify where `'B'` lands against `eventHasMagnitude` switch at `PerceptionCodec.java:690+` before applying edit 2 (the no-magnitude switch arm); record the decision in 01a SUMMARY so 01d's helper Set.of can be aligned. |

### Plan 02

| ID | Source | Edit |
|---|---|---|
| D3-T1 | codex HIGH (also opencode MEDIUM, claude LOW) | Replaced stale lowercase `toxinPathGenerator.generatePath` with static `ToxinPathGenerator.generatePath` form on lines 31 (`artifacts.contains`), 52 (`key_links` regex pattern), 57 (`proofs` grep), 320 (post-edit pseudo-code), 349 (acceptance grep). After C2.1 the field is gone — every lowercase grep would have returned 0 and all acceptance gates would fail. Lines 97 / 307 retained as narrative explaining the OLD form being migrated away from. |
| D3-M3 | codex MEDIUM | Replaced bad fallback recipe in C2.2 (line 172): the `.thenComparing(System::identityHashCode)` advice would have re-introduced determinism bug because `identityHashCode` varies across JVM runs. Replaced with `.thenComparing(g -> g.sourceEventId())` (or whatever monotonic source-event sequence exists) and "if no monotonic field exists, the invariant becomes load-bearing — require an ADR before adding any buff source that violates it". |

### Plan 03

| ID | Source | Edit |
|---|---|---|
| D3-T6 | claude HIGH | Plan 03 was tagged `autonomous: true` at frontmatter line 14 while Task 03c carried `human_needed: confirm injection seam shape before execution` at lines 389, 512, 642 — autonomous orchestrator loops would not pause for that confirmation. Pre-decided the seam choice now: reuse existing `OutboundSender.attachSession(WebSocketSession, int queueCapacity)` at `OutboundSender.java:117`. Active `human_needed` directives at lines 389 and 512 removed (line 642 retained as historical amendment narrative). `autonomous: true` preserved on the plan. |
| D3-M1 | codex MEDIUM | Plan 03 B3.1 wrapping `try { sendOutOfBand(...) } catch (IllegalStateException e) {…}` at lines 309–315 was dead code: `sendOutOfBand` at `WorldWebSocketHandler.java:929` already catches `IOException \| RuntimeException` (and `IllegalStateException extends RuntimeException`). Replaced with a single-line call plus a comment: `// sendOutOfBand swallows IOException + RuntimeException internally — no wrapping needed`. Acceptance criteria did not assert the wrapper; no further updates. |
| D3-M2 | codex MEDIUM | Plan 03 C3.1 proposed adding a new package-private `registerSessionForTest` setter on `OutboundSender`. Verified `OutboundSender.attachSession(WebSocketSession, int)` at `:117` already accepts arbitrary `WebSocketSession` (incl. test fakes) and starts the drain VT. C3.1 amendment narrative folded into D3-T6: the existing `attachSession` IS the seam — tests should call it directly with a fake `WebSocketSession`. The "If no such seam exists, ADD ONE" wording is replaced with the positive statement. Task 03c `<action>` Inject-the-fake paragraph rewritten to call `outboundSender.attachSession(fakeWebSocketSession, queueCapacity)` directly. |

### Plan 04

| ID | Source | Edit |
|---|---|---|
| D3-M5 | gemini HIGH, codex MEDIUM, claude MEDIUM | Plan 04 C4.1 caller-audit HARD STOP gate text appended with explicit pre-state and expected outcome: "Pre-state: `markStalled` does NOT call `cleanupByEntityId` (it sets attrs + detaches; rebind path is `AdmissionGate.tryRebind`). Expected audit result: A4.3 placement-before-early-return lands safely. HARD STOP fires only if a NEW caller has been introduced since this plan was written." Removes ambiguity about whether the audit is expected to pass or fail; surfaces the actual risk profile. |

### Plan 05

| ID | Source | Edit |
|---|---|---|
| D3-T2 | codex HIGH × 2, claude MEDIUM | Task 05c old test skeleton (lines 497–561) still used `installDrainObserverForD20Pin(...)` and `capturedActionsForLastTick(sessionId)` even after C5.4 amendment narrative said "drop these". Rewrote both `void overwriteMidDrainDefersToNextTick()` and `void noOverwriteDrainsCleanly()` test bodies to use ONLY the C5.4 pattern: `AtomicReference<Map<String, ActionFrame>> captured = new AtomicReference<>(); actionResolver.setDrainObserverForD20Pin(captured::set); ...`. Deleted the contradictory comment block at lines 556–561 referencing the removed helpers. Acceptance grep at line 605 updated to `assertThat(captured2.get()).containsEntry(sessionId, newAction)`. |
| D3-T3 | codex HIGH | Task 05a line 229: `entitySnapshot()` does not exist in `GoldenTraceEquivalenceTest`. Source `GoldenTraceEquivalenceTest.java:379` uses `liveEntityRegistry.snapshot()`. Replaced with `liveEntityRegistry.snapshot()`. (The SimulationEngine `entitySnapshot()` hoist in Task 05f is correct — that lives in `SimulationEngine`, not the test class — and is untouched.) |
| D3-T4 | claude HIGH | Task 05a referenced `numpadDir(seed)` and `numpadBallot(seed)` but never defined them. Two implementers would have produced two different fixtures. Pinned the helper bodies verbatim in Task 05a `<action>`: `numpadDir` returns one of `{'2','4','6','8'}` indexed by `Math.floorMod(seed, 4)`; `numpadBallot` returns a 3-char string built from three independent floorMod indices. |
| D3-T5 | codex HIGH | Task 05b line 375 `R` action passed `P2.id()` (an entity UUID) where `ActionResolver.directionOf:542` expects a numpad char via `arg.charAt(0)`. Reproduce takes a target-cell numpad direction relative to parent, not a mate id. Replaced with `actionResolver.queueAction(s1, buildActionFrame('R', "6"))`. Stripped the misleading `/* mate ref */` comment. Test 2 `<behavior>` prose rewritten: "seed 1 high-energy parent + 1 empty target cell adjacent (numpad direction 6); the second particle would block reproduction, not mate." |

### Confirmed false positives — no edit

- **FP-1** (codex): Plan 03 Task 03b acceptance grep includes `, CloseStatus` — codex misread.
- **FP-2** (codex): Plan 03 B3.3 already captures `Thread t` BEFORE detach.
- **FP-3** (claude): Plan 01 Task 01a does not depend on `buildMinimalEventFrame` — that helper is only used in Task 01d's round-trip test.

### Unverified — accept and monitor (no edit)

- **U-1** (gemini, single reviewer): `markDead` removing `ATTR_ENTITY_ID` may bypass Plan 04 cleanups. Worth a 5-minute audit before P21 confirming `DeathFinalizer` is the only post-`markDead` cleanup path AND it clears BuffRegistry/infection/FLEEING. Deferred — accept-and-monitor.
- **U-2** (codex HIGH, claude LOW): Plan 05 90% processed-action gate may be flaky for V/R/E semantics. Calibration note already in C5.5; acceptable to ship and let CI surface — if a real fixture-edge legit-skip case shows up, replace with deterministic per-action assertions per codex's suggestion. Deferred — accept-and-monitor.

## D-amendment Pass (review pass-3) — verification

After all D3 edits land:

1. `grep -n 'toxinPathGenerator\.generatePath' .planning/phases/19.1-*/19.1-02-PLAN.md` returns only narrative refs at lines 97 and 307 (no acceptance/key_links/artifacts entries).
2. `grep -n 'identityHashCode' .planning/phases/19.1-*/19.1-02-PLAN.md` returns only the M-T3 cautionary text (line 172) — no fallback recipe.
3. `grep -n 'human_needed' .planning/phases/19.1-*/19.1-03-PLAN.md` returns only the historical amendment narrative at line ~635 — no active read_first directive.
4. `grep -nE 'try \{ sendOutOfBand' .planning/phases/19.1-*/19.1-03-PLAN.md` returns 0 (M-T1 wrapper removed).
5. `grep -n 'numpadDir\|numpadBallot' .planning/phases/19.1-*/19.1-05-PLAN.md` shows the helper definitions (`private static String`).
6. `grep -nE 'P2\.id\(\)' .planning/phases/19.1-*/19.1-05-PLAN.md` returns 0 inside Task 05b R-action callsite (but may appear elsewhere if narrative-only).
7. `grep -n 'installDrainObserverForD20Pin\|capturedActionsForLastTick' .planning/phases/19.1-*/19.1-05-PLAN.md` returns ONLY narrative references explaining what was removed — never inside the test code body block under Task 05c.
8. `grep -n 'liveEntityRegistry.snapshot()' .planning/phases/19.1-*/19.1-05-PLAN.md` returns at least 2 hits (Task 05a driveScenario body + at least one other reference).
9. All 5 PLAN.md files still parse YAML frontmatter (`head -20` shows valid `---...---` block).

**Go straight to `/gsd-execute-phase 19.1`.** No fourth review pass for D-amendments — pass-3 was the residual cleanup; pass-4 would be diminishing returns. If executor surfaces new factual drift during execution, it goes to a fifth amendment block (E-amendment) here, not a re-plan.

## E-amendments (post pass-4)

Applied 2026-05-04 from pass-4 cross-AI review (gemini, claude, codex, opencode of post-D-amendment plans). Triage decision in `19.1-MULTI-REVIEW-pass4-TRIAGE.md`: 6 reviewer-flagged HIGHs, 4 MEDIUMs, plus a tail of LOWs. After triage against actual plan text and source code, the substantive items are surgical: pseudo-code fixes, falsifier rewording, one missing test, denominator correction, and small `<read_first>` additions. Three reviewer findings are dismissable (concerns already addressed by C/D-amendments the reviewers didn't fully read).

Why E-amendments rather than `/gsd-plan-phase 19.1 --reviews`: re-plans drift factual references (line numbers, method signatures) and require a fresh review pass each time. All actionable findings target named tasks/sections/lines; none requires re-deriving the plan structure, dependency graph, file targets, or analog choices. The B/C/D pattern already establishes a review-driven amendment workflow — adding an E block matches the precedent.

### Plan 01

| ID | Source | Edit |
|---|---|---|
| E1.1 | claude (LOW pre-flight) | Task 01d `buildMinimalEventFrame` helper: add `<read_first>` line requiring confirmation that `vectors()` at `PerceptionCodecRoundTripTest.java:32` contains at least one single-event row in EACH magnitude class (one `v6H3`-shape and one `vS`-shape) before authoring the two-branch helper. If either class is unrepresented, STOP rather than fabricating a row. |

### Plan 02

| ID | Source | Edit |
|---|---|---|
| E2.1 | claude/opencode HIGH | Falsifier reverses semantics. C2.3 falsifier read "byte-identical … across two calls" — but per-call equality IS the static-stateless invariant; the falsifier should describe byte-DIFFERENT outcomes (i.e., the failure mode that would indicate the contract is broken). Reworded line 63 accordingly. |
| E2.3 | claude (LOW non-applicability note → MED post-pass-5 correction) | Restate C2.2 totality argument: cures DO flow through `pendingGrants` via `EnvironmentEngine.reduceInfection` (`:909`) and via `tickBuffsAndInfections` (`:744`). Comparator totality still holds because each cure produces at most one `PendingGrant` per `(entity, initialTicks, position)` tuple — `reduceInfection` early-returns when `infections.get(entityId) == null`, so a second cure for the same entity in the same tick is a no-op. If a future code path enqueues two same-(entity, initialTicks, position) grants in one tick, the load-bearing-invariant ADR becomes required. (Pass-5 triage 2026-05-04 / F4 corrected the original "do not flow" wording.) |

### Plan 03

| ID | Source | Edit |
|---|---|---|
| E3.1 | codex HIGH | Task 03c B3.4 pseudo-code: `handler.markStalled(fake, "stuck-VT-test")` — wrong second-arg type. `markStalled(WebSocketSession, long stallTick)` takes a numeric tick literal, not a String tag. Replaced with `handler.markStalled(fake, 3L)` and inline note. |
| E3.2 | claude/codex MEDIUM | Add D-14 behavioural test `detachTimeoutIncrementsCounter` to `OutboundSenderTest`. Drives an uninterruptible drain VT, calls `detachSession(String)` (the join-timeout path), asserts the `paralife.outbound.detach.timeout` counter incremented by 1. Update Task 03a `proofs:` to include `--tests OutboundSenderTest.detachTimeoutIncrementsCounter exits 0`. Without this test, the L1 metric increment is observable in production but unverified in CI. |
| E3.3 | codex MEDIUM | Task 03c VT-exit clarification: capture `Thread t = senderThread(id)` BEFORE invoking `markStalled` (Step 5 originally re-looked up the thread post-detach, which returns `null` because `detachSession` removes the map entry first per B3.3). Assert `t.join(2s)` and `!t.isAlive()` on the captured local. |
| E3.4 | codex (LOW doc precision) | Task 03b CLAUDE.md update note: cite the `sendOutOfBand` `isOpen()` guard at `WorldWebSocketHandler.java:923`. The OOB call after close is structurally short-circuited by the `isOpen()` guard, not merely exception-swallowed — material for readers reasoning about post-close behaviour. |

### Plan 04

| ID | Source | Edit |
|---|---|---|
| E4.1 | gemini/codex MEDIUM | Task 04c grep-only acceptance for D-10 (FLEEING transfer at composite formation + dissolve) replaced with single-tick behavioural test. Two new `@Test` methods (`fleeingTransfersOnCompositeFormation`, `fleeingTransfersOnCompositeDissolve`) appended to `SimulationEngineCompositeFormationTest`. Each drives ONE `TickEvent` and asserts `getFleeing(oldId)` empty + `getFleeing(newId)` non-empty post-transition. Single-tick scope is well under FLEEING auto-expiry; behavioural pinning becomes cost-effective. `files_modified` updated. |

### Plan 05

| ID | Source | Edit |
|---|---|---|
| E5.1 | claude HIGH | Task 05c `<read_first>` adds session-validation contract requirement: read `ActionResolver.queueAction` body (not just signature) to confirm whether it drops actions for unregistered session ids. If so, test setup MUST register synthetic `sessionId = "S1"` / `"S2"` via `botRegistry.register(...)` BEFORE the first `queueAction` call. Otherwise the staged OLD action never lands in `pendingActions`, the drain observer fires on an empty snapshot, and the contract assertion fails for the wrong reason. |
| E5.2 | claude HIGH | Task 05c DR-pin observer hook-point clarified: the observer fires INSIDE the per-entry iteration loop — after `var e = it.next()` (or equivalent local capture) and BEFORE that entry's `pendingActions.remove(e.getKey(), e.getValue())` call — NOT once over the snapshot up-front. Explicit statement that no snapshot-phase refactor is implied; the seam is one null-checked line inserted into the existing loop body. |
| E5.3 | claude/codex/opencode MEDIUM (consensus) | Task 05a Step 4: replace `int totalQueued = BOT_COUNT * TICK_COUNT` with per-tick running sum (`totalQueued += liveEntityRegistry.snapshot().size()` inside `driveScenario` before each per-bot queueAction). Mortality during the trace reduces the live population; the static product overstates the denominator, silently relaxing the 90% floor below an inflated theoretical-max. `truths:` line 30 (baseline-commit gate) updated to cite the corrected denominator. Brings forward the U-2 deferred 90%-floor flake concern as an in-place fix rather than continuing to defer. |
| E5.4 | claude (LOW type correctness) | Task 05c qualify map type as `Map<String, Frame.ActionFrame>` (not bare `ActionFrame`) at all three sites (interface signature, `AtomicReference` declaration, local variable). Add `@DirtiesContext(AFTER_EACH_TEST_METHOD)` on `ActionResolverDrainSemanticsTest` so the package-private `DrainObserverForD20Pin` observer registered on `ActionResolver` is reset between tests — otherwise a stale observer from `overwriteMidDrainDefersToNextTick` fires during `noOverwriteDrainsCleanly` and the second test's capture sees the wrong snapshot. |

## E-amendments — verification

After all 6 commits land:

1. `git log --oneline | head -8` — 6 new `docs(19.1): apply pass-4 E-amendments...` commits in expected order (Plans 01 → 02 → 03 → 04 → 05 → AMENDMENTS register).
2. `wc -l .planning/phases/19.1-*/19.1-*-PLAN.md` — line counts grow modestly (no plan should grow > 30 lines from this pass; E3.2 and E4.1 add the most because they introduce new test descriptions).
3. Per-edit grep checks:
   - **E3.1:** `grep -n 'markStalled(fake' .planning/phases/19.1-*/19.1-03-PLAN.md` shows `, 3L)` — no String literal.
   - **E2.1:** `grep -n 'byte-DIFFERENT\|byte-different' .planning/phases/19.1-*/19.1-02-PLAN.md` returns ≥ 1; `grep -n 'byte-identical.*two calls' .planning/phases/19.1-*/19.1-02-PLAN.md` shows the falsifier line is reworded (no remaining "byte-identical … across two calls" statements in the falsifier list).
   - **E5.1:** `grep -n 'queueAction.*read_first\|read_first.*queueAction\|session-validation' .planning/phases/19.1-*/19.1-05-PLAN.md` returns ≥ 1.
   - **E5.2:** `grep -n 'it.next()\|after.*next.*before.*remove' .planning/phases/19.1-*/19.1-05-PLAN.md` returns ≥ 1 in Task 05c context.
   - **E5.3:** `grep -nE 'totalQueued.*=|totalQueued \+=' .planning/phases/19.1-*/19.1-05-PLAN.md` shows running-sum form, not `BOT_COUNT * TICK_COUNT`.
   - **E3.2:** `grep -n 'detachTimeoutIncrementsCounter\|detachTimeoutCounter' .planning/phases/19.1-*/19.1-03-PLAN.md` returns ≥ 1.
   - **E4.1:** `grep -nE 'fleeingTransfers.*test|FLEEING.*formation.*test|fleeing.*behavioural' .planning/phases/19.1-*/19.1-04-PLAN.md` returns ≥ 1.
   - **E3.3:** `grep -n 'Thread t = senderThread\|capture.*Thread.*before\|BEFORE invoking' .planning/phases/19.1-*/19.1-03-PLAN.md` returns ≥ 1.
   - **E5.4:** `grep -n 'Frame.ActionFrame\|@DirtiesContext\|DirtiesContext' .planning/phases/19.1-*/19.1-05-PLAN.md` returns ≥ 2 (one per concern).
   - **E1.1:** `grep -nE 'vectors\(\).*single-event|vectors\(\).*magnitude class' .planning/phases/19.1-*/19.1-01-PLAN.md` returns ≥ 1.
   - **E3.4:** `grep -n 'isOpen.*guard\|sendOutOfBand.*923\|line 923' .planning/phases/19.1-*/19.1-03-PLAN.md` returns ≥ 1.
   - **E2.3:** `grep -n 'pendingGrants\|cure events do not\|ADR becomes required' .planning/phases/19.1-*/19.1-02-PLAN.md` returns ≥ 1.
4. Reading each plan's `must_haves.truths` and `falsifiers` lists end-to-end confirms no contradictions introduced.

Then proceed to `/gsd-execute-phase 19.1`. **No fifth review pass.** The user's stated concern about review-replan churn is the controlling consideration; pass-4 reviewers themselves observed convergence (Claude: "pass-5 diminishing returns"; OpenCode: zero HIGHs). The 90%-floor change (E5.3), the new D-14 behavioural test (E3.2), and the FLEEING behavioural test (E4.1) are the executable surface that will tell us whether the surgical approach was right; if any of them surfaces a structural issue at execute time, that is the moment to consider re-planning the affected sub-plan only — not re-planning the whole phase.

## Pass-4 push-backs (dismissed)

Three reviewer-flagged HIGHs are dismissed because the concerns were already addressed by C/D-amendments the reviewers did not fully read against the post-amendment plan text:

- **Codex HIGH "Plan 02a comparator ties preserve CHM order":** dismissed. Plan 02 Task 02a (`19.1-02-PLAN.md:158-162`) already specifies a 4-key comparator: `entityId → initialTicks → position.x → position.y`, and D3-M3 already documents `.thenComparing(g -> g.sourceEventId())` as the fallback if the totality invariant ever weakens. Codex flagged a 1-key sort that does not exist in the current plan.
- **Codex HIGH "Plan 05d OD grep `@Order\([0-9]+\)` misses constants":** Pass-4 dismissal cited the enumeration grep at line 685 (`[^)]+`), not the acceptance set-equality at line 716 (`[0-9]+`). The acceptance grep was indeed narrow; corrected in F-amendment F2 (pass-5 triage 2026-05-04). The enumeration command at line 685 (`grep -rnE '@Order\([^)]+\)' src/main/java | sort`) is unchanged and remains correct. The acceptance-side set-equality command at line 716 has been broadened to `[^)]+` so `@Order(Integer.MAX_VALUE)` (TickHealthMonitor) participates in the diff — the C5.2 CLAUDE.md row would otherwise never match a `[0-9]+` form.
- **Claude HIGH "Plan 02 D-04 cure-event invariant unwritten ADR":** restated, not dismissed. Cures DO flow through `pendingGrants` (verified `EnvironmentEngine.java:909`); the empirical impossibility of `(entity, initialTicks, position)` ties holds via `reduceInfection`'s early-return guard. Captured in E2.3 (post-pass-5 correction). Bulletproofing via a `sourceEventId` field on `PendingGrant` is deferred — see anti-reflag comment in `EnvCleanupHooksBean.java` near the `PendingGrant` record. (F4, pass-5 triage 2026-05-04.)

Other LOWs not actioned this pass (kept as accept-and-monitor):

- **Plan 03c sendMessage race window (OpenCode MEDIUM, reclassified LOW):** sub-microsecond race; test passes in practice. Documentation-only concern; no edit.
- **Plan 03c bound jitter (Gemini LOW):** calibrate `baselineMaxMs * 5 + 20ms floor` if CI flakes — already documented in C3.3. No edit.
- **Plan 05d "drop the row" wording (Claude LOW):** either wording is fine. No edit.

## F-amendments (post pass-5)

Applied 2026-05-04 from pass-5 cross-AI review (gemini, claude, codex, opencode of post-E-amendment plans). Triage in `19.1-REVIEWS-pass5-TRIAGE.md`. **5 in-place fixes + 1 anti-reflag-comments amendment**, plus dismissals/downgrades. No structural redesign; no source code edits in this phase — those land during `/gsd-execute-phase 19.1`.

| ID | Plan file(s) | Severity | Summary |
|---|---|---|---|
| F1 | `19.1-05-PLAN.md` Task 05a Step 4 + C5.5 acceptance | HIGH | Replace hand-wavy `countObservableFootprintPerVerb(capturedFrames)` with package-private `ActionResolver.processedCountForVerb(char)` accessor backed by 4 `LongAdder` fields incremented at per-verb success sites (M:602, E:493, R:703, V composite-vote success site). Drop V from per-verb >=1 floor (composite-only; fixture stages no composites). Aggregate 90% floor unchanged and still includes V. |
| F2 | `19.1-05-PLAN.md` Task 05d acceptance line 716 + C5.2 spelling + AMENDMENTS dismissal bullet | HIGH | Change set-equality regex `[0-9]+` to `[^)]+` so `@Order(Integer.MAX_VALUE)` and `@Order(TICK_ORDER)` participate in the diff. Pin canonical CLAUDE.md spelling to `Integer.MAX_VALUE` (matches source; abbreviated `MAX_VALUE` would silently desync the diff). Correct the pass-4 dismissal bullet that conflated line-685 enumeration grep (already broad) with line-716 acceptance grep (was narrow). |
| F3 | `19.1-05-PLAN.md` Task 05a `<read_first>` + Task 05f `<read_first>` + Task 05f / 05e `<done>` | MED | Reorder Plan 05 task execution: 05f (processInteractions hoist) MUST land before 05a (GoldenTraceWithActionsTest baseline pin) so the baseline encodes post-hoist behavior. Task IDs unchanged; phase-closeout responsibility moves from 05f to 05e. |
| F4 | `19.1-AMENDMENTS-PLAN.md` lines 262 + 317 | MED | Restate E2.3 — cures DO flow through `pendingGrants` via `reduceInfection:909`; comparator totality holds via the early-return guard (`infections.get(entityId) == null`), not via the false claim that `pendingGrants` is buff-spawn-only. Update matching dismissal bullet from "dismissed" to "restated, not dismissed". |
| F5 | `19.1-01-PLAN.md` Task 01d C1.2 acceptance | MED | Pin reverse-drift gate path to `PerceptionCodec.decode(buildMinimalEventFrame(c))` accept-count loop via the public `decode` API. Forbid reflection. Forbid relaxing `validateEventCode` visibility. Helper from B1.2 / C1.1 is reusable. |
| F6 | Plans 02a / 03c / 05a `must_haves.truths` + Javadoc directive | LOW | Anti-reflag annotations at deferred sites (F6 PendingGrant.sourceEventId, F7 Plan 03c production-callback-path tightening, F1 V-exclusion). Each note documents the deferred status, the underlying invariant, and the source-comment text the executor should mirror in code. Prevents pass-6 (if ever run) from re-flagging items intentionally deferred per pass-5 triage. |

### F-amendments — verification

After all 6 F-edits land:

1. **Plan-text consistency:**
   - `grep -n "countObservableFootprintPerVerb" .planning/phases/19.1-*/19.1-05-PLAN.md` returns 0 (replaced by F1 accessor reference).
   - `grep -nE '@Order\\\\?\\\([0-9]\\+\\\\?\\\)' .planning/phases/19.1-*/19.1-05-PLAN.md` line-716 acceptance returns 0 (replaced by `[^)]+` per F2).
   - `grep -n "do not flow through" .planning/phases/19.1-*/19.1-AMENDMENTS-PLAN.md` returns 0 (E2.3 wording corrected per F4).
   - `grep -c "validateEventCode" .planning/phases/19.1-*/19.1-01-PLAN.md` reflects the F5 path-pin (mentions `decode(buildMinimalEventFrame(c))`).
   - Task 05f read_first contains "execute this task BEFORE Task 05a" (F3); Task 05a read_first contains "Task 05f MUST land before this task" (F3).
2. **Anti-reflag comment coverage:** `grep -c "pass-5 triage 2026-05-04" .planning/phases/19.1-*/*.md` returns >= 5 (F4 + F6's three sites + F1 V-exclusion + F2 + F3 + F5).
3. **Triage doc landed:** `test -f .planning/phases/19.1-*/19.1-REVIEWS-pass5-TRIAGE.md` and the file contains per-finding triage table + push-back rationale + deferred-items list.
4. **No source-code drift:** `git diff src/` returns empty (this F-block is plan-only).
5. **Single commit:** message `docs(19.1): apply pass-5 F-amendments (F1-F6) plus anti-reflag annotations`. Files: 5 plan files + new TRIAGE.

After commit, proceed to `/gsd-execute-phase 19.1`. **No pass-6 review.** Deferred concerns (F6 sites) carry anti-reflag annotations that future reviewers will see as "deferred per pass-5 triage" rather than re-raise as fresh findings.

## G-amendments (post pass-6)

Applied 2026-05-04 from pass-6 cross-AI review (gemini, claude, codex, opencode of post-F-amendment plans), refined by a verb-coverage deep-dive against `ActionResolver.java` at HEAD. Triage in `19.1-REVIEWS-pass6-TRIAGE.md`. **8 in-place plan-text fixes + new anti-reflag annotations + 1 new test method**, plus dismissals (L1 @DirtiesContext-redundant, L3 meterRegistry-parallel — both don't apply under forkEvery=1). No structural redesign; F1's source-code instrumentation prescription is amended (G1-bis) but the source-code touch surface is unchanged from the original F1 prescription.

**Forward-reference (added 2026-05-04):** the verb-coverage deep-dive that drove G1-revised also surfaced the codec-vs-resolver verb-eligibility mismatch (codec accepts V/A from any session; resolver silently rests solo V/A). This was captured as **Phase 999.3 backlog** in `ROADMAP.md` at commit `4c2dcfa`: M-A-001 (disallow solo V), M-A-002 (enable solo directional A for emergent pack tactics), M-A-003 (research RPS multi-target resolution as design gate for M-A-002). G1-revised pins current semantics in the trace baseline; 999.3 promotion will rebaseline `golden-trace-with-actions-phase19.json` deliberately. No 19.1 work is gated on 999.3 — the cross-reference is documentation-only.

| ID | Plan file(s) | Severity | Summary |
|---|---|---|---|
| G1 (revised) | `19.1-05-PLAN.md` lines 30, 38, 311-314, 344-356, 365-377, 382, plus new Task 05a Step 5 | HIGH | Driver cycles `{M,E,R,V,A}` at `tick % 5` (V and A act as drain-pressure noise; their solo Phase 2 path is silent rest by design). Per-verb `>=1` floor restricted to `{M,E,R}`. Aggregate 90% floor restricted to `{M,E,R}`-queued denominator (`totalQueuedMER`). L is excluded from the cycle (special channel: synchronous dispatch in `queueAction:367-372` bypasses `drainActions`); L coverage lives in a new dedicated test method `lAlarmRoutesViaQueueActionForCompositeMember`. New anti-reflag truths document the V/A solo-rest invariant and L's bypass. |
| G1-bis | `19.1-05-PLAN.md` lines 315-333 + new anti-reflag truth | HIGH | Relocate F1's E success-increment from `ActionResolver.java:493` (dispatch-time `consumeCount++`) to inside `resolveConsume` after the nutrient-null early-return at `:622`. The original `:493` site fires before the silent-drop branch, making the E `>=1` floor a false-negative gate (passes green even when every E action silently no-ops). New anti-reflag note pins the distinction between `consumeCount` (dispatch) and `processedE` (success). |
| G2 | `19.1-05-PLAN.md` lines 730, 748, 755 | HIGH | Filter source-grep to non-comment lines (`grep -vE '^\s*(\*\|//)'`) so Javadoc references like `@Order(100)` don't pollute the source set. Spell EnvironmentEngine and EnvPostActionReconciler rows in CLAUDE.md as `@Order(TICK_ORDER)` (matching source) with the resolved value in an inline comment. Set-equality diff now satisfiable. |
| G3 | `19.1-05-PLAN.md` mechanical task reorder + new banner + line 904 strike | MED | Physically move `<task>05f</task>` block ahead of `<task>05a</task>` so the document order matches F3's declared execution order. Add explanatory banner inside `<tasks>`. Strike "BOTH BEFORE AND AFTER the hoist commit" from Task 05f's golden-trace gate (the test class does not exist before 05a per F3, so the pre-hoist gate was impossible). Post-hoist green is the only gate. |
| G4 | `19.1-03-PLAN.md` lines 232-256 (E3.2 prescription) | MED | Promote `CountDownLatch never` from method-local to test-class field; add `@AfterEach releaseStuckVT()` that counts it down. Prevents drain-VT leak across forkEvery=1 runs (TD-22-A leaked-thread alarm). |
| G5 | `19.1-02-PLAN.md` line 175 (E2.3 paragraph) | MED | Sync plan body with F4's amendment-register correction. Replace "Cure events do not flow through pendingGrants" with the empirically-correct paragraph: cures DO flow via `tickBuffsAndInfections:743` and `reduceInfection:908`; comparator totality holds via the early-return guard. |
| G6 | `19.1-05-PLAN.md` lines 620, 660-661 + new anti-reflag truth | MED | DR-pin observer capture uses `Map.copyOf(snapshot)` defensively. Production `drainActions()` iterates `pendingActions.entrySet().iterator()` directly with no snapshot copy (`ActionResolver.java:408-418`); a captured live reference would freeze to mutated state and the assertion would read post-overwrite content. |
| G7 | `19.1-03-PLAN.md` Task 03b acceptance line ~388 | LOW | Tighten grep to `outboundSender\.detachSession\(session,\s*CloseStatus\.SERVICE_RESTARTED\)` so the acceptance gate pins the D-07 close-aware overload specifically, not any unrelated `detachSession(session)` call. |

### G-amendments — verification

After all G-edits land:

1. **Plan-text consistency:**
   - `grep -E "tick % 5" .planning/phases/19.1-*/19.1-05-PLAN.md` returns >= 1 (G1 5-verb cycle).
   - `grep -E "totalQueuedMER" .planning/phases/19.1-*/19.1-05-PLAN.md` returns >= 2 (G1 denominator rename).
   - `grep -n "lAlarmRoutesViaQueueActionForCompositeMember" .planning/phases/19.1-*/19.1-05-PLAN.md` returns >= 1 (G1 L test method).
   - `grep -n "after the nutrient-null early-return" .planning/phases/19.1-*/19.1-05-PLAN.md` returns >= 1 (G1-bis E site relocation).
   - `grep -n "BOTH BEFORE AND AFTER" .planning/phases/19.1-*/19.1-05-PLAN.md` returns 0 (G3 strike).
   - `grep -n "releaseStuckVT" .planning/phases/19.1-*/19.1-03-PLAN.md` returns >= 1 (G4 teardown).
   - `grep -n "Cure events do not flow" .planning/phases/19.1-*/19.1-02-PLAN.md` returns 0 (G5 sync).
   - `grep -n "Map.copyOf(snapshot)" .planning/phases/19.1-*/19.1-05-PLAN.md` returns >= 2 (G6 captures).
   - `grep -nE 'CloseStatus\.SERVICE_RESTARTED' .planning/phases/19.1-*/19.1-03-PLAN.md` returns >= 1 (G7).
2. **G2 grep shape present:** `grep -F "grep -vE '^\\s*(\\*|//)'" .planning/phases/19.1-*/19.1-05-PLAN.md` returns >= 2 (G2 filter at verification + acceptance lines).
3. **Anti-reflag comment coverage:** `grep -c "pass-6 triage 2026-05-04" .planning/phases/19.1-*/*.md` returns >= 8 (G1-revised V/A + G1-revised L + G1-bis + G3 + G4 + G5 + G6 + G7 anti-reflag bullets and inline comments).
4. **Triage doc landed:** `test -f .planning/phases/19.1-*/19.1-REVIEWS-pass6-TRIAGE.md` (already committed at a0d1503).
5. **No source-code drift:** `git diff src/` returns empty.
6. **05f physical position:** `awk '/<task type="auto" tdd="true">/{ block_line = NR } /Task 05[af]:/{ print $0, block_line }' .planning/phases/19.1-*/19.1-05-PLAN.md` reports the `<task>` block opening 05f at a smaller line number than the `<task>` block opening 05a.
7. **Per-plan commit cadence:** Plan 02, Plan 03, Plan 05, AMENDMENTS each get one commit. Four commits total: `docs(19.1): apply pass-6 G-amendments to Plan 02 (G5)`, `… Plan 03 (G4, G7)`, `… Plan 05 (G1-revised, G1-bis, G2, G3, G6)`, `docs(19.1): append pass-6 G-amendment block to amendments register`.

Pass-7 review is recommended only as a convergence check, not as a contract requirement. If pass-7 returns clean, ready for `/gsd-execute-phase 19.1`. If pass-7 surfaces new findings, triage and decide replan-vs-amend per the same rubric used at pass 6.

## H-amendments (post pass-7)

Pass-7 cross-AI review (`19.1-REVIEWS-pass7.md`) and triage (`19.1-REVIEWS-pass7-TRIAGE.md`) identified 14 spot-edits across plans 02, 03, 05 (plans 01 and 04 receive only the trailing 166-floor note). All findings are localised text/code amendments; no replan required. The H-block applies the triage's recommended actions and pins anti-reflag notes for items intentionally deferred.

| ID | Plan file(s) | Severity | Edit |
|---|---|---|---|
| H1 | `19.1-05-PLAN.md` line 326 (driveScenario body) | **HIGH (verified)** | Replace `botRegistry.getSessionByEntity(entry.entityId()).orElseThrow()` with `Optional` + `continue`-on-empty. Forced bonding seeds (`seedAdjacentBondingPair` at `GoldenTraceEquivalenceTest:316-333`) register only in `liveEntityRegistry`, never `botRegistry`; the original would crash on tick 0. |
| H2 | `19.1-05-PLAN.md` Task 05a Step 4 + `ActionResolver.clearStateForTest()` | MED | Add `processedM/E/R.reset()` to `clearStateForTest()`. Add delta-snapshot block before/after driveScenario. Add conditional fallback clause to truth-line ~30 — if the 90% aggregate floor is empirically unreachable, executor MAY downgrade to per-verb `>= 1` floors only and document in SUMMARY. |
| H3 | `19.1-05-PLAN.md` Task 05a Step 5 `<read_first>` + helper-body inline pin | MED | Cite `SimulationEngineCompositeFormationTest`, `CompositeRegistryTest`, `AlarmQueue.java`. Pin `soloParticleSessionId()`, `compositeMemberSessionId()`, `compositeIdFor()`, `stagedCompositeIds()` bodies inline in the action block. |
| H4 | `19.1-05-PLAN.md` Task 05a `<read_first>` | MED | Append pre-flight grep directive: `grep -nE 'queueAction\|case .V.\|case .A.' src/main/java/com/paralife/engine/ActionResolver.java`. If V or A short-circuit at queueAction (like L does at :367-372), drop the affected verb from `switch (tick % 5)` and reduce modulo. |
| M1 | `19.1-05-PLAN.md` Task 05c E5.4 amendment area | MED | Drop `@DirtiesContext(AFTER_EACH_TEST_METHOD)`. Replace with `@AfterEach { actionResolver.setDrainObserverForD20Pin(null); }`. Anti-reflag: G6's `Map.copyOf` + always-overwrite-before-read makes `@DirtiesContext` redundant; full Spring restart per method is large CI-time cost for no incremental safety. |
| M2 | `19.1-03-PLAN.md` Task 03c lines 508 + 511-516 + 620-623 | LOW | Rename primary assertion to `"markStalled returns and drain VT exits under stuck write (tick-thread service-time bounded as secondary signal)"`. Relax floor: `long bound = (baselineMaxMs == 0) ? 50 : baselineMaxMs * 5 + 20;`. Update C3.3 rationale to reference 50ms. |
| M3 | `19.1-03-PLAN.md` Task 03a around lines 233-270 | LOW | Capture `drainThread` field. Extend `@AfterEach releaseStuckVT()` to call `drainThread.join(200)` after `never.countDown()` and assert the thread exited. |
| M4 | `19.1-05-PLAN.md` F1 amendment area + driveScenario | LOW + simplify | Drop V instrumentation entirely (`LongAdder processedV` field, `.increment()` call site, V case in `processedCountForVerb`). G1-revised already excludes V from per-verb floor AND `totalQueuedMER` aggregate denominator → V counter has zero assertions reading it. Anti-reflag pin: do NOT re-add V instrumentation without first re-introducing a V-gated floor. |
| M5 | `19.1-02-PLAN.md` Task 02c `<read_first>` line 271-277 | LOW | Append pre-flight grep directive: `grep -rln 'ToxinPathGenerator' src/main src/test`. Document outcome in Task 02c body. |
| L1 | `19.1-05-PLAN.md` line 30 truth | LOW | Replace `* 3 / 5` heuristic with reference to `totalQueuedMER` accumulator. |
| L2 | Plans 01-04 truth lines mentioning 166-test suite | LOW | Append `(pass-7 L2: 166 at this plan's isolation point; cumulative phase-closeout floor is 180 per B5.5 in Plan 05)`. |
| L3 | `19.1-03-PLAN.md` line 20 truth | LOW | Strike `"OOB 408 is wrapped in try/catch"` → `"OOB 408 is best-effort (sendOutOfBand swallows internally); no wrapping needed (D3-M1)"`. |
| L4 | `19.1-05-PLAN.md` Task 05a `<read_first>` | LOW | Append `git log --oneline -5 -- src/main/java/com/paralife/engine/SimulationEngine.java` precondition; require Task 05f committed AND `git status --porcelain` empty before driving the trace. |
| L5 | `19.1-03-PLAN.md` line 235 field declaration | LOW | Pin `private CountDownLatch never = null;` + Javadoc note "JUnit defaults to PER_METHOD; do NOT add `@TestInstance(PER_CLASS)` without re-evaluating teardown." |

### H-block verification

After applying H1-H4 + M1-M5 + L1-L5:

- [ ] `grep -n 'orElseThrow' .planning/phases/19.1-*/19.1-05-PLAN.md` returns no match inside the driveScenario body (lines 305-340).
- [ ] `grep -n '@DirtiesContext' .planning/phases/19.1-*/19.1-05-PLAN.md` returns no match in the Task 05c block.
- [ ] `grep -nE 'processedV|LongAdder.*V' .planning/phases/19.1-*/19.1-05-PLAN.md` returns no match.
- [ ] `grep -n 'wrapped in try/catch' .planning/phases/19.1-*/19.1-03-PLAN.md` returns no match.
- [ ] `grep -n '\* 3 / 5' .planning/phases/19.1-*/19.1-05-PLAN.md` returns no match in truth section (lines 28-42).
- [ ] H-block table appears in `19.1-AMENDMENTS-PLAN.md` after the G-block verification list, all 14 row IDs present (H1-H4 + M1-M5 + L1-L5).
- [ ] All 8 push-back entries appear in the H-block dismissals section.

### Dismissals / push-backs (pass-7)

| Item | Source | Reason for dismissal |
|---|---|---|
| Plan 02 active totality runtime guard | gemini MED | Comparator cascade + documented invariant + anti-reflag pin sufficient. Adding a runtime hard-warn pollutes production with test-only assertion. Gold-plating. |
| Plan 03 JUnit parallel-execution explicit disable | gemini LOW | Project `junit-platform.properties` doesn't enable parallel; concern is purely speculative. |
| Plan 02 amendment register stale "byte-identical"/`identityHashCode` text | codex LOW | Marked superseded; historical text is intentional in append-only register. |
| Plan 02 ToxinPathGenerator compatibility shim | codex MED | Plan 02 already enumerates collateral; shims for tests-only callers add API surface for nothing. |
| Plan 04 HARD STOP audit caller-stability concern | claude M4 | The HARD STOP gate IS the mechanism. Self-flagged "as-written prose is adequate". |
| Plan 01 F5 reverse-drift loop ASCII range | claude L4 | Event codes are uppercase ASCII per protocol contract; self-flagged "none needed". |
| Plan 05 awk method-end anchor fragility | opencode LOW | Pre-flight `STOP if count != 3` directive already catches this; self-flagged "no change needed". |
| Plan 05 production-code surface in "polish" plan framing | claude M1 | Editorial framing only; commit message will accurately describe the diff. |
