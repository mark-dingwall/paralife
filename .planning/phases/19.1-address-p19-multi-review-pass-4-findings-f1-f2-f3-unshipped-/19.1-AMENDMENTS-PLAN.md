---
phase: 19.1
parent: 19.1-REVIEWS-TRIAGE.md
type: amendments-plan
amendment_prefix: B
sequence: per-plan, sequential commits
verification: /gsd-review --phase 19.1 --all (post-edit, NOT /gsd-plan-phase --reviews)
---

# Phase 19.1 — B-Amendments Plan

Triage decided: in-place edits, no rewrite. This plan sequences the B-amendment commits, one per PLAN.md, mirroring the existing A1.x/A2.x/A3.x/A4.x/A5.x scheme.

## Conventions

- **Amendment IDs:** `B<plan-number>.<n>` (e.g., `B2.1`, `B3.4`).
- **Plan edit pattern:** append a new `### Amendment Bn.x — <one-line>` block under the existing "## Amendments" section of each PLAN.md, citing `(reviewer: claude/codex/opencode, finding: <quoted phrase>)`. Update affected pseudo-code/YAML inline; do NOT delete prior A-amendments.
- **Commit pattern:** `docs(19.1): apply pass-4 review triage to Plan NN (BN.1, BN.2, ...)`.
- **One commit per plan**, in order: Plan 01 → 02 → 03 → 04 → 05. Sequential because amendments touch different files but reviewers should see one plan's amendments stabilise before the next.

## Pre-flight (run once before any edit)

1. Confirm working tree clean — current branch `main`, last commit `86b64ca`.
2. Skim `19.1-REVIEWS-TRIAGE.md` once for context.
3. No code changes in this pass — only `.planning/phases/19.1-.../19.1-NN-PLAN.md` files.

## Plan 01 — B-amendments (3 edits)

| ID | Source | Edit |
|---|---|---|
| B1.1 | codex HIGH | Task 01a target file: change `PerceptionCodecTest.java` → `PerceptionCodecErrorTest.java`. Add new `@ParameterizedTest` method to that file. F2 round-trip gate (Task 01d) goes into `PerceptionCodecRoundTripTest.java`. |
| B1.2 | codex MEDIUM | `Event.ALL_CODES` shape: change from `public static final char[]` to `public static final String`. Iterate via `.toCharArray()` in tests. Compact-ctor logic uses `ALL_CODES.indexOf(code) >= 0` or `ALL_CODES.chars()`. |
| B1.3 | codex MEDIUM | Task 01b F1 test mode: note `updateBotRegistryForFormation` is private at `SimulationEngine.java:881`. Specify event-driven test (publish a composite-formation `TickEvent` via `ApplicationEventPublisher`, observe registry state) — NOT direct private-method invocation. |

Plan 01 NON-edits (rejected/already addressed): `buildMinimalEventFrame` micro-concern (define once in 01a, reference from 01d — already implicit); ALL_CODES future-ordering (premature).

## Plan 02 — B-amendments (4 edits)

| ID | Source | Edit |
|---|---|---|
| B2.1 | codex HIGH | D-04 tiebreaker: replace `.thenComparing(PendingGrant::buffType)` with `.thenComparing(PendingGrant::initialTicks).thenComparing(g -> g.position().x()).thenComparing(g -> g.position().y())`. Total order via verified record fields at `EnvCleanupHooksBean.java:69`. |
| B2.2 | claude MEDIUM | `files_modified` frontmatter: add `src/test/java/com/paralife/engine/ToxinPathGeneratorTest.java` AND `src/test/java/com/paralife/engine/LightningTest.java` (uses no-arg `new ToxinPathGenerator()` at `:78` — collateral). Document Option A migration: drop ctor calls site-by-site, switch to static `ToxinPathGenerator.generatePath(..., seed)`. |
| B2.3 | claude/opencode MEDIUM | D-06 `forceSpawnToxinForTest` seed: pin to `private static final long TEST_TOXIN_SEED = 0xDEADBEEFL` per call. State explicitly: not `0L` (avoids collapsed path diversity). |
| B2.4 | codex HIGH | Test placement: relocate composite-RNG determinism check OUT of `EnvironmentDeterminismTest` (env-only contract) into a new `CompositeEnergyDistributorDeterminismTest` (or extend `CompositeEnergyDistributorTest`). Drop the "3+ simultaneous toxin spawns" sub-test (single-active-toxin guard prevents it); replace with sequential `generatePath(seed)` direct calls with reset between. |

## Plan 03 — B-amendments (5 edits)

| ID | Source | Edit |
|---|---|---|
| B3.1 | codex HIGH | `sendOutOfBand` swallows `IOException` internally — note this in plan; in `markStalled`, catch only unchecked (`IllegalStateException`) at the OOB call site. Alternative considered: change `sendOutOfBand` to return `boolean` and rethrow IOException; reject because current callers depend on the swallow contract. |
| B3.2 | claude/codex/opencode HIGH | Task 03c test driver: `tickEngine.tickOnce()` does NOT exist (verified — no match in `TickEngine.java`). Substitute `applicationEventPublisher.publishEvent(new TickEvent(tickNumber))` and inject `ApplicationEventPublisher` into the test. Update Task 03c pseudo-code accordingly. |
| B3.3 | codex HIGH | Capture sender thread BEFORE detach: `Thread t = sender.senderThread(id); detachSession(...); assertThat(t.join(Duration.ofMillis(bound))).isTrue()`. Post-detach `senderThread(id)` returns `null` because the map entry is removed first. |
| B3.4 | claude MEDIUM | Cross-thread coordination in fake-session test: add `CountDownLatch markStalledStarted = new CountDownLatch(1)` and `markStalledFinished = new CountDownLatch(1)`. Fake `sendMessage` blocks on `markStalledStarted.await()` until close releases it. Test thread asserts via `markStalledFinished.await(bound, MILLIS)`. |
| B3.5 | claude→push-back | Strike Task 03a wording that implies a NEW `OutboundSender` ctor parameter for metrics. Verified: `OutboundSender(AdmissionMetrics)` already exists at `:97` and tests use it (`OutboundSenderTest:36`). Just reference the existing field; no DI change. |

## Plan 04 — B-amendments (2 edits)

| ID | Source | Edit |
|---|---|---|
| B4.1 | claude/codex MEDIUM | Replace BL probe `git worktree` workflow with `@Disabled("baseline — confirms BL leak; flip to assertEmpty after fix")` test in main worktree. Same commit flips the annotation off and rewrites assertion to `assertThat(...).isEmpty()`. Equivalent TDD discipline; removes worktree CI surface. |
| B4.2 | claude MEDIUM | A4.3 caller enumeration: add `<read_first>` step in Task 04b: `grep -rn "cleanupByEntityId" src/main src/test` to confirm no caller depends on env-state preservation on the `sessionId == null` early-return branch (notably reconnect-after-mark-dead within grace window). If any does, escalate. |

Plan 04 NON-edit: counter-invariance claim in idempotency Test 3 (opencode) — fold into Plan 04's existing test sketch as "enumerate which `admissionMetrics` counters are expected to stay invariant; assert those specifically" but treat as `<inline>` clarification within B4.1 commit (no separate amendment ID).

## Plan 05 — B-amendments (5 edits)

| ID | Source | Edit |
|---|---|---|
| B5.1 | claude/codex HIGH | Task 05b: `actionResolver.queueAction(P1.id(), ...)` → `actionResolver.queueAction(botRegistry.getSessionByEntity(P1.id()).orElseThrow(), ...)`. `queueAction` is sessionId-keyed (verified at `ActionResolver.java:351`). |
| B5.2 | codex/opencode HIGH | Task 05a payloads: replace `"0"`/`"1"` placeholder with valid numpad chars (e.g., `"6"` east, `"4"` west) and 3-char numpad ballot `"684"` for verb V. Add a post-drive assertion that proves actions were processed (not silently dropped) — e.g., expose `ActionResolver.processedCountForTick(t)` as a package-private accessor or assert a digest invariant that depends on processing. |
| B5.3 | codex/opencode HIGH | Task 05c DrainObserver: (a) add `src/main/java/com/paralife/engine/ActionResolver.java` to `files_modified`; (b) DROP the "prefer seam-free stress test" fallback wording — replace with hard requirement of deterministic `CountDownLatch` interleave; (c) name the seam class `DrainObserverForD20Pin` (file `DrainObserverForD20Pin.java`) so removal is a visible refactor, not a quiet deletion. |
| B5.4 | claude/opencode MEDIUM | Task 05a baseline gate: promote "first-run baseline requires manual operator commit" into plan-level `must_haves.truths`. Add probe assertion ("scenario produced ≥N processed actions, ≥M deaths, ≥K nutrient consumes") that runs BEFORE digest write so a bug-baked digest cannot be committed silently. |
| B5.5 | claude LOW | Whole-suite gate count: change `>= 166` to `>= 180` after all plans land (Plans 01–05 collectively add ~15-20 new tests). Single-line change in Plan 05 frontmatter or per-plan acceptance. |

Plan 05 NON-edit: ES hoist call count disagreement (codex 3, claude/opencode 2) — already resolved by A5.3's awk-scoped check; reviewer disagreement surfaces at execution time, no edit needed. Add a one-line note in Task 05f's `<read_first>` flagging the disagreement so executor doesn't ignore the awk command.

## Wave ordering

Edits sequential, one commit per plan:

```
01 → 02 → 03 → 04 → 05  (commits B1..B5)
```

Justification: amendments don't depend on each other technically (different PLAN.md files), but sequential commits give a clean per-plan diff for the next review pass.

Estimated wall time: 30–45 min for all 5 commits.

## Verification

After all 5 commits:

1. `git log --oneline | head -10` — confirm 5 new `docs(19.1): apply pass-4 review triage...` commits.
2. Re-run `/gsd-review --phase 19.1 --all` — confirm previous HIGH/MEDIUM concerns now closed.
3. Do **NOT** run `/gsd-plan-phase 19.1 --reviews` — this is in-place amendment, not re-plan.
4. If new HIGH findings emerge in the post-amendment review, repeat triage cycle (write `19.1-REVIEWS-pass5-TRIAGE.md`).

## Out of scope

- Code changes — this pass edits PLAN.md files only.
- Dependency-chain restructuring (`depends_on`) — claude flagged over-serialisation but opencode disagreed; treat as conservative drift-mitigation, leave as-is.
- Rewriting any plan's "## Amendments" history — only append new B-amendments.

## Rollback

If a B-amendment introduces inconsistency, revert that single commit. Each plan is isolated in its own commit.

## C-amendments (post pass-2)

Applied 2026-05-04 from pass-2 cross-AI review (`19.1-REVIEWS.md`). Triage decision: ~14 actionable findings against Plans 01–05 are **C-amendments**, not a re-plan — plan structure, dependency ordering, and task breakdown are sound. Findings are factually-anchored (wrong call-counts, ghost API references, missing CLAUDE.md rows, underspecified test seams). Mirror B-amendment cadence: one commit per plan.

### Plan 01 (commit `dbb68c2`)

| ID | Source | Edit |
|---|---|---|
| C1.1 | claude C8 (HIGH) | Spec `buildMinimalEventFrame(char code)` concretely in Task 01d (helper copies one row from `vectors()` MethodSource at `PerceptionCodecRoundTripTest.java:32`, substitutes the event-code char). Acceptance: `grep buildMinimalEventFrame ≥ 2 hits`. |
| C1.2 | claude (LOW reverse-drift gate) | Add reverse-drift gate to Task 01d acceptance: iterate 0..127, count chars for which `validateEventCode` does NOT throw, assert equality with `Event.ALL_CODES.length()`. Catches "codec accepts char not in ALL_CODES" inverse drift. |
| C1.3 | gemini (LOW F1 pollution) | Add `<read_first>` directive on Task 01b (F1 event-driven test): verify `SimpleApplicationEventMulticaster` is synchronous (no `TaskExecutor`) so `publishEvent(new TickEvent(...))` is observed within timing window. |

### Plan 02 (commit `48a44a7`)

| ID | Source | Edit |
|---|---|---|
| C2.1 | claude C11 (MEDIUM) | Pin `ToxinPathGenerator` Option A end-state to fully-static (no ctor): `generatePath` becomes `public static`; both ctors deleted; callers (`EnvironmentEngine.java:405,1615`, `LightningTest.java:78`, `ToxinPathGeneratorTest.java:41,42,53,61,75`) switch to static-call form; the autowired `toxinPathGenerator` field on `EnvironmentEngine` is removed. |
| C2.2 | claude C6 (MEDIUM) | Add comparator-totality justification on B2.1: `(entityId, initialTicks, position.x, position.y)` is total iff no two `PendingGrant` instances share all four fields, justified via `PendingGrant` ctor invariant (one grant per `(entity, buff-source-event, tick)`; distinct buff sources occupy distinct cells or distinct durations). Fallback: append `.thenComparing(System::identityHashCode)` if invariant later weakened. |
| C2.3 | opencode (LOW falsifier wording) | Replace stale falsifier "Two same-seed runs produce different toxin-path placements" with "Same-seed `ToxinPathGenerator.generatePath(w, h, 0xDEADBEEFL)` produces byte-identical `List<Position>` across two calls in same JVM." Aligned with C2.1 static-stateless contract. |

### Plan 03 (commit `a39cd4b`)

| ID | Source | Edit |
|---|---|---|
| C3.1 | claude C7 (MEDIUM) | Pin Task 03c fake-session injection seam concretely. Read `OutboundSender.java` to identify setter/registration entry point or ADD package-private seam in this commit. Rule out `BlockingWebSocketClient` — verified at `src/test/java/com/paralife/websocket/BlockingWebSocketClient.java`, it stalls INBOUND receive thread via latch, NOT outbound `sendMessage`. Add fake-session shape pseudo-code (sendMessage awaits `closeLatch` then throws `IOException`). Tag `human_needed: confirm injection seam shape before execution`. |
| C3.2 | gemini (LOW event multicaster) | Same as C1.3: confirm `SimpleApplicationEventMulticaster` is synchronous in test config so `publishEvent(new TickEvent(...))` is observed in timing window. |
| C3.3 | claude (LOW calibration floor) | Document calibration-floor rationale on B3.4: when `baselineMaxMs == 0`, bound = 20ms; multiplier irrelevant. 20ms adequate for order-of-magnitude deadlock detection (stuck-VT scenario blocks for seconds). Out-of-scope: sub-20ms perf drift. |

### Plan 04 (commit `a55849f`)

| ID | Source | Edit |
|---|---|---|
| C4.1 | claude C5 (MEDIUM) | Promote B4.2 caller audit from advisory to **HARD STOP** in Task 04b `<read_first>`. If any caller of `cleanupByEntityId` relies on env-state preservation across the `sessionId == null` branch (notably `markStalled` grace-window reconnect), do NOT proceed with A4.3 placement change — open a 19.2 follow-up entry and defer. The audit is a gate, not a checkpoint. |
| C4.2 | opencode (LOW visibility doc) | Correct `EnvironmentEngine.fleeing` visibility doc: verified `private`, not package-private as PATTERNS.md / plan-text claimed. Helper accesses field from inside same class so correctness unchanged — only the plan-text wording was wrong. |

### Plan 05 (commit `5861bef`)

| ID | Source | Edit |
|---|---|---|
| C5.1 | claude C1 (HIGH) | Correct entitySnapshot count in `processInteractions` from 2 to 3 (codex pass-4 was right). Verified at relative offsets +5, +130, +213 (method body closes at +440); absolute lines may drift. Updated `<interfaces>`, Task 05f `<read_first>`, action body, acceptance criteria. File-total grep: was 8, becomes 6 post-hoist. |
| C5.2 | claude C2 (HIGH) | Re-author Task 05d (CLAUDE.md tick pipeline). Verified end-state chain: 9 entries from `@Order(1) ResumeTokenRegistry` to `@Order(MAX_VALUE) TickHealthMonitor` (CLAUDE.md was wrong on TickBroadcaster's `@Order`, missing CompositeEnergyDistributor row, fictional PerceptionBroadcaster row). Stragglers at lines 54, 63, 72, 187 reconciled. Out-of-scope: stale `@Order` references in source comments at `ActionResolver.java:394`, `EnvPostActionReconciler.java:14`, `TickHealthMonitor.java:16` — added to plan frontmatter `out_of_scope` list. |
| C5.3 | claude C3 (HIGH) | Replace all 8 `tickEngine.tickOnce()` pseudo-code occurrences with `applicationEventPublisher.publishEvent(new TickEvent(t))` + `awaitAllSessionQueuesDrained()` per `GoldenTraceEquivalenceTest:309-312` canonical pattern. `TickEvent.java:9-13` convenience overload `(long tickNumber)` cited. Add `@Autowired ApplicationEventPublisher` to test class fields. Task 05a `<read_first>` adds C5.3 directive. |
| C5.4 | claude C4 (HIGH) | Spec `DrainObserverForD20Pin` seam concretely on Task 05c. Package-private interface + null-safe setter + null-guard in `drainPendingActions`. `AtomicReference<Map<String, ActionFrame>>` capture pattern in test replaces prior `installDrainObserverForD20Pin(...)` / `capturedActionsForLastTick(sessionId)` helper sketches. |
| C5.5 | claude C10 (MEDIUM) | Tighten 50% observable-footprint floor: removed. Replaced with two-part gate — per verb in {M,E,R,V} >= 1 processed AND total >= 90% of queued. `must_haves.truths` baseline-commit-gate updated to cite C5.5 floors. |
| C5.6 | opencode (LOW A5.4 wording) | Reconcile A5.4 wording on `entitySnapshot(` file-total counts: "3 in-method calls collapse to 1 hoisted; 5 in OTHER methods unchanged". File-total grep: 8 → 6 post-hoist. Aligned with C5.1's count correction. |

## C-amendments — verification

After all 6 commits land:

1. `git log --oneline | head -10` — 6 new C-amendment commits (`dbb68c2`, `48a44a7`, `a39cd4b`, `a55849f`, `5861bef`, this commit).
2. `grep -n "tickOnce" .planning/phases/19.1-*/19.1-05-PLAN.md` — only C5.3 explanatory references (no pseudo-code calls).
3. `grep -n "buildMinimalEventFrame" .planning/phases/19.1-*/19.1-01-PLAN.md` returns ≥ 1.
4. `grep -n "DrainObserverForD20Pin" .planning/phases/19.1-*/19.1-05-PLAN.md` shows the interface signature, not just bare references.
5. `grep -E "verified 2|verified 3" .planning/phases/19.1-*/19.1-05-PLAN.md` — only "verified 3" appears.
6. `19.1-AMENDMENTS-PLAN.md` contains this `## C-amendments (post pass-2)` section.

**No third review pass.** Go straight to `/gsd-execute-phase 19.1`.

If during execution the executor surfaces new factual drift (e.g. a caller dependency in C4.1's audit, or an injection-seam blocker in C3.1), it goes to `19.1-AMENDMENTS-PLAN.md` as a D-amendment — not a re-plan.

## C-amendments — skipped (push-backs, not amending)

- **C9 (varied insertion-order fixtures):** LOW. Comparator (C2.2) closes the structural risk; varied-order fixtures hedge against a class of bug already prevented. Future-hardening note only.
- **C12 (line-number drift preflight):** LOW. Acceptance criteria are grep-by-text, not offset-anchored. `<read_first>` directives that cite line offsets are advisory. Adding preflight overhead per task wastes more time than it saves.

## Deferred (push-back register)

- **C9 — varied insertion-order fixtures for D-04 comparator-totality.** Documented as future hardening if a same-entity multi-grant scenario is later observed in production. Not blocking.
