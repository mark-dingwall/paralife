# Phase 19.1 — pass-2 post-fix-sweep multi-review

You are reviewing the **second** multi-review pass on Paralife Phase 19.1.
Pass-1 ran 2026-05-05 (REF + INL) and returned 3/4 SHIP. The DO-NOT-SHIP
plus a cluster of LOW doc-drift items were addressed in commit `f6da129`
("fix(19.1): pass-1 multi-review follow-up sweep").

Your job: verify the fix landed correctly AND surface any new findings.

## What pass-1 flagged and what was changed

| # | Finding (pass-1) | Reviewer(s) | Severity | Fix in `f6da129` |
|---|---|---|---|---|
| T1 | `PerceptionBroadcaster` doc/comment refs (renamed to `TickBroadcaster` Phase 17) and stale `@Order(100)` references | claude, codex, opencode | LOW (cluster) | Rename + `@Order(50)` sweep across 11 files (CLAUDE.md, TickHealthMonitor, PerceptionCodec, ActionResolver, CompositeRegistry, EntityIds, EnvPostActionReconciler, EnvironmentEngine, SimulationEngine, WorldWebSocketHandler, golden-trace-phase19.json plus 7 test files for related comment fixes) |
| T2 | `SimulationEngine.processDeaths` panic-zone iterates over registry without snapshot+sort before `simRng.nextDouble()` — non-deterministic on JVM hash reorder | codex | MED (DO-NOT-SHIP) | Snapshot to `ArrayList`, sort by `compositeId` (matches CompositeEnergyDistributor.onTick:101-102 fix shape from plan 02), then iterate |
| T3 | `Event.eventHasMagnitude(byte)` hard-coded list — silent foot-gun if a new MAG-bearing event code is added | claude (latent) | LOW | New `Event.MAG_CODES` `byte[]` constant; `eventHasMagnitude` consults it; static-init `assert` block crosschecks `MAG_CODES ⊆ ALL_CODES` and fires `AssertionError` at class load if drift |
| T4 | Golden trace baseline `golden-trace-phase19.json` digests for `trace-sess-9`/`trace-sess-21` — would diverge once T2 lands | n/a (consequence) | n/a | Surgical 2-key refresh; other 24 keys untouched (verify via diff) |

## Pass-1 deferred items — DO NOT re-flag

| Item | Justification |
|---|---|
| `OutboundSenderTest` 5s `join` (vs 2s spec) | 5s strictly weaker than spec 2s — defensible; test still asserts the bound |
| `WorldWebSocketHandler.markDead` "partial leak" | Unreachable per FSM — `ATTR_ENTITY_ID` is always set before `markDead` can reach the cleanup branch |
| `StallRecoveryIntegrationTest` 408 contract adaptation | Pass-1 D-07 close-first detach changed wire contract; test was correctly adapted |

## Specific things to check hard

1. **T2 sort total-order.** `SimulationEngine.java` panic-zone block (~line 1135-1156 before fix; line numbers may shift). `Comparator.comparing(CompositeState::getCompositeId)` — confirm `compositeId` is unique per registry entry (no ties → total order). Confirm sort happens before *every* `simRng` consumption in that method (including `checkPanicZone` ~line 1406 if it also rolls).

2. **T3 invariant gate.** `Event.java:46-56` static-init block. Confirm `AssertionError` fires at JVM class load if `MAG_CODES` drifts from `ALL_CODES` (i.e. any byte in `MAG_CODES` not present in `ALL_CODES`). Confirm `PerceptionCodecRoundTripTest` actually exercises the magnitude-frame branch via the new `MAG_CODES` path (not just the no-mag branch).

3. **T1 sweep completeness.** Grep for any remaining `PerceptionBroadcaster` refs (should be zero) and any `@Order(100)` doc comments referring to TickBroadcaster (should be zero — actual `@Order` is 50).

4. **T4 baseline correctness.** Only two keys (`trace-sess-9`, `trace-sess-21`) changed; other 24 untouched. Confirm via diff that no other digest drifted.

5. **Regression surface.** Did the panic-zone sort introduce a per-tick allocation cost (new `ArrayList` + sort each tick)? Acceptable on a typically-small registry, but worth flagging if registry size could grow O(N) with N composites or if this fires hot.

## Out of scope — do not raise

- DR (`ActionResolver.drainActions` race) — pass-2 M-A deferred
- CR concurrent registration races — Phase 20 deferred
- EN/NS/ES perf rework — Phase 21 deferred
- OS FrameEmitListener for OOB — accepted gap
- Four-layer architecture, WS:entity 1:1, synchronized-monitor contract — invariants
- OutboundSenderTest 5s join — defensible (5s strictly weaker than spec 2s)
- markDead "partial leak" — unreachable per FSM (ATTR_ENTITY_ID always set first)
- Full test suite hangs — known Phase 22 leak audit in flight

## Format

One-line top verdict (`SHIP` / `SHIP WITH FOLLOW-UPS` / `DO NOT SHIP`).

Per-task table (T1..T4): `CONFIRMED` / `CONFIRMED WITH CONCERNS` / `MISSING`.

"Other findings" section for new bugs in adjacent code or test gaps.
Bullet style: `file:line` + finding + smallest fix.
