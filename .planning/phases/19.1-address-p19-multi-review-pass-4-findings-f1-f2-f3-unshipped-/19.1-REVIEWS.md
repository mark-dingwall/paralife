---
phase: 19.1
reviewers: [claude, codex, opencode]
reviewer_models:
  claude: claude-opus-4-7
  codex: gpt-5.5 (reasoning effort high)
  opencode: openrouter/anthropic/claude-opus-4.7
reviewed_at: 2026-05-04T07:01:00Z
plans_reviewed:
  - 19.1-01-PLAN.md
  - 19.1-02-PLAN.md
  - 19.1-03-PLAN.md
  - 19.1-04-PLAN.md
  - 19.1-05-PLAN.md
notes: |
  Re-review post-mitigation (commits 79e463e, 2f2637a, 424c906, 4adfb04, b40bf19).
  Gemini was attempted but excluded — gemini-2.5-pro returned 429 MODEL_CAPACITY_EXHAUSTED;
  gemini-2.5-flash output is known to be low-quality on this project so its review was discarded.
---

# Cross-AI Plan Review — Phase 19.1

## Claude Review

# Phase 19.1 Multi-Plan Cross-Review

## Summary

5 well-grounded plans; exact analogs pinned at line numbers. Post-mitigation revision tightens prior gaps (Event.ALL_CODES single-source-of-truth, ToxinPathGenerator Option A stateless, calibrated tick-bound, BL TDD probe in worktree, public-surface DR-pin). Main residual risks: (1) ToxinPathGenerator API break ripples to existing `ToxinPathGeneratorTest` not flagged in plans, (2) markStalled fake-session emulation accuracy, (3) `depends_on` chain over-serializes plans 02/03/04 that the ROADMAP itself calls "largely independent", (4) line-number drift between plan-write time and Wave 2+ execution time.

---

### Plan 01 — F1/F2/F3 + D-15 codec gate

**Strengths**
- D-15 promoted into Plan 01 alongside F2 spot fix — fixes both symptom AND structural cause in one ship.
- `Event.ALL_CODES` single-source-of-truth eliminates dual-list trap by construction.
- `@MethodSource` driven from `ALL_CODES` (not `@ValueSource`) — test physically references canonical array.
- F1 fix is verbatim copy of `:1259` analog; F3 mirrors `cleanupBot:847-858`.

**Concerns**
- **MEDIUM** — D-15 enforces codec ⊇ Event.ALL_CODES (every Event code round-trips), but NOT inverse. If codec accepts a code not in `ALL_CODES`, no gate fails. Cheap to add: `assertEquals(ALL_CODES.length, codecAcceptedCount)` after the parameterised loop.
- **LOW** — Task 01a test sketch references `buildMinimalEventFrame(code)` helper that "may already exist". Plan 01d also references it. If neither exists, both tasks duplicate the helper.
- **LOW** — Compact-ctor refactor (`Event.java`) preserves "exact exception type + message style" by reading first. Risk if existing tests assert on if-chain message format that differs from new HashSet-derived form. Low because Plan flags it.
- **LOW** — `static { Set<Character> s = new HashSet<>(); ... }` initialisation runs once at class-load — fine for prod but slightly slower test boot. Trivial.

**Suggestions**
- Add reverse gate in `PerceptionCodecRoundTripTest`: assert codec switch length matches `Event.ALL_CODES.length` (catches drift either direction).
- If `buildMinimalEventFrame` not in tree, write it once in `PerceptionCodecRoundTripTest` and have `PerceptionCodecTest` call it via static import.

**Risk: LOW**

---

### Plan 02 — F4/CE/TX determinism

**Strengths**
- Option A (stateless ToxinPathGenerator) preserves single `rng.nextLong()` draw at `:405` — golden-trace stays byte-exact.
- Tiebreaker `.thenComparing(PendingGrant::buffType)` for same-entity multi-grant ticks — total order under all plausible cases.
- A2.3 verifies `composite.getMemberIds()` already deterministic via `CopyOnWriteArrayList` — no inner-shuffle pre-sort needed.
- Falsifier explicitly checks "second `nextLong()` draw" — guards against accidentally double-drawing.

**Concerns**
- **MEDIUM** — `PendingGrant::buffType` may not exist as record accessor. Plan flags "verify actual fields" but doesn't enumerate alternatives concretely. If only `entityId` is unique, the tiebreaker line silently no-ops; same-entity multi-grant tick still non-deterministic.
- **MEDIUM** — Existing `ToxinPathGeneratorTest` is not in `files_modified`. Option A deletes both ctors AND the `final Random rng` field. Any test instantiating `new ToxinPathGenerator(seededRng)` breaks compilation. Plan should add `ToxinPathGeneratorTest.java` to `files_modified` and update assertions.
- **MEDIUM** — `forceSpawnToxinForTest:1615` — Plan says "if the test path doesn't already draw a seed, use a deterministic per-call literal (e.g., `0L` or a tick-derived value)". Picking `0L` makes every forced-spawn use identical path → may collapse test-fixture diversity. Tick-derived (`tickNumber * 31 + 17` or similar) is safer.
- **LOW** — Task 02d "write test → git stash → re-apply" workflow is fragile. Real flow: write test against fixed tree, ship together. The stash dance is informational not load-bearing.

**Suggestions**
- Read `PendingGrant.java` BEFORE writing the comparator; if no second discriminator field, add the comparator over `(entityId, System.identityHashCode)` as last-resort tiebreaker — at least makes order stable per-JVM. Or sort `infections.entrySet()` upstream (the original CHM iteration source).
- Add `src/test/java/com/paralife/engine/ToxinPathGeneratorTest.java` to `files_modified` in Plan 02 frontmatter; document the API migration explicitly.

**Risk: MEDIUM**

---

### Plan 03 — markStalled + observability

**Strengths**
- New `detachSession(WebSocketSession, CloseStatus)` overload + 1-line delegate preserves every existing caller.
- Calibrated `baselineMax * 5 + 20ms` bound — survives CI jitter, catches order-of-magnitude regressions.
- A3.4 amendment — fake session whose `sendMessage` blocks until `close()` properly emulates Jetty IOException-out-of-blocked-write path (rejected the FrameEmitListener-latch design that wouldn't reproduce production unblock).
- Trailing redundant `session.close(SERVICE_RESTARTED)` removed (A3.2/A3.3) — correct since new overload already closes with that status.

**Concerns**
- **HIGH** — `tickEngine.tickOnce()` referenced in test — verify this method exists and exposes deterministic single-tick driving. CLAUDE.md mentions `paralife.tick.auto-start=false` but doesn't document a public `tickOnce()` API. If absent, test needs a different driver (publish `TickEvent` directly via `ApplicationEventPublisher`).
- **MEDIUM** — `depends_on: [19.1-01]` is suspicious. F1/F2/F3 touch SimulationEngine, PerceptionCodec, WorldWebSocketHandler.markDead — none overlap with markStalled, OutboundSender, AdmissionMetrics. Why serialize?
- **MEDIUM** — `OutboundSender` may not have an `AdmissionMetrics metrics` field today. Plan says "autowire it via constructor injection following the existing DI pattern" — adding a ctor parameter ripples to every caller of `new OutboundSender(...)` (production wiring + every test that hand-constructs a sender). Should enumerate test impact in plan.
- **MEDIUM** — Fake-session test sketch invokes `markStalled` "from a separate thread" but the test class isn't `@Transactional` or otherwise scoped — race conditions between the WS-thread-side fake invocation and the tick-thread-side observation aren't deterministic. Needs explicit barrier/latch.
- **LOW** — D-13 test uses `Thread.join(Duration)` (Java 19+). Project is Java 21 — fine, but worth confirming.

**Suggestions**
- Read `TickEngine.java` BEFORE writing Task 03c; if no `tickOnce()`, document the alternative driving strategy.
- Drop `depends_on: [19.1-01]` unless concrete file overlap is shown.
- If adding `metrics` ctor param to `OutboundSender`, list every caller in `<read_first>` section so the executor doesn't miss test fixtures.

**Risk: MEDIUM**

---

### Plan 04 — AM/BL/FL leaks

**Strengths**
- BL TDD probe in isolated git worktree — proves leak exists pre-fix instead of trusting grep-only review (TRIAGE confidence note explicitly admits BL was grep-verified).
- A4.1 verifies `clearInfectionOnDeath` is pure-removal — discards claude C-06 rename concern as verified-unnecessary.
- A4.3 placement — env cleanup BEFORE `sessionId == null` early-return — closes leak on entityId-only paths (admission abort, post-markDead).
- Idempotent helpers (`fleeingRemove`, `remapBucketTags`, `clearInfectionOnDeath`) all tolerant of double-invocation.

**Concerns**
- **MEDIUM** — A4.3 changes call ordering for `cleanupByEntityId` paths that previously early-returned BEFORE any env touch. If any other caller relies on env-state being preserved on the early-return branch (e.g., reconnect-after-mark-dead within grace window), this fix breaks reconnect. Plan asserts "no such dependency exists" but doesn't enumerate callers. Run `grep -rn "cleanupByEntityId" src/main` first.
- **MEDIUM** — `depends_on: [19.1-01, 19.1-02, 19.1-03]` — Plan 04 touches AdmissionMetrics, WorldWebSocketHandler.cleanupBot/cleanupByEntityId, EnvironmentEngine.fleeingRemove, SimulationEngine composite sites. None of these collide with Plan 01-03 file regions. Over-serialised.
- **LOW** — BL probe worktree dance (`git worktree add ../paralife-probe`) adds CI flake surface. Probe could just be a test that runs against pre-fix tree as a `@Disabled("baseline only — confirms leak; flip to assertEmpty after fix")` annotation, then unflag after fix lands. Same outcome, no worktree mgmt.
- **LOW** — `transferFleeing` at composite formation auto-expires within seconds. Tests aren't included for FL functional verification (plan acknowledges and defers). If state-machine logic ever changes such that FLEEING becomes long-lived, regression won't be caught.

**Suggestions**
- Add `grep -rn "cleanupByEntityId" src/main src/test` as a `<read_first>` step in Task 04b to enumerate caller assumptions before re-ordering env cleanup.
- Drop `depends_on: [19.1-01, 19.1-02, 19.1-03]`. Plans 01–04 are independent per ROADMAP (Plan 01 says so explicitly).
- Replace worktree probe with `@Disabled` baseline test in same file; flip to `assertThat(...).isEmpty()` after fix lands.

**Risk: LOW-MEDIUM**

---

### Plan 05 — Test coverage + polish (merged 05/06)

**Strengths**
- DR-pin via package-private `DrainObserver` test-only seam carries explicit "do not remove without amending the contract" comment — protects intent.
- A5.3/A5.4 — D-19 hoist scope corrected (only 2 calls in `processInteractions`, not 4 as PATTERNS.md claimed). Golden-trace gate added before/after hoist.
- A5.6 — set-equality `diff` for `@Order` doc accuracy catches both fictional classes AND wrong numbers (count-only check would pass-by-accident).
- DR-pin pinned via PUBLIC SURFACE (`tickEngine.tickOnce()` + observer) instead of reflection on `private drainActions()`.

**Concerns**
- **MEDIUM** — Task 05a baseline JSON requires manual review + commit on first run. Not autonomous-completable. Should be flagged in plan-level `must_haves` as needing operator confirmation, not just left in `<action>` step 3.
- **MEDIUM** — `actionResolver.queueAction(P1.id(), ...)` in Task 05b — but interfaces section confirms `queueAction` is keyed by **sessionId, NOT entityId**. Task 05b uses `P1.id()` (an entityId from `seedParticle`) directly. This is wrong — must translate via `botRegistry.getSessionByEntity(P1.id()).orElseThrow()`. Same as Task 05a got right.
- **MEDIUM** — Task 05c DrainObserver — adding test-only package-private interface to ActionResolver couples prod to test. Plan accepts but doesn't show what `installDrainObserver` actually does in the production class. Risk: executor adds a `setDrainObserver(...)` method that's a permanent prod artefact.
- **LOW** — `grep -oE '@Order\([0-9]+\)' src/main/java/com/paralife/**/*.java` won't expand `**` in non-extglob shells. Acceptance criterion `bash` block uses `--include` correctly via `grep -rh` — works. But the proof in `must_haves.proofs` uses the un-recursive form. Inconsistent.
- **LOW** — Task 05a verb sequencing `tick % 4` always picks M on tick 0 — for entities not yet at "ready to act" state, M may fail validation. Risk of digest noise from rejected actions. Better to drive a warm-up phase first.
- **LOW** — Task 05f hoist invalidates the existing `golden-trace-phase19.json` digest IF the inline calls had observable side effects (snapshot allocation timing). Plan says "no behaviour change" but the calls are stateful (`liveEntityRegistry.snapshot()` returns a fresh `ArrayList` each time — same content, different identity). Fine for golden-trace digest IF digest is content-based, broken IF identity-based. Verify before hoist.

**Suggestions**
- Move "first-run baseline commit" requirement to Plan 05 frontmatter `must_haves.truths` and the SUMMARY output, not buried in `<action>`.
- Fix Task 05b to translate entityId→sessionId via `BotRegistry.getSessionByEntity(...)` before `queueAction`.
- Specify `DrainObserver` interface concretely in Task 05c — package-private, no setter on `ActionResolver`, observer registered via `@TestConfiguration` bean post-processor or constructor injection.
- Run a 5-tick warm-up in `GoldenTraceWithActionsTest.driveScenario` before queueing M/E/R/V — gives entities a chance to enter eligible-for-action states.

**Risk: LOW**

---

### Cross-Plan Concerns

#### Dependency over-serialization (MEDIUM)
ROADMAP §"Sequencing" says **"Plans 01, 02, 03, 04 are largely independent (different files / different invariants); they can run in parallel waves."** YAML frontmatter contradicts:
- Plan 03: `depends_on: [19.1-01]`
- Plan 04: `depends_on: [19.1-01, 19.1-02, 19.1-03]`

Justification absent. Real overlaps:
- Plan 01 → Plan 04: both touch `WorldWebSocketHandler` (Plan 01 modifies `markDead:944`, Plan 04 modifies `cleanupBot:855-866` + `cleanupByEntityId`). Different methods, no merge conflict expected.
- Plan 03 → Plan 04: both touch `WorldWebSocketHandler` (Plan 03 modifies `markStalled:728`, Plan 04 modifies cleanup paths). Different methods.

Recommendation: drop deps. Run 01/02/03/04 as Wave 1, Plan 05 as Wave 2. Saves wall time on a 5-plan phase.

#### Line-number drift (MEDIUM)
PATTERNS.md/CONTEXT.md were written 2026-05-04 against HEAD `d976975`. Plans pin numerous line numbers (`SimulationEngine.java:888`, `:763`, `:1272`, `WorldWebSocketHandler.java:728`, `:944`, etc.). Plan 01 lands first → modifies `:888` and `:944`. Subsequent plans referencing those files may find lines shifted by ±10.

Recommendation: every Wave-2+ task should re-verify line numbers via grep before applying changes. Acceptance criteria already do this in many places (`grep -n "remapEntity"`). Make it consistent.

#### Test count baseline (LOW)
STATE.md says "166 tests". Plans collectively add ~15-20 new tests. Whole-suite gate should be `>= 180` not `>= 166` after all plans land.

#### Whole-suite gate per plan (LOW)
Each plan re-runs `./gradlew test` at completion. For 5 plans = 5× full-suite cost. Acceptable for correctness but inefficient.

---

### Phase Goal Coverage vs Roadmap Success Criteria

| Roadmap SC | Plan | Coverage |
|---|---|---|
| F1 fixed: composite formation uses `remapEntity`; drainDeaths empty | 01 Task 01b | ✅ |
| F2 fixed: codec accepts `'B'`; round-trip gate ships in same plan | 01 Task 01a + 01d | ✅ (D-15 promoted) |
| F3 fixed: markDead clearActive + ATTR_RESUME_TOKEN removal | 01 Task 01c | ✅ |
| F4/CE/TX byte-exact across 2 same-seed runs | 02 all tasks | ✅ |
| MS markStalled tick-bound under stuck-VT | 03 Task 03c | ⚠️ depends on `tickOnce()` existing |
| BotClient disconnect leaves zero residuals in 4 maps | 04 Task 04b | ✅ |
| DR-pin shipped | 05 Task 05c | ✅ |
| GoldenTraceWithActionsTest pinned baseline | 05 Task 05a | ⚠️ first-run baseline needs human commit |
| LiveEntityRegistryInvariantTest movement + reproduction | 05 Task 05b | ⚠️ entityId/sessionId bug in queueAction call |
| CLAUDE.md @Order matches grep | 05 Task 05d | ✅ |
| 166-test suite stays green | all | ✅ |

### Overall Risk: **MEDIUM**

Justification: phase scope is correct, plans are well-grounded, prior mitigations have closed real gaps. Residual MEDIUM risks: (1) `ToxinPathGenerator` API break without explicit existing-test update path, (2) `markStalled` integration test depends on `tickEngine.tickOnce()` existing as a public driver, (3) Task 05b uses entityId where queueAction expects sessionId, (4) `OutboundSender` ctor change for metrics injection ripples to test fixtures.

Pre-execution checklist (before Wave 1):
1. Verify `PendingGrant.java` fields → confirm tiebreaker key for Plan 02 D-04.
2. Verify `TickEngine.tickOnce()` exists → confirm Plan 03 Task 03c viable.
3. List `cleanupByEntityId` callers → confirm Plan 04 A4.3 placement-change is safe.
4. Add `ToxinPathGeneratorTest.java` to Plan 02 `files_modified`.
5. Drop `depends_on` chains on Plans 03/04 unless concrete overlap shown.

---

## Codex Review

### Overall Summary

The post-mitigation plans are much stronger than a raw fix list: they tie each defect to a concrete acceptance signal, include regression tests, and mostly preserve the phase boundary. The main remaining risk is not intent, but executability: several tasks still contain pseudo-code that conflicts with current source signatures or test seams. The highest-risk areas are Plan 02's determinism tests, Plan 03's close-aware detach test design, and Plan 05's action/golden-trace and `ActionResolver` drain-semantics tests.

### 19.1-01-PLAN.md

**Summary**
Plan 01 targets the right blockers and pairing F2 with a codec drift gate is sound. Risk is moderate because a few test/file assumptions do not match the current tree.

**Strengths**
- Correctly identifies the three real HIGH fixes: codec `'B'`, composite `remapEntity`, and `markDead` token cleanup.
- Moving the codec convention gate next to the F2 fix is a good sequencing decision.
- F1 and F3 source changes have clear in-tree analogs.

**Concerns**
- **HIGH:** `src/test/java/com/paralife/codec/PerceptionCodecTest.java` does not exist in the current tree; codec tests currently include `PerceptionCodecRoundTripTest` and `PerceptionCodecErrorTest`.
- **MEDIUM:** `Event.ALL_CODES` as `public static final char[]` is mutable. External tests or code can mutate the array and corrupt the canonical set.
- **MEDIUM:** `updateBotRegistryForFormation` is private in [SimulationEngine.java](src/main/java/com/paralife/engine/SimulationEngine.java:881), so the "direct call" unit-test option is not viable without a production seam.
- **LOW:** Success criteria mention a tight diff size but the plan already lists more files due to D-15.

**Suggestions**
- Put the spot codec test in `PerceptionCodecRoundTripTest` or `PerceptionCodecErrorTest`, or explicitly create `PerceptionCodecTest`.
- Use `public static final String ALL_CODES = "EAHTMLNSDB..."` or expose a defensive-copy accessor instead of a mutable array.
- Specify the F1 test as event-driven/Spring-driven, not direct private-method invocation.

**Risk Assessment: MEDIUM**

### 19.1-02-PLAN.md

**Summary**
The source-level determinism strategy is mostly correct: stable sort before RNG consumption, stable composite iteration, and per-call toxin path seeding. The plan's test design is the weak point.

**Strengths**
- Correctly preserves the existing toxin seed draw at [EnvironmentEngine.java](src/main/java/com/paralife/engine/EnvironmentEngine.java:404), avoiding golden-trace drift.
- Stateless `ToxinPathGenerator.generatePath(..., seed)` is cleaner than trying to reset a final RNG field in [ToxinPathGenerator.java](src/main/java/com/paralife/engine/ToxinPathGenerator.java:29).
- Sorting `CompositeRegistry.getAll()` before `processCompositeEnergy` directly addresses CHM iteration nondeterminism.

**Concerns**
- **HIGH:** `PendingGrant::buffType` does not exist. The actual record is `PendingGrant(String entityId, int initialTicks, Entity capturedOccupant, Position position)` in [EnvCleanupHooksBean.java](src/main/java/com/paralife/engine/EnvCleanupHooksBean.java:69).
- **HIGH:** The proposed `EnvironmentDeterminismTest` multi-composite fixture conflicts with that test class's current env-only/particle-only contract.
- **HIGH:** "3+ toxin-path spawn events on the same tick" conflicts with `activeToxin != null` guarding a single active toxin.
- **MEDIUM:** Tests described as "must fail pre-fix" are hard to guarantee with ordinary same-seed runs unless insertion order is deliberately varied.

**Suggestions**
- Sort pending grants by `entityId`, then `position.x`, `position.y`, `initialTicks`, and stable occupant id/type where available.
- Put composite RNG determinism in `CompositeEnergyDistributorTest` or a new focused test, not the env-only harness.
- Test toxin determinism by calling `generatePath` with explicit seeds or by sequential forced spawns with reset/clear, not simultaneous active toxins.

**Risk Assessment: HIGH**

### 19.1-03-PLAN.md

**Summary**
The plan correctly targets the markStalled deadlock, but the detailed implementation has compile and observability risks around `sendOutOfBand` and sender-thread assertions.

**Strengths**
- The close-first strategy is the right way to unblock a stuck Jetty write.
- Adding a `CloseStatus` overload avoids losing `SERVICE_RESTARTED` semantics.
- The detach-timeout metric is a useful operational signal.

**Concerns**
- **HIGH:** `sendOutOfBand` catches `IOException` internally and does not throw it, so wrapping the call with `catch (IOException | IllegalStateException)` in `markStalled` will not compile unless the method signature changes.
- **HIGH:** `OutboundSender.detachSession(WebSocketSession)` removes the thread from `senderThreads` before the proposed post-detach `senderThread(sessionId)` assertion, so the test may get `null` and fail to prove VT exit.
- **MEDIUM:** The new `detachSession(WebSocketSession, CloseStatus)` is specified as non-joining, but the plan still relies on proving prompt VT exit. Capture the thread before removal or add a proper test hook.
- **LOW:** `sendOutOfBand` already has an `isOpen()` guard, so after close-aware detach the OOB 408 will usually be skipped. That is acceptable, but the plan should state it plainly.

**Suggestions**
- Either change `sendOutOfBand` to return a boolean and catch internally, or catch only unchecked exceptions at the call site.
- In tests, capture `Thread t = sender.senderThread(id)` before invoking detach, then assert it exits.
- Make `MarkStalledTickBoundIntegrationTest` verify both tick bound and that the blocked fake `sendMessage` was released by close.

**Risk Assessment: HIGH**

### 19.1-04-PLAN.md

**Summary**
Plan 04 is well-scoped lifecycle cleanup work with good symmetry across remap, disconnect, and identity transitions. The main risk is over-heavy TDD probe workflow and under-testing FLEEING transfer.

**Strengths**
- `AdmissionMetrics.remapBucketTags` mirrors existing release/lookup patterns cleanly.
- Cleanup is correctly placed before `cleanupByEntityId`'s session-null early return.
- Adding an explicit `fleeingRemove` helper avoids reaching into package-private state.

**Concerns**
- **MEDIUM:** The separate `git worktree` pre-fix probe is heavy and may not fit constrained execution environments. It also risks distracting from the actual fix.
- **MEDIUM:** FL is accepted mostly by grep count. Given identity-transition bugs are the theme, a small functional FLEEING transfer test would be more valuable.
- **MEDIUM:** Cleanup tests that expect AdmissionMetrics bucket tags to empty must simulate a fully admitted/registered session with `ATTR_ENTITY_TYPE`; otherwise cleanup can legitimately skip metric release.
- **LOW:** `grep -c transferFleeing` is a brittle acceptance signal because existing comments or future helper names can skew it.

**Suggestions**
- Replace the worktree probe with a normal red/green test committed in final form.
- Add a focused composite formation/dissolve FLEEING transfer assertion.
- Specify exact test setup for admission metrics: call the production active-bucket path or seed bucket tags through a helper.

**Risk Assessment: MEDIUM**

### 19.1-05-PLAN.md

**Summary**
Plan 05 has good goals but is the most overloaded and least executable as written. It combines golden trace, lifecycle invariants, drain semantics, docs, Javadocs, and a refactor; several task sketches conflict with current APIs.

**Strengths**
- Adding `GoldenTraceWithActionsTest` is important; the current golden trace does not drive actions.
- Pinning the declined `ActionResolver` race contract is a good way to stop recurring false-positive reviews.
- OD/FE/FD are low-risk documentation fixes.
- ES hoist is cheap and relevant to scale work.

**Concerns**
- **HIGH:** `GoldenTraceWithActionsTest` pseudo-code calls `entitySnapshot()`, but the cloned test has no such helper; it should use `liveEntityRegistry.snapshot()`.
- **HIGH:** The action args shown are likely invalid. `Direction.fromNumpad` expects numpad chars, so `"0"` is not a valid move direction in [ActionResolver.java](src/main/java/com/paralife/engine/ActionResolver.java:543). Verb `V` expects raw 3-char numpad ballots, not `"0"`/`"1"`.
- **HIGH:** `ActionResolver.drainActions()` is private in [ActionResolver.java](src/main/java/com/paralife/engine/ActionResolver.java:408). The plan says public-surface-only, then describes a latch at an internal snapshot/remove point that is not observable without modifying production code. `files_modified` omits `ActionResolver.java`.
- **MEDIUM:** LiveEntityRegistry movement/reproduction pseudo-code queues by entity id, but `queueAction` is keyed by session id.
- **MEDIUM:** ES task says `processInteractions` has 2 snapshot calls, but current code has 3 inside the method at [SimulationEngine.java](src/main/java/com/paralife/engine/SimulationEngine.java:411), `:540`, and `:623`.
- **LOW:** The CLAUDE.md `@Order` set-equality command is likely too broad because docs contain historical/conceptual `@Order` mentions outside the tick pipeline.

**Suggestions**
- Split Plan 05 or at least make DR-pin its own small plan with an explicit production test seam decision.
- Use valid action payloads: movement/reproduction numpad chars like `"6"`/`"4"`, and vote ballots like `"684"`.
- Update ES acceptance to expect one hoisted snapshot replacing all 3 method-local calls.
- Scope the CLAUDE.md verification to the tick-pipeline section, not every `@Order` mention in the file.

**Risk Assessment: HIGH**

### Phase-Level Risk

**Overall risk: MEDIUM-HIGH.**
The plans cover the roadmap success criteria for Phase 19.1 in intent: F1/F2/F3, determinism, markStalled, lifecycle leaks, GT/LM/DR coverage, docs, and the full test gate. The remaining risk is execution fidelity. Before implementation, I would tighten Plan 02 tests, Plan 03 sender-thread/OOB details, and Plan 05 action/drain-seam details. These are the places most likely to produce failed builds or misleading green tests.

---

## OpenCode Review

# Phase 19.1 Cross-AI Plan Review

## Overall Assessment

This is a **well-structured hardening phase** that addresses a concrete problem: fixes that a pass-2 VALIDATED projection falsely marked as shipped, plus new findings from pass-4. The plans are unusually concrete — nearly every change has a pinned line number and an in-tree analog, which dramatically reduces execution risk. Post-mitigation, the plans have absorbed substantive feedback (Option A stateless `ToxinPathGenerator`, calibrated tick-bound assertion, public-surface-only DR-pin, awk-scoped grep for ES hoist). The remaining concerns are mostly about test realism, a few lingering edge cases, and one genuine ordering risk between Plans 03 and 05.

Risk level: **LOW-MEDIUM**. Execution is bounded by the pinned-line approach; the main residual risk is test brittleness and one RNG-semantics subtlety in Plan 02.

---

### Plan 19.1-01: Unshipped fix verification & landing

**Summary**
Three symptom-level fixes (F1/F2/F3) plus one structural defense (D-15 `Event.ALL_CODES`). Each fix is an exact-match analog copy at a pinned line. The D-15 structural pivot is the strongest element — it converts the dual-list maintenance trap that caused F2 into a single-source-of-truth that is physically impossible to drift.

**Strengths**
- D-15 pairing with F2 is excellent design: prevents the same class of bug recurring.
- `@MethodSource`-driven round-trip test (not `@ValueSource`) correctly eliminates the dual-list trap — a `@ValueSource` fix would just move the trap into the test.
- F1 fix reuses `remapEntity` (existing working sibling at `:732`, `:1259`), not a new abstraction.
- F3 fix is a verbatim mirror of `cleanupBot:847-858`, the canonical analog.
- Acceptance criteria are falsifiable: `grep -c` counts, specific method references.

**Concerns**
- **MEDIUM — Task 01a test helper dependency**: the F2 test says "construct minimal frame containing 'B' event — see PerceptionCodecRoundTripTest for vector helpers" but that helper may not exist or may not be callable from `PerceptionCodecTest`. The fallback "drive via `PerceptionCodec.decode` of a synthetic v-block" requires hand-crafting wire format — risk of test being tautological or wrong.
- **MEDIUM — Task 01d `buildMinimalEventFrame` handwave**: "The `buildMinimalEventFrame` helper may already exist in the class (verify via grep for `private String build`)". If it doesn't exist, the executor must hand-craft wire-format frames for every event code — including ones with magnitude encoding differences. Underspecified.
- **LOW — F1 "apply to both per-member calls"**: Task 01b says "Apply this fix to BOTH per-member calls if `updateBotRegistryForFormation` iterates over both bp members (verify by reading the full method body)." Composite formation involves 2 bondedPair inputs → 2 member outputs → likely 2 remap calls per formation event. The test `drainDeaths().isEmpty()` only catches this if BOTH are fixed.
- **LOW — Event.ALL_CODES ordering**: the array is ordered as written. If a future contributor inserts at the beginning, test iteration order changes. Mitigation: sort the array when iterating in test, or pin lexicographic order in ALL_CODES itself.

**Suggestions**
- Make the F2 test explicitly test `validateEventCode` directly — if it's private, promote to package-private with `@VisibleForTesting` rather than driving it indirectly through `decode`. The indirect path risks false positives.
- In Task 01d, verify `buildMinimalEventFrame` exists BEFORE authoring the test; if it doesn't, add a sub-task to build it. Don't leave it as "verify via grep" — make it explicit.
- Add an explicit assertion in Task 01b that `updateBotRegistryForFormation` is called exactly N times per formation, where N matches the expected remap count (guards against "fixed one of two" bugs).

**Risk: LOW**

---

### Plan 19.1-02: Determinism hardening

**Summary**
Three RNG-determinism fixes (F4 sort pendingGrants, CE sort composite snapshot, TX stateless ToxinPathGenerator) plus extended determinism test. The Option A decision (stateless generator with per-call seed) is correct — the verified `final` Random field made Option B impossible without removing `final`. The plumbing reuses the seed already drawn at `:405`, preserving golden-trace byte equivalence.

**Strengths**
- The "reuse existing `rng.nextLong()` at :405, don't draw twice" constraint is stated explicitly and with a clear falsifier. This is exactly the kind of subtle determinism trap that plans usually miss.
- Comparator tiebreaker amendment for D-04 (`.thenComparing(PendingGrant::buffType)`) correctly handles same-entity multi-grant ties.
- Acceptance criteria include `GoldenTraceEquivalenceTest` passing — this is the load-bearing check that the RNG draw count hasn't changed.
- A2.3 note explicitly discards the claude C-10 inner-shuffle concern as verified-invalid (memberIds is CopyOnWriteArrayList).

**Concerns**
- **MEDIUM — D-04 tiebreaker field existence**: "Verify the actual `PendingGrant` record fields when amending — if `buffType` is not a member, use the next stable field". The plan hedges on whether `buffType` exists. Without verification, executor may pick a non-stable field. Should be pinned before execution.
- **MEDIUM — Task 02d red-then-green workflow**: "These tests MUST currently FAIL on a tree without Tasks 02a/02b/02c (red), then GREEN once those land. Since this plan ships them together, the test ordering in the task list is informational — implementers can write the test first, watch it fail against an unmodified tree (e.g., a `git stash`), then re-apply 02a-c". This red-before-green workflow is asserted but not enforced. Without a process gate, executor may write test against post-fix tree, defeating the regression guarantee.
- **LOW — D-06 `forceSpawnToxinForTest` seed source**: "If the test path doesn't already draw a seed, use a deterministic per-call literal (e.g., `0L` or a tick-derived value)". A literal `0L` shared across calls gives repeatable but identical paths — may mask tests that should see variation. Needs a concrete policy.
- **LOW — D-05 composite iteration**: the outer fix is correct, but `processCompositeEnergy` itself may call into other CHM-backed registries (buff, infection). If any of those consume seeded RNG in CHM iteration order, the fix is incomplete. Needs a one-line statement of completeness.

**Suggestions**
- Pre-verify the `PendingGrant` record shape BEFORE executing Task 02a; pin the tiebreaker field explicitly.
- Enforce the red-then-green workflow by requiring Task 02d's commit to reference the pre-fix stash commit hash in its message, OR run Task 02d as a separate wave that can only execute after 02a-c commits exist.
- For D-06 `forceSpawnToxinForTest`, pin the seed source explicitly (e.g., `TEST_TOXIN_SEED = 0xDEADBEEFL`) rather than leaving it as a discretion call.
- Add an explicit statement: "D-05's outer sort is sufficient because `processCompositeEnergy` does not consume RNG in CHM iteration order (verified: memberIds is CoWAL, no other CHM accesses)."

**Risk: LOW-MEDIUM**

---

### Plan 19.1-03: Backpressure correctness

**Summary**
MS fix (markStalled close-aware detach) + new `detachSession(WebSocketSession, CloseStatus)` overload + EL test extension + L1 metric. Introduces a genuinely new API overload and a new integration test with a calibrated bound. The calibrated bound (baseline*5 + 20ms floor) and fake-session design (not FrameEmitListener-latch) are both post-mitigation improvements.

**Strengths**
- The A3.4 amendment explaining why FrameEmitListener-latch is wrong (blocks listener inside monitor, not transport) is excellent — demonstrates understanding of the actual unblock path.
- The calibrated bound (A3.5 amendment) replaces the brittle fixed 50ms with runtime-computed baseline, exactly right for CI variability.
- The new overload design preserves every existing caller via 1-line delegate — zero regression risk for `detachSession(WebSocketSession)` users.
- Threat T-19.1-06 correctly identifies this as a DoS surface (slow client can stall tick loop).

**Concerns**
- **HIGH — Dependency ordering with Plan 05 OD task**: Plan 03 Task 03b adds a paragraph to CLAUDE.md's "Outbound concurrency" section. Plan 05 Task 05d edits CLAUDE.md's "Tick pipeline" section. These sections are adjacent and both execute against CLAUDE.md. The `depends_on` metadata shows Plan 05 depends on Plan 03 — good — but the acceptance criterion in 05d ("The D-07 markStalled paragraph is still present") only catches complete removal, not partial corruption from a merge/edit conflict. Wave ordering mitigates this, but worth flagging.
- **MEDIUM — Task 03c test harness complexity**: "Wire the fake into the handler (the existing test scaffolding likely has a session-registration entry point — find via grep on StallRecoveryIntegrationTest)". The fake-session wiring is underspecified. If no clean injection point exists, the test may require production-API changes (new `@TestConfiguration` seams) that are out of scope.
- **MEDIUM — `metrics != null` guard in Task 03a Step 3**: `if (metrics != null) metrics.incDetachTimeout();`. If `OutboundSender` normally has non-null `metrics` injection but tests sometimes construct it without, the null guard is correct — but the guard's presence suggests `metrics` is nullable, which should be documented on the field.
- **LOW — `Thread.join(Duration)` is Java 19+**: the plan says "use it directly". Verify project uses Java 19+ (CLAUDE.md says Java 21, so this is fine — but flag it explicitly in the task).
- **LOW — Removing the trailing `session.close`**: Task 03b removes `try { session.close(CloseStatus.SERVICE_RESTARTED); } catch (IOException ignored) {}`. This is correct given the new overload already closes with SERVICE_RESTARTED — but worth verifying that NO other markStalled caller path (or other code) depends on that explicit close firing last.

**Suggestions**
- Elevate the CLAUDE.md edit to an explicit atomic-merge check: after Plan 05 Task 05d runs, re-grep for the D-07 paragraph content (not just `markStalled`) to confirm byte-level preservation.
- Pre-verify the fake-session injection point in `StallRecoveryIntegrationTest` scaffolding BEFORE Task 03c executes. If injection requires new production seams, escalate scope.
- Add a one-line assertion to Task 03b that the markStalled caller graph has exactly one caller of `session.close(SERVICE_RESTARTED)` before the change, zero after.
- Document `metrics` nullability policy on the field declaration as part of Task 03a.

**Risk: MEDIUM** (driven by Task 03c harness complexity and the CLAUDE.md dependency)

---

### Plan 19.1-04: Lifecycle leaks

**Summary**
AM remap helper + BL disconnect cleanup + FL composite-boundary FLEEING transfer. The BL probe workflow (git-worktree pre-fix failure check) is defensive and correct — BL is the single-source gemini finding and confidence is stated as "grep only, not full-trace". Verifying the leak exists pre-fix avoids shipping a fix to a non-bug.

**Strengths**
- The Step 0 BL probe (separate worktree, test must fail on pre-fix tree, STOP if it passes) is excellent TDD discipline. This is exactly how to handle a MEDIUM-confidence finding.
- A4.1 `clearInfectionOnDeath` purity verification (idempotent, no side-effects) is exactly the right pre-check before calling it from a new path.
- A4.3 placement amendment (env-state cleanup BEFORE `sessionId == null` early-return in `cleanupByEntityId`) catches a real bug the naive "add alongside unregister" would miss.
- Test 3 idempotency check (second cleanup is a true no-op) pins the contract explicitly.

**Concerns**
- **MEDIUM — Git worktree cleanup on probe failure**: the probe workflow says "Remove the probe worktree: `git worktree remove ../paralife-probe`" on the STOP path, but doesn't specify what to do with the probe test file authored there. If the executor returns to main worktree without copying the test, the work is lost. Needs explicit copy-out instruction.
- **MEDIUM — Test 3 (idempotency) counter invariance claim**: "assert no `BuffRegistry` / infection-map / FLEEING leftovers AND no exceptions thrown AND `admissionMetrics` counters unchanged on the second call". But `clearInfectionOnDeath` idempotency doesn't guarantee metric invariance — if any of the registry calls emit a "cleanup performed" counter, the second call may increment it even when the maps are already empty. Needs verification of which specific counters are expected to stay unchanged.
- **LOW — Task 04c (D-10) variable name hedging**: "Variable names (`cf`, `bp1`, `bp2`, `memberId1`, `memberId2`) MAY differ in the actual code — use whatever the surrounding loop iterates". Pinned line numbers but unpinned variable names — executor must read carefully.
- **LOW — FL auto-expiry rationalization**: "Functional verification deferred to existing FLEEING tests (auto-expiry behaviour means a dedicated regression test would expire its assertion within seconds and is not cost-effective)". Accepting this, but it means FL regressions are only caught by integration tests — not by a dedicated unit gate. If FLEEING auto-expiry window is configurable, a test with a long-enough window is cheap.

**Suggestions**
- In Step 0 of Task 04b, make the test-file copy-out explicit: "commit the probe test in the worktree, note the commit hash, then cherry-pick/copy into the main worktree". Or simpler: author the test in the main worktree first, then test it against a `git stash` of the production changes.
- Enumerate the specific `admissionMetrics` counters expected to NOT change on double-cleanup; assert those specifically rather than "counters unchanged" broadly.
- Add a cheap FL regression test with a configured long expiry window OR explicitly document in SUMMARY why none is warranted.

**Risk: LOW**

---

### Plan 19.1-05: Regression coverage + polish

**Summary**
Combined test-coverage + doc/nit plan (originally Plans 05 + 06, merged per pass-4 follow-up). Six small tasks including the DR-pin test that makes the declined D-20 finding enforceable-as-contract. The merge rationale is sound: after moving D-15 into Plan 01, Plan 05 shrank to 2 tasks and merging kept the wave-4 work in one PLAN.md.

**Strengths**
- The DR-pin public-surface-only design (A5.5 amendment) correctly avoids coupling production code to test seams. The package-private DrainObserver with a comment block is an acceptable compromise when pure-public interleaving isn't feasible.
- A5.6 set-equality diff for CLAUDE.md `@Order` accuracy is a genuinely better check than the originally proposed count-based grep.
- A5.3 awk-scoped grep for `processInteractions` snapshot count is method-aware, avoiding the line-range grep that missed `:540`.
- A5.4 golden-trace gate before-AND-after the ES hoist catches semantic divergence.

**Concerns**
- **HIGH — Task 05c DrainObserver test seam risk**: the DR-pin test needs to interleave `queueAction` against `drainActions` at exactly the right point (between snapshot capture and `remove(k,v)`). The plan says "If the interleave can be reproduced WITHOUT any seam (e.g., via a real concurrent stress test that races queueAction against tickOnce many times — flaky but seam-free), prefer that." Flaky stress tests are worse than no test. A package-private DrainObserver is acceptable, but the test's RELIABILITY is not pinned — a CountDownLatch-based deterministic interleave is required, not "prefer seam-free stress".
- **MEDIUM — Task 05a baseline workflow**: the golden-trace baseline is seeded on first green run then committed manually. If the implementer's first run produces a semantically wrong digest (e.g., due to a bug in `buildDeterministicAction` arg encoding), they commit a bad baseline. The "manually verify the digest looks plausible" gate is weak.
- **MEDIUM — Task 05a verb arg payload hedging**: `buildDeterministicAction` uses `Math.floorMod(seed, 4)` as "placeholder for deterministic but valid". If ActionResolver rejects invalid args silently (e.g., logs warning, drops action), the test drives thousands of dropped actions and the digest is stable but meaningless. Needs an explicit check: after driving scenario, assert actions were actually processed (not dropped).
- **MEDIUM — Task 05c DR-pin test-seam comment block**: "must carry the comment `// Phase 19.1 D-20 test seam — pins drainActions semantics; do not remove without amending the contract`". A comment is not a contract. If someone removes the seam anyway, the test silently breaks. Consider making the seam explicitly named (`DrainObserverForD20Pin`) so removal is a visible refactor, not a quiet deletion.
- **LOW — Task 05d `PerceptionBroadcaster` might still exist**: CONTEXT.md says "there is no `PerceptionBroadcaster` class". Verify with `find src/main -name "PerceptionBroadcaster*"` BEFORE the doc edit — if the class was silently added between CONTEXT.md authoring and execution, the "delete the line" action is wrong.
- **LOW — Task 05f scope creep risk**: "SCOPE BOUND: ONLY `processInteractions`. DO NOT touch the calls at :623, :904, :1041, :1087, :1103 (5 call sites in OTHER methods)." The bound is stated, but if the executor is thorough they may "helpfully" hoist the others too. The method-aware grep acceptance criterion catches net-new hoists but not coincidental untouched calls becoming uncalled.

**Suggestions**
- For Task 05c, explicitly require a `CountDownLatch`-based deterministic interleave. Remove the "prefer seam-free stress test" fallback — it's worse than having the seam.
- For Task 05a, add a post-drive assertion: `actionResolver.getProcessedCountForTick(t) == BOT_COUNT` (or equivalent), proving actions weren't silently dropped. The arg-encoding correctness should be validated by a small hand-written probe BEFORE baseline commit.
- For Task 05c, name the seam explicitly (e.g., `DrainObserverForD20Pin` in a file named `_D20Pin_DrainObserver.java`) so its role is visible to anyone grep-ing.
- For Task 05d, add a one-line pre-check: `find src/main -name "PerceptionBroadcaster*"` must be empty before proceeding with the doc deletion.

**Risk: MEDIUM** (driven by Task 05c seam reliability and 05a baseline correctness)

---

### Cross-Plan Analysis

#### Dependency ordering
- Stated waves: Plan 01 + 02 (wave 1, parallel), Plan 03 (wave 2, depends on 01), Plan 04 (wave 3, depends on 01-03), Plan 05 (wave 4, depends on all).
- Actual dependencies look correct:
  - Plan 03 CLAUDE.md edit must precede Plan 05 CLAUDE.md edit ✓
  - Plan 05 ES hoist benefits from Plan 02's determinism fixes (golden-trace stability) ✓
  - Plan 01's D-15 codec gate is self-contained ✓
- **One risk**: Plan 02 Task 02d (determinism test) and Plan 05 Task 05a (GoldenTraceWithActionsTest) both pin baselines. If executed without understanding, Plan 05's baseline will bake in any non-determinism Plan 02 missed. Mitigated by wave ordering but should be explicit: Plan 05 Task 05a baseline MUST be seeded AFTER Plan 02 is fully merged.

#### Scope creep
- **None observed** in the current plans. The merge of old Plan 05 + 06 is a de-scoping. The D-15 move from Plan 05 to Plan 01 is a correctness improvement.

#### Achievement of roadmap success criteria
All roadmap criteria are covered.

### Overall Risk Assessment: **LOW-MEDIUM**

### Recommended pre-execution actions
1. Verify `PendingGrant` record fields (Plan 02 tiebreaker).
2. Verify `buildMinimalEventFrame` helper exists in `PerceptionCodecRoundTripTest` (Plan 01 Task 01d).
3. Verify fake-session injection point in `StallRecoveryIntegrationTest` (Plan 03 Task 03c).
4. Remove the "prefer seam-free stress test" fallback in Plan 05 Task 05c; require deterministic `CountDownLatch` interleave.
5. Pin `forceSpawnToxinForTest` seed source (Plan 02 Task 02c) instead of leaving as discretion.

---

## Consensus Summary

### Agreed Strengths (≥2 reviewers)
- **D-15 codec gate promoted into Plan 01 with F2 spot fix** — structural single-source-of-truth (`Event.ALL_CODES`) eliminates the dual-list trap that caused F2. (claude, opencode)
- **Option A stateless `ToxinPathGenerator` with seed reuse at `:405`** — preserves byte-exact golden-trace, no second `nextLong()` draw. (claude, codex, opencode)
- **`detachSession(WebSocketSession, CloseStatus)` overload via 1-line delegate** — preserves every existing caller; close-first unblocks Jetty IOException-out-of-blocked-write. (claude, codex, opencode)
- **Calibrated tick-bound `baselineMax * 5 + 20ms`** — survives CI jitter, catches order-of-magnitude regressions. (claude, opencode)
- **Public-surface DR-pin via `tickEngine.tickOnce()` + DrainObserver** — avoids reflection on private `drainActions()`. (claude, opencode)
- **Sorted `CompositeRegistry.getAll()` snapshot fixes CHM iteration nondeterminism.** (claude, codex, opencode)
- **F1/F3 use exact in-tree analogs (`remapEntity`, `cleanupBot:847-858`).** (claude, codex, opencode)

### Agreed Concerns (≥2 reviewers — highest priority)

| Severity | Concern | Reviewers |
|---|---|---|
| **HIGH** | Plan 02 D-04 `PendingGrant::buffType` does not exist; codex pins actual record as `(entityId, initialTicks, capturedOccupant, position)` in `EnvCleanupHooksBean.java:69`. Tiebreaker silently no-ops or fails to compile. | claude (M), codex (H), opencode (M) |
| **HIGH** | Plan 03 Task 03c depends on `tickEngine.tickOnce()` / a clean fake-session injection point in `StallRecoveryIntegrationTest`; neither is verified to exist. Test may need new production seams. | claude, codex, opencode |
| **HIGH** | Plan 05 Task 05b queues actions by **entityId**, but `actionResolver.queueAction` is keyed by **sessionId**. Test as written will fail or no-op. | claude, codex |
| **HIGH** | Plan 05 Task 05a action arg payloads are likely invalid — `Direction.fromNumpad` rejects `"0"`, `V` expects 3-char numpad ballots not `"0"`/`"1"`. Without a post-drive "actions processed" assertion the digest may be stable but meaningless. | codex, opencode |
| **HIGH** | Plan 05 Task 05c DrainObserver: codex notes `drainActions()` is private and `files_modified` omits `ActionResolver.java`; opencode flags the "prefer seam-free stress test" fallback as actively dangerous. Need a `CountDownLatch`-based deterministic interleave with a named seam. | codex (H), opencode (H) |
| **MEDIUM** | Plan 02 omits existing `ToxinPathGeneratorTest` from `files_modified`; Option A removes both ctors and the `final Random rng` field, breaking that test's compilation. | claude |
| **MEDIUM** | Plan 03 `OutboundSender` constructor change to inject `AdmissionMetrics` ripples to every test fixture that hand-constructs a sender; not enumerated. | claude, opencode |
| **MEDIUM** | Plan 04 A4.3 reorders `cleanupByEntityId` env-cleanup BEFORE the `sessionId == null` early-return — callers (e.g., reconnect-after-mark-dead within grace window) not enumerated. | claude |
| **MEDIUM** | Plan 02 D-06 `forceSpawnToxinForTest` seed source unpinned (`0L` literal collapses path diversity, tick-derived is safer). | claude, opencode |
| **MEDIUM** | Plan 04 BL probe via `git worktree` is heavy and CI-flaky; simpler red/green via `@Disabled` baseline or `git stash` is equivalent. | claude, codex |
| **MEDIUM** | Plan 05 Task 05a baseline JSON requires manual operator commit — not autonomous; weak digest-validity gate. | claude, opencode |
| **MEDIUM** | Plan 03 Task 03c fake-session test invokes `markStalled` cross-thread without an explicit barrier/latch — race conditions undermine determinism. | claude, codex |
| **MEDIUM** | Plan 02 `EnvironmentDeterminismTest` extension may conflict with that class's env-only/particle-only contract; toxin determinism better tested via `generatePath` direct calls than simultaneous active toxins (single-active-toxin guard). | codex |
| **HIGH** | Plan 03 Task 03a `markStalled` cannot `catch (IOException | IllegalStateException)` around `sendOutOfBand` because `sendOutOfBand` already swallows `IOException` internally — won't compile as written. | codex |
| **HIGH** | Plan 03 Task 03c post-detach `senderThread(sessionId)` returns `null` because `detachSession(WebSocketSession)` removes the thread first; capture `Thread t = sender.senderThread(id)` BEFORE detach. | codex |
| **HIGH** | Plan 01 Task 01a target file `PerceptionCodecTest.java` does not exist (codec tests are split into `PerceptionCodecRoundTripTest` + `PerceptionCodecErrorTest`). | codex |
| **MEDIUM** | `Event.ALL_CODES` as `public static final char[]` is mutable; prefer `String` or defensive-copy accessor. | codex |
| **MEDIUM** | `updateBotRegistryForFormation` is `private` in `SimulationEngine.java:881` — F1 unit-test cannot direct-call it without a production seam; use event-driven/Spring-driven test. | codex |

### Divergent Views

- **ES hoist call count in `processInteractions`.** Codex says **3 calls** (`:411`, `:540`, `:623`); claude and opencode (citing the A5.3 amendment) say **2 calls** (`:415`, `:540`). The line `:623` lies in `processCompositeEnergy`/another method per the A5.3 amendment — the awk-scoped check (`awk '/private.*processInteractions/,/^    }/'`) resolves this, but the disagreement signals that the executor must re-verify with that exact awk command before the hoist lands.
- **`depends_on` chain serialization.** Claude flags Plans 03 and 04's `depends_on: [...]` as over-serialised against the ROADMAP's "largely independent" claim and recommends running 01–04 in Wave 1. Opencode says the stated waves "look correct". Both agree wave ordering is at worst conservative; claude says it's wasted wall time on a 5-plan phase.
- **Overall risk verdict.** Claude = **MEDIUM**. Codex = **MEDIUM-HIGH**. Opencode = **LOW-MEDIUM**. Reflects different weightings: codex (most pessimistic) penalises every API mismatch as HIGH; opencode (most optimistic) credits the post-mitigation amendments as having absorbed prior gaps; claude lands between.

### Pre-Execution Checklist (composite)

Before Wave 1 starts, the executor must verify in code:

1. `PendingGrant` record actual fields → pin Plan 02 D-04 tiebreaker. (codex confirms it's `(entityId, initialTicks, capturedOccupant, position)` at `EnvCleanupHooksBean.java:69`.)
2. `TickEngine.tickOnce()` exists as a public driver, OR a documented `ApplicationEventPublisher`-based alternative.
3. `cleanupByEntityId` callers enumerated → confirm Plan 04 A4.3 placement-change is safe for reconnect-after-mark-dead.
4. Add `ToxinPathGeneratorTest.java` to Plan 02 `files_modified`; document the API migration.
5. `PerceptionCodecTest` exists OR redirect F2 spot test into `PerceptionCodecRoundTripTest`/`ErrorTest`.
6. `Event.ALL_CODES` exposure: prefer `String` or defensive-copy accessor over mutable `char[]`.
7. `processInteractions` snapshot count: run the awk-scoped check before the hoist; expect 2 calls inside the method body (codex's third call is in another method).
8. Plan 05 Task 05b: translate `entityId → sessionId` via `botRegistry.getSessionByEntity(...)` before `queueAction`.
9. Plan 05 Task 05a: verify action arg payloads are valid (numpad chars `"6"`/`"4"`, V ballot `"684"`); add post-drive assertion that actions were processed (not silently dropped).
10. Plan 05 Task 05c: require deterministic `CountDownLatch` interleave; remove the "prefer seam-free stress test" fallback; name the seam explicitly (`DrainObserverForD20Pin`).
11. `OutboundSender` `markStalled`/`sendOutOfBand` signature: `sendOutOfBand` swallows `IOException` internally — adjust the catch in `markStalled` accordingly (catch only unchecked, or change `sendOutOfBand` to return boolean).
12. Plan 03 Task 03c: capture `Thread t = sender.senderThread(id)` BEFORE detach to prove VT exit (post-detach lookup returns `null`).

### Note on Excluded Reviewers

Gemini was attempted but excluded:
- `gemini-2.5-pro` returned `429 RESOURCE_EXHAUSTED / MODEL_CAPACITY_EXHAUSTED` after 11 minutes of internal retries.
- `gemini-2.5-flash` ran successfully but its output is empirically low-quality on this project and was discarded by user direction.
