---
phase: 19.1
pass: 3
reviewers: [gemini, claude, codex, opencode]
reviewer_models:
  gemini: gemini-3.1-pro-preview
  claude: claude-opus-4-7 (effort xhigh)
  codex: gpt-5.5 (reasoning effort high)
  opencode: openrouter/deepseek/deepseek-v4-pro
reviewed_at: 2026-05-04T00:00:00Z
plans_reviewed:
  - 19.1-01-PLAN.md
  - 19.1-02-PLAN.md
  - 19.1-03-PLAN.md
  - 19.1-04-PLAN.md
  - 19.1-05-PLAN.md
  - 19.1-AMENDMENTS-PLAN.md
prior_passes:
  - 19.1-REVIEWS-pass1.md
  - 19.1-REVIEWS-pass2.md
overall_verdicts:
  gemini: LOW
  codex: MEDIUM-HIGH
  claude: MEDIUM-LOW
  opencode: LOW
---

# Cross-AI Plan Review — Phase 19.1 (Pass 3)

> Iterative review loop continues. Pass 1 and pass 2 produced amendments now embedded
> in the plans (see `19.1-AMENDMENTS-PLAN.md` plus inline `(C2.x/C3.x/C4.x/C5.x)`
> annotations). This pass focuses on residual issues in current plan text.

## Gemini Review

```markdown
# Phase 19.1 Multi-Review Pass-3 Synthesis

## 1. Summary
The Phase 19.1 plans successfully address the pass-4 multi-review findings, comprehensively closing the "unshipped" P19 gaps (F1, F2, F3), hardening RNG determinism (F4, CE, TX), resolving the `markStalled` tick-thread deadlock (MS), and patching several observability and lifecycle leaks (AM, BL, FL). The merged Plan 05 effectively covers the regression invariants (GT, LM, DR-pin). The C-amendments correctly tightened validation floors, clarified test seams, and hardened injection points. The current plan set is robust, executable, and safely clears the runway for Phase 20 connection multiplexing without introducing scope creep. All roadmap Success Criteria are met.

## 2. Strengths
- **Determinism Hardening (TX/F4/CE):** The transition of `ToxinPathGenerator` to a fully static, stateless model (Plan 02, Task 02c, C2.1) ensures precise seed tracking without risking mutable state leaks. The reuse of the already-drawn `spawnRng` seed (`src/main/java/com/paralife/engine/EnvironmentEngine.java:405`) successfully lands in the spec and guarantees golden-trace equivalence.
- **Deadlock Resolution (MS):** Substituting the string-keyed `detachSession` with the close-status-aware `WebSocketSession` overload in `markStalled` (Plan 03, Task 03b) correctly leverages transport closure to unblock Jetty writes. The explicit, latch-coordinated tick-time boundary test (`MarkStalledTickBoundIntegrationTest`) provides robust regression insurance.
- **Contract Enforcement (DR-pin/GT/codec-gate):** The DR-pin test (Plan 05, Task 05c) smartly utilizes the `DrainObserverForD20Pin` package-private seam to test the M-A `remove(k,v)` deferred-not-lost semantic, transforming an implicit behavioral contract into an executable guarantee. The `GoldenTraceWithActionsTest` with the tightened C5.5 validation floor (`>= 90%` processed) prevents silent payload drops.
- **TDD Alignment (BL):** Discarding the isolated worktree approach for an `@Disabled` baseline assertion in Plan 04 (Task 04b, B4.1) allows developers to see the behavioral leak and its fix in a single, atomic commit context.

## 3. Concerns

- **[HIGH] `cleanupByEntityId` Caller Audit (Plan 04, Task 04b):**
  The C4.1 HARD STOP gate correctly cautions against env-state clearing if `cleanupByEntityId` is invoked during successful `markStalled` grace-window reconnects. While rebinds typically update `BotRegistry` without invoking cleanup, any unexpected caller tracing back to `cleanupByEntityId` (e.g., from old-session deferred closures or unregister events) could prematurely strip buffs/infections from a reconnecting entity. The audit is critical to ensure `cleanupByEntityId` is strictly terminal.
- **[MEDIUM] `markDead` Attribute Cleanup Bypassing `cleanupBot` Env-State Removal (Plan 01, Task 01c):**
  `WorldWebSocketHandler.markDead` removes `ATTR_ENTITY_ID` from the session. This prevents subsequent `cleanupBot(session)` invocations from knowing the entity ID, effectively skipping the new Plan 04 env-state map cleanups (`buffRegistry`, `infections`, `fleeing`). This relies heavily on `DeathFinalizer` independently clearing these maps. If `markDead` is ever called in a non-lethal context where `DeathFinalizer` does not fire, those env-state entries will leak.
- **[LOW] `OutboundSender.detachSession` Internal Exception Handling (Plan 03, Task 03b):**
  Wrapping the OOB 408 frame in `markStalled` with a try/catch for `IllegalStateException` accommodates pre-closed sessions. However, the plan assumes `OutboundSender.detachSession` handles any underlying `IOException` from its internal `session.close()` call gracefully without propagating it upwards. If it propagates, the thread will abort before executing the OOB 408 best-effort send.

## 4. Suggestions

- **Plan 04 (Task 04b):** Reiterate the C4.1 caller audit by explicitly ensuring `grep -rn "cleanupByEntityId" src/main src/test` runs prior to the A4.3 placement change. Explicitly document that rebinds do not trace to `cleanupByEntityId` and the method is exclusively used for terminal sweeps.
- **Plan 01 (Task 01c) / Plan 04 (Task 04b):** Add a validation check ensuring `DeathFinalizer` and `markDead` are strictly 1:1 correlated so that bypassing `cleanupBot`'s env-state removal remains safe. If they are not 1:1, consider having `markDead` retain `ATTR_ENTITY_ID` but use a different flag to signal death to the registry.
- **Plan 03 (Task 03b):** Add a note clarifying that `src/main/java/com/paralife/admission/OutboundSender.java`'s internal `session.close()` call silently catches or safely propagates `IOException` so the subsequent OOB frame execution block is predictably reached.
- **Dependency Ordering:** The execution order (`19.1-01` -> `19.1-02` -> `19.1-03` -> `19.1-04` -> `19.1-05`) is logical and conservative. No changes are recommended; it safely prevents regressions and isolates state changes before the final test polish (Plan 05) executes.

## 5. Risk Assessment

**Overall Risk: LOW**

**Justification:**
The proposed plans rely heavily on exact-match analogs and well-defined extensions to existing logic. The determinism (TX, CE, F4) and backpressure (MS) fixes are highly targeted and well-contained. The extensive use of parameter-driven tests, explicit bounds checking, and tight regression tracking minimizes the risk of collateral damage to the core simulation loop. The C-amendments effectively closed the major systemic blind spots (such as undefined test seams and missing coverage floors), leaving only minor edge-case validation for the execution phase. The roadmap's Success Criteria for Phase 19.1 are fully and explicitly met by the structured task breakdown.
```

---

## Claude Review

# Phase 19.1 Multi-Plan Review — Pass 3

## Summary

Plans 01–05 are tight after pass-2 C-amendments. Most pass-1/2 risks closed: F1/F2/F3 grounded with verified analogs; F4/CE/TX determinism fully spec'd via static-stateless `ToxinPathGenerator` + sort-before-RNG; MS deadlock has both code change and calibrated integration test; lifecycle leaks have placement-correct cleanup with caller-audit gate; DR-pin uses public-surface-only seam. Residual concerns are execution blockers (one missing helper definition in Plan 05, one chicken-and-egg between 01a and 01d) and defense-in-depth gaps (test multicaster config not pinned, FL site-grep weak, comment doc drift deferred).

---

## Plan 01 — F1/F2/F3 + D-15 codec gate

**Strengths**
- `Event.ALL_CODES` as String (B1.2) + `CODE_SET` derived = single source of truth, immutable.
- C1.2 reverse-drift gate (count chars 0..127 that don't throw, equate to `ALL_CODES.length`) closes the inverse drift class.
- C1.3 multicaster sync-check before relying on `publishEvent` for F1 test = correct defensive read.
- B1.3 event-driven F1 test (no reflection, no visibility leak) is right.

**Concerns**

| Sev | Issue |
|-----|-------|
| HIGH | Task 01a's `validateEventCodeAcceptsB` test depends on a "minimal frame containing 'B'" but `validateEventCode` is `private` (so no direct call) and the frame-construction helper `buildMinimalEventFrame(char)` is defined in Task 01d (per C1.1). Task 01a will block until 01d's helper exists, OR 01a duplicates the helper inline. Plan must spell out: "01a uses inline minimal frame OR 01d ships first." |
| LOW | C1.2 reverse-drift gate iterates 0..127 (ASCII only). Java `char` is 16-bit. Non-ASCII codes silently slip through. Add `assertThat(Event.ALL_CODES.chars().allMatch(c -> c < 128))` to pin the ASCII-only convention. |
| LOW | `Event.ALL_CODES = "EAHTMRLNSDB"` ordering not asserted as canonical. If a future contributor inserts a code mid-string, B1.2 still works but `@MethodSource` order changes. Not a correctness issue but document: "Order is irrelevant; insertion at any position is allowed." |

**Suggestions**
- Resolve 01a→01d sequencing: either inline a minimal-frame builder in 01a (1-row from `vectors()` substituted) and let 01d generalise it, OR mark Task 01d as a hard prerequisite for 01a and re-order tasks within the plan.

---

## Plan 02 — F4/CE/TX determinism

**Strengths**
- C2.1 fully-static `ToxinPathGenerator` (no ctor, no field) cleanly removes the `final rng` tension. Caller migration list pinned.
- B2.1 4-key cascade tiebreaker uses verified `PendingGrant` fields.
- C2.2 totality justification grounded in CA invariant; fallback (`identityHashCode`) documented.
- B2.2 collateral test files explicitly listed.
- B2.4 relocates composite test to right file; multi-toxin reframed as static-call equivalence.

**Concerns**

| Sev | Issue |
|-----|-------|
| MEDIUM | Task 02d acceptance allows multi-toxin test to live "in either file or in `ToxinPathGeneratorTest`". Three test files across one task obscures verification. Pin: multi-toxin lives in `ToxinPathGeneratorTest` (it's now a property of the stateless generator per C2.3), not in EnvironmentDeterminismTest. |
| LOW | C2.2 totality reasoning relies on `PendingGrant` ctor invariant ("at most one per `(entity, buff-source-event, tick)` tuple") but doesn't add `.thenComparing(g -> g.capturedOccupant().toString())` as cheap defense. Add it — it's free totality insurance against any future PendingGrant field expansion. |
| LOW | Task 02c verification "single `nextLong` draw preserved" is hand-counted via a wide grep. Tighten: `awk '/spawnToxin/,/^    }/' EnvironmentEngine.java \| grep -c 'rng\.nextLong()'` returns exactly 1 before AND after. Same for `forceSpawnToxinForTest`. |
| LOW | C2.1 removes autowired `toxinPathGenerator` field but doesn't grep for `@Bean` registration of the type. If a `@Configuration` declares it as a bean, the bean stays unused. Add: `grep -rn '@Bean.*ToxinPathGenerator' src/main` returns 0 (or remove the bean if found). |

---

## Plan 03 — MS deadlock + EL + L1

**Strengths**
- New `detachSession(WebSocketSession, CloseStatus)` overload + 1-line delegate for default = clean API extension; preserves every existing caller.
- B3.1 narrows OOB catch to `IllegalStateException` (verified IOException is swallowed in sendOutOfBand).
- B3.4 paired latches (`markStalledStarted`/`markStalledFinished`) eliminate cross-thread race.
- B3.3 captures thread BEFORE detach (post-detach map entry is removed).
- C3.1 correctly rules out `BlockingWebSocketClient` (inbound-only).

**Concerns**

| Sev | Issue |
|-----|-------|
| HIGH | Plan 03 frontmatter `autonomous: true`, but Task 03c carries `human_needed: confirm injection seam shape before execution`. Contradiction. The execute-phase orchestrator may interpret these inconsistently. Resolve: either change plan-level to `autonomous: false`, or remove the human_needed tag and pre-decide the seam shape (recommend: package-private setter `OutboundSender.registerSessionForTest(String, WebSocketSession)` — pin it now). |
| MEDIUM | A3.3 removes the trailing `session.close(SERVICE_RESTARTED)` based on "verify by reading the surrounding 10 lines". This is an instruction to executor, not a plan-side guarantee. If a downstream listener (e.g., `BotRegistry.unregisterBySession` invoked via `afterConnectionClosed`) depends on the explicit-close ordering, removal silently breaks STALLED FSM. Add `<read_first>`: enumerate `afterConnectionClosed` listeners and confirm none observe the second close as a distinct event. |
| MEDIUM | Calibrated bound `baselineMaxMs * 5 + 20ms` has no upper cap. On a slow runner (baseline 200ms), bound becomes 1020ms — large enough to mask order-of-magnitude regressions. Add `Math.min(baselineMaxMs * 5 + 20, 500)` upper clamp. C3.3 documents the lower-floor rationale; add symmetric upper-clamp rationale. |
| LOW | Task 03c Step 5 final assertion `assertThat(t == null \|\| !t.isAlive())` is loose. Reuse Task 03a's "capture-before-detach + assert isAlive==false" pattern uniformly for clarity. |
| LOW | L1 metric description references hardcoded "100ms" — verify against actual `DETACH_JOIN_TIMEOUT_MS` constant value before writing the description string. Use `String.format` with the constant. |

---

## Plan 04 — AM/BL/FL lifecycle leaks

**Strengths**
- A4.1 verified `clearInfectionOnDeath` is pure-removal (idempotent, no death events).
- C4.1 promoted caller audit to HARD STOP — appropriate gate.
- C4.2 corrected `fleeing` visibility doc.
- A4.3 placement-before-early-return is correct.
- B4.1 in-place `@Disabled("baseline")` probe is cleaner than worktree.

**Concerns**

| Sev | Issue |
|-----|-------|
| MEDIUM | B4.1 baseline-disabled probe pattern requires force-running the disabled test with `-Djunit.jupiter.conditions.deactivate=...`. No `<verify>` step actually requires the executor to run this and observe the leak. Without it, the BL probe is advisory; the executor may flip @Disabled off and invert assertions without ever observing the pre-fix leak — defeating the TDD discipline. Add: explicit `<verify>` step `./gradlew test --tests "...Cleanup*Test.cleanupBotLeaksAtLeastOneEnvMap_baseline" -Djunit.jupiter.conditions.deactivate='org\\.junit\\..*DisabledCondition'` exits 0 (test PASSES = leak observed). |
| MEDIUM | C4.1 HARD STOP is appropriate but doesn't state the EXPECTED outcome of the audit. Reading the codebase: `markStalled` does NOT call `cleanupByEntityId` (it sets attrs + detaches; rebind path is `AdmissionGate.tryRebind`). Pre-state expected outcome: "no caller depends on env-state preservation; A4.3 should land safely. HARD STOP is the exception path, not the expected one." Otherwise executor may waste cycles second-guessing. |
| LOW | D-09 calls `clearInfectionOnDeath` from `cleanupBot` (disconnect, not death). A4.1 verified pure-removal. Push-back rejected the rename. Compromise: add 1-line Javadoc on `EnvCleanupHooksBean.clearInfectionOnDeath`: "Also called from disconnect cleanup paths (Phase 19.1 D-09)." Cheap doc defense against future "why call death-named method on disconnect?" confusion. |
| LOW | FL D-10 verification only checks `grep -c 'transferFleeing' >= 4`. Doesn't verify the calls are at the right SITES (could be 4 calls all at bond formation). Add per-site check: `grep -B2 -A8 'composite formation\\|bp→cm' SimulationEngine.java \| grep -c transferFleeing >= 1` and similarly for dissolve. |
| LOW | Test 3 idempotency (`admissionMetrics` counter invariance) defers enumeration to SUMMARY. Pre-spec: `cleanupBot` itself increments NO `admissionMetrics` counters directly (it removes from `bucketTagsByEntityId` only). Assertion is `metrics.snapshot()` equality across 2 cleanup calls. Saves executor a discovery loop. |

---

## Plan 05 — GT/LM/DR-pin + polish

**Strengths**
- C5.4 spec'd `DrainObserverForD20Pin` with full interface signature + `AtomicReference` capture pattern.
- C5.3 replaced 8 ghost `tickOnce()` calls with canonical `publishEvent` pattern.
- C5.2 re-authored CLAUDE.md tick pipeline (9-row chain verified).
- B5.5 tightened test-count gate to 180.
- C5.1 corrected entitySnapshot count to 3 (codex was right).

**Concerns**

| Sev | Issue |
|-----|-------|
| HIGH | Task 05a references `numpadDir(seed)` and `numpadBallot(seed)` helpers but never defines them. Executor must invent the bodies. Pin: `private static String numpadDir(int seed) { char[] dirs = {'2','4','6','8'}; return String.valueOf(dirs[Math.floorMod(seed, 4)]); }` and `numpadBallot(seed)` returning a 3-char concatenation of distinct numpad chars. Without this, B5.2's silent-drop fix is implementer-defined and may regress. |
| MEDIUM | Task 05c `<action>` describes TWO conflicting capture mechanisms: (1) the C5.4 `AtomicReference<Map<String, ActionFrame>> captured = ...; actionResolver.setDrainObserverForD20Pin(captured::set)` direct pattern; (2) the older `installDrainObserverForD20Pin(...)` + `capturedActionsForLastTick(sessionId)` + `@TestConfiguration` prose still present in the test skeleton. Reconcile by deleting the older prose (the C5.4 pattern is cleaner). Currently the action body shows both. |
| MEDIUM | C5.2 out-of-scope list defers source-comment doc fixes (`ActionResolver.java:394`, `EnvPostActionReconciler.java:14`, `TickHealthMonitor.java:16`). Trivial 1-line edits. Defer is wrong: those WILL surface in the next review pass. Either fix them in Plan 05 (cost: 3 line edits) or document the defer reason explicitly ("waiting for class-rename phase"). Currently `out_of_scope` cites no reason. |
| MEDIUM | Plan 05 frontmatter `depends_on: [19.1-01, 19.1-02, 19.1-03, 19.1-04]`. Task 05c (DR-pin) is pure ActionResolver work — no dependency on 02/03/04. Over-serializes. Tradeoff: cleaner review pass vs faster wall time. Acceptable for this phase but flag for future. |
| LOW | Task 05f awk `awk '/private.*processInteractions/,/^    }/'` could over-match if another method name contains "processInteractions" as substring. Tighten regex: `/^[[:space:]]+private[^(]+processInteractions\\(/`. |
| LOW | Task 05a baseline JSON workflow: Step 3 says "implementer runs the test, observes the produced digest, manually verifies". The "manually verifies" step has no acceptance gate. The B5.4 sanity probe (>= 90% processed) catches the silent-drop case but doesn't catch a "looks plausible but wrong" digest. Cheaper to skip manual verification — the digest equals what it equals; subsequent runs guard the invariant. Drop the manual-review prose. |

---

## Cross-cutting concerns

| Sev | Issue |
|-----|-------|
| MEDIUM | Multiple tests rely on synchronous `SimpleApplicationEventMulticaster` (C1.3, C3.2 are read-first checks, not enforcement). If the project ever adds a `TaskExecutor` to the multicaster, tests in 01b/03c/05a/05b/05c silently flake. Add a shared `@Configuration` in test base (or `@SpringBootTest` setup assertion): explicitly configure `SimpleApplicationEventMulticaster` with no `TaskExecutor`, OR assert at test startup `multicaster.getTaskExecutor() == null`. Defense-in-depth against future config drift across all 5 plans. |
| LOW | Acceptance gates inconsistent: some use `grep -c X >= N` (count gate, can be fooled), others use awk-scoped grep (better), others use full-suite count. Standardize on awk-scoped per-method grep where placement matters. |
| LOW | Phase Success Criteria coverage check: roadmap-line item "DR-pin shipped: `ActionResolverDrainSemanticsTest` enforces the pass-2 M-A `remove(k,v)` deferred-not-lost contract" — Plan 05 Task 05c covers this. ✓. All 9 SC items map to plan tasks. No gap. |

---

## Risk Assessment

**Overall: MEDIUM-LOW**

Plans are amendment-rich and well-grounded. Most concerns are residual cleanups (numpadDir definition, conflicting capture-mechanism prose in 05c, BL probe verify-step missing). Two HIGHs are real but cheap fixes:
- Plan 01: 01a→01d ordering or inline minimal-frame helper (5 min)
- Plan 03: autonomous-vs-human_needed contradiction (1-line resolution)
- Plan 05: numpadDir/numpadBallot helper bodies (5 lines)

No fundamental flaws in determinism plumbing, MS deadlock fix, lifecycle-leak placement, or DR-pin seam design. The C-amendment cycle materially improved factual accuracy — the pass-3 plan is execution-ready after addressing the three HIGH items above.

**Recommendation:** Apply 3 HIGH fixes inline (no new amendment letter; D-amendments only if execution surfaces drift per AMENDMENTS-PLAN.md policy), then `/gsd-execute-phase 19.1`.

---

## Codex Review

**Summary**

The plans are much stronger after the B/C amendments: most earlier factual drift has been corrected, and the phase now has clear coverage for F1/F2/F3, RNG determinism, markStalled, lifecycle leaks, and regression pinning. The remaining issues are concentrated in executable-test details and a few acceptance criteria that no longer match amended designs. I would not execute as-is: Plans 02, 03, and 05 still contain HIGH-risk contradictions that can make implementation fail or, worse, produce green tests that do not prove the intended behavior.

**Strengths**

- Plan 01 correctly pairs the F2 spot fix with a structural codec drift gate.
- Plan 02 now explicitly preserves the existing toxin seed draw at `EnvironmentEngine.spawnToxin`, which is the right determinism invariant.
- Plan 03 correctly moves toward close-first markStalled semantics and removes the redundant second close.
- Plan 04 has the right blocking audit for `cleanupByEntityId`, which is important because this is lifecycle-sensitive code.
- Plan 05 usefully pins the declined DR finding as an executable contract instead of changing production semantics.

**Concerns**

**Plan 01**

- **HIGH:** `buildMinimalEventFrame(char)` is still underspecified for magnitude-bearing event codes. Copying a single `vS`-style row and substituting `E/A/H/T/M/R/L` creates invalid frames because those codes require a magnitude per `PerceptionCodec.eventHasMagnitude`. The gate may fail for the wrong reason.
- **MEDIUM:** The reverse-drift gate says to count codes accepted by `PerceptionCodec.validateEventCode`, but that method is currently private in `src/main/java/com/paralife/codec/PerceptionCodec.java`. The plan needs either reflection, package-private visibility, or a decode-based public-surface probe.

**Plan 02**

- **HIGH:** C2.1 pins `ToxinPathGenerator.generatePath(...)` as `public static`, but Plan 02 still has artifacts/proofs/acceptance criteria expecting `toxinPathGenerator.generatePath` in `EnvironmentEngine.java`. Those greps will fail after the intended implementation.
- **MEDIUM:** `forceSpawnToxinForTest` is internally inconsistent: it says use `TEST_TOXIN_SEED = 0xDEADBEEFL`, while the interface text still references reusing the existing `rng.nextLong()` pattern. For the test path, the plan should say whether the `ToxinEvent.seed` is also `TEST_TOXIN_SEED` and whether the old `rng.nextLong()` line is removed.
- **MEDIUM:** C2.2's fallback suggestion to append `System::identityHashCode` would reintroduce nondeterminism. If equal `(entityId, initialTicks, x, y)` grants become possible, the fix should add a deterministic sequence/source key, not identity hash ordering.

**Plan 03**

- **HIGH:** Task 03b acceptance checks for `outboundSender.detachSession(session)` but the must-have requires `outboundSender.detachSession(session, CloseStatus.SERVICE_RESTARTED)`. The acceptance grep will not match the intended two-arg call.
- **HIGH:** Task 03c's VT-exit assertion can pass trivially because `senderThread(sessionId)` returns `null` after detach removes the map entry. The test must capture the sender thread before `markStalled`/detach, or expose a completed detached-thread handle.
- **MEDIUM:** B3.1 says catch `IllegalStateException` around `sendOutOfBand`, but `sendOutOfBand` catches `RuntimeException` internally today, so `IllegalStateException` is also swallowed before the caller can catch it.
- **MEDIUM:** C3.1 may cause unnecessary production seam creep. `OutboundSender.attachSession(WebSocketSession, int)` already accepts a fake `WebSocketSession` and starts a drain VT, so the test likely does not need a new `registerSessionForTest` seam.

**Plan 04**

- **MEDIUM:** The Plan 04 caller audit is correctly marked HARD STOP, but the plan should explicitly list expected current callers before execution. `ResumeTokenRegistry` wires `WorldWebSocketHandler::cleanupByEntityId`; that path is terminal-expiry cleanup and likely okay, but it must be documented in the summary.
- **LOW:** Task 04b says the cleanup test asserts all four maps empty, including AdmissionMetrics bucket tags, but the code action focuses on BuffRegistry/infection/FLEEING. Add explicit assertion/source step for `admissionMetrics.releaseBucketTags(entityId)` on both cleanup paths.

**Plan 05**

- **HIGH:** Task 05a's `entitySnapshot()` helper does not exist in `GoldenTraceEquivalenceTest`; use `liveEntityRegistry.snapshot()` or add a local helper.
- **HIGH:** The GT "90% processed actions" gate is likely not achievable with the described scenario. `E`, `R`, and especially `V` do not necessarily create observable effects for arbitrary entities/ticks; `V` only matters for composite LOCOMOTOR voting. This could make the test flaky or force meaningless instrumentation.
- **HIGH:** Task 05b reproduction uses `R` with a mate/entity id, but `ActionResolver.resolveReproduce` derives direction via `directionOf(action)`. The argument must be a numpad direction, not `P2.id()`. The current plan describes a two-parent reproduction model that the code does not implement.
- **HIGH:** Task 05c's DR-pin seam is not concrete enough for current `ActionResolver.drainActions`. The current implementation does not take a full snapshot before removals; adding `afterSnapshotBeforeRemove(Map...)` implies a refactor to copy `pendingActions` first. The plan should specify that exact refactor.
- **HIGH:** Task 05c still references `capturedActionsForLastTick(sessionId)` in pseudo-code/acceptance, but C5.4 removed that helper. The proposed observer only captures the drain snapshot; it does not prove that `NEW_ACTION` was processed on tick 2.
- **MEDIUM:** Task 05d's `@Order` set-equality command ignores `@Order(Integer.MAX_VALUE)` because it only greps numeric forms. The planned CLAUDE table also says `@Order(MAX_VALUE)`, not the source's exact `@Order(Integer.MAX_VALUE)`.

**Suggestions**

- Plan 01: Build event round-trip frames from `Event.ALL_CODES` with code-specific magnitude rules. For example, use `v6H3`-shape entries for `E/A/H/T/M/R/L` and `vS`-shape entries for `N/S/D/B`.
- Plan 01: For reverse drift, either make `validateEventCode` package-private intentionally, or avoid visibility changes and count accepted codes by decoding minimal frames for all ASCII chars.
- Plan 02: Replace every `toxinPathGenerator.generatePath` grep/proof with `ToxinPathGenerator.generatePath`.
- Plan 02: State that `forceSpawnToxinForTest` uses `TEST_TOXIN_SEED` for both generated path and `ToxinEvent.seed`, with no extra `rng.nextLong()` draw.
- Plan 03: Change Task 03b acceptance to grep `outboundSender.detachSession(session, CloseStatus.SERVICE_RESTARTED)`.
- Plan 03: In the markStalled integration test, attach the fake session normally with `outboundSender.attachSession(fake, queueSize)`, offer a frame, wait for `sendStarted`, capture `Thread t = outboundSender.senderThread(fake.getId())`, then call `markStalled` and assert `t.join(...)`.
- Plan 05: Change LM reproduction to queue `new Frame.ActionFrame('R', Optional.of("6"))` or another valid numpad direction, and seed only one high-energy parent plus an empty target cell.
- Plan 05: For DR-pin, refactor `drainActions` explicitly to `snapshot = new HashMap<>(pendingActions); observer(...); for snapshot.entrySet() remove(k,v)`. Then add either a second test observer for resolved actions or assert a deterministic world-state effect from `NEW_ACTION` on tick 2.
- Plan 05: Replace the GT 90% processed-action gate with deterministic fixture-specific assertions: at least one successful movement, one consume, one reproduce, and one composite vote/move, each backed by explicit setup.

**Risk Assessment: MEDIUM-HIGH**

The phase goals are right, and most production fixes are well scoped. The residual risk is not conceptual; it is execution risk. Several acceptance criteria contradict amended implementation choices, and Plan 05 still has test designs that may compile-fail or assert impossible behavior. Fixing the listed issues should bring the phase down to LOW-MEDIUM risk before execution.

---

## OpenCode Review

# Phase 19.1 — Pass-3 Cross-AI Plan Review

**Reviewer:** opencode (with source-verified ground truth against HEAD `d976975`)
**Date:** 2026-05-04
**Documents reviewed:** 19.1-01-PLAN.md through 19.1-05-PLAN.md, 19.1-AMENDMENTS-PLAN.md, 19.1-CONTEXT.md, 19.1-PATTERNS.md

---

## 1. Summary

The five plans are thorough, well-structured, and correctly decompose the 19 findings from the pass-4 TRIAGE into independent execution waves. The A/B/C amendment cadence demonstrates careful iterative hardening — C5.1 (entitySnapshot count 2→3), C5.2 (CLAUDE.md full re-author), and C5.4 (DrainObserverForD20Pin seam spec) are particularly valuable corrections that caught real source-drift. The in-tree analog pattern (PATTERNS.md) gives every fix a verifiable same-package sibling. The dependency ordering (Plans 01-04 wave-1 parallel, Plan 05 wave-4 after all) is correct. After three amendment passes, the residual concerns are all execution-level details, not plan-structural defects.

---

## 2. Strengths

- **Source-verification hygiene.** The review process caught three "shipped-but-not-landed" fixes (F1/F2/F3) and three plan-count errors (entitySnapshot 2→3, CLAUDE.md @Order chain fully wrong, `tickOnce` non-existent API). Each was corrected before execution.
- **D-15 structural defense paired with D-01 spot fix.** Moving the codec convention gate into Plan 01 (alongside the F2 spot fix) instead of Plan 05 means the whole `Event.ALL_CODES` + `@MethodSource` round-trip mechanism is the first thing that lands, immediately preventing re-introduction of the dual-list trap.
- **D-07 deadlock fix is properly modelled.** The `detachSession(WebSocketSession, CloseStatus)` overload + B3.1 sendOutOfBand catch-narrowing + B3.4 paired-latch cross-thread coordination + A3.5 calibrated-baseline assertion is the correct end-to-end shape for a tick-thread-blocking bug.
- **DR-pin (Task 05c) is the right approach for D-20.** Declining the drainActions race as a false positive while pinning the existing contract via an executable test (`DrainObserverForD20Pin`) is the correct balance — prevents both the current reviewer noise AND future silent regressions.
- **C4.1 HARD STOP on cleanupByEntityId caller audit.** The promotion from advisory to gate is the right call for the placement-change risk.

---

## 3. Concerns

### HIGH

None. All pass-1/pass-2 HIGH findings are resolved by the current amendments.

### MEDIUM

**M1 — `ToxinPathGenerator` static-migration Spring bean ripple (Plan 02, C2.1).** C2.1 makes `generatePath` static and removes both constructors. B2.2 adds `LightningTest.java` and `ToxinPathGeneratorTest.java` to `files_modified`. But if `ToxinPathGenerator` is a Spring `@Component`/`@Bean` anywhere in the config tree, removing its constructors breaks startup. A grep for `ToxinPathGenerator` bean definitions in `@Configuration` or `@Bean` methods should be part of the C2.1 execution pre-flight. *Mitigation:* the C2.1 prose says "the autowired `toxinPathGenerator` field on `EnvironmentEngine` is removed" — but doesn't distinguish between field-level `@Autowired` injection and constructor injection. The executor should also check for `@Bean ToxinPathGenerator toxinPathGenerator()` in any `@Configuration` class.

*Specific check to add to Task 02c `<read_first>`:* `grep -rn "ToxinPathGenerator" src/main/java --include="*.java" | grep -v "import\|//"` before removing the field.

**M2 — `entitySnapshot` awk scoping may miss method boundary (Plan 05, C5.1).** The acceptance criteria use `awk '/private.*processInteractions/,/^    }/'` to scope the grep. If `processInteractions` uses different indentation (tabs, or closing brace at different column), the awk pattern may not match. *Mitigation:* the plan already instructs the executor to "re-grep absolute lines pre-execution." Recommend adding a fallback: if awk returns anything other than the expected count, drop to manual line-number inspection via `grep -n "entitySnapshot\|private.*processInteractions"` and count manually.

### LOW

**L1 — `Event.ALL_CODES` compact-ctor refactoring (Plan 01, Task 01d).** The plan says to replace the existing if-chain with `CODE_SET.contains(code)`. The current `Event.java` compact ctor (per the `<interfaces>` block in Plan 01: `codes accepted: 'E','A','H','T','M','R','L','N','S','D','B'`) needs to be matched exactly — if the existing ctor has additional validation logic beyond the code check, the refactoring should preserve it. Low risk since `Event.java` is ~40 lines.

**L2 — Plan 05 frontmatter `out_of_scope` stale-source-comment list may be incomplete.** The plan defers three stale `@Order` comment fixes (`ActionResolver.java:394`, `EnvPostActionReconciler.java:14`, `TickHealthMonitor.java:16`). A quick grep for `PerceptionBroadcaster` in source comments shows one more: `GoldenTraceEquivalenceTest.java:308` (comment in `driveScenario` mentions `PerceptionBroadcaster @50`). Not blocking — the deferred list can be extended during execution.

**L3 — C5.2 CLAUDE.md re-author: `Env state projection` section also references `PerceptionBroadcaster`.** CLAUDE.md line 72 says `PerceptionBroadcaster.cellToView` as the Layer 3 surface owner. C5.2 addresses Line 72 in its straggler-fix list ("verify via `grep -rln "cellStatus\|entityStatus" src/main/java/com/paralife/websocket`"), so this is covered — but the plan doesn't commit to a specific replacement class name. The executor will discover it dynamically. Acceptable.

**L4 — Plan 05 says "whole-suite test count >= 180" but gives a 14-test breakdown that sums to 13 (B5.5).** The B5.5 breakdown lists: "codec-ordering + F1 event-driven + D-04 tiebreaker + composite-RNG + ToxinPathGenerator-stateless + RPN markStalled-overhead + RPN deferred-stall + D-11 GT + 2x LM + DR-pin + 3 polish coverage" = 14 items. The count is fine; the parenthetical just needed a re-check.

---

## 4. Suggestions

### S1 — Add ToxinPathGenerator bean-definition pre-flight to Task 02c `<read_first>` (addresses M1)

Add after the existing `<read_first>` items:

```
- Run `grep -rn "ToxinPathGenerator" src/main/java --include="*.java" | grep -v "import\|//\|^ *\*"` to enumerate
  every production reference: @Component/@Bean definitions, @Autowired fields, and constructor injection.
  If a @Bean definition exists in a @Configuration class, it must be removed or restructured alongside
  the static migration so Spring startup does not fail.
```

### S2 — Add fallback to entitySnapshot awk check in Task 05f (addresses M2)

In Task 05f `<acceptance_criteria>`, after the awk command block, add:

```
If awk returns an unexpected count (not 3 pre-hoist or 1 post-hoist), fall back to manual inspection:
`grep -n "entitySnapshot\|private.*processInteractions" src/main/java/.../SimulationEngine.java`
and count calls between the method header and closing brace manually.
```

### S3 — Extend Plan 05 `out_of_scope` list with `GoldenTraceEquivalenceTest.java:308` (addresses L2)

In the Plan 05 frontmatter `out_of_scope` list, append:
```
- "GoldenTraceEquivalenceTest.java:308 — comment references PerceptionBroadcaster @50 (non-existent class)"
```

### S4 — Codec convention gate: verify `Event.java` compact-ctor refactoring preserves all logic (addresses L1)

In Task 01d `<action>` Step 1, after "Read the file before editing — preserve the exact exception type + message style", add:
```
Also verify the compact-ctor contains ONLY a code-validation check (no additional assignment logic
beyond the record field defaults). If the ctor does more than code validation, the refactoring
must preserve every statement after the !CODE_SET.contains(code) check.
```

---

## 5. Risk Assessment: **LOW**

**Justification:** Three amendment passes have resolved all structural concerns. The four remaining issues (M1, M2, L1-L4) are all execution-level — they manifest at implementation time, not planning time, and each has a straightforward pre-flight check or fallback already partially specified in the plans. The dependency ordering is sound, the in-tree analog coverage is complete (14/14 sites matched), and the verification gates (`./gradlew test`, grep proofs, whole-suite green) are comprehensive.

**Highest execution risk:** Plan 03 Task 03c (markStalled integration test) — the C3.1 `human_needed` tag correctly identifies the fake-session injection seam as the least-precedented artefact in the entire phase. The `BlockingFakeSession` implementation and the `OutboundSender` package-private registration setter need operator confirmation before execution. This is the appropriate level of caution for a new test seam on a production concurrency class.

**Determinism guarantee (F4/CE/TX):** The seed-reuse design at `EnvironmentEngine.java:404-406` is correctly specified — the already-drawn `seed = rng.nextLong()` at line 405 is passed to `ToxinPathGenerator.generatePath(..., seed)` at line 406, with no second `nextLong()` call. This preserves `GoldenTraceEquivalenceTest` stability. The snapshot-and-sort pattern for D-04/D-05 is the standard in-tree idiom. The three same-seed equivalence fixtures in Task 02d provide the regression gate.

**Backpressure correctness (MS):** The close-aware `detachSession(WebSocketSession, CloseStatus.SERVICE_RESTARTED)` + best-effort OOB 408 + calibrated-baseline tick-bound assertion is the correct shape. The paired-latch cross-thread coordination in B3.4 eliminates the flake risk from the earlier `Thread.sleep(50)` approach.

**Lifecycle completeness (AM/BL/FL):** All four identity-transition sites (bond formation, bond revert, composite formation, composite dissolve) call `transferFleeing` after D-10 lands. `cleanupBot` and `cleanupByEntityId` both clear BuffRegistry/infection/FLEEING after D-09 lands. The C4.1 HARD STOP gate on `cleanupByEntityId` caller audit appropriately guards the placement-change risk.

---

## 6. Scorecard

| Plan | Verdict | Residual Issues |
|------|---------|-----------------|
| 19.1-01 | **Ready** | L1 (Event compact-ctor refactoring preservation) |
| 19.1-02 | **Ready** (with S1 pre-flight) | M1 (ToxinPathGenerator bean wiring ripple) |
| 19.1-03 | **Ready** | C3.1 `human_needed` tag is self-documenting |
| 19.1-04 | **Ready** | C4.2 field-visibility doc correction was minor |
| 19.1-05 | **Ready** (with S2/S3) | M2 (awk scoping), L2 (incomplete stale-comment list) |

**Overall: APPROVED for execution.** Apply S1-S4 as inline additions to the plan texts, then proceed to `/gsd-execute-phase 19.1`.

---

## Consensus Summary

### Verdict spread

| Reviewer | Risk | HIGH count | Recommendation |
|----------|------|-----------|----------------|
| Gemini | LOW | 1 (caller audit) | Execute as-is |
| Claude | MEDIUM-LOW | 3 | Apply 3 inline HIGH fixes, then execute |
| Codex | **MEDIUM-HIGH** | 8 | Do **not** execute as-is; resolve contradictions first |
| OpenCode | LOW | 0 | Apply S1-S4, then execute |

Codex is the outlier on severity. Source-verification depth differs: codex and opencode both
audited HEAD code; opencode's static analysis happened to find structural soundness while
codex's text-level analysis surfaced more acceptance-criteria/text-design contradictions
that the others missed or rated lower. **Codex should be taken seriously** — its findings
are concrete and trace to specific plan text vs current source.

### Agreed Strengths (≥2 reviewers)

- **MS deadlock fix shape is right** (gemini, claude, codex, opencode) — `detachSession(WebSocketSession, CloseStatus)` + paired latches + calibrated-baseline tick-bound test is the correct end-to-end design.
- **Determinism plumbing is grounded** (gemini, claude, opencode) — seed-reuse at `EnvironmentEngine.java:404-406` preserves `GoldenTraceEquivalenceTest`; static `ToxinPathGenerator` migration is a clean simplification.
- **DR-pin via `DrainObserverForD20Pin` seam is correct** (gemini, claude, codex, opencode) — declining the false positive while pinning the contract is the right balance.
- **D-15 codec gate paired with F2 spot fix** (gemini, claude, codex, opencode) — `Event.ALL_CODES` single-source-of-truth + round-trip gate is a structural defense, not just a patch.
- **C4.1 HARD STOP on `cleanupByEntityId` caller audit** (gemini, claude, opencode) — promoting the audit to a gate is appropriate for the placement-change risk.

### Agreed Concerns (≥2 reviewers — highest priority)

| Theme | Reviewers | Plan/Task | Severity (worst) |
|-------|-----------|-----------|------------------|
| Plan 02 stale `toxinPathGenerator.generatePath` greps after C2.1 static migration | codex (HIGH), claude (LOW @ bean grep), opencode (M1 bean-ripple) | 02c | **HIGH** |
| Plan 05 Task 05c has two conflicting capture mechanisms (C5.4 direct vs. older `installDrainObserverForD20Pin`/`capturedActionsForLastTick` prose still present) | codex (HIGH × 2), claude (MEDIUM) | 05c | **HIGH** |
| Plan 05 Task 05a undefined helpers / wrong signatures — `entitySnapshot()` (codex HIGH), `numpadDir`/`numpadBallot` (claude HIGH), `R` action with mate-id not numpad direction (codex HIGH) | codex, claude | 05a/05b | **HIGH** |
| Plan 03 Task 03b acceptance grep is single-arg `detachSession(session)` but must-have is two-arg with `CloseStatus.SERVICE_RESTARTED` | codex (HIGH), implicit in claude 03b strengths | 03b | **HIGH** |
| Plan 03 Task 03c VT-exit assertion can pass trivially because `senderThread()` returns null after detach (need capture-before-detach) | codex (HIGH), claude (LOW × similar `t == null \|\| !t.isAlive()` looseness) | 03c | **HIGH** |
| Plan 01 Task 01a chicken-and-egg with 01d's `buildMinimalEventFrame` helper, plus helper underspec for magnitude-bearing codes | codex (HIGH), claude (HIGH) | 01a/01d | **HIGH** |
| `cleanupByEntityId` caller audit needs to publish expected outcome, not just gate | gemini (HIGH), codex (MEDIUM), claude (MEDIUM) | 04b | **HIGH** |
| `PerceptionCodec.validateEventCode` private — reverse-drift gate needs visibility decision or decode-based public probe | codex (MEDIUM), claude (implicit) | 01a/01d | MEDIUM |
| Plan 05 GT "90% processed actions" gate flaky / not achievable for V/R/E semantics | codex (HIGH), claude (LOW: drop manual-verify prose) | 05a | MEDIUM |
| Plan 02/05 awk scoping fragility for "single nextLong draw preserved" / entitySnapshot checks | claude (LOW), opencode (M2 similar) | 02c, 05f | MEDIUM |
| Plan 03 calibrated bound has no upper cap; B3.1 catches IllegalStateException already swallowed by sendOutOfBand | claude (MEDIUM × 2), codex (MEDIUM) | 03 | MEDIUM |
| Plan 02 C2.2 `identityHashCode` fallback would reintroduce nondeterminism — replace with deterministic sequence/source key | codex (MEDIUM, unique but important) | 02 | MEDIUM |

### Divergent Views

- **Severity of Plan 05 issues.** Codex calls 5 HIGHs in Plan 05; opencode says no HIGHs anywhere; claude calls 1 HIGH (numpadDir helper undefined). Worth investigating: are codex's 05c findings reading older plan prose that C5.4 was supposed to replace but didn't actually delete? If so, this is a real text-cleanup gap. **Action:** read Plan 05 Task 05c `<action>` body and check whether both capture mechanisms are present.
- **Plan 03 `autonomous: true` vs `human_needed` tag.** Claude calls this a HIGH contradiction. Opencode treats the `human_needed` tag as "self-documenting" (acceptable). Resolve by either (a) removing `human_needed` and pinning the seam shape now (`OutboundSender.registerSessionForTest`), or (b) flipping `autonomous: false`. Don't ship the contradiction.
- **`markDead` removing `ATTR_ENTITY_ID` bypasses Plan 04 cleanups.** Gemini's MEDIUM finding (Plan 01 Task 01c). Other reviewers did not raise this. Worth a 5-minute audit: confirm `DeathFinalizer` is the only cleanup path post-`markDead`, and that env-state cleanup happens there too — otherwise Plan 04's BL-leak fix has a death-path gap.
- **Spring bean wiring ripple for static `ToxinPathGenerator`.** Opencode's M1 (pre-flight grep needed). Other reviewers either didn't raise it or rated it LOW (claude). Quick `grep -rn "ToxinPathGenerator" src/main/java | grep -E "@Component|@Bean"` would settle it before execution.

### Recommended next step

Two high-leverage cleanup passes are warranted before `/gsd-execute-phase`:

1. **Pass-3 D-amendment** (per AMENDMENTS-PLAN.md policy — D-amendments only after execution drift normally, but pass-3 found enough concrete contradictions to justify a planning-side D-pass) — focused on:
   - **Plan 02:** replace all `toxinPathGenerator.generatePath` greps with `ToxinPathGenerator.generatePath`; add bean-definition pre-flight (opencode S1); replace `identityHashCode` fallback with deterministic sequence key; pin `forceSpawnToxinForTest` seed semantics.
   - **Plan 03:** change Task 03b acceptance grep to two-arg signature with `CloseStatus.SERVICE_RESTARTED`; resolve `autonomous`-vs-`human_needed` contradiction; tighten Task 03c thread-capture pattern (capture before detach); add upper-cap to calibrated bound; clarify B3.1 catch-narrowing given sendOutOfBand internal swallow.
   - **Plan 05:** delete the older `capturedActionsForLastTick`/`installDrainObserverForD20Pin` prose in 05c so only the C5.4 pattern remains; pin `numpadDir`/`numpadBallot` bodies in 05a; fix `R`-as-direction in 05b reproduction (numpad char, not entity id); either drop the 90% gate or replace with deterministic per-action assertions; fix `entitySnapshot` helper definition; awk fallback for 05f.
   - **Plan 04:** publish expected outcome of C4.1 caller audit in plan body ("markStalled does NOT call cleanupByEntityId; A4.3 should land safely"); add `<verify>` step that force-runs B4.1 disabled probe and observes leak.
   - **Plan 01:** resolve 01a→01d helper sequencing (inline in 01a or reorder); specify magnitude-aware `buildMinimalEventFrame`; decide visibility of `validateEventCode` (recommend package-private + decode-based probe).

2. **Then re-run pass-4 review** if any HIGH-severity item remains contested across reviewers.

Skipping the cleanup will cost more than the cleanup itself — Plan 05 Task 05c specifically
risks shipping a green test that does not prove the intended contract.
