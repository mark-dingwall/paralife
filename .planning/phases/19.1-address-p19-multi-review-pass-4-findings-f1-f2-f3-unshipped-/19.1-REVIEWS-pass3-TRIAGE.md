---
phase: 19.1
pass: 3
artifact: triage
generated_at: 2026-05-04
sources:
  - 19.1-REVIEWS.md (pass-3 raw)
verified_against:
  - HEAD source code (src/main/java/com/paralife/{codec,engine,admission,websocket}/...)
  - Current plan files (19.1-{01,02,03,04,05,AMENDMENTS}-PLAN.md)
---

# Phase 19.1 Pass-3 Review Triage

Reviewers found 8 HIGHs (codex) + 3 HIGHs (claude) + 1 HIGH (gemini) + 0 HIGHs (opencode).
Verified each against current plan text and source code. **6 real HIGHs, 5 real MEDIUMs,
3 confirmed false positives, 2 unverified.**

## Confirmed REAL — execution blockers

### H-T1 Plan 02 stale `toxinPathGenerator.generatePath` greps after C2.1 static migration
**Reviewers:** codex HIGH, opencode MED (M1), claude LOW
**Evidence:**
- Plan 02 line 31 `contains: "toxinPathGenerator.generatePath"` (instance form)
- Plan 02 line 52 `pattern: "toxinPathGenerator\\.generatePath\\(.*seed"` (instance form)
- Plan 02 lines 57, 349, 431 grep proofs use lowercase form
- Plan 02 line 471 amendment text: "the autowired `toxinPathGenerator` field on `EnvironmentEngine` is removed"
- Plan 02 line 50 migration target: `to: "ToxinPathGenerator.generatePath(... long seed)"` (static form)

After C2.1, the field is gone — lowercase `toxinPathGenerator.generatePath` greps return 0, all acceptance gates fail.

**Fix:** global s/`toxinPathGenerator\.generatePath`/`ToxinPathGenerator.generatePath`/ in lines 31, 52, 57, 349, 431. Update key_links pattern at line 52 too.

---

### H-T2 Plan 05 Task 05c — old test skeleton contradicts new C5.4 spec
**Reviewers:** codex HIGH × 2, claude MEDIUM
**Evidence:**
- Lines 449-464 (C5.4): explicit "Drop the prior `installDrainObserverForD20Pin(...)` and `capturedActionsForLastTick(sessionId)` helper sketches" + new `setDrainObserverForD20Pin(captured::set)` + `AtomicReference` pattern
- Lines 518, 520, 540, 553, 556 (test skeleton): STILL uses `installDrainObserverForD20Pin(...)` and `capturedActionsForLastTick(sessionId)`

The amendment narrative says "drop" but the actual test skeleton wasn't rewritten. Executor sees two contradictory patterns.

**Fix:** rewrite the `void overwriteMidDrainDefersToNextTick()` and `void noOverwriteDrainsCleanly()` test bodies (lines 497-554) to use only the C5.4 `AtomicReference<Map<String, ActionFrame>> captured + setDrainObserverForD20Pin(captured::set)` pattern. Delete the comment block at lines 556-561.

---

### H-T3 Plan 05 Task 05a — `entitySnapshot()` helper does not exist in the test class
**Reviewers:** codex HIGH
**Evidence:**
- Plan 05 line 229: `for (var entry : entitySnapshot()) {`
- Source `GoldenTraceEquivalenceTest.java:379`: `for (LiveEntityRegistry.EntityEntry entry : liveEntityRegistry.snapshot()) {`

The plan invents a method name that doesn't exist in the parent class.

**Fix:** replace `entitySnapshot()` with `liveEntityRegistry.snapshot()` in Plan 05 line 229.

(Note: this is **separate** from the SimulationEngine `entitySnapshot()` hoist in Task 05f — that one is real and lives in `SimulationEngine`, not the test class.)

---

### H-T4 Plan 05 Task 05a — `numpadDir` and `numpadBallot` referenced but never defined
**Reviewers:** claude HIGH
**Evidence:**
- Plan 05 lines 264, 267, 269: `Optional.of(numpadDir(seed))`, `Optional.of(numpadBallot(seed))`
- B5.2 amendment text (line 855) only says "switch arms now use `numpadDir(seed)` and `numpadBallot(seed)` helpers; inline `// B5.2` block-comment explains why" — never spells out helper bodies

Executor must invent the bodies. Two implementers will produce two different test fixtures.

**Fix:** pin in Task 05a `<action>` body:
```java
private static String numpadDir(int seed) {
    char[] dirs = {'2','4','6','8'};
    return String.valueOf(dirs[Math.floorMod(seed, 4)]);
}
private static String numpadBallot(int seed) {
    // 3-char ballot of distinct numpad chars; deterministic from seed
    char[] dirs = {'2','4','6','8'};
    int a = Math.floorMod(seed, 4);
    int b = Math.floorMod(seed / 4, 4);
    int c = Math.floorMod(seed / 16, 4);
    return new String(new char[]{dirs[a], dirs[b], dirs[c]});
}
```

---

### H-T5 Plan 05 Task 05b — `R` action passes entity id where numpad direction expected
**Reviewers:** codex HIGH
**Evidence:**
- Plan 05 line 375: `actionResolver.queueAction(s1, buildActionFrame('R', /* mate ref */ P2.id()))`
- Source `ActionResolver.java:542 directionOf` parses `arg.charAt(0)` via `Direction.fromNumpad(...)`. Entity id is a UUID-style string, not a numpad char.
- Source `ActionResolver.java:649 resolveReproduce` calls `directionOf(ra.action)` at :664; returns `false` (no reproduce) if direction is null.

The `// mate ref` comment is wrong — Paralife's reproduce takes a direction (target cell relative to parent), not a mate id.

**Fix:** Plan 05 line 375 → `actionResolver.queueAction(s1, buildActionFrame('R', "6"))` (or any numpad direction). Strip the misleading `/* mate ref */` comment. Update Task 05b `<behavior>` Test 2 prose: "seed 1 high-energy parent + 1 empty target cell adjacent" (codex's suggestion — the second particle isn't a mate, it would block reproduction).

---

### H-T6 Plan 03 frontmatter — `autonomous: true` contradicts Task 03c `human_needed`
**Reviewers:** claude HIGH
**Evidence:**
- Plan 03 line 14: `autonomous: true`
- Plan 03 lines 389, 512, 642: Task 03c tagged `human_needed: confirm injection seam shape before execution`

The execute-phase orchestrator may interpret these inconsistently — autonomous loops won't pause for confirmation.

**Fix:** pre-decide the seam choice now and remove `human_needed`. Recommend reusing existing `OutboundSender.attachSession(WebSocketSession, int)` at `OutboundSender.java:117` — it already accepts a fake `WebSocketSession` and starts a drain VT. Drop the proposed new `registerSessionForTest` setter (also resolves M-T2 below). If reuse is genuinely impossible, flip plan-level to `autonomous: false`.

---

## Confirmed REAL — MEDIUM

### M-T1 Plan 03 B3.1 — `IllegalStateException` catch is dead code
**Reviewers:** codex MEDIUM
**Evidence:**
- Source `WorldWebSocketHandler.java:929`: `catch (IOException | RuntimeException e)` inside `sendOutOfBand` — `IllegalStateException extends RuntimeException`, so it IS swallowed
- Plan 03 lines 309-315 (B3.1): comment says "sendOutOfBand already SWALLOWS IOException internally" and proceeds to wrap call in `try { sendOutOfBand(...) } catch (IllegalStateException e) {...}`

The wrapping catch never fires because RuntimeException is also swallowed inside sendOutOfBand. Either drop the wrapping try/catch entirely (preferred), or change `sendOutOfBand`'s catch to `IOException` only and rethrow other RuntimeExceptions.

**Fix:** drop the outer try/catch; document in B3.1 comment that `sendOutOfBand` is fully self-contained.

---

### M-T2 Plan 03 C3.1 — likely unnecessary new test seam
**Reviewers:** codex MEDIUM
**Evidence:**
- Source `OutboundSender.java:117`: `public void attachSession(WebSocketSession session, int queueCapacity)` — already accepts arbitrary `WebSocketSession` (incl. test fakes), starts the drain VT
- Plan 03 line 389 (C3.1): proposes adding new package-private `void registerSessionForTest(String sessionId, WebSocketSession fake)`

Tests can already inject a fake via `attachSession`. Adding a parallel seam expands public surface for no win.

**Fix:** in C3.1 amendment, change the conditional ("If no such seam exists, ADD ONE") to a positive statement — `attachSession` IS the seam; tests should call it directly with a fake `WebSocketSession`. Drop the `registerSessionForTest` proposal. Combines with H-T6 fix.

---

### M-T3 Plan 02 C2.2 — `identityHashCode` fallback would reintroduce nondeterminism
**Reviewers:** codex MEDIUM
**Evidence:**
- Plan 02 line 172 (C2.2): "If this invariant is later weakened, append `.thenComparing(System::identityHashCode)` to restore totality"
- `System.identityHashCode` varies across JVM runs — appending it to a sort comparator destroys the determinism guarantee that the whole plan exists to enforce

Documented as hypothetical but bad guidance — a future maintainer following the recipe creates the bug the rest of the plan is fixing.

**Fix:** replace the fallback recipe in C2.2 with: append `.thenComparing(g -> g.sourceEventId())` (or whatever monotonic source-event sequence exists), or — if no such field exists — declare the invariant load-bearing and require an ADR before adding any buff source that violates it.

---

### M-T4 Plan 01 Task 01d — `buildMinimalEventFrame` doesn't address per-code magnitude
**Reviewers:** codex HIGH (down-rated to MED here — helper IS marked as a stub)
**Evidence:**
- Source `PerceptionCodec.java:691`: `eventHasMagnitude` accepts E/A/H/T/M/R/L (require magnitude) and rejects N/S/D
- Plan 01 lines 482-489: `buildMinimalEventFrame` body is a stub `return "..."` with comment "executor copies a real `vectors()` row, then substitutes the event-code char"
- Substituting a no-magnitude code for a magnitude code (or vice versa) produces a frame that fails decode — gate fails for the wrong reason

**Fix:** in Task 01d Step 2, spell out two builder paths — `vS`-shape for N/S/D/B (no magnitude); `v6H3`-shape for E/A/H/T/M/R/L (magnitude). Branch inside the helper based on `Set.of('E','A','H','T','M','R','L').contains(code)`. Or note that B's magnitude requirement must be looked up — verify against `eventHasMagnitude` switch in `PerceptionCodec.java:690+` before coding.

(Side note: `eventHasMagnitude` does NOT yet list 'B' explicitly — pre-flight check needed during Task 01a to determine where B falls.)

---

### M-T5 Plan 04 C4.1 — caller audit lacks documented expected outcome
**Reviewers:** gemini HIGH, codex MEDIUM, claude MEDIUM
**Evidence:**
- Plan 04 task 04b carries C4.1 HARD STOP gate on `cleanupByEntityId` caller audit — but doesn't state the pre-state expected result

Source check: `markStalled` does NOT call `cleanupByEntityId` (it sets attrs + detaches; rebind path is `AdmissionGate.tryRebind`). The audit should land safely on green; HARD STOP is the exception path.

**Fix:** in C4.1 amendment text, add: "Pre-state: `markStalled` does NOT call `cleanupByEntityId`. Expected audit result: A4.3 placement-before-early-return lands safely. HARD STOP fires only if a NEW caller has been introduced since this plan was written."

---

## Confirmed FALSE POSITIVES

### FP-1 Codex: Plan 03 Task 03b acceptance grep is single-arg
**Refutation:** Plan 03 line 56 pattern is `'outboundSender\\.detachSession\\(session,\\s*CloseStatus'` — INCLUDES `, CloseStatus`. Codex misread. No fix needed.

### FP-2 Codex: Plan 03 Task 03c VT-exit assertion trivially passes (senderThread null)
**Refutation:** Plan 03 lines 208-223 (B3.3 amendment) explicitly handles this: "capture the thread reference BEFORE detach. Post-detach, `senderThreads.get(id)` returns null because `detachSession` removes the map entry first. Capture the `Thread` ... THEN call markStalled". Code shown: `Thread t = sender.senderThread("session-sr");` before detach. Already resolved.

### FP-3 Claude: Plan 01 Task 01a → Task 01d helper sequencing chicken-and-egg
**Refutation:** Plan 01 lines 181-194 (Task 01a) drives the test via `PerceptionCodec.decode(<frame containing 'B'>)` — does NOT reference `buildMinimalEventFrame`. The helper is only used in Task 01d's round-trip test. 01a stands alone. Claude conflated the two tasks.

---

## Unverified — single-reviewer, lower priority

### U-1 Gemini: `markDead` removing `ATTR_ENTITY_ID` bypasses Plan 04 cleanups
**Reviewer:** gemini MEDIUM (single)
**Status:** unverified. Worth a 5-minute audit confirming `DeathFinalizer` is the only post-`markDead` cleanup path AND that DeathFinalizer also clears BuffRegistry/infection/FLEEING. If yes, no action needed. If no, Plan 04 has a death-path leak.

### U-2 Codex: Plan 05 90% processed-action gate flaky for V/R/E semantics
**Reviewer:** codex HIGH, claude LOW (different angle)
**Status:** judgment call. The 90% floor IS calibrated against the silent-drop failure mode (per C5.5 amendment text), but the calibration data isn't shown. If executor hits a real fixture-edge legit-skip case, they'll be tempted to relax the floor. Acceptable to ship and let CI surface; or replace with deterministic per-action assertions per codex's suggestion.

---

## Recommended action

**D-amendment pass** addressing the 6 real HIGHs and 4 of the 5 real MEDIUMs (skipping M-T5 if you trust executor judgment). Estimated effort: 30-60 min of focused plan edits. No new code, no new tests.

After amendments, re-run a quick pass-4 review (gemini + codex only — opencode and claude already approved) to confirm the contradictions are gone. Then `/gsd-execute-phase 19.1`.

**Alternative:** ship as-is and trust the executor agent to spot the contradictions during read-first checks. Risk: H-T2 (Plan 05 05c dual capture mechanism) is the most likely to slip through silently — executor may pattern-match on the older skeleton.
