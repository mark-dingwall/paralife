---
task: code
mode: reference
reviewers_succeeded: ["claude", "gemini", "codex", "opencode"]
reviewers_failed: []
reviewed_at: 2026-05-05T06:02:00Z
files: ["src/main/java/com/paralife/admission/TickHealthMonitor.java", "src/main/java/com/paralife/codec/Event.java", "src/main/java/com/paralife/codec/PerceptionCodec.java", "src/main/java/com/paralife/engine/ActionResolver.java", "src/main/java/com/paralife/engine/CompositeRegistry.java", "src/main/java/com/paralife/engine/EntityIds.java", "src/main/java/com/paralife/engine/EnvPostActionReconciler.java", "src/main/java/com/paralife/engine/EnvironmentEngine.java", "src/main/java/com/paralife/engine/SimulationEngine.java", "src/main/java/com/paralife/websocket/WorldWebSocketHandler.java", "src/test/java/com/paralife/codec/PerceptionCodecRoundTripTest.java", "src/test/java/com/paralife/engine/EnvPostActionReconcilerTest.java", "src/test/java/com/paralife/engine/EnvironmentFullStackSmokeTest.java", "src/test/java/com/paralife/engine/EnvironmentPhaseGateIntegrationTest.java", "src/test/java/com/paralife/engine/GoldenTraceEquivalenceTest.java", "src/test/java/com/paralife/engine/MetabolismIntegrationTest.java", "src/test/resources/golden-trace-phase19.json"]
usage:
  claude: { input: 19224, output: 1172, cached: 2196477, tool_calls: 16, elapsed_s: 147.8 }
  gemini: { input: 205885, output: 992, cached: 107130, tool_calls: 15, elapsed_s: 125.9 }
  codex: { input: 681017, output: 6825, cached: 601344, tool_calls: 23, elapsed_s: 158.8 }
  opencode: { input: 0, output: 0, cached: 0, tool_calls: 17, elapsed_s: 309.9 }
synthesizer: claude
synthesized_at: 2026-05-05T06:02:00Z
---

# Cross-AI Review

## Claude Review

Verdict ready. Drafting response.

## Verdict

**SHIP**

## Per-task

| Task | Status | Evidence |
|------|--------|----------|
| T1 — `PerceptionBroadcaster` + `@Order(100)` sweep | CONFIRMED | `grep` finds zero `PerceptionBroadcaster` and zero `@Order(100)` in `src/`. Residual hits live only in `.planning/` historical docs (out of scope). `TickHealthMonitor.java:16`, `ActionResolver.java:394` correctly cite `@Order(50)`. |
| T2 — panic-zone snapshot+sort before `simRng` | CONFIRMED | `SimulationEngine.java:1135-1136` snapshots `compositeRegistry.getAll()` into `ArrayList` then `sort(Comparator.comparing(getCompositeId))`. Sorted list is reused at lines 1144 (`checkPanicZone` → `simRng.nextDouble()` :1226, :1406) and 1153 (previousPoolEnergy tracking). compositeId is the CHM key (`composites.put(compositeId, state)` :139), so unique → total order. |
| T3 — `MAG_CODES` invariant gate | CONFIRMED | `Event.java:42` `MAG_CODES = "EAHTMRL"`. `eventHasMagnitude` (`PerceptionCodec.java:690-692`) reads `Event.MAG_CODES.indexOf(code) >= 0`. Static-init at `Event.java:51-55` throws `AssertionError` if any `MAG_CODES` char is absent from `ALL_CODES` — fires at class load. `PerceptionCodecRoundTripTest.buildMinimalEventFrame` (:89) routes via `Event.MAG_CODES.indexOf(code)` so adding a new MAG code auto-routes through the magnitude branch. |
| T4 — golden-trace surgical 2-key refresh | CONFIRMED | `git diff f6da129^ f6da129 -- src/test/resources/golden-trace-phase19.json`: only `trace-sess-9` and `trace-sess-21` digests changed; other 24 untouched. |

## Other findings

- **Regression surface (T2 allocation cost) — not a blocker.** `processDeaths` now allocates a fresh `ArrayList` + `sort` on every tick. Registry typically small (`compositeConfig` rolling pool), and the list is reused 3× per call (snapshot, prevPool map fill, panic-zone iter). No per-element work added. Matches the existing `CompositeEnergyDistributor.onTick:101-102` pattern. Acceptable; flag only if `compositeRegistry.size()` grows pathologically (TD-21 perf scope).
- **`eventHasMagnitude` lookup cost — negligible nit.** `String.indexOf` on a 7-char string per parsed event is fine but slower than a switch. If decode hot path matters, swap to a precomputed `BitSet`/`HashSet<Character>` mirror of `MAG_CODES` (mirrors the `CODE_SET` pattern at `Event.java:44-49`). Defer; not worth a follow-up commit.
- **Closure on Phase-3b RNG ordering — non-issue, document only if you want.** Phase 3b at `SimulationEngine.java:1124-1128` iterates `entitySnapshot()` which `LiveEntityRegistry.snapshot()` (`:167-171`) sorts by row-major linear index — already deterministic. `handleMemberDeath`'s `simRng.nextDouble()` (`:1226`) consumes RNG in row-major order, not compositeId order. Different from Phase 3c's compositeId order, but both are stable across runs at same seed. No drift. (Worth a one-line `// row-major iteration is the determinism source` comment if a future reviewer asks again.)
- **Unrelated-but-adjacent: `validateActionArg` (`PerceptionCodec.java:825-851`) verb list is not driven from a constant.** Same shape of foot-gun T3 closes for event codes. Out of scope for 19.1; flag for the Phase 21/codec-convention follow-up.

---

## Gemini Review

SHIP

| # | Task | Status |
|---|---|---|
| T1 | `PerceptionBroadcaster` doc/comment refs and stale `@Order(100)` sweep | CONFIRMED |
| T2 | `SimulationEngine.processDeaths` panic-zone sort determinism | CONFIRMED |
| T3 | `Event.eventHasMagnitude(byte)` hard-coded list fix and invariant | CONFIRMED |
| T4 | Golden trace baseline refresh (surgical 2-key change) | CONFIRMED |

## Other findings

- `SimulationEngine.java:1121`: The T2 fix introduces a per-tick `new ArrayList<>(compositeRegistry.getAll())` allocation followed by a `.sort()` operation. While acceptable for a typically-small registry, this introduces O(N log N) overhead and GC pressure every tick if the number of composites grows large. *Smallest fix:* Acceptable for now, but should be tracked for the Phase 21 performance pass to avoid per-tick allocation (e.g., maintaining a pre-sorted snapshot on mutation).

---

## Codex Review

SHIP WITH FOLLOW-UPS

| Task | Status | Notes |
|---|---|---|
| T1 | CONFIRMED WITH CONCERNS | `src/`, `CLAUDE.md`, and `golden-trace-phase19.json` are clean for `PerceptionBroadcaster` and `@Order(100)`. One adjacent stale TickBroadcaster-order comment remains, below. |
| T2 | CONFIRMED WITH CONCERNS | Panic-zone now snapshots and sorts by `compositeId` before `checkPanicZone` consumes `simRng` at [SimulationEngine.java:1135](/home/mark/kramtime/paralife/src/main/java/com/paralife/engine/SimulationEngine.java:1135). Production IDs are monotonic `composite-<counter>` at [SimulationEngine.java:788](/home/mark/kramtime/paralife/src/main/java/com/paralife/engine/SimulationEngine.java:788), stored by key in `CompositeRegistry`, so no production ties. |
| T3 | CONFIRMED WITH CONCERNS | `Event` class-load invariant throws `AssertionError` if `MAG_CODES` contains a code absent from `ALL_CODES` at [Event.java:46](/home/mark/kramtime/paralife/src/main/java/com/paralife/codec/Event.java:46). Round-trip test does exercise the magnitude branch via `Event.MAG_CODES` at [PerceptionCodecRoundTripTest.java:89](/home/mark/kramtime/paralife/src/test/java/com/paralife/codec/PerceptionCodecRoundTripTest.java:89). |
| T4 | CONFIRMED | Diff changes only `trace-sess-21` and `trace-sess-9` in [golden-trace-phase19.json](/home/mark/kramtime/paralife/src/test/resources/golden-trace-phase19.json:16); the other 24 digests are untouched. |

**Other Findings**

- [SimulationEngine.java:343](/home/mark/kramtime/paralife/src/main/java/com/paralife/engine/SimulationEngine.java:343) still says TickBroadcaster has default order `Integer.MAX_VALUE`; actual order is `@Order(50)`. Smallest fix: update the class and listener comments to say `Before TickBroadcaster(@Order(50))`.

- [SimulationEngine.java:1138](/home/mark/kramtime/paralife/src/main/java/com/paralife/engine/SimulationEngine.java:1138) captures `currentPoolEnergies` before `checkPanicZone`; if panic-zone dissolves a composite, [SimulationEngine.java:1151](/home/mark/kramtime/paralife/src/main/java/com/paralife/engine/SimulationEngine.java:1151) re-adds the dissolved ID to `previousPoolEnergy` after `checkPanicZone` removed it. Smallest fix: rebuild `previousPoolEnergy` after panic checks from surviving, unprocessed composites.

- [PerceptionCodecRoundTripTest.java:89](/home/mark/kramtime/paralife/src/test/java/com/paralife/codec/PerceptionCodecRoundTripTest.java:89) uses the same `Event.MAG_CODES` source as production `eventHasMagnitude`, so it will not catch a future schema change where a new magnitude-bearing code is added to `ALL_CODES` but omitted from `MAG_CODES`. Smallest fix: add an independent schema-pinned assertion for current magnitude codes, or move code metadata to one enum/record source.

Regression note: the new panic-zone allocation is one `ArrayList` plus `O(C log C)` sort per death pass, where `C` is active composites. That is acceptable for the current expected registry size; keep it on the Phase 21 perf radar if composite counts grow.

I could not run targeted tests in this sandbox: Gradle wrapper tried to lock read-only `/home/mark/.gradle`, and local `gradle --gradle-user-home .gradle` failed before build start with wildcard-IP detection.

---

## Opencode Review

Let me read the key files to verify the pass-1 fix landed correctly and check for any new issues.Let me verify the full test suite compiles and runs (just the panic-zone and codec tests):## Review Verdict: **SHIP**

---

### Per-task table

| # | Task | Verdict |
|---|------|---------|
| T1 | PerceptionBroadcaster / @Order(100) sweep | **CONFIRMED** — zero hits across all source + CLAUDE.md |
| T2 | panic-zone snapshot+sort | **CONFIRMED** — `sortedComposites` sorts by `compositeId` (CHM key → unique → total order) at `SimulationEngine.java:1135-1136`, feeds `checkPanicZone` at :1146 which consumes `simRng.nextDouble()` at :1406 |
| T3 | Event.java invariant gate + codec round-trip | **CONFIRMED** — static-init block (`Event.java:46-56`) asserts `MAG_CODES ⊆ ALL_CODES` at class load; `eventHasMagnitude` delegates to `Event.MAG_CODES.indexOf(code) >= 0` (`PerceptionCodec.java:691`); `PerceptionCodecRoundTripTest.everyEventCodeRoundTrips` is `@MethodSource`-driven from `Event.ALL_CODES` and routes via `MAG_CODES` to exercise both magnitude and no-magnitude branches |
| T4 | golden-trace baseline correctness | **CONFIRMED** — only 2 of 26 keys changed (`trace-sess-9`, `trace-sess-21`); diff is strict 2-key surgical refresh |

---

### Other findings

- **Allocation overhead** — `new ArrayList<>` + `.sort()` per tick at `SimulationEngine.java:1135-1136` mirrors the existing pattern in `CompositeEnergyDistributor.java:101-102`. Acceptable for typical small-N composite registries. Not a regression.

---

## Consensus Summary

### Agreed Strengths
- T1 (PerceptionBroadcaster + `@Order(100)` sweep): all 4 reviewers CONFIRMED — `src/` and `CLAUDE.md` clean, residual hits only in historical `.planning/` docs.
- T2 (panic-zone snapshot+sort before `simRng`): all 4 CONFIRMED — `SimulationEngine.java:1135-1136` snapshots `compositeRegistry.getAll()` and sorts by `compositeId` (CHM key → unique → total order) before `checkPanicZone` consumes `simRng`.
- T3 (`MAG_CODES` invariant gate): all 4 CONFIRMED — `Event.java` static-init throws `AssertionError` if `MAG_CODES ⊄ ALL_CODES`; `eventHasMagnitude` and round-trip test both delegate to `Event.MAG_CODES`.
- T4 (golden-trace surgical refresh): all 4 CONFIRMED — only `trace-sess-9` and `trace-sess-21` digests changed; other 24 untouched.

### Agreed Concerns
- **T2 per-tick allocation cost (low / defer)** — claude, gemini, codex, opencode all flag the new `new ArrayList<>(compositeRegistry.getAll())` + `O(C log C)` sort per death pass. Mirrors existing `CompositeEnergyDistributor.onTick:101-102` pattern; acceptable for current registry size, track for Phase 21 perf pass if composite count grows.

### Divergent Views
- **Verdict tone**: claude/gemini/opencode = SHIP; codex = SHIP WITH FOLLOW-UPS. Substantive — codex raises two issues the others did not:
  - **Stale comment at `SimulationEngine.java:343`** claiming `TickBroadcaster` default order is `Integer.MAX_VALUE`; actual `@Order(50)`. Smallest fix: update comment to "Before TickBroadcaster(@Order(50))". Adjacent to T1's stale-doc cleanup; worth folding in.
  - **`previousPoolEnergy` reconstruction after `checkPanicZone`** at `SimulationEngine.java:1138/1151` — if panic-zone dissolves a composite, dissolved ID gets re-added. Smallest fix: rebuild `previousPoolEnergy` from surviving composites after panic checks. Not flagged by other 3 reviewers; warrants verification before deciding scope.
  - **T3 round-trip test source coupling** — codex notes test uses same `Event.MAG_CODES` constant as production, so won't catch a schema drift where a new magnitude code is added to `ALL_CODES` but omitted from `MAG_CODES`. Class-load `AssertionError` already covers this at boot, so codex's concern is partially mitigated; the static-init gate is the primary defence, not the test.
- **`eventHasMagnitude` lookup micro-opt** (claude only): swap `String.indexOf` for `BitSet`/`HashSet<Character>` mirror of `MAG_CODES`. Negligible; defer.
- **`validateActionArg` verb-list foot-gun** (claude only): same class of issue T3 closes; out of scope for 19.1, flag for Phase 21 codec-convention pass.
- **Phase 3b RNG-ordering closure note** (claude only): row-major iteration via `LiveEntityRegistry.snapshot()` already deterministic; one-line comment optional.
- **Test execution**: codex could not run gradle in sandbox (read-only `/home/mark/.gradle`, wildcard-IP detection); other 3 did not report execution attempts. Not a blocker — verification was static.
