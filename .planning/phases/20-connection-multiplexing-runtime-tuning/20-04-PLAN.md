---
phase: 20-connection-multiplexing-runtime-tuning
plan: 04
type: execute
wave: 4
depends_on: [20-01b, 20-02, 20-03]
files_modified:
  - .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md
autonomous: false
requirements: [SCALE-09]

must_haves:
  truths:
    - "20-RUNTIME.md exists and contains §3 Per-Scale-Tier Recipes populated with copy-pasteable launch commands for 100/500/1000-bot tiers (D-14: 20-RUNTIME.md is the canonical operator deliverable for Phase 20)."
    - "20-RUNTIME.md §1 includes the D-03 forward-note: revisit-multiplex-shape trigger conditions (Phase 21 evidence shows per-connection overhead is the binding constraint at 5000 conns/JVM AND tuning has been exhausted) — codifies that D-01 (single-connection) is the locked decision and D-03 is the only escape valve."
    - "Each recipe cites a specific JFR baseline file from Plan 1's profiles/ directory by exact filename including the c22e487 SHA suffix."
    - "JVM flags are documented presets only — no wrapper scripts are added (D-08)."
    - "Each recipe includes both server launch flags AND `loadHarnessJar` invocation, so Phase 21 can copy-paste."
    - "Per-tier GC and heap choices are either: (a) cited to a specific baseline JFR finding, OR (b) explicitly marked `Pending — JFR-driven; tuned in Plan 5/6` (D-13: no untraceable recommendations)."
    - "**Pass-2 Concern #8:** Yaml override examples in §3.2 / §3.3 do NOT include `queue-watermark-pct` overrides (the field is `[reserved — no effect in Phase 20]` per Plan 3 + Pass-2 Concern #7); recipe text explicitly notes the field is reserved, with link back to §2.2."
  artifacts:
    - path: ".planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md"
      provides: "20-RUNTIME.md skeleton with §1 (locked from CONTEXT D-01/D-02 rationale), §2 (Tuning Surface table), §3 (Per-Scale-Tier Recipes — populated by this plan), §4-6 (placeholders Plan 6 finalises)."
      contains: "§3 Per-Scale-Tier Recipes"
      min_lines: 200
  key_links:
    - from: "20-RUNTIME.md §3.1, §3.2, §3.3"
      to: "profiles/jfr-{100,500,1000}bots-baseline-c22e487.jfr"
      via: "filename citation in recipe text"
      pattern: "jfr-[0-9]+bots-baseline-c22e487\\.jfr"
    - from: "20-RUNTIME.md §3"
      to: "src/main/java/com/paralife/runtime/JettyRuntimeConfig.java + AppRuntimeConfig.java"
      via: "yaml override examples in recipes"
      pattern: "paralife\\.runtime\\."
---

<objective>
Create `20-RUNTIME.md` as the canonical Phase 20 spec doc (D-14) with the structural skeleton in place AND **§3 Per-Scale-Tier Recipes populated** with copy-pasteable JVM-flag presets for 100/500/1000-bot tiers (D-07 layer 1, D-08).

Purpose: Phase 21 benchmark scripts consume `20-RUNTIME.md` per-tier recipes directly — they MUST be copy-pasteable shell + yaml. D-13 demands every recipe cites a specific JFR. JVM flags ship as documentation, not wrapper scripts (D-08).

Output: `20-RUNTIME.md` with §1-§6 skeleton; §1 (D-01/D-02 rationale), §2 (Tuning Surface), §3 (per-tier recipes — fully populated by this plan) populated. §4 (Profile Findings) and §6 (Profile Index) get placeholders; Plan 6 finalises them. §5 (Forward Notes) populated.

**Pass-2 amendments:** Concern #7 retags `frameSizeBudgetBytes` `[reserved]` in §2.2 RUNTIME table. Concern #8 strips `queue-watermark-pct` override examples from §3.2 and §3.3 recipes (the knob has no Phase-20 consumer; presenting it as an effective tuning lever in copy-paste recipes that Phase 21 consumes verbatim would mislead).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-CONTEXT.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-RESEARCH.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-01-SUMMARY.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-02-SUMMARY.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-03-SUMMARY.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-REVIEW-DISPOSITIONS.md
@.planning/phases/17-durable-admission-control-backpressure/17-ADMISSION.md
@.planning/phases/18-external-load-harness-harness-identity/18-HARNESS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Write 20-RUNTIME.md skeleton with §1, §2, §5, §6 placeholders</name>
  <read_first>
    - .planning/phases/17-durable-admission-control-backpressure/17-ADMISSION.md (HEAD: lines 1-30 — header style, frontmatter, §1 token-taxonomy framing)
    - .planning/phases/18-external-load-harness-harness-identity/18-HARNESS.md (lines 1-50 — §1 architectural-principle framing for the WS:entity 1:1 rationale)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-CONTEXT.md (D-01, D-02, D-07 layer table, D-13/D-18 headline gauges, D-14 outline, **D-21 four-outcome SCALE-08 evidence acceptance + noise-floor convention**)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-RESEARCH.md (lines 738-805 — §7 RECOMMENDED 20-RUNTIME.md OUTLINE: every section, every table, every cross-ref)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-REVIEW-DISPOSITIONS.md (Pass-2 Concerns #7 + #8 — `frameSizeBudgetBytes` retagged `[reserved]`, no `queue-watermark-pct` overrides in recipes)
  </read_first>
  <files>.planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md</files>
  <action>
Create `.planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md` mirroring the `17-ADMISSION.md` / `18-HARNESS.md` style (target ~25 KB final size, this plan ships ~50% of that).

```markdown
# Phase 20: Runtime Tuning Spec

**Phase:** 20-connection-multiplexing-runtime-tuning
**Status:** Authoritative — locks D-01..D-21 from 20-CONTEXT.md
**Requirements:** SCALE-08 (overhead reduction, equivalent-strategy escape hatch), SCALE-09 (measured tuning, not guesswork)
**Profile baseline:** Commit SHA `c22e487` (Phase 19.1 close)

See also: `17-ADMISSION.md` §3 (STALLED FSM, the path Phase 20 tunes for stability under), `18-HARNESS.md` §1 (5000-conn/JVM design ceiling), `15-SCHEMA.md` §12 (codec validation bounds — Phase 20 D-10 codec opts MUST NOT relax).

***

## §1 Architectural Principle: WS:entity 1:1 (D-01 / D-02)

**Paralife treats every bot as if it were a user connecting from their own
machine.** Many concurrent independent WebSocket connections is a stated
architectural property (CLAUDE.md §Connection model; 18-HARNESS.md §1 D-05/D-21);
it is not an inefficiency to fix.

Phase 20 takes SCALE-08's "or equivalent transport-level scale strategy" escape
hatch (REQUIREMENTS.md):

> **SCALE-08:** High bot-count runs reduce socket or process overhead through
> connection multiplexing **or an equivalent transport-level scale strategy.**

The equivalent strategy is the four-layer tuning surface in §2: per-connection
cost is reduced by JVM/Jetty/application/codec tuning, never by collapsing
multiple entities onto a shared connection.

**This is a deliberate choice, NOT a missing optimisation.** Future contributors
who notice the 1000+ VT count or the per-session bounded queue should not "fix"
the design without an ADR per Phase 18 D-21.

**Forward note (D-03):** Revisit §1 only if Phase 21 evidence shows the
per-connection overhead path is the binding constraint at 5000 conns/JVM (the
18-HARNESS.md §1 D-02 ceiling) AND tuning under §2 has been exhausted.

***

## §2 Tuning Surface (D-07)

| Layer | Where it lives | Change-time | Phase 20 ships |
|-------|---------------|-------------|----------------|
| 1. JVM/runtime | `java -jar` launch flags, documented in §3 per-tier recipes | launch only | doc-only presets (D-08 — no wrapper script) |
| 2. Jetty/network | `paralife.runtime.jetty.*` `@ConfigurationProperties` (`JettyRuntimeConfig`) | launch (per Jetty `Configurable` API contract) | record + bindings + project-current defaults (Jetty defaults except `idleTimeoutMs=60000`) |
| 3. Application | `paralife.runtime.app.*` `@ConfigurationProperties` (`AppRuntimeConfig` with nested `OutboundConfig`, `EncodeConfig`) | mixed (live-tunable seams reserved for M5 admin UI) | record + bindings + defaults |
| 4. Codec impl | internal constants in `PerceptionCodec` and `Base64Codec` — JFR-validated | code change only | hot-path opts, no public surface |

### §2.1 Layer 2 — Jetty knobs (`JettyRuntimeConfig`)

| Knob | Default | Bound on | Notes |
|------|---------|----------|-------|
| `paralife.runtime.jetty.input-buffer-size` | 4096 | `Configurable.setInputBufferSize` | Jetty default; bytes |
| `paralife.runtime.jetty.output-buffer-size` | 4096 | `Configurable.setOutputBufferSize` | Jetty default; bytes |
| `paralife.runtime.jetty.max-frame-size` | 65536 | `Configurable.setMaxFrameSize` | T-20-DOS-1 cap |
| `paralife.runtime.jetty.max-binary-message-size` | 65536 | `Configurable.setMaxBinaryMessageSize` | bytes |
| `paralife.runtime.jetty.max-text-message-size` | 65536 | `Configurable.setMaxTextMessageSize` | bytes |
| `paralife.runtime.jetty.idle-timeout-ms` | 60000 | `Configurable.setIdleTimeout` | Project-current default (Jetty's own default is 30000); also bound via legacy `paralife.websocket.idle-timeout-ms` for back-compat |
| `paralife.runtime.jetty.auto-fragment` | true | `Configurable.setAutoFragment` | |

All seven are launch-only per Jetty 12's `Configurable` contract.

### §2.2 Layer 3 — Application knobs (`AppRuntimeConfig`)

**Pass-2 Concern #7:** All four fields in `AppRuntimeConfig` are tagged `[reserved — no effect in Phase 20]`. The binding surface exists for future consumers (M5 admin UI, Phase 19.1 parallel encode, future codec API extension); changing any of these in Phase 20 is a no-op observable to operators.

| Knob | Default | Notes |
|------|---------|-------|
| `paralife.runtime.app.outbound.queue-watermark-pct` | 80 | [reserved — no effect in Phase 20] slow-client warning threshold; consumer wiring deferred (M5 admin UI) |
| `paralife.runtime.app.outbound.frame-size-budget-bytes` | 1024 | [reserved — no effect in Phase 20] codec sizing hint; PerceptionCodec.encode(Frame) takes no capacity arg, consumer wiring deferred (Pass-2 Concern #7 — Plan 5 forbids the API change required to consume this) |
| `paralife.runtime.app.encode.parallel-encode-threshold` | -1 | [reserved — no effect in Phase 20] Phase 19.1 reservation; -1 = disabled |
| `paralife.runtime.app.encode.encode-batch-hint` | 8 | [reserved — no effect in Phase 20] tick-broadcast batch hint |

**D-20:** the existing `paralife.admission.backpressure.outbound-queue-size`
(default 128) is **NOT** moved into `paralife.runtime.app.*`. It stays in
`AdmissionConfig` for Phase 20; namespace consolidation is Phase 999.4. **The 128
queue depth remains the single most important live-tunable runtime knob in Phase
20** (it has a real consumer at `WorldWebSocketHandler.java:320`); see Phase 17
17-ADMISSION.md §3 for tuning guidance.

### §2.3 Layer 4 — Codec impl (D-10, JFR-driven only)

Internal constants in `src/main/java/com/paralife/codec/PerceptionCodec.java`
and `Base64Codec.java`. Specific opts are picked from JFR evidence per Plan 5;
no public configuration surface. Wire format stays bit-exact (`15-SCHEMA.md`
LOCKED).

***

## §3 Per-Scale-Tier Recipes

> Plan 4 ships these recipes with **placeholder GC choices** marked `Pending —
> JFR-driven; tuned in Plan 5`. Plan 5 (codec opts) and Plan 6 (final docs)
> upgrade them with measured-justified choices once the tuned-state JFRs land.

[Per-tier recipes go here — Task 4.2 populates.]

***

## §4 Profile Findings

> Populated by Plan 5 (codec opts) and Plan 6 (final write-up). Headline numbers
> (D-13 / D-18) live in §4.2.

### §4.1 Methodology

The Phase 20 capture ritual is documented in
`.planning/phases/20-connection-multiplexing-runtime-tuning/profiles/README.md`
and `tools/async-profiler-bootstrap.md`. Every committed JFR cites the source
SHA in its filename per D-19. **Headline gauges (`paralife.tick.health.work-time-ms`,
`paralife.outbound.detach.timeout`) are sourced from `/actuator/metrics/{name}`
JSON sidecars per Pass-2 Concern #10, not from server log grepping.** Noise-floor
convention per D-21: ±5% of baseline mean OR ±1σ, whichever is larger, computed
across the JFR sample window.

### §4.2 Headline numbers (D-13 / D-18)

| Metric | 100 baseline | 100 tuned | 500 baseline | 500 tuned | 1000 baseline | 1000 tuned |
|--------|------|------|------|------|------|------|
| `paralife.tick.health.work-time-ms` (mean ms) | _Pending — Plan 1b actuator sidecar_ | _baseline-only — see Phase 21_ | _Pending — Plan 1b actuator sidecar_ | _baseline-only — see Phase 21_ | _Pending — Plan 1b actuator sidecar_ | _Pending — Plan 5 actuator sidecar_ |
| `paralife.outbound.detach.timeout` (count) | _Pending_ | _baseline-only — see Phase 21_ | _Pending_ | _baseline-only — see Phase 21_ | _Pending_ | _Pending_ |
| `jdk.VirtualThreadPinned` (events/min @ 20ms) | _Pending_ | _baseline-only — see Phase 21_ | _Pending_ | _baseline-only — see Phase 21_ | _Pending_ | _Pending_ |

### §4.3 Per-tier narrative

_Plan 6 populates._

### §4.4 Codec hot-path opts (D-10)

_Plan 5 populates per-opt entries (JFR signal, code change summary, three-gate
stack run record, tuned-state JFR delta) — OR a single "(null-result)" / "(dominant pinning with backlog-handoff)" row per D-21 outcomes 3/4._

***

## §5 Forward Notes

- **Admin-UI live-tune (M5):** Both runtime records reserve `[live-tunable]` field tags and `@RefreshScope`-compatible shapes for the M5 admin observer UI.
- **Automated config search:** The CLI override surface (`-Dparalife.runtime.x=y` / env / `@TestPropertySource`) enables future ILP/grid/Bayesian sweeps over the `paralife.runtime.*` knobs without record redesign. Search algorithm itself is deferred (post-MVP).
- **Revisit-multiplex trigger (D-03):** Only if Phase 21 evidence shows per-connection overhead at 5000 conns/JVM is the binding constraint AND §3 tuning has been exhausted.
- **Namespace consolidation (Phase 999.4):** Future fold of `paralife.admission.backpressure.outbound-queue-size` → `paralife.runtime.app.outbound.queue-size` with deprecate-and-alias migration. D-20 deferred this to keep P20 MVP-direct.
- **Reserved-field consumer wiring (Phase 999.4 + Phase 19.1):** The four `[reserved]` fields in `AppRuntimeConfig` await consumers — `parallel-encode-threshold` for Phase 19.1 parallel encode; `frame-size-budget-bytes` for any future PerceptionCodec API that accepts capacity hints; `queue-watermark-pct` and `encode-batch-hint` for M5 admin UI.
- **Phase 19.1 follow-up:** parallel `PerceptionBroadcaster` will consume `AppRuntimeConfig.encode.parallelEncodeThreshold` (currently sentinel-disabled at -1).
- **Phase 999.5:** Re-capture the baseline against post-M4 HEAD for fresh apples-to-apples comparison; the c22e487-anchored baseline (D-19) is intentionally frozen for reproducibility.
- **Phase 999.6 (added Pass-1 Concern #2 disposition):** `vt-pinning-reentrantlock-conversion` — lifts if Plan 5 outcome 4 (dominant pinning with backlog-handoff per D-21) is reached, OR if Phase 21 benchmark evidence shows pinning is binding.

***

## §6 Profile Index

| Filename | Scenario | Source SHA | Captured | Size | Notes |
|----------|----------|------------|----------|------|-------|
| `profiles/jfr-100bots-baseline-c22e487.jfr` | 100 bots, balanced mix, seed=20251205 | c22e487 | _Plan 1b_ | _≤10 MB_ | baseline |
| `profiles/jfr-500bots-baseline-c22e487.jfr` | 500 bots, balanced mix | c22e487 | _Plan 1b_ | _≤10 MB_ | baseline |
| `profiles/jfr-1000bots-baseline-c22e487.jfr` | 1000 bots, balanced mix | c22e487 | _Plan 1b_ | _≤10 MB_ | baseline |
| `profiles/cpu-1000bots-baseline-c22e487.html` | async-profiler CPU @ 1000 | c22e487 | _Plan 1b_ | _≤10 MB_ | flamegraph |
| `profiles/alloc-1000bots-baseline-c22e487.html` | async-profiler alloc @ 1000 | c22e487 | _Plan 1b_ | _≤10 MB_ | flamegraph |
| `profiles/lock-1000bots-baseline-c22e487.html` | async-profiler lock @ 1000 | c22e487 | _Plan 1b_ | _≤10 MB_ | flamegraph |
| `profiles/metrics-{100,500,1000}bots-baseline-c22e487.json` | actuator metric sidecars (Pass-2 Concern #10) | c22e487 | _Plan 1b_ | _<1 KB_ | headline-gauge JSON snapshots |
| `profiles/jfr-1000bots-tuned-<HEAD>.jfr` | 1000 bots, tuned-state | _HEAD post-Plan 5_ | _Plan 5_ | _≤10 MB_ | post-codec-opts; Plan 5 commits |
| `profiles/metrics-1000bots-tuned-<HEAD>.json` | actuator metric sidecar (tuned) | _HEAD post-Plan 5_ | _Plan 5_ | _<1 KB_ | tuned-state headline-gauge JSON snapshot |

***

*Phase 20-connection-multiplexing-runtime-tuning · v1 · 2026-05-XX*
```

Replace `_Plan X_` and "_Pending_" markers as those plans land. Task 4.2 fills §3.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md && grep -q "## §1 Architectural Principle: WS:entity 1:1" .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md && grep -q "## §2 Tuning Surface" .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md && grep -q "## §3 Per-Scale-Tier Recipes" .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md && grep -q "## §6 Profile Index" .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md`
    - All six top-level sections present (§1..§6)
    - `grep -q "WS:entity 1:1" 20-RUNTIME.md` exits 0 (D-02 codification location 1)
    - `grep -q "c22e487" 20-RUNTIME.md` exits 0 (D-19 anchor cited)
    - `grep -q "D-20" 20-RUNTIME.md` exits 0 (alongside-not-move documented in §2.2)
    - `grep -q "D-08" 20-RUNTIME.md` exits 0 (no-wrapper-script principle documented in §2)
    - `grep -q "Phase 999.4" 20-RUNTIME.md` exits 0 (forward note for namespace consolidation)
    - `grep -q "Phase 999.6" 20-RUNTIME.md` exits 0 (forward note for ReentrantLock conversion per Pass-1 Concern #2 + D-21 outcome 4)
    - `grep -cE "\[reserved" 20-RUNTIME.md | awk '$1 >= 4 { exit 0 } { exit 1 }'` (Pass-2 Concern #7: §2.2 RUNTIME table tags all 4 fields as `[reserved]` including `frame-size-budget-bytes`)
    - `wc -l 20-RUNTIME.md` reports ≥120 lines (skeleton substantive)
  </acceptance_criteria>
  <done>20-RUNTIME.md skeleton exists with §1, §2 fully populated; §3-§6 placeholders ready for Task 4.2 / Plan 5 / Plan 6.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.2: Populate §3 Per-Scale-Tier Recipes — copy-pasteable for Phase 21 (human verification of recipe runnability)</name>
  <action>Edit 20-RUNTIME.md to fill §3 with three per-tier recipes (§3.1 100-bot, §3.2 500-bot, §3.3 1000-bot) using the templates in <how-to-verify>; each recipe cites the corresponding baseline JFR by exact filename including the c22e487 SHA suffix; each recipe explicitly marks GC choice as `Pending — JFR-driven` per D-13. Then a human runs each recipe to confirm copy-pasteability (server boots, harness connects).</action>
  <read_first>
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md (skeleton from Task 4.1)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-RESEARCH.md (lines 360-405 — verified JFR + bootJar + harness commands)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-01-SUMMARY.md (baseline JFR filenames; A8 ZGC default-on outcome)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/profiles/README.md (filename convention)
    - build.gradle.kts (lines 95-102 — exact `loadHarnessJar` task name + output jar pattern)
    - src/main/resources/application.yml (yaml override syntax for the new keys from Plans 2/3)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-REVIEW-DISPOSITIONS.md (Pass-2 Concern #8 — strip `queue-watermark-pct` from override blocks)
  </read_first>
  <what-built>Task 4.1 wrote the skeleton. This task populates §3 with three copy-pasteable per-tier recipes (100/500/1000 bots) AND a human verifies each recipe boots a server and accepts harness traffic at HEAD (no tuning needed yet — placeholder GC + heap is fine).</what-built>
  <how-to-verify>
**Step 1: AI populates §3.** The executor edits 20-RUNTIME.md to fill §3 with three sub-sections matching this template. **Pass-2 Concern #8: yaml override examples in §3.2 + §3.3 do NOT include `queue-watermark-pct`. The field is `[reserved]` per §2.2; adding it as an override would mislead Phase 21 benchmark scripts that copy-paste these recipes verbatim.**

```markdown
## §3 Per-Scale-Tier Recipes

> Each recipe is intentionally copy-pasteable. Phase 21 benchmark scripts
> consume these as-is. JVM flags are documented presets (D-08 — no wrapper
> script). GC choice is **JFR-driven**: the Plan 1 baseline JFR is cited in
> each recipe; Plan 5 / Plan 6 will replace `Pending — JFR-driven` markers
> with measured-justified choices.
>
> **Pass-2 Concern #8:** `paralife.runtime.app.outbound.queue-watermark-pct` is
> `[reserved — no effect in Phase 20]` per §2.2 — it has no Phase 20 consumer
> and overriding it produces no measurable effect. Recipe override examples
> deliberately omit it; the only live-tunable backpressure knob in Phase 20 is
> `paralife.admission.backpressure.outbound-queue-size` (default 128, see Phase
> 17 17-ADMISSION.md §3 for tuning guidance).

### §3.1 100-bot tier (operator-friendly, BotRunner-class)

**Server launch:**
```bash
java \
  -Xms1g -Xmx1g \
  -XX:+UseG1GC \
  -XX:StartFlightRecording=duration=180s,filename=jfr-100bots.jfr,settings=profile,name=p20-100 \
  -Djdk.virtualThreadScheduler.parallelism=4 \
  -jar build/libs/paralife-*.jar \
  --paralife.simulation.spawn.seed=20251205
```

**Harness:**
```bash
java -jar build/libs/paralife-*-load-harness.jar \
  --server-uri ws://localhost:8080/ws/world \
  --count 100 --duration 60 --ramp-up rate:20 \
  --harness-id bench-100
```

**Yaml overrides:** none required at default tier.

**Baseline JFR:** `profiles/jfr-100bots-baseline-c22e487.jfr` (Plan 1b).

**GC choice rationale:** _Pending — JFR-driven; G1 is the conservative default._

### §3.2 500-bot tier (single-harness)

**Server launch:** _(same as §3.1 with `-Xms1g -Xmx2g` and parallelism=6, JFR filename `jfr-500bots.jfr`)_

**Harness:** _(same as §3.1 with `--count 500 --duration 90 --ramp-up rate:50`, `--harness-id bench-500`)_

**Yaml overrides (optional):**
```yaml
# Pass-2 Concern #8: paralife.runtime.app.outbound.queue-watermark-pct is
# [reserved — no effect in Phase 20] per §2.2 — see "Pass-2 Concern #8" note
# at the top of §3 for rationale.
# The live-tunable backpressure knob is paralife.admission.backpressure.outbound-queue-size
# (default 128); tighten cautiously per 17-ADMISSION.md §3.
paralife:
  admission:
    backpressure:
      outbound-queue-size: 128   # default; reduce only with measured slow-client evidence
```

**Baseline JFR:** `profiles/jfr-500bots-baseline-c22e487.jfr` (Plan 1b).

**GC choice rationale:** _Pending — JFR-driven; G1 default; ZGC candidate at this tier if JFR shows >2% GC pause time._

### §3.3 1000-bot tier (M4 target)

**Server launch:**
```bash
java \
  -Xms2g -Xmx2g \
  -XX:+UseG1GC \
  -XX:StartFlightRecording=duration=180s,filename=jfr-1000bots.jfr,settings=profile,name=p20-1000 \
  -Djdk.virtualThreadScheduler.parallelism=8 \
  -jar build/libs/paralife-*.jar \
  --paralife.simulation.spawn.seed=20251205
```

**Harness:**
```bash
java -jar build/libs/paralife-*-load-harness.jar \
  --server-uri ws://localhost:8080/ws/world \
  --count 1000 --duration 180 --ramp-up rate:50 \
  --harness-id bench-1000
```

**Yaml overrides (optional, JFR-driven):**
```yaml
# Pass-2 Concern #8: paralife.runtime.app.outbound.queue-watermark-pct is
# [reserved — no effect in Phase 20] per §2.2 — see "Pass-2 Concern #8" note
# at the top of §3 for rationale.
paralife:
  runtime:
    jetty:
      input-buffer-size: 1024     # only if JFR shows TLAB pressure on Jetty buffers (Pitfall 5)
      output-buffer-size: 1024
  admission:
    backpressure:
      outbound-queue-size: 128    # default; tighten only with measured slow-client evidence per 17-ADMISSION.md §3
```

**Baseline JFR:** `profiles/jfr-1000bots-baseline-c22e487.jfr` + flamegraphs (Plan 1b).

**Tuned JFR target:** `profiles/jfr-1000bots-tuned-<HEAD>.jfr` (Plan 5 produces).

**GC choice rationale:** _Pending — JFR-driven; G1 baseline; ZGC switch IFF baseline JFR shows GC mean pause >5ms or `jdk.VirtualThreadPinned` correlates with stop-the-world events. Plan 5 replaces this paragraph with the chosen GC and the JFR finding that justified it._

**Pinning monitor:** `jdk.VirtualThreadPinned` event count from the baseline JFR is the headline. If count is non-trivial, Plan 5 may convert `synchronized(session)` → `ReentrantLock` per RESEARCH §Pitfall 2 (only with JFR evidence per D-10) — OR file Phase 999.6 per D-21 outcome 4 (dominant pinning with backlog-handoff).
```

**Step 2: Human verifies each recipe boots cleanly.**

For each tier (100, 500, 1000), the human:
1. Copies the §3.X recipe verbatim into a shell.
2. Confirms the server prints "Started ParalifeApplication" within 30s.
3. Confirms the harness connects (look for `connected=N` in stdout reaching the target count).
4. Stops the server cleanly (SIGTERM).
5. Confirms a JFR file with the recipe-named filename appears in CWD.

This is **smoke verification only** — no tuning numbers are gathered yet. The recipes must be copy-pasteable; that's the whole gate.

**Verification before reply:**
- §3.1 recipe boots and accepts 100-bot harness traffic
- §3.2 recipe boots and accepts 500-bot harness traffic
- §3.3 recipe boots and accepts 1000-bot harness traffic
- 20-RUNTIME.md §3 cites all three baseline JFR filenames from Plan 1b
- All three recipes carry an explicit `Pending — JFR-driven` marker for GC choice (D-13 honesty: no untraceable recommendations)
- Recipe yaml override blocks DO NOT include `queue-watermark-pct` (Pass-2 Concern #8 — verified by `grep -A 5 "yaml override" 20-RUNTIME.md | grep -c "queue-watermark-pct"` returning 0 inside the override yaml blocks; references in the explanatory prose are fine)
  </how-to-verify>
  <resume-signal>Reply `recipes-verified` with confirmation that §3.1 / §3.2 / §3.3 each booted server + accepted harness traffic. If any recipe fails to boot, name the broken line and the executor will fix-and-resubmit.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → 20-RUNTIME.md | doc only; no executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Plan 4 ships only documentation. The recipes' JVM flags themselves are operator-controlled and outside the application's threat surface. |
</threat_model>

<verification>
- 20-RUNTIME.md exists with §1-§6 skeleton populated
- §3 contains three sub-sections (§3.1, §3.2, §3.3) with copy-pasteable shell + yaml
- Each recipe cites a baseline JFR filename matching the c22e487 anchor
- Each recipe explicitly marks GC-choice rationale `Pending — JFR-driven` (D-13)
- Recipe yaml override blocks omit `queue-watermark-pct` (Pass-2 Concern #8)
- Human-verified: each recipe boots a server + accepts harness traffic at HEAD
</verification>

<success_criteria>
- 20-RUNTIME.md exists; §1, §2, §3, §5, §6 substantive; §4 placeholders ready for Plan 5/6
- Three per-tier recipes are runnable as-is
- D-08 honoured: no wrapper script committed; recipes are doc-only
- D-13 honoured: GC choices marked `Pending — JFR-driven` (Plan 5/6 fills with measured numbers)
- D-19 honoured: every JFR citation in §3 contains the c22e487 SHA suffix
- Pass-2 Concern #7 honoured: §2.2 RUNTIME table tags `frame-size-budget-bytes` `[reserved]`
- Pass-2 Concern #8 honoured: §3.2 + §3.3 recipe yaml override blocks omit `queue-watermark-pct` (with explanatory note at the top of §3)
</success_criteria>

<output>
After completion, create `.planning/phases/20-connection-multiplexing-runtime-tuning/20-04-SUMMARY.md` capturing:
- §3 recipe count (3) + each recipe's baseline JFR citation
- Human verification result per tier (100/500/1000 — booted yes/no)
- Pending markers Plan 5/6 must replace (list line numbers in 20-RUNTIME.md)
- Pass-2 Concerns #7 + #8 confirmation (frame-size-budget-bytes tagged [reserved] in §2.2; queue-watermark-pct absent from §3.2/§3.3 override blocks)
</output>
