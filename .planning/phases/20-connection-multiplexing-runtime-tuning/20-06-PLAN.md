---
phase: 20-connection-multiplexing-runtime-tuning
plan: 06
type: execute
wave: 5
depends_on: [20-01c, 20-02, 20-03, 20-04, 20-05]
files_modified:
  - .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md
  - .planning/phases/20-connection-multiplexing-runtime-tuning/20-VALIDATION.md  # Pass-3 Concern #30 — Task 6.5 flips frontmatter; previously omitted
  - .planning/phases/20-connection-multiplexing-runtime-tuning/20-CONTEXT.md  # de-stale 2026-06-04 — D-19 re-anchor reconciliation promised by 20-RUNTIME.md §6 (Task 6.1 step 7)
  - .planning/phases/20-connection-multiplexing-runtime-tuning/profiles/README.md  # R1 multi-review — "baseline (always c22e487)" framing superseded by Plan 1c re-anchor (Task 6.1 step 7)
  - CLAUDE.md
  - README.md
  - src/main/java/com/paralife/websocket/WorldWebSocketHandler.java
  - src/main/java/com/paralife/admission/OutboundSender.java
autonomous: true
requirements: [SCALE-08, SCALE-09]

must_haves:
  truths:
    - "20-RUNTIME.md is fully populated: §1, §2, §3, §4 (incl. headline numbers + per-tier narrative), §5, §6 (full Profile Index)."
    - "**D-02 three-place rationale codification is complete:** README.md, CLAUDE.md §Runtime tuning, AND inline comments at WorldWebSocketHandler WS-upgrade + OutboundSender attachSession sites — all three locations grep for `WS:entity 1:1`."
    - "CLAUDE.md §Runtime tuning subsection lands AFTER the existing §Connection model (Phase 18, D-05 / D-21) block (D-15: Phase 20 demands a CLAUDE.md cross-reference subsection)."
    - "README.md gains an operator paragraph (D-16) plus minimal scaffolding (project name, one-line value-prop) — current 1-line file is replaced with a small but readable structure."
    - "Inline comments cite Phase 20 D-02 + reference 20-RUNTIME.md §1 + 18-HARNESS.md §1 + CLAUDE.md §Connection model."
    - "Three-gate stack + full suite stay green after the doc/comment changes."
    - "**SCALE-08 inheritance (REVISED per cross-AI review concern #1 + Pass-2 Concern #9 amendment to D-21):** Plan 6 inherits SCALE-08 by citing the recipe-runnable evidence in `20-RUNTIME.md` §3 (per-tier recipes copy-pasteable for Phase 21) and §4 (headline numbers + per-tier narrative + codec/runtime opt log). Plan 5 produces ONE OF the four D-21 outcomes: (1) measured delta on `paralife.tick.health.work-time-ms` or `paralife.outbound.detach.timeout` from a codec opt, (2) JFR-justified runtime-knob tightening with measured delta, (3) a documented null-result citing baseline-floor JFR evidence, OR (4) **dominant pinning with backlog-handoff** (Pass-2 Concern #9 — Phase 999.6 backlog stub + tuned-state equivalence capture). All four are valid SCALE-08 evidence per D-21. **Per-tier evidence concentrates at the 1000-bot tier (Pass-2 Concern #17); 100/500-bot baselines are reproducibility anchors handed off to Phase 21.** Plan 6 verifies §4.2 / §4.4 visibility of whichever Plan 5 outcome shipped, not the delta direction itself."
    - "20-VALIDATION.md frontmatter flipped to `nyquist_compliant: true` and `wave_0_complete: true` with the Per-Task Verification Map populated from actual task IDs across all 8 plans — 20-01, 20-01b (superseded by 20-01c; rows kept with superseded status), 20-01c (no GSD task-ID structure; mapped as prose-section rows A/B/C), 20-02, 20-03, 20-04, 20-05, 20-06 (I7 fix)."
  artifacts:
    - path: ".planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md"
      provides: "FINAL — every section populated, every Pending marker replaced, Profile Index lists every committed JFR + flamegraph + actuator metric sidecar."
      contains: "## §4.3 Per-tier narrative"
      min_lines: "tiered per Pass-2 Concern #13: ≥350 for outcomes 1/2/4 (codec / knob / pinning — substantial narrative justified); ≥250 for outcome 3 (concise null-result write-up correct) — pinning regrouped to 350 per the canonical disposition, R3 multi-review"
    - path: "CLAUDE.md"
      provides: "MUTATED: new ### Runtime tuning (Phase 20) subsection after § Connection model block."
    - path: "README.md"
      provides: "MUTATED: minimal structure + ## Runtime tuning operator paragraph (D-16)."
    - path: "src/main/java/com/paralife/websocket/WorldWebSocketHandler.java"
      provides: "MUTATED: D-02 inline comment block above the outboundSender.attachSession(...) call at line ~320."
    - path: "src/main/java/com/paralife/admission/OutboundSender.java"
      provides: "MUTATED: D-02 inline comment block above the Thread.ofVirtual().name(\"ws-sender-...\")... start() call at ~L136-138 (match on code block, not line numbers)."
  key_links:
    - from: "README.md"
      to: ".planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md + CLAUDE.md §Connection model"
      via: "markdown links"
      pattern: "20-RUNTIME\\.md"
    - from: "CLAUDE.md §Runtime tuning"
      to: "20-RUNTIME.md §1 + §3"
      via: "markdown reference"
      pattern: "20-RUNTIME\\.md"
    - from: "WorldWebSocketHandler.java + OutboundSender.java inline comments"
      to: "20-RUNTIME.md §1 + 18-HARNESS.md §1 + CLAUDE.md §Connection model"
      via: "javadoc-style citation"
      pattern: "Phase 20 D-02"
---

<objective>
Finalise the Phase 20 deliverable: complete `20-RUNTIME.md` (§4.3 per-tier narrative, full §6 Profile Index), land the **D-02 three-place rationale codification** (`README.md`, `CLAUDE.md` §Runtime tuning, in-code comments at the WS upgrade and `OutboundSender.attachSession` sites), and confirm three-gate + full suite stay green.

Purpose: D-14 (canonical 20-RUNTIME.md spec doc); D-15 (CLAUDE.md cross-reference); D-16 (README.md operator paragraph); D-02 (three-place rationale lock against future "optimisation" of the WS:entity 1:1 design); D-13/D-18 headline numbers visible in §4.2.

Output: complete `20-RUNTIME.md`, mutated `CLAUDE.md` + `README.md`, two inline comment blocks in source. Tests stay green.

**Pass-2 amendments:** Concern #9 amends SCALE-08 inheritance truth to enumerate all four D-21 outcomes including dominant-pinning-with-backlog-handoff. Concern #13 tiers the line-count floor (≥350 for outcomes 1/2/4; ≥250 for outcome 3 null-result — honesty over volume; pinning regrouped per the canonical disposition, R3/R4 multi-review). Concern #17 reframes 100/500-bot tuned-tier expectation: those tiers are baseline-only in Phase 20; per-tier benchmark evidence is Phase 21's deliverable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-CONTEXT.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-RESEARCH.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-PATTERNS.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-01-SUMMARY.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-01c-SUMMARY.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-02-SUMMARY.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-03-SUMMARY.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-04-SUMMARY.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-05-SUMMARY.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md
@.planning/phases/20-connection-multiplexing-runtime-tuning/20-REVIEW-DISPOSITIONS.md
@CLAUDE.md
@README.md
@src/main/java/com/paralife/websocket/WorldWebSocketHandler.java
@src/main/java/com/paralife/admission/OutboundSender.java

<interfaces>
<!-- WorldWebSocketHandler comment-insertion site (line 317-320): -->

```java
@Override
public void afterConnectionEstablished(WebSocketSession session) {
    sessionRegistry.register(session);
    if (outboundSender != null) {
        outboundSender.attachSession(session, admissionConfig.backpressure().outboundQueueSize());
    }
```

<!-- OutboundSender.attachSession comment-insertion site (~L128-138 as of 2026-06-04; match on the code block, not line numbers): -->

```java
public void attachSession(WebSocketSession session, int queueCapacity) {
    String id = session.getId();
    ...
    ArrayBlockingQueue<Frame> queue = new ArrayBlockingQueue<>(queueCapacity);
    queues.put(id, queue);
    overflowFiredFlags.put(id, new AtomicBoolean(false));
    Thread t = Thread.ofVirtual()
            .name("ws-sender-" + id)
            .start(() -> drainLoop(session, queue));
```

<!-- CLAUDE.md insertion point: after the last content line of `### Connection model (Phase 18, D-05 / D-21)` (~L141) and BEFORE the `GSD:architecture-end` marker comment (~L142) — Runtime tuning is architecture content and must stay inside the GSD architecture block. `### Outbound concurrency (Phase 17, D-10)` sits ABOVE Connection model — do not insert there. RESEARCH §8 says "after §Connection model" — verify exact lines by Read first; they drift. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Finalise 20-RUNTIME.md — populate §4.3 per-tier narrative + complete §6 Profile Index</name>
  <read_first>
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md (current state from Plans 4 + 5 — note which Pending markers remain)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-01c-SUMMARY.md (canonical baseline provenance — `62c1b44` churn series + `103a615` active-50xfood series; **Pass-2 Concern #10:** per-tier metric sidecar baseline values. 20-01b's `c22e487` series is SUPERSEDED — on disk for history only)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-05-SUMMARY.md (tuned JFR + tuned metric sidecar filename + delta summary; **Plan 5 outcome label per D-21 — determines line-count tier per Pass-2 Concern #13**)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-04-SUMMARY.md (Pending markers Plan 6 must replace)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/profiles/ (`ls -lh` to get exact filenames + sizes)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/profiles/metrics-1000bots-active-50xfood-103a615.json + metrics-1000bots-active-50xfood-tuned-424e06d.json (Pass-2 Concern #10 — JSON source for §4.2 1000-tier values; ALREADY populated by Plan 5 — verify only) + metrics-{100,500}bots-baseline-62c1b44.json (source for the §4.2 100/500-baseline `_Pending_` cells)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-REVIEW-DISPOSITIONS.md (Pass-2 Concerns #13 + #17)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-05-TRIAGE.md (outcome-label contract — step 4 checks §4.4 consistency against its line 1; R2 multi-review: referenced by step 4 but previously absent here)
  </read_first>
  <files>.planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md</files>
  <action>
Edit `20-RUNTIME.md`:

1. **§3 per-tier recipes** — replace ALL remaining `Pending — JFR-driven` markers (GC-choice rationale ×3 at §3.1/§3.2/§3.3, heap-presets preamble, VT-scheduler-parallelism rationale at §3.3). For **GC choice** and **parallelism=8**, write measured-justified statements from the Plan 5 evidence (D-21 outcome 3 null-result: tuned JFR `jfr-1000bots-active-50xfood-tuned-424e06d.jfr` shows 4 GCPhasePause ≈ 0.05% wall-clock + 0 VirtualThreadPinned; baseline 90 s window captured 0 pauses; no carrier saturation observed). G1 stays at all tiers — state that explicitly with the JFR citation. For **heap presets**, do NOT claim measured justification (R3 multi-review): every capture — churn `62c1b44`, active `103a615`, tuned `424e06d` — ran `-Xms2g -Xmx2g` (per the `.meta.json` `jvm_flags`), so the 100-tier `1g/1g` and 500-tier `1g/2g` recipe presets were never exercised by any JFR run. Replace the heap `Pending` marker with the honest statement: no heap retune was performed in Phase 20; lower-tier presets are operator smoke presets sized by headroom judgement, NOT JFR-validated; baseline-comparable reproduction must use the `2g/2g` capture shape from the meta sidecars. Do not change the recipe values themselves. **Also settle the stale forward-reference prose (R1 multi-review):** rewrite the §3 intro at ~L100 ("Plan 5 / Plan 6 will replace `Pending — JFR-driven` markers…") and the full ~L118-119 sentence ("Plan 5 tunes against the active profile; Plan 6 finalises §4 numbers") to past tense; rewrite the "Plan 5/6 wires the reproducible per-tier active recipe" clauses at ~L143/~L155 to settled form (no rig was wired — null-result; recipes stand as-documented, Phase 21 consumes them); fix the §3.3 **Tuned JFR target** line at ~L314 — the placeholder `jfr-1000bots-tuned-<HEAD>.jfr (Plan 5 produces)` never shipped under that name; the actual artifact is `jfr-1000bots-active-50xfood-tuned-424e06d.jfr`. Word the rewrites so the step-6 audit grep comes back clean.

2. **§4.3 Per-tier narrative** — write 1-3 paragraphs per tier (100/500/1000) describing what the JFR + flamegraphs + actuator metric sidecar revealed, what (if anything) was changed, and the resulting delta. Each paragraph cites a specific JFR file by name (containing the SHA segment). **Each §4.3.N heading must carry its tier's `jfr-{N}bots-baseline-62c1b44.jfr` filename verbatim as in the template below** — the ≥9 citation-count gate has zero margin without them (R2 multi-review). Format:

```markdown
### §4.3 Per-tier narrative

#### §4.3.1 100-bot tier (`profiles/jfr-100bots-baseline-62c1b44.jfr` + `profiles/metrics-100bots-baseline-62c1b44.json`; active-scenario contrast: `profiles/jfr-100bots-active-50xfood-103a615.jfr`)

[1-2 paragraphs: what the JFR + actuator metric sidecar showed at 100 bots — GC pause distribution, allocation rate, pinning event count, baseline `paralife.tick.health.work-time-ms` mean, baseline `paralife.outbound.detach.timeout` count, any standout flamegraph stack. Likely conclusion at 100: "headroom abundant, no opt needed at this tier; default G1 showed headroom in the 2g/2g capture; the 1g recipe heap is an operator smoke preset, not JFR-validated" — R4 multi-review: do NOT write "1g heap sustains"; the captures ran 2g/2g (see step 1 heap-honesty rule).]

**Pass-2 Concern #17:** 100-bot tier is **baseline-only** in Phase 20. Per-tier benchmark evidence (i.e., re-running the harness against tuned HEAD) is Phase 21's deliverable; D-13 inheritance truth applies to the tier where tuning is shipped (1000-bot).

#### §4.3.2 500-bot tier (`profiles/jfr-500bots-baseline-62c1b44.jfr` + `profiles/metrics-500bots-baseline-62c1b44.json`; active-scenario contrast: `profiles/jfr-500bots-active-50xfood-103a615.jfr`)

[1-2 paragraphs: same shape. Likely conclusion: "behaviour scales linearly; <observation>."]

**Pass-2 Concern #17:** 500-bot tier is **baseline-only** in Phase 20 (same rationale as §4.3.1).

#### §4.3.3 1000-bot tier (`profiles/jfr-1000bots-active-50xfood-103a615.jfr` + `profiles/jfr-1000bots-active-50xfood-tuned-424e06d.jfr` + matching metric sidecars; churn baseline `profiles/jfr-1000bots-baseline-62c1b44.jfr` for contrast)

[2-3 paragraphs: the meat of the work — every codec opt from Plan 5 referenced (or null-result / dominant-pinning-with-backlog-handoff per D-21 outcomes 3/4 explicitly framed), every recipe override in §3.3 justified, and the tuned-state delta on `paralife.tick.health.work-time-ms` + `paralife.outbound.detach.timeout` (sourced from the actuator metric sidecars per Pass-2 Concern #10) + `jdk.VirtualThreadPinned` quantified. Classification vs noise floor (D-21 ±5% of baseline mean OR ±1σ — Pass-2 Concern #11) recorded.]
```

3. **§4.2 Headline numbers** — 1000-tier cells are ALREADY populated (Plan 5 + EXEC-R1 fix round `870440d`) from `metrics-1000bots-active-50xfood-103a615.json` + `metrics-1000bots-active-50xfood-tuned-424e06d.json` per the §4.2 1000-tier footnote — verify, do not re-derive (Pass-2 Concern #10 satisfied). Remaining work: fill ALL THREE rows' 100/500-**baseline** cells (R1 multi-review — the plain `_Pending_` cells on the detach.timeout and VirtualThreadPinned rows are in scope, not just the work-time-ms row's `_Pending — Plan 1c actuator sidecar_` cells). Sources: `paralife.tick.health.work-time-ms` + `paralife.outbound.detach.timeout` from the `samples` arrays in `metrics-100bots-baseline-62c1b44.json` + `metrics-500bots-baseline-62c1b44.json` (keys `paralife_tick_health_work-time-ms`, `paralife_outbound_detach_timeout`); `jdk.VirtualThreadPinned` from `jfr print --events jdk.VirtualThreadPinned .planning/phases/20-connection-multiplexing-runtime-tuning/profiles/jfr-{100,500}bots-baseline-62c1b44.jfr` (full repo-root path — R3 multi-review; window duration from the matching `.meta.json`). **Add a one-line scenario note to the §4.2 footnote (R3 multi-review):** the baseline row mixes scenarios — 100/500 baseline = churn `62c1b44`, 1000 baseline = active-50xfood `103a615` (per the 20-01c-SUMMARY:144-147 directive); say so explicitly so the headline table isn't silently apples-to-oranges. **Preserve the existing table markdown format** — backtick-wrapped metric names, pipe-delimited columns — the acceptance gauge grep depends on it (R2 multi-review). **Pass-2 Concern #17:** 100-bot and 500-bot tiers are baseline-only in Phase 20 (the M4 target is 1000 bots; per-tier benchmark evidence is Phase 21's deliverable) — the "_baseline-only — see Phase 21_" tuned cells are already in place; leave them. D-13 per-tier evidence requirement applies to the tier where tuning is shipped; incidental effects at lower tiers are Phase 21's gate.

4. **§4.4 Codec hot-path opts** — Plan 5 declared **outcome 3 (null-result)** and already populated this table (single null-result row citing the active-50xfood-103a615 JFR signals), and the EXEC-R1 fix round (`870440d`) reworded the GC framing criterion-explicitly (90.8 ms ≈ 0.05% wall-clock vs the >2% GC-pause-time ZGC trigger). Verify the row is present and its outcome label is consistent with `20-05-TRIAGE.md` line 1 (`null-result: ...`). Do NOT reformat toward any template — the shipped row is richer than the original plan template and is the reviewed-converged form (EXEC-R1, `20-05-MULTIREVIEW-EXEC-R1.md`).

5. **§6 Profile Index** — structurally complete for the LIVE generations only (`62c1b44` churn, `103a615` active-50xfood, `424e06d` tuned). **The superseded `c22e487` generation has NO index rows despite 12 committed files on disk (R1 multi-review — the must-have says the index lists every committed artifact).** Remaining work:
   - Replace placeholder cells: sizes (`_≤10 MB_` / `_~KB-class_`) from `ls -lh .planning/phases/20-connection-multiplexing-runtime-tuning/profiles/`; capture dates from the `.meta.json` **`captured_utc`** field (R4 multi-review — the field is `captured_utc`, not `captured_at`; present in the `62c1b44` and `tuned-424e06d` metas). **The `103a615` active metas carry NO timestamp field** — for those, use the first `sample_utc` from the matching `metrics-*-active-50xfood-103a615.json` sidecar and mark the cell `(from metric sidecar)`. Do not invent dates. **Keep the provenance labels** — the Captured column should read like `Plan 1c — 2026-05-27`, not a bare timestamp (R3 multi-review: `_Plan 1c_` is a traceability link, don't lose it).
   - **Stamp the footer:** the last line of `20-RUNTIME.md` reads `· v1 · 2026-06-XX` — replace `2026-06-XX` with the completion date (R4 multi-review — the placeholder evades the zero-pending audit; a "FINAL" doc must not ship with `XX`).
   - **Add a history-only row group for the superseded `c22e487` series** (compressed-brace form matching the existing index style): `jfr-{100,500,1000}bots-baseline-c22e487.jfr` + matching `.meta.json` sidecars + `{cpu,alloc,lock}-1000bots-baseline-c22e487.html` + `metrics-{100,500,1000}bots-baseline-c22e487.json` (R2 multi-review — all 12 committed files, incl. the 3 actuator metric sidecars; mirror the 62c1b44 group's sidecar row) — Notes column marks them "superseded by Plan 1c re-anchor (`62c1b44`); retained on disk for history; not cited by §3/§4".
   - **Reword the active-50xfood Notes cell** (~L388 "**transport-overhead evidence — Plan 5/6 tune against this set**") to settled form, e.g. "transport-overhead evidence — Plan 5's tuning evidence set (null-result; see §4.4)" (R2 multi-review — stale forward-ref the audit's verb list previously missed; avoid the literal sequence "Plan 5 tuned" so the widened audit regex stays clean).
   - **Amend the tuned-flamegraph row** (`{cpu,alloc,lock}-1000bots-active-50xfood-tuned-424e06d.html`, currently "_deferred to Plan 6_"): decision 2026-06-04 — NOT captured. Reword to "not captured — null-result (D-21 outcome 3); JFR-event triage sufficed; baseline/active flamegraphs remain the reference". No capture run in this plan.
   - Do NOT add a table row for `profiles/README.md` (R4 multi-review — it's a documentation file, not a profile artifact; the §6 table schema is Filename/Scenario/SHA/Captured/Size, and the README is already cited in the §6 preamble at ~L332).

6. **Audit pass:** every `_Pending_` / `_Plan X populates_` marker AND every future-tense forward-reference is replaced (R1 multi-review — widened so the audit also catches the §3 prose step 1 rewrites). Run `grep -inE "pending|plan [0-9/]+ (populates|produces|finalises|wires|will|tunes?)" 20-RUNTIME.md` — expect ZERO matches (R2: `tunes?` added for the ~L388 Notes cell). If a hit remains, it is either a missed marker (fix it) or prose that must be reworded to settled past tense (the §4.2 100-tuned / 500-tuned cells read `_baseline-only — see Phase 21_` per Pass-2 Concern #17, which contains neither pattern and is intentional Phase-21 hand-off, not pending).

7. **D-19 doc-drift reconcile (promised by §6):** `20-RUNTIME.md` §6 preamble + Phase 999.5 note both state "D-19 in 20-CONTEXT.md still nominally cites the superseded `c22e487` capture — Plan 6 reconciles". Apply it:
   - Annotate the D-19 bullet in `20-CONTEXT.md` (~L208) with "(re-anchored to `62c1b44` by Plan 1c F6; active-scenario evidence set `103a615`; tuned capture `424e06d` — see 20-RUNTIME.md §6)".
   - **Annotate the other three stale `c22e487` references in `20-CONTEXT.md` the same way (R1 multi-review — the D-19 bullet alone leaves the file self-contradictory):** ~L225 (D-21 outcome-3 definition "the c22e487 baseline is at the … performance floor" — actual evidence set is `103a615` active + `424e06d` tuned), ~L327 (sidecar filename expectations `metrics-{N}bots-baseline-c22e487.json` / `metrics-1000bots-tuned-{HEAD}.json` — shipped names use `62c1b44` / `active-50xfood-tuned-424e06d`), ~L463 (frozen-baseline note). Append a short "(superseded by Plan 1c re-anchor — see D-19 annotation)" parenthetical to each; do NOT rewrite the surrounding decision history.
   - **De-stale `profiles/README.md` (R1 multi-review — operator-facing and actively misleading):** the "`baseline` (always `c22e487`)" framing at ~L11-12/~L25 and the "## c22e487 baseline ritual" section predate the Plan 1c re-anchor. Update: state is `baseline` (churn, canonical `62c1b44`; original `c22e487` series superseded/history-only), `active-50xfood` (`103a615`), or `tuned` (HEAD short-SHA, e.g. `424e06d`). Retitle/annotate the ritual section so an operator reproducing the baseline checks out `62c1b44`, not `c22e487`. **Also update every tuned filename example (R3 multi-review):** the naming table (~L17 `jfr-{N}bots-tuned-{HEAD_SHA}.jfr`) and the "## Re-running (tuned-state captures)" section (~L75-99) still show the non-scenario tuned shape; the shipped pattern is scenario-aware — `jfr-{N}bots-{scenario}-tuned-{HEAD_SHA}.jfr`, concrete example `jfr-1000bots-active-50xfood-tuned-424e06d.jfr`. Keep the edit minimal — naming-convention table + ritual SHA/filename references, no restructure.
   - Strip both now-resolved deferred-cleanup parentheticals from `20-RUNTIME.md` — the first lives in **§5 Forward Notes** (~L370 Phase 999.5 bullet, "…Plan 6 reconciles…"), the second in **§6** (~L377 "…known doc-drift cleanup deferred to Plan 6 + the 20-06-PLAN VALIDATION flip…") (R2 multi-review — previous wording placed both in §6; locate by the quoted text, not the section).
  </action>
  <verify>
    <automated>test -f .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md && grep -q "## §4.3 Per-tier narrative" .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md && grep -qE "jfr-1000bots-active-50xfood-(103a615|tuned-424e06d)" .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md && grep -q "metrics-1000bots-active-50xfood-103a615" .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md && grep -q "metrics-1000bots-active-50xfood-tuned-424e06d" .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md</automated>
  </verify>
  <acceptance_criteria>
    - **Working directory (R2 multi-review):** bare `20-RUNTIME.md` paths below are phase-dir-relative — run these from `.planning/phases/20-connection-multiplexing-runtime-tuning/` (the `<verify><automated>` block carries repo-root full paths).
    - **Pass-2 Concern #13 (tiered line-count floor):** Read Plan 5's outcome label from `20-05-SUMMARY.md`. If outcome was `triaged: ...` (codec opts shipped — D-21 outcome 1) OR `runtime-knob-tightened: ...` (D-21 outcome 2): `wc -l 20-RUNTIME.md` reports ≥350 lines (substantial narrative justified). If outcome was `pinning-dominates-with-backlog-handoff: ...` (D-21 outcome 4 — Pass-2 Concern #9): ALSO ≥350 (R3 multi-review — the Concern #13 disposition groups pinning with the substantial-narrative outcomes). If outcome was `null-result: ...` (D-21 outcome 3): `wc -l 20-RUNTIME.md` reports ≥250 lines (concise null-result write-up correct; padding to 350 is anti-pattern per Pass-2 Concern #13). The verification command should test the appropriate floor for the actual Plan 5 outcome:
      ```
      OUTCOME=$(grep -E "^Plan 5 outcome:" .planning/phases/20-connection-multiplexing-runtime-tuning/20-05-SUMMARY.md | head -1)
      LINES=$(wc -l < .planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md)
      case "$OUTCOME" in
        *"triaged"*|*"runtime-knob-tightened"*|*"pinning-dominates-with-backlog-handoff"*) [ "$LINES" -ge 350 ] || exit 1 ;;
        *"null-result"*) [ "$LINES" -ge 250 ] || exit 1 ;;
        *) [ "$LINES" -ge 250 ] || exit 1 ;;  # default to lower bar if outcome unclear
      esac
      # R1 multi-review: no trailing colons in the patterns — the live signal is `Plan 5 outcome: null-result`
      # (no colon after the label); colon-suffixed patterns silently fall through to the default branch.
      # R3 multi-review: pinning-dominates moved to the >=350 branch per the canonical Concern #13 disposition
      # (substantial narrative justified for codec/knob/pinning; only a documented null-result earns 250).
      # Inert this run (Plan 5 outcome = null-result) — keeps plan and disposition aligned.
      ```
    - `grep -q "## §4.3 Per-tier narrative" 20-RUNTIME.md` — narrative section exists
    - `grep -q "## §4.4 Codec hot-path opts" 20-RUNTIME.md` — codec section exists
    - `for t in 100 500 1000; do grep -q "jfr-${t}bots-baseline-62c1b44\\.jfr" 20-RUNTIME.md || exit 1; done` — each canonical (62c1b44) baseline JFR tier cited explicitly (R1 multi-review: per-tier check, not just a total)
    - `grep -cE "jfr-(100|500|1000)bots-baseline-62c1b44\\.jfr" 20-RUNTIME.md | awk '$1 >= 9 { exit 0 } { exit 1 }'` — citations grow past the pre-execution count of 6 once the §4.3 narrative cites each tier (R1 multi-review: ≥6 was already satisfied before execution — under-constrained)
    - `grep -qE "jfr-(\\{[0-9,]+\\}|1000)bots-baseline-c22e487\\.jfr" 20-RUNTIME.md && grep -qE "metrics-(\\{[0-9,]+\\}|1000)bots-baseline-c22e487\\.json" 20-RUNTIME.md` — superseded `c22e487` history-only row group present in §6 incl. the actuator metric sidecars (R1 multi-review; sidecar grep added R2; R3: patterns tolerate BOTH compressed-brace form `{100,500,1000}bots` AND literal per-tier rows — the previous literal-only greps rejected the brace form step 5 itself prescribes, the R1-H1 defect class)
    - `grep -qE "jfr-1000bots-active-50xfood-tuned-424e06d" 20-RUNTIME.md` — tuned JFR cited
    - **Pass-2 Concern #10:** `grep -qE "metrics-1000bots-active-50xfood-(103a615|tuned-424e06d)" 20-RUNTIME.md` — actuator metric sidecars cited in §4 narrative + §6 index
    - `grep -E "^\\| .?paralife\\.(tick\\.health\\.work-time-ms|outbound\\.detach\\.timeout)" 20-RUNTIME.md | grep -vE "Pending|TBD|^_pending_" | wc -l | awk '$1 >= 2 { exit 0 } { exit 1 }'` — D-13 + D-18 gauges have non-Pending populated rows (R1 multi-review: `.?` absorbs the markdown backtick the table rows wrap the metric name in — the previous pattern matched nothing even on a fully populated table; the `_baseline-only — see Phase 21_` cells are NOT Pending — they're explicit Phase-21-handoff per Pass-2 Concern #17)
    - **Pass-2 Concern #17:** `grep -qE "baseline-only.*Phase 21" 20-RUNTIME.md` — 100/500-tuned cells explicitly framed as Phase 21 hand-off
    - **Zero-pending audit as a gate (R3 multi-review):** `! grep -iqE "pending|plan [0-9/]+ (populates|produces|finalises|wires|will|tunes?)" 20-RUNTIME.md` — the step-6 audit is enforced here, not just described; this is what catches a still-`_Pending_` `jdk.VirtualThreadPinned` cell (the gauge grep above only covers the two `paralife.*` rows)
  </acceptance_criteria>
  <done>20-RUNTIME.md is fully populated and matches the 17-ADMISSION.md / 18-HARNESS.md depth standard, with line-count floor tiered by Plan 5 outcome (Pass-2 Concern #13) and 100/500-tier baseline-only framing (Pass-2 Concern #17).</done>
</task>

<task type="auto">
  <name>Task 6.2: Add CLAUDE.md §Runtime tuning subsection (D-15)</name>
  <read_first>
    - CLAUDE.md (FULL FILE — locate `### Connection model (Phase 18, D-05 / D-21)` block, end at the line before next `###` heading, OR end before `## Project Skills` section if it's the last subsection)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-RESEARCH.md (lines 814-832 — exact CLAUDE.md insertion template)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-PATTERNS.md (entry for CLAUDE.md — style template from §markStalled close-then-best-effort-OOB)
  </read_first>
  <files>CLAUDE.md</files>
  <action>
Insert the new `### Runtime tuning (Phase 20)` subsection AFTER the existing `### Connection model (Phase 18, D-05 / D-21)` block and BEFORE the `<!-- GSD:architecture-end -->` marker comment (R2 multi-review — the subsection is architecture content; keep it inside the GSD architecture block). Use the Read tool first to locate the exact line where Connection model content ends.

Insertion content (mirrors RESEARCH §8 template + style of `### markStalled close-then-best-effort-OOB`):

```markdown
### Runtime tuning (Phase 20)

Per-connection overhead reduction at scale lives in `paralife.runtime.jetty.*` and
`paralife.runtime.app.*` `@ConfigurationProperties` records (Phase 20 D-07 layers
2 + 3, see `JettyRuntimeConfig` and `AppRuntimeConfig`). JVM flags ship as
documented per-scale-tier presets in
`.planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md` §3, NOT
as wrapper scripts (D-08).

**The WS:entity 1:1 model from §Connection model is non-negotiable.** Tuning
reduces per-connection cost; it does not collapse connections. SCALE-08's "or
equivalent transport-level scale strategy" escape hatch was intentionally taken.
See `20-RUNTIME.md` §1 for the full rationale and §3 for per-tier recipes.

The two metric gauges to watch when tuning are `paralife.tick.health.work-time-ms`
(`AdmissionMetrics.java:70`) and `paralife.outbound.detach.timeout`
(`AdmissionMetrics.java:79`, P19.1 D-18). Profile artifacts under
`.planning/phases/20-connection-multiplexing-runtime-tuning/profiles/` are pinned
to commit SHAs (e.g. `62c1b44` churn baseline, `103a615` active-scenario) for
reproducibility per D-19. Headline-gauge
values are sampled from `/actuator/metrics/{name}` into JSON sidecars at capture
time (Pass-2 Concern #10) — `application.yml:15` exposes the `metrics` actuator
endpoint that Plan 1c + Plan 5 capture from.

D-20 keeps `paralife.admission.backpressure.outbound-queue-size` in
`AdmissionConfig` rather than moving it under `paralife.runtime.app.*`; namespace
consolidation is Phase 999.4. Codec hot-path opts (D-10, layer 4 of the tuning
surface) are JFR-driven and never cross the wire — `15-SCHEMA.md` stays bit-exact.
```

Use the Edit tool. Do not modify any other CLAUDE.md content.
  </action>
  <verify>
    <automated>grep -q "### Runtime tuning (Phase 20)" CLAUDE.md && grep -q "WS:entity 1:1 model from §Connection model is non-negotiable" CLAUDE.md && grep -q "20-RUNTIME.md" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "### Runtime tuning (Phase 20)" CLAUDE.md` exits 0
    - `grep -q "WS:entity 1:1" CLAUDE.md` exits 0 (D-02 codification location 2)
    - `grep -q "paralife.runtime.jetty" CLAUDE.md && grep -q "paralife.runtime.app" CLAUDE.md` (both record names cited)
    - `grep -cE "AdmissionMetrics\\.java:[0-9]+" CLAUDE.md | awk '$1 >= 2 { exit 0 } { exit 1 }'` (D-13 + D-18 gauge sites cited with line numbers — R1 multi-review: pattern is line-number-agnostic because the constants drift; the executor MUST re-verify the actual line numbers via `grep -n "M_TICK_WORK_MS\\|M_DETACH_TIMEOUT" src/main/java/com/paralife/admission/AdmissionMetrics.java` and write those — `:70`/`:79` as of 2026-06-04. Same re-verify for the template's `application.yml:15` actuator-exposure line ref — `grep -n "include:" src/main/resources/application.yml` — R2 multi-review)
    - `grep -q "20-RUNTIME.md" CLAUDE.md` (canonical doc cross-reference)
    - `grep -q "Phase 999.4" CLAUDE.md` (forward note for namespace consolidation)
    - `grep -q "actuator/metrics" CLAUDE.md` (Pass-2 Concern #10 — actuator endpoint cited)
    - Subsection placement: `awk '/### Connection model \\(Phase 18/,/### Runtime tuning \\(Phase 20/' CLAUDE.md | wc -l | awk '$1 >= 5 { exit 0 } { exit 1 }'` (Runtime tuning appears AFTER Connection model with no other `###` heading in between) — sanity check on order
  </acceptance_criteria>
  <done>CLAUDE.md has the new §Runtime tuning subsection in the right place, citing 20-RUNTIME.md + both gauges + the actuator endpoint (Pass-2 Concern #10) + the D-02 rationale.</done>
</task>

<task type="auto">
  <name>Task 6.3: Update README.md with operator paragraph + minimal scaffolding (D-16)</name>
  <read_first>
    - README.md (current state — single line `# paralife`)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-RESEARCH.md (lines 836-849 — README operator paragraph template)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-PATTERNS.md (entry for README.md — minimal scaffolding note)
    - CLAUDE.md (line ~3-12 — project description for one-line value-prop)
  </read_first>
  <files>README.md</files>
  <action>
Replace the current 1-line README.md with a minimal but readable structure that includes the D-16 Runtime tuning paragraph. Use the Write tool (this file is small).

```markdown
# paralife

A toroidal 2D living simulation. A Spring Boot server runs the physics tick
loop, broadcasts state via WebSocket, and receives actions from autonomous
heuristic bot clients. Built with Java 21 virtual threads for massive
concurrency with simple blocking code.

Three competing entity types (Catalyst, Membrane, Spore) follow rock-paper-scissors
dynamics in a closed energy economy — emergent spatial behaviour (spiral waves,
population oscillations, niche formation) arises from simple local rules.

## Build & run

```bash
./gradlew test              # JUnit 5 unit + integration suite
./gradlew bootRun           # Start server on :8080
./gradlew loadHarnessJar    # Build the standalone load harness
./gradlew runBot            # Single-bot operator CLI (≤100 bots)
```

## Project layout

- `src/main/java/com/paralife/` — server (`world`, `engine`, `websocket`, `admission`, `codec`, `runtime`, `harness`, `metrics`, `diagnostics`, `bot`)
- `src/test/java/com/paralife/` — JUnit 5 unit + integration tests
- `.planning/` — GSD-managed phase plans, summaries, research, and audits
- [`CLAUDE.md`](CLAUDE.md) — project conventions, architecture, concurrency invariants

## Runtime tuning

Paralife is built around many concurrent WebSocket connections — one per entity,
by design (see [`CLAUDE.md`](CLAUDE.md) §Connection model). At scale,
per-connection overhead is reduced via the four-layer tuning surface in
[`.planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md`](.planning/phases/20-connection-multiplexing-runtime-tuning/20-RUNTIME.md):
JVM flags, Jetty/network knobs (`paralife.runtime.jetty.*`), application-level
knobs (`paralife.runtime.app.*`), and codec internals.

**Multi-entity-per-connection is not part of the design** — operational scale-out
is achieved by running more JVMs and more connections, not by collapsing them
(WS:entity 1:1 is the architectural identity; SCALE-08's "or equivalent
transport-level scale strategy" escape hatch — Phase 20 D-01).
```

This is ~30 lines and gives a contributor enough context without bloating into a separate doc. The Runtime tuning paragraph carries the D-02 codification location 1 explicitly via the `WS:entity 1:1` literal so `grep -q "WS:entity 1:1" README.md` exits 0.
  </action>
  <verify>
    <automated>grep -q "WS:entity 1:1" README.md && grep -q "20-RUNTIME.md" README.md && grep -q "## Runtime tuning" README.md && wc -l README.md | awk '$1 >= 25 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "WS:entity 1:1" README.md` exits 0 (D-02 codification location 1)
    - `grep -q "## Runtime tuning" README.md` exits 0 (operator paragraph present)
    - `grep -q "20-RUNTIME.md" README.md` exits 0 (canonical doc link)
    - `grep -q "paralife.runtime.jetty" README.md && grep -q "paralife.runtime.app" README.md` (both records named)
    - `grep -q "Phase 20 D-01" README.md` (decision cited)
    - `wc -l README.md` reports ≥25 lines (substantive scaffolding, not just a paragraph)
  </acceptance_criteria>
  <done>README.md replaces the 1-line stub with project structure + Runtime tuning paragraph carrying the D-16 + D-02 content.</done>
</task>

<task type="auto">
  <name>Task 6.4: Add D-02 inline rationale comments at the WS upgrade + OutboundSender.attachSession sites</name>
  <read_first>
    - src/main/java/com/paralife/websocket/WorldWebSocketHandler.java (lines 315-325 — the afterConnectionEstablished method)
    - src/main/java/com/paralife/admission/OutboundSender.java (lines 120-145 — the attachSession method including the Thread.ofVirtual() call)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-RESEARCH.md (lines 850-872 — exact comment templates)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-PATTERNS.md (entries for WorldWebSocketHandler + OutboundSender — comment placement guidance)
  </read_first>
  <files>src/main/java/com/paralife/websocket/WorldWebSocketHandler.java, src/main/java/com/paralife/admission/OutboundSender.java</files>
  <action>
**File 1: `src/main/java/com/paralife/websocket/WorldWebSocketHandler.java`**

Locate `afterConnectionEstablished(WebSocketSession session)` at line ~317. Insert the D-02 comment block immediately above the `outboundSender.attachSession(...)` call at line 320:

```java
@Override
public void afterConnectionEstablished(WebSocketSession session) {
    sessionRegistry.register(session);
    if (outboundSender != null) {
        // Phase 20 D-02 — WS:entity 1:1 is a deliberate architectural choice, not an
        // optimisation gap. See 20-RUNTIME.md §1 (and 18-HARNESS.md §1, CLAUDE.md
        // §Connection model). Tuning per-connection cost is the equivalent
        // transport-level scale strategy (SCALE-08); collapsing entities onto a shared
        // session would require explicit ADR per D-21 of Phase 18.
        outboundSender.attachSession(session, admissionConfig.backpressure().outboundQueueSize());
    }
```

Use the Edit tool. Comment is comment-only — zero behavioural change.

**File 2: `src/main/java/com/paralife/admission/OutboundSender.java`**

Locate the `Thread.ofVirtual().name("ws-sender-" + id).start(() -> drainLoop(session, queue))` call at line ~132. Insert the D-02 comment block immediately above it:

```java
ArrayBlockingQueue<Frame> queue = new ArrayBlockingQueue<>(queueCapacity);
queues.put(id, queue);
overflowFiredFlags.put(id, new AtomicBoolean(false));
// Phase 20 D-02 — One drain VT per session is structural per the WS:entity 1:1
// model (CLAUDE.md §Connection model, 18-HARNESS.md §1, 20-RUNTIME.md §1).
// Per-VT cost is a few KB heap; 1000+ VTs is acceptable. Per-connection cost
// is reduced via paralife.runtime.* tuning (see 20-RUNTIME.md §3), NOT by
// sharing the drain VT across sessions. Multi-entity-per-VT requires explicit
// ADR per Phase 18 D-21.
Thread t = Thread.ofVirtual()
        .name("ws-sender-" + id)
        .start(() -> drainLoop(session, queue));
```

Use the Edit tool. Comment is comment-only.

After both inserts, run:
- `./gradlew compileJava` (catches any syntax mistake)
- Three-gate stack in-suite
- Full suite

**Infra-flake protocol (R1+R4 multi-review, mirrors Plan 5 Concern #14):** a comment-only change cannot break tests, so two documented pre-existing environment failures do NOT block this task:
- **TD-22-E:** full suite fails ONLY with `Could not write XML test results` / `forkEvery=1` contention (Plan 5's SUMMARY records exactly this BUILD FAILED with zero test-logic failures) → retry once and record the green run's SHA.
- **HundredBotIntegrationTest WSL2 timeout (R4):** `hundredBotsConnectAndReceiveTicks` timing out under WSL2 + `forkEvery=1` is pre-existing and documented in 20-01c-SUMMARY Caveat #1 (verified at parent `14e96ea`, before any Phase 20 change). If the ONLY failure is this timeout, retry once; if it recurs, record it as the known pre-existing condition (cite Caveat #1) and treat the in-suite three-gate stack as the load-bearing gate.

The two conditions may co-occur in one run — treat any failure set drawn ONLY from these two the same way (retry once, then record + three-gate load-bearing). Do not revert the comments on either condition. Any other test failure = stop and report.
  </action>
  <verify>
    <automated>grep -q "Phase 20 D-02" src/main/java/com/paralife/websocket/WorldWebSocketHandler.java && grep -q "WS:entity 1:1" src/main/java/com/paralife/websocket/WorldWebSocketHandler.java && grep -q "Phase 20 D-02" src/main/java/com/paralife/admission/OutboundSender.java && grep -q "WS:entity 1:1" src/main/java/com/paralife/admission/OutboundSender.java && ./gradlew test --tests GoldenTraceEquivalenceTest --tests GoldenTraceWithActionsTest --tests LiveEntityRegistryInvariantTest && ./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 20 D-02" src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` exits 0
    - `grep -q "WS:entity 1:1" src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` exits 0 (D-02 codification location 3a)
    - `grep -q "Phase 20 D-02" src/main/java/com/paralife/admission/OutboundSender.java` exits 0
    - `grep -q "WS:entity 1:1" src/main/java/com/paralife/admission/OutboundSender.java` exits 0 (D-02 codification location 3b)
    - **Three-place D-02 codification verified by single grep:** `grep -lE "WS:entity 1:1" README.md CLAUDE.md src/main/java/com/paralife/websocket/WorldWebSocketHandler.java src/main/java/com/paralife/admission/OutboundSender.java | wc -l | awk '$1 == 4 { exit 0 } { exit 1 }'` (4 files contain the literal — README + CLAUDE + 2 source files = 4)
    - `./gradlew test --tests GoldenTraceEquivalenceTest --tests GoldenTraceWithActionsTest --tests LiveEntityRegistryInvariantTest` exits 0 (in-suite three-gate stack green per D-11)
    - `./gradlew test` exits 0 (full suite — R2 multi-review: the old "4 P22 `@Disabled`" count is stale at HEAD; live inventory is 6 `@Disabled` annotations across 5 files (TD-22-A `MetabolismIntegrationTest`, TD-22-B `EncodeDeflatePerformanceGateTest`, `PopulationDynamicsTest`→backlog, `ToxinTest` ×2 Plan-06 TODOs, `CellularAutomatonTest` perf-only); TD-22-D `HundredBotIntegrationTest` was RE-ENABLED. D-12's rule = don't re-enable the remaining P22 deferrals; snapshot with `grep -rn "@Disabled(" src/test/java` at execution time)
  </acceptance_criteria>
  <done>D-02 three-place rationale codification complete: README.md + CLAUDE.md + 2 source-file inline comments. Tests stay green.</done>
</task>

<task type="auto">
  <name>Task 6.5: Flip 20-VALIDATION.md frontmatter to compliant + populate Per-Task Verification Map (I7 fix)</name>
  <read_first>
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-VALIDATION.md (current — `nyquist_compliant: false`, `wave_0_complete: false`, stub Per-Task table)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-01-PLAN.md (Tasks 1.0, 1.1)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-01b-PLAN.md (Tasks 1b.0, 1b.1 — SUPERSEDED by 20-01c; rows kept with superseded status)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-01c-PLAN.md (supersedes 20-01b; NO GSD task-ID structure — prose §Phase A/B/C; map as rows 20-01c-A / 20-01c-B / 20-01c-C)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-02-PLAN.md (task IDs)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-03-PLAN.md (task IDs)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-04-PLAN.md (task IDs)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-05-PLAN.md (Tasks 5.0, 5.1, 5.2)
    - .planning/phases/20-connection-multiplexing-runtime-tuning/20-06-PLAN.md (THIS plan — Tasks 6.1–6.5; R1 multi-review: 20-06 rows are part of the 8-plan map, don't omit the plan being executed)
  </read_first>
  <files>.planning/phases/20-connection-multiplexing-runtime-tuning/20-VALIDATION.md</files>
  <action>
Edit `20-VALIDATION.md`:

1. **Frontmatter:** flip `nyquist_compliant: false` → `nyquist_compliant: true` and `wave_0_complete: false` → `wave_0_complete: true`.

2. **Per-Task Verification Map:** replace the stub `20-01-* / 20-02-* / ...` rows with one row per actual task ID across all 8 plans (20-01, 20-01b [superseded], 20-01c, 20-02, 20-03, 20-04, 20-05, 20-06). 20-01b rows carry Status = "superseded by 20-01c" (kept for the historical `c22e487` capture record). 20-01c has no task-ID structure — map its prose sections as rows `20-01c-A` (source instrumentation), `20-01c-B` (re-baseline @ `62c1b44` + active-50xfood @ `103a615`), `20-01c-C` (docs), with verification drawn from 20-01c-PLAN §Verification + PR #1 merge `ddf0dfa`. Each row carries:
   - Task ID (e.g. `20-01-1.0`, `20-01-1.1`, `20-01b-1b.0`, `20-01b-1b.1`, `20-02-2.1`, ...)
   - Plan number
   - Wave
   - Requirement (SCALE-08 / SCALE-09)
   - Threat ref (e.g. T-20-V5 for codec; T-20-DOS-1 for queue growth; — when not applicable)
   - Secure behavior (1-line)
   - Test type (manual / unit / regression / three-gate)
   - Automated command (the exact command from the task's `<verify><automated>` block, OR the manual procedure when type=manual)
   - File exists (✅ once plan ships, ❌ until then)
   - Status (⬜ pending until executor runs)

   Read each PLAN.md task block to extract the actual `<verify><automated>` command. The map is the canonical executor-facing index — no hand-waving.

3. **Wave 0 Requirements checklist:** verify each "MUST create" line (Plan 1 → 20-01 + 20-01b split, superseded by Plan 1c's `62c1b44` re-baseline + `103a615` active-scenario captures; Plan 2 → JettyRuntimeConfig.java; Plan 3 → AppRuntimeConfig.java; Plan 6 → 20-RUNTIME.md, CLAUDE.md, README.md, inline comments). Tick each box as the executor lands the artifact. Two R1 multi-review corrections to apply while there:
   - **The checklist has NO Plan 5 line — ADD it** before ticking: `- [x] 20-05-TRIAGE.md + jfr-1000bots-active-50xfood-tuned-424e06d.{jfr,meta.json} + metrics-1000bots-active-50xfood-tuned-424e06d.json — Plan 5 (Pass-2 Concern #10)`. The current list predates Plan 5's artifact contract; don't silently tick a box that doesn't exist.
   - **The Plan 1 JFR line (~L67) still names `jfr-{100,500,1000}bots-baseline-c22e487.jfr`** — annotate it "(as originally captured; superseded by Plan 1c re-anchor → `62c1b44` series; `c22e487` files retained on disk for history)". Same one-line annotation on the single `c22e487` Manual-Only Verification row (~L81, "Profile artifacts captured against c22e487"); the adjacent tuned-JFR row (~L82) names `jfr-1000bots-tuned-<sha>.jfr` — annotate it with the shipped name `jfr-1000bots-active-50xfood-tuned-424e06d.jfr` (R2 multi-review — only one row literally carries the SHA; the other carries a stale filename pattern). While editing the same table, fix the dead branch in the D-02 row (~L84): the `see 20-RUNTIME` alternative in `grep -rE "WS:entity 1:1\|see 20-RUNTIME"` matches neither planned comment (case mismatch — comments say "See 20-RUNTIME.md"); drop the dead branch or change it to `20-RUNTIME\.md` (R4 multi-review).

4. **Validation Sign-Off:** tick all boxes that are now true:
   - [x] All tasks have `<automated>` verify (three-gate stack) OR documented manual capture (D-13/D-14)
   - [x] Sampling continuity: every plan wave runs full suite; no >2 consecutive tasks without quick-stack verify
   - [x] Wave 0 covers all MISSING references
   - [x] No watch-mode flags
   - [x] Feedback latency < 60 s (quick) and < 15 min (full)
   - [x] `nyquist_compliant: true` set in frontmatter
   - [x] D-12 enforced: remaining P22 `@Disabled` deferrals untouched — R2 multi-review: do NOT tick with the stale "4 tests" wording; rewrite the sign-off line AND the `20-VALIDATION.md` full-suite-command row (~L24 "excludes 4 P22 `@Disabled` tests") AND the Wave-0 intro sentence (~L62 "the four P22 `@Disabled` tests (TD-22-A … TD-22-D `HundredBotIntegrationTest`)" — drop the re-enabled TD-22-D from the disabled list; R3 multi-review) to the live inventory first (TD-22-D `HundredBotIntegrationTest` re-enabled; 6 annotations live — see Task 6.4 acceptance note); cite a fresh `grep -rn "@Disabled(" src/test/java` snapshot in the sign-off
   - [x] D-19 enforced (as re-anchored by Plan 1c F6): canonical churn baseline series cites `62c1b44`, active-scenario evidence set cites `103a615`, tuned capture cites `424e06d`; superseded `c22e487` series retained on disk for history (Task 6.1 step 7 reconciles the 20-CONTEXT.md D-19 wording)
   Set "Approval: pending" → "Approval: planner-signed-off ($SHA on $DATE)" once Task 6.4 has landed and the three-gate stack is green.

5. **Sampling Rate** section: confirm the after-every-task-commit / after-every-plan-wave / before-`/gsd-verify-work` triggers carry over unchanged (no edit needed; the Plan 5 reframing per cross-AI review concern #1 + Pass-2 Concern #9 did not change sampling expectations — it only widened acceptance to permit a documented null-result OR dominant-pinning-with-backlog-handoff).
  </action>
  <verify>
    <automated>head -10 .planning/phases/20-connection-multiplexing-runtime-tuning/20-VALIDATION.md | grep -q "nyquist_compliant: true" && head -10 .planning/phases/20-connection-multiplexing-runtime-tuning/20-VALIDATION.md | grep -q "wave_0_complete: true" && grep -q "^| 20-01-" .planning/phases/20-connection-multiplexing-runtime-tuning/20-VALIDATION.md && grep -q "^| 20-01b-" .planning/phases/20-connection-multiplexing-runtime-tuning/20-VALIDATION.md && grep -q "^| 20-01c-" .planning/phases/20-connection-multiplexing-runtime-tuning/20-VALIDATION.md && grep -q "^| 20-05-5\." .planning/phases/20-connection-multiplexing-runtime-tuning/20-VALIDATION.md && grep -q "^| 20-06-6\." .planning/phases/20-connection-multiplexing-runtime-tuning/20-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - **Working directory (R2 multi-review):** bare `20-VALIDATION.md` paths below are phase-dir-relative — run from `.planning/phases/20-connection-multiplexing-runtime-tuning/`.
    - `head -10 20-VALIDATION.md | grep -q 'nyquist_compliant: true'` exits 0 (I7 fix)
    - `head -10 20-VALIDATION.md | grep -q 'wave_0_complete: true'` exits 0
    - `grep -q '^| 20-01-' 20-VALIDATION.md` exits 0 (Plan 1 task rows present, not stubs)
    - `grep -q '^| 20-01b-' 20-VALIDATION.md` exits 0 (Plan 1b task rows present, marked superseded)
    - `grep -q '^| 20-01c-' 20-VALIDATION.md` exits 0 (Plan 1c prose-section rows A/B/C present)
    - `grep -q '^| 20-05-5\.' 20-VALIDATION.md` exits 0 (Plan 5 task rows present)
    - `grep -q '^| 20-06-6\.' 20-VALIDATION.md` exits 0 (Plan 6 task rows present — R1 multi-review: without this the map can omit the executing plan and still pass)
    - `grep -c '^| 20-' 20-VALIDATION.md | awk '$1 >= 20 { exit 0 } { exit 1 }'` — at least 20 task rows total (20-01: 2, 20-01b: 2 superseded, 20-01c: 3 [A/B/C], 20-02: 3, 20-03: 2, 20-04: 2, 20-05: 3, 20-06: 5 = ~22; ≥20 lower bound — R1 multi-review raised from 16, which pre-20-06 rows alone could satisfy)
  </acceptance_criteria>
  <done>20-VALIDATION.md frontmatter is compliant + the Per-Task Verification Map carries every actual task ID across all 8 plans (incl. superseded 20-01b rows + 20-01c prose-section rows). Validation Sign-Off is approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → README.md / CLAUDE.md / 20-RUNTIME.md | doc only |
| contributor → source comments | comment-only changes; no behavioural diff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-V7 | I (Information disclosure) — doc leak | README.md / CLAUDE.md / 20-RUNTIME.md | accept | non-secret architectural rationale; no secrets surfaced |
| T-20-DOC-DRIFT | T (Tampering) — future contributor "fixes" the design without ADR | source comments | mitigate | three-place codification (D-02) makes the design choice impossible to miss; comments cite D-21 ADR requirement; CLAUDE.md + README.md + inline comment all enforce the same rule |
</threat_model>

<verification>
- `grep -lE "WS:entity 1:1" README.md CLAUDE.md src/main/java/com/paralife/websocket/WorldWebSocketHandler.java src/main/java/com/paralife/admission/OutboundSender.java | wc -l` = 4 (D-02 three-place codification — note: 4 files because the rationale is in 3 *places* but 2 of those places live in 2 source files, totalling 4 file matches)
- `./gradlew test --tests GoldenTraceEquivalenceTest --tests GoldenTraceWithActionsTest --tests LiveEntityRegistryInvariantTest` exits 0
- `./gradlew test` exits 0 (full suite minus live `@Disabled` inventory — see Task 6.4 acceptance note; stale "4" count corrected R2)
- 20-RUNTIME.md line-count tier honoured per Plan 5 outcome (Pass-2 Concern #13: ≥350 for outcomes 1/2/4; ≥250 for outcome 3 null-result)
- All `_Pending_` markers replaced (the `_baseline-only — see Phase 21_` cells in §4.2 100/500-tuned columns are intentional Phase-21-handoff per Pass-2 Concern #17, NOT pending)
</verification>

<success_criteria>
- 20-RUNTIME.md fully populated; mirrors 17-ADMISSION.md / 18-HARNESS.md depth
- CLAUDE.md §Runtime tuning subsection in place (D-15)
- README.md operator paragraph in place + minimal scaffolding (D-16)
- D-02 inline comments at WorldWebSocketHandler + OutboundSender (location 3 of 3)
- D-02 three-place codification grep-verifiable across all 4 file matches
- Tests green; D-12 P22 deferrals still disabled (per live inventory, not the stale "4" count)
- Pass-2 Concern #9: SCALE-08 inheritance truth enumerates all four D-21 outcomes (codec opts / runtime-knob tightening / null-result / dominant-pinning-with-backlog-handoff)
- Pass-2 Concern #13: line-count floor tiered by Plan 5 outcome
- Pass-2 Concern #17: 100/500-bot tuned-tier framed as "_baseline-only — see Phase 21_"
</success_criteria>

<output>
After completion, create `.planning/phases/20-connection-multiplexing-runtime-tuning/20-06-SUMMARY.md` capturing:
- 20-RUNTIME.md final size + section list (line count + Plan 5 outcome label cited per Pass-2 Concern #13)
- D-02 grep verification (4-file match)
- CLAUDE.md insertion location (line range)
- README.md before/after line count
- Three-gate + full-suite green commit SHA + timestamp
- Pass-2 Concerns #9 / #13 / #17 confirmation: SCALE-08 inheritance truth enumerates all 4 D-21 outcomes; line-count tier honoured; 100/500-tuned cells framed as Phase-21 hand-off

Tracking note: ROADMAP.md's 20-05-era annotation "7/8 plans executed (checkbox count incl. superseded 20-01b; 6/7 active plans per STATE.md)" self-erases here — the completion update replaces it with the final 8/8-checkbox / 7/7-active count.

Plan 6 closes the phase. After this plan ships, `/gsd-verify-work 20` is the next action.
</output>
