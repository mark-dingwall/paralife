---
phase: 15-protocol-transport-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md
autonomous: true
requirements: [R20, R21, R22, R23, R24, R25, R26, R27, R28, R29]
requirements_addressed: [R20, R21, R22, R23, R24, R25, R26, R27, R28, R29]
threat_refs: []
tags: [gsd, requirements, roadmap, schema-fix]

must_haves:
  truths:
    - "ROADMAP Phase 15 success criterion for precompress fan-out is removed (D-03 scope reduction)"
    - "REQUIREMENTS.md has new R20-R29 IDs for Phase 15 concrete acceptance targets"
    - "REQUIREMENTS.md R15-R19 are remapped to Phase 16 (Emergent Behavior Tests)"
    - "15-SCHEMA.md §10 Vector 9 uses the standard 4-char relative coord form (v+F-3L5); NO 6-char extended coord exists in the grammar"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 15 goal + updated success-criteria list (no fan-out bullet)"
      contains: "Phase 15"
    - path: ".planning/REQUIREMENTS.md"
      provides: "R01-R29 authoritative requirement table mapped by phase"
      contains: "| R20 |"
    - path: ".planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md"
      provides: "Phase 15 wire schema with Vector 9 corrected to v+F-3L5"
      contains: "v+F-3L5"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/15-protocol-transport-overhaul/*.md"
      via: "R20-R29 ids surfaced in downstream plan frontmatter"
      pattern: "R2[0-9]"
---

<objective>
Reconcile the planning-artifact bookkeeping drift called out in CONTEXT D-47 and D-03, and apply the Vector 9 fix locked during cross-AI review replan:

1. Remove the "Precompress fan-out infrastructure" success criterion from ROADMAP Phase 15 (moved to M005 per D-03).
2. Introduce R20-R29 in REQUIREMENTS.md as the verifiable acceptance targets for Phase 15 (derived from ROADMAP lines 127-139 + 15-CONTEXT.md + 15-SCHEMA.md success criteria).
3. Remap R15-R19 (which currently point to Phase 15 but describe emergence tests) to Phase 16 in the traceability table.
4. **Apply the Vector 9 fix** — correct the SCHEMA §10 Vector 9 literal from the broken 6-char form `v+0F-03L5` to the standard 4-char relative form `v+F-3L5`. This MUST land BEFORE plan 15-02 encodes the 13 RED vectors into the round-trip test.

Purpose: every downstream Phase 15 plan must cite concrete requirement IDs in its `requirements` frontmatter, and the verification step at end of phase needs a checklist of measurable outcomes. The Vector 9 fix is load-bearing: plan 15-02 Task 3 will write the literal into the test file — if the schema still says `v+0F-03L5`, the test seeds a broken oracle and every downstream codec task is poisoned.

Output: updated ROADMAP.md, REQUIREMENTS.md, and 15-SCHEMA.md, committed together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-protocol-transport-overhaul/15-CONTEXT.md
@.planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove fan-out success criterion from ROADMAP Phase 15</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 126-139 (current Phase 15 block — fan-out bullet is at line 134)
    - .planning/phases/15-protocol-transport-overhaul/15-CONTEXT.md lines 16-22 (D-03 scope + out-of-scope items)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`. Delete exactly this line from the Phase 15 success criteria list (currently line 134):

    ```
    - Precompress fan-out infrastructure (`BroadcastChannel` + `CompressedFrame`) ready for future visualizer broadcast channel
    ```

    Do NOT touch other bullets. Do NOT add a replacement. The remaining Phase 15 success-criteria block must match the 15-CONTEXT.md scope (compact codec; Tomcat→Jetty swap; permessage-deflate; stateless bot redesign; zero-trust filtering; shared PerceptionCodec; bot→server compact action format; actuator custom metrics; rock generation; all existing tests pass).

    After removal the block should remain grammatical — the goal line stays as written. Do not renumber Phase 15 or change its position.

    No other edits. Do not touch Phase 16 block. Do not re-order sections.
  </action>
  <verify>
    <automated>! grep -n "Precompress fan-out infrastructure" .planning/ROADMAP.md &amp;&amp; grep -qc "Phase 15: Protocol &amp; Transport Overhaul" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Precompress fan-out infrastructure" .planning/ROADMAP.md` returns nothing (exit 1).
    - `grep -c "Phase 15: Protocol" .planning/ROADMAP.md` returns exactly `1`.
    - `grep -c "All existing tests pass under new protocol" .planning/ROADMAP.md` returns exactly `1` (other success criteria intact).
    - Phase 16 block unchanged: `grep -c "Phase 16: Emergent Behavior Tests" .planning/ROADMAP.md` returns `1`.
  </acceptance_criteria>
  <done>ROADMAP Phase 15 reflects the post-D-03 scope; no fan-out bullet; all other Phase 15 success criteria intact.</done>
</task>

<task type="auto">
  <name>Task 2: Renumber R15-R19 to Phase 16 and add R20-R29 for Phase 15</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (all 40 lines — current table + traceability)
    - .planning/phases/15-protocol-transport-overhaul/15-CONTEXT.md §decisions (decision list D-01..D-47)
    - .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md §5 (frame inventory), §7 (authority tiers), §9 (new scope additions)
    - .planning/ROADMAP.md Phase 15 success criteria (post Task 1 edit)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`.

    Step A — remap R15-R19 to Phase 16:
    - Rows R15-R19: change the `Phase` column value from `15` to `16`. Descriptions untouched.

    Step B — add R20-R29 rows for Phase 15 concrete deliverables. Append these rows AFTER R19 in the same table:

    ```
    | R20 | Compact text wire protocol with 64-char base64 alphabet, 3 coordinate forms, 5 frame types (r/S/T/a/E) per 15-SCHEMA.md §1-§6 | Must | 15 | Pending |
    | R21 | PerceptionCodec in com.paralife.codec with 13 locked round-trip vectors (15-SCHEMA.md §10) all byte-exact | Must | 15 | Pending |
    | R22 | Tomcat→Jetty 12 container swap via starter exclusion, permessage-deflate with server_no_context_takeover negotiated both sides | Must | 15 | Pending |
    | R23 | Handshake integration test asserts Sec-WebSocket-Extensions response contains permessage-deflate; server_no_context_takeover | Must | 15 | Pending |
    | R24 | Fail-fast: server refuses upgrade when client omits extension; BotClient closes if server omits | Must | 15 | Pending |
    | R25 | Zero-trust vision filtering — no entity ids, no bonded-secondary type on wire | Must | 15 | Pending |
    | R26 | Stateless bot: BotClient uses Jetty-native WebSocketClient + codec (no Jackson); HeuristicBrain is pure function of decoded TickFrame | Must | 15 | Pending |
    | R27 | Client-side respawn flow: session stays open post-death, re-register within per-session cap else E\|429 | Must | 15 | Pending |
    | R28 | PNG-based rock generation with configurable seed (paralife.world.rock.seed); same seed → identical rock grid | Must | 15 | Pending |
    | R29 | Two Micrometer metrics published (paralife.ws.active.sessions gauge, paralife.ws.tick.frame.bytes distribution summary); all reachable via /actuator/metrics. Bytes-saved Counter deferred per 15-SCHEMA.md §13 | Must | 15 | Pending |
    ```

    Step C — update the traceability table at the bottom. Replace the existing two rows:
    ```
    | 14 — Environmental Rules | R12, R13, R14 |
    | 15 — Emergent Behavior Tests | R15, R16, R17, R18, R19 |
    ```
    with three rows:
    ```
    | 14 — Environmental Rules | R12, R13, R14 |
    | 15 — Protocol & Transport Overhaul | R20, R21, R22, R23, R24, R25, R26, R27, R28, R29 |
    | 16 — Emergent Behavior Tests | R15, R16, R17, R18, R19 |
    ```

    Do not modify R01-R14 rows. Do not touch the `## Goal` section or the file header.
  </action>
  <verify>
    <automated>grep -c "^| R2[0-9] |" .planning/REQUIREMENTS.md | grep -qx 10 &amp;&amp; grep -qE "^\| R15 \|.*\| 16 \|" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^\| 15 — Protocol &amp; Transport Overhaul \| R20" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^\| 16 — Emergent Behavior Tests \| R15" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\| R2[0-9] \|" .planning/REQUIREMENTS.md` returns exactly `10`.
    - `grep -cE "^\| R1[5-9] \|.*\| 15 \|" .planning/REQUIREMENTS.md` returns `0` (all R15-R19 now map to Phase 16).
    - `grep -cE "^\| R1[5-9] \|.*\| 16 \|" .planning/REQUIREMENTS.md` returns `5`.
    - Traceability table has a `15 — Protocol & Transport Overhaul` row listing R20..R29.
    - Traceability table has a `16 — Emergent Behavior Tests` row listing R15..R19.
    - R01..R14 rows are unchanged.
  </acceptance_criteria>
  <done>REQUIREMENTS.md has R20-R29 for Phase 15; R15-R19 now map to Phase 16; traceability table reflects both changes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Vector 9 schema fix is in place (v+F-3L5, no 6-char extended coord)</name>
  <files>.planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md</files>
  <read_first>
    - .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md §2 (coord encodings — only 3 forms, relative is always 4 chars)
    - .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md §10 (Vector 9 row)
    - .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md §13 (bytes-saved deferral note)
    - .planning/phases/15-protocol-transport-overhaul/15-REVIEWS.md Consensus #1 (Vector 9)
  </read_first>
  <action>
    The schema was corrected in place during the replan that produced this plan set — Vector 9 now reads `T|001|0A1B|15/80|2|fF:2E:0F03|v+F-3L5` (not `v+0F-03L5`). This task is a **verification gate**: confirm the corrected schema landed on disk before plan 15-02 executes.

    Concrete checks the executor runs:

    1. `grep -F 'v+F-3L5' .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md` returns exactly one line (the corrected Vector 9 row in §10 table).
    2. `grep -F 'v+0F-03L5' .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md` returns nothing (no residual broken form).
    3. `grep -F 'extended relative' .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md` returns nothing (no "extended 6-char relative" entry in §2).
    4. `grep -c 'bytes.saved' .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md` returns at least `1` — §13 carries the deferral note for plan 15-10.
    5. §10 vector note for Vector 9 explains the encoding: sign `+` magnitude `F` (base64 → 15), sign `-` magnitude `3` (base64 → 3), code `L`, magnitude `5`. Confirm by `grep -F 'sign \`+\`, magnitude \`F\`' 15-SCHEMA.md` returns at least one line (or equivalent wording — the point is the note documents the 4-char parse).

    If any of checks 1-4 fails, STOP and fix the schema before any downstream plan runs. Do NOT proceed to plan 15-02 with a broken schema.

    This task performs NO schema edits itself — the edit has already been applied during replan. The task is present so the executor has a grep-verifiable gate in its run log and anyone auditing the replan can see the check was performed.
  </action>
  <verify>
    <automated>grep -qF 'v+F-3L5' .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md &amp;&amp; ! grep -qF 'v+0F-03L5' .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md &amp;&amp; ! grep -qiF 'extended relative' .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md &amp;&amp; grep -qF 'bytes.saved' .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md</automated>
  </verify>
  <acceptance_criteria>
    - Vector 9 literal is `v+F-3L5` on the §10 row.
    - No residual `v+0F-03L5` anywhere in the schema.
    - No `extended relative` language anywhere in §2 or elsewhere.
    - §13 Known Follow-ups carries the `bytes.saved` deferral note.
    - The schema's Vector 9 note (§10) explains the 4-char parse.
  </acceptance_criteria>
  <done>Schema is consistent with §2's 3-form coord grammar; Vector 9 ready to be encoded as a GREEN test literal in plan 15-02 Task 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None at this layer — planning-artifact edits only, no runtime surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-00 | Tampering | planning artifacts | accept | Artifacts are tracked via git; any divergence between REQUIREMENTS.md and plan frontmatter surfaces during plan-checker validation. No runtime threat exposure. |
</threat_model>

<verification>
- ROADMAP Phase 15 success-criteria list has no fan-out bullet.
- REQUIREMENTS.md has R20-R29 rows for Phase 15 and R15-R19 rows map to Phase 16.
- Traceability table reflects both changes.
- 15-SCHEMA.md §10 Vector 9 reads `v+F-3L5`; no `v+0F-03L5` anywhere; no "extended relative" language.
- Downstream plan frontmatter references R20-R29 (verified after later plans land).
</verification>

<success_criteria>
- Three edited files committed in a single unit (no partial state).
- Downstream plans (15-02..15-11) can cite R20-R29 without re-editing REQUIREMENTS.md.
- Plan 15-02 Task 3 will write Vector 9 literal as `v+F-3L5` and the codec impl in 15-05 will round-trip it with no checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/15-protocol-transport-overhaul/15-01-SUMMARY.md`. Record the Vector 9 fix (before/after literal) and confirm the schema grep checks all passed.
</output>
