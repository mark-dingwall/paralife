---
phase: 15-protocol-transport-overhaul
plan: 07
type: execute
wave: 4
depends_on: [15-06]
files_modified:
  - src/main/java/com/paralife/websocket/TickBroadcaster.java
  - src/test/java/com/paralife/websocket/TickBroadcasterTest.java
  - src/main/java/com/paralife/engine/PerceptionBroadcaster.java
autonomous: true
requirements: [R20, R25]
requirements_addressed: [R20, R25]
threat_refs: [T-15-03]
tags: [rename, broadcaster, zero-trust, delete-heartbeat]

must_haves:
  truths:
    - "The old heartbeat TickBroadcaster is deleted BEFORE the rename"
    - "engine/PerceptionBroadcaster.java is renamed to websocket/TickBroadcaster.java with git-detected rename"
    - "New TickBroadcaster still lives at @Order(50) in the tick pipeline"
    - "Renamed class keeps PerceptionBroadcaster's vision-scoped OVERCROWDED mask-and-OR verbatim (lines 381-418 of old file)"
  artifacts:
    - path: "src/main/java/com/paralife/websocket/TickBroadcaster.java"
      provides: "Renamed broadcaster (from engine/PerceptionBroadcaster)"
      contains: "@Order(50)"
  key_links:
    - from: "src/main/java/com/paralife/websocket/TickBroadcaster.java"
      to: "TickEvent"
      via: "@EventListener @Order(50)"
      pattern: "@Order\\(50\\)"
---

<objective>
Untangle the file-rename sequencing flagged by the pattern mapper. Two file moves in one plan so git-detected renames work cleanly:

1. DELETE `src/main/java/com/paralife/websocket/TickBroadcaster.java` (the old global-stats heartbeat broadcaster — functionality moves to M005 per D-02).
2. DELETE `src/test/java/com/paralife/websocket/TickBroadcasterTest.java` (paired test — D-02 also drops this).
3. `git mv src/main/java/com/paralife/engine/PerceptionBroadcaster.java src/main/java/com/paralife/websocket/TickBroadcaster.java` — moving the 511-line broadcaster to its new home with its new name.
4. Update the moved file's package declaration + class name + any self-references. Preserve the `@Order(50)` tick-pipeline position.
5. Preserve the vision-scoped OVERCROWDED mask-and-OR block verbatim (was lines 381-418 of the old file — load-bearing per CLAUDE.md §Architecture).

Purpose: plan 15-08 rewrites the projection step to emit codec Frames — but the class rename must land cleanly first so Spring's `@Component` + `@EventListener` wiring still points at one broadcaster bean (not two half-broadcasters).

Output: old TickBroadcaster gone (with its test); engine/PerceptionBroadcaster.java gone; websocket/TickBroadcaster.java exists with identical logic and renamed class; compile succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-protocol-transport-overhaul/15-CONTEXT.md
@.planning/phases/15-protocol-transport-overhaul/15-PATTERNS.md
@src/main/java/com/paralife/websocket/TickBroadcaster.java
@src/main/java/com/paralife/engine/PerceptionBroadcaster.java
@src/test/java/com/paralife/websocket/TickBroadcasterTest.java

<interfaces>
Current files and their roles:
- src/main/java/com/paralife/websocket/TickBroadcaster.java (95 lines) — old global-stats heartbeat. `@Component`, `@EventListener @Order(100)`. Broadcasts `{entityCount, bondCount, compositeCount, season}` to ALL sessions (same payload).
- src/main/java/com/paralife/engine/PerceptionBroadcaster.java (511 lines) — per-bot perception broadcaster. `@Component`, `@EventListener @Order(50)`. Builds per-bot vision + sends individual Messages.Perception.

After this plan:
- websocket/TickBroadcaster.java (renamed from engine/PerceptionBroadcaster.java) — same logic temporarily (Jackson-writing to Messages.CellView). Plan 15-08 swaps the projection to codec.
- Old websocket/TickBroadcaster.java — deleted.
- Old engine/PerceptionBroadcaster.java — deleted (moved).

Preserve verbatim (CLAUDE.md load-bearing):
- PerceptionBroadcaster.java:381-418 — vision-scoped OVERCROWDED mask-and-OR in cellToView.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old heartbeat TickBroadcaster and its test</name>
  <files>
    src/main/java/com/paralife/websocket/TickBroadcaster.java
    src/test/java/com/paralife/websocket/TickBroadcasterTest.java
  </files>
  <read_first>
    - src/main/java/com/paralife/websocket/TickBroadcaster.java (all 95 lines — confirm it is the heartbeat, not already renamed)
    - src/test/java/com/paralife/websocket/TickBroadcasterTest.java (identify whether it targets the heartbeat or the per-bot broadcaster — if already migrated, skip)
    - .planning/phases/15-protocol-transport-overhaul/15-CONTEXT.md D-02 (rename + deletion mandate)
  </read_first>
  <action>
    Delete both files:

    ```bash
    git rm src/main/java/com/paralife/websocket/TickBroadcaster.java
    git rm src/test/java/com/paralife/websocket/TickBroadcasterTest.java
    ```

    Before deleting, grep for any `@Autowired` or constructor-injected references to `TickBroadcaster` in other source files. If there are any (beyond the test and the class itself), collect them — they need removal in plan 15-08 or a separate migration step.

    Likely-callsites to inspect:
    - `com.paralife.engine.TickEngine` — does it depend on TickBroadcaster? (usually no — TickEngine publishes TickEvent via ApplicationEventPublisher; broadcasters listen.)
    - `com.paralife.websocket.WebSocketConfig` — does not reference broadcasters.
    - Any `@SpringBootTest` that `@MockBean` the old broadcaster — migrate the mock to the renamed class or delete that test.

    If any non-trivial reference remains, document it in the plan SUMMARY with the migration action taken.
  </action>
  <verify>
    <automated>! test -f src/main/java/com/paralife/websocket/TickBroadcaster.java &amp;&amp; ! test -f src/test/java/com/paralife/websocket/TickBroadcasterTest.java</automated>
  </verify>
  <acceptance_criteria>
    - Both files no longer exist in the working tree.
    - `git status` shows two `deleted:` entries (ready for commit).
    - `grep -r "TickBroadcaster" src/main/java/ 2>/dev/null` returns 0 matches (because engine/PerceptionBroadcaster still has its old name at this point). After Task 2 the reference will re-appear inside the renamed file.
  </acceptance_criteria>
  <done>Heartbeat broadcaster removed; filename freed for the rename target.</done>
</task>

<task type="auto">
  <name>Task 2: git mv engine/PerceptionBroadcaster.java → websocket/TickBroadcaster.java + package/class rename</name>
  <files>
    src/main/java/com/paralife/engine/PerceptionBroadcaster.java
    src/main/java/com/paralife/websocket/TickBroadcaster.java
  </files>
  <read_first>
    - src/main/java/com/paralife/engine/PerceptionBroadcaster.java (entire file — 511 lines; at minimum lines 1-20 for imports/package, lines 53-104 for class declaration + fields, lines 381-418 for the mask-and-OR that must survive verbatim)
    - .planning/phases/15-protocol-transport-overhaul/15-PATTERNS.md lines 345-354 (vision-scoped OVERCROWDED block — load-bearing)
    - CLAUDE.md §Architecture "Env state projection — three layers" (why line 395 mask-and-OR matters)
  </read_first>
  <action>
    Step 1 — move the file:

    ```bash
    git mv src/main/java/com/paralife/engine/PerceptionBroadcaster.java \
           src/main/java/com/paralife/websocket/TickBroadcaster.java
    ```

    Step 2 — edit the moved file:

    - Change `package com.paralife.engine;` → `package com.paralife.websocket;`.
    - Rename the public class: `public class PerceptionBroadcaster` → `public class TickBroadcaster`.
    - Update any constructor name(s) and log-line class references.
    - Update imports if the file used to reference same-package classes that now need explicit imports (e.g. if the file used `BuffRegistry` without import because they shared the `engine` package — add `import com.paralife.engine.BuffRegistry;`).

    Step 3 — update ALL callsites across the project:

    ```bash
    grep -rl "PerceptionBroadcaster" src/ --include="*.java"
    ```

    For each result, replace `PerceptionBroadcaster` → `TickBroadcaster` and update any `import com.paralife.engine.PerceptionBroadcaster` → `import com.paralife.websocket.TickBroadcaster`. Common callsites:

    - `src/test/java/com/paralife/engine/PerceptionBroadcasterTest.java` — rename the test class and update imports; OR delete/defer — in plan 15-09 these tests are re-migrated anyway. Recommend: **rename the test file** to `src/test/java/com/paralife/websocket/TickBroadcasterProjectionTest.java` (avoiding a collision with the deleted heartbeat test) and update imports. Full test body re-migration happens in plan 15-09.
    - Any `@MockBean PerceptionBroadcaster` — update the type.
    - Any `@Autowired PerceptionBroadcaster` field — update the type.
    - Any `new PerceptionBroadcaster(...)` constructor call in tests — update.

    Step 4 — preserve verbatim the vision-scoped OVERCROWDED block:

    The code at old line 381-418 (look for `computeVisionScopedOvercrowded` + the `(cached & ~BIT_OVERCROWDED) | perBotOvercrowdedBit` expression). After the rename, this block must still read:

    ```java
    // cycle-6 MEDIUM #9: start with cached env cellStatus, STRIP the global
    // OVERCROWDED bit, then OR in the per-bot vision-scoped value.
    byte cached = environmentEngine != null ? environmentEngine.getCellStatus(cellPos) : (byte) 0;
    byte perBotOvercrowdedBit = computeVisionScopedOvercrowded(
            worldGrid, cellPos, botPos, radius, simulationConfig.overcrowdingThreshold())
            ? BIT_OVERCROWDED : 0x00;
    byte cellStatus = (byte) ((cached & ~BIT_OVERCROWDED) | perBotOvercrowdedBit);
    ```

    This is the Phase 14 D-40 mask-and-OR. DO NOT simplify, refactor, rename the variables, or change expression shape. Plan 15-08 downstream consumes `cellStatus` into the codec's `envState` byte — the invariant must be preserved through the rename.

    Step 5 — do NOT change the projection to codec-Frames yet. That is plan 15-08's responsibility. This plan is pure rename + delete, so keep the Jackson-writing `ObjectMapper` path intact even though 15-06 already rewrote WorldWebSocketHandler. The project may be temporarily incoherent (handler uses codec, broadcaster uses Jackson) between plans 15-07 and 15-08 — this is by design for dependency-wave parallelism.

    Step 6 — sanity-check Spring component wiring:

    - File has `@Component` annotation (unchanged).
    - `@EventListener @Order(50)` annotation on the tick handler method (unchanged).
    - Constructor has `@Autowired` (or is the single ctor — whichever was present pre-rename).

    Commit as a separate atomic commit so `git mv` shows as a rename in `git log --follow`.
  </action>
  <verify>
    <automated>! test -f src/main/java/com/paralife/engine/PerceptionBroadcaster.java &amp;&amp; test -f src/main/java/com/paralife/websocket/TickBroadcaster.java &amp;&amp; grep -q "class TickBroadcaster" src/main/java/com/paralife/websocket/TickBroadcaster.java &amp;&amp; grep -q "package com.paralife.websocket" src/main/java/com/paralife/websocket/TickBroadcaster.java &amp;&amp; grep -q "@Order(50)" src/main/java/com/paralife/websocket/TickBroadcaster.java &amp;&amp; grep -q "cached &amp; ~BIT_OVERCROWDED" src/main/java/com/paralife/websocket/TickBroadcaster.java &amp;&amp; ! grep -rE "import com\.paralife\.engine\.PerceptionBroadcaster" src/main/ src/test/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/paralife/engine/PerceptionBroadcaster.java` does NOT exist.
    - `src/main/java/com/paralife/websocket/TickBroadcaster.java` exists with `class TickBroadcaster` + `package com.paralife.websocket`.
    - `@Order(50)` annotation is preserved (unchanged tick-pipeline position).
    - The mask-and-OR expression `cached & ~BIT_OVERCROWDED` is present in the renamed file.
    - No file imports `com.paralife.engine.PerceptionBroadcaster` (no stale references).
    - `./gradlew compileJava` succeeds.
    - If test compilation has residual breakage from the Messages.java strip in plan 15-06, that is acceptable — plan 15-09 migrates tests. Main-source compile must succeed.
    - `git log --follow --name-only -- src/main/java/com/paralife/websocket/TickBroadcaster.java | head -30` shows the rename from `engine/PerceptionBroadcaster.java`.
  </acceptance_criteria>
  <done>Rename complete; mask-and-OR preserved; tick-pipeline order intact; no stale imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None at this layer — pure file reorganisation; no runtime behaviour change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | vision-scoped OVERCROWDED correctness | mitigate | The mask-and-OR preservation is the contract that stops a bot from learning the global overcrowding state of cells outside its vision. Verify literal expression is intact via grep in the acceptance criteria. |
</threat_model>

<verification>
- `./gradlew compileJava` succeeds.
- File moves are git-detected renames (verified via `git log --follow`).
- No orphaned imports of the old class name.
</verification>

<success_criteria>
- One broadcaster bean (`websocket.TickBroadcaster`), renamed from `engine.PerceptionBroadcaster`, in place.
- Old heartbeat broadcaster + its test gone.
- Vision-scoped OVERCROWDED invariant preserved verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/15-protocol-transport-overhaul/15-07-SUMMARY.md`. List any residual callsites that still needed migration after the rename (tests, @MockBean, etc.) and what was done to each.
</output>
