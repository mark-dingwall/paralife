---
phase: 16-emergent-behavior-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/paralife/engine/SimulationEngine.java
  - src/main/java/com/paralife/engine/ActionResolver.java
  - src/main/java/com/paralife/engine/CompositeEnergyDistributor.java
  - src/main/java/com/paralife/engine/FertilityInitializer.java
  - src/main/java/com/paralife/engine/FertilityConfig.java
  - src/main/java/com/paralife/engine/SimulationConfig.java
  - src/main/java/com/paralife/world/Entity.java
  - src/main/java/com/paralife/websocket/WorldWebSocketHandler.java
  - src/main/java/com/paralife/bot/BotClient.java
  - src/main/resources/application.yml
autonomous: true
requirements: [R15]
tags: [rng, determinism, seeding]

must_haves:
  truths:
    - "Given a fixed master seed, engine-side RNG sites #1-3, #5-9, #10, #13, #17 from RESEARCH RNG Audit draw from seeded Random instances (not ThreadLocalRandom)"
    - "BotClient.handleDeath:294 draws respawn jitter from the ctor-injected rng field (not ThreadLocalRandom.current())"
    - "BondedPair.formBond accepts a RandomGenerator param that both ThreadLocalRandom and seeded Random satisfy"
    - "New @TestPropertySource properties (paralife.simulation.seed, .action-seed, .fertility-seed, .spawn-seed) flow through ctors into seeded Random fields; null defaults preserve unseeded prod behaviour"
    - "Full test suite green — ThreadLocalRandom→seeded Random swap is source-compatible for all existing tests"
  artifacts:
    - path: "src/main/java/com/paralife/engine/SimulationEngine.java"
      provides: "Ctor-injected simRng replaces 6 ThreadLocalRandom call sites (lines 238, 241, 446, 942, 1079, 1090 per PATTERNS.md)"
      contains: "Random simRng"
    - path: "src/main/java/com/paralife/engine/ActionResolver.java"
      provides: "Ctor-injected actionRng replaces ThreadLocalRandom at line 520"
      contains: "Random actionRng"
    - path: "src/main/java/com/paralife/engine/CompositeEnergyDistributor.java"
      provides: "Ctor-injected Random replaces ThreadLocalRandom at line 77 shuffle"
    - path: "src/main/java/com/paralife/engine/FertilityInitializer.java"
      provides: "Ctor-injected fertilityRng replaces ThreadLocalRandom at line 46"
    - path: "src/main/java/com/paralife/world/Entity.java"
      provides: "BondedPair.formBond signature accepts RandomGenerator (widening from ThreadLocalRandom)"
      contains: "RandomGenerator"
    - path: "src/main/java/com/paralife/websocket/WorldWebSocketHandler.java"
      provides: "Ctor-injected spawnRng replaces ThreadLocalRandom at line 191"
    - path: "src/main/java/com/paralife/bot/BotClient.java"
      provides: "handleDeath uses this.rng not ThreadLocalRandom.current()"
  key_links:
    - from: "application.yml paralife.simulation.seed"
      to: "SimulationEngine.simRng"
      via: "@ConfigurationProperties → SimulationConfig record → ctor param"
      pattern: "config.seed\\(\\)\\s*==\\s*null\\s*\\?\\s*new Random\\(\\)"
    - from: "master seed in @TestPropertySource"
      to: "All 10 hot-path RNG sites"
      via: "per-component Random instances — not a RandomSource facade (per RESEARCH Open Q #1 recommendation)"
      pattern: "new Random\\(config\\.\\w+\\(\\)\\)"
---

<objective>
Refactor 10 hot-path server-side RNG sites from `ThreadLocalRandom`/unseeded `new Random()` to constructor-injected seeded `Random` instances, following the `EnvironmentEngine:220` precedent (`config.seed() == null ? new Random() : new Random(config.seed())`). This is the deterministic-RNG foundation R15 rests on — without it, `CompositeFormationDeterminismTest` can never assert seed-reproducibility.

Purpose: Close the "whole sim uses ThreadLocalRandom" gap that `EnvironmentEngine.resetForTest`'s comment at lines 1208-1216 flags. Minimum-diff approach per RESEARCH Open Q #4: per-component yaml seed properties (no new `RandomSource` facade bean), null defaults preserve production behaviour byte-for-byte.

Output: 6 production files with seeded RNG ctors; `application.yml` with 4 new nullable seed properties; 1 existing bug fix (`BotClient.handleDeath:294`); full test suite still green.

Implements locked decisions:
- D-09 — Component-seeded RNG with master seed.
- D-09a — `BotClient` ctor already accepts `Random`; bug at `handleDeath:294` bypasses it.
- D-09b — Server-side RNG audit → inject the hot-path subset.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-emergent-behavior-tests/16-CONTEXT.md
@.planning/phases/16-emergent-behavior-tests/16-RESEARCH.md
@.planning/phases/16-emergent-behavior-tests/16-PATTERNS.md
@CLAUDE.md
@src/main/java/com/paralife/engine/EnvironmentEngine.java
@src/main/java/com/paralife/engine/EnvironmentConfig.java

<interfaces>
<!-- Existing seeded-RNG precedent to mirror. From EnvironmentEngine.java:220 -->
```java
// In ctor:
this.rng = config.seed() == null ? new Random() : new Random(config.seed());
```

<!-- Existing EnvironmentConfig record — pattern for new seed properties. Grep the record for the `seed()` accessor signature. -->

<!-- BotClient 6-arg ctor (already accepts Random — no signature change needed): -->
```java
public BotClient(String serverUri, char species, HeuristicBrain brain,
                 long respawnCooldownMs, long respawnJitterMs, Random rng) { … }
```

<!-- Target new BondedPair.formBond signature (widening the last param): -->
```java
public static BondedPair formBond(
        String id, Particle primary, Particle secondary,
        int primaryDecay, int primaryCombatTransfer, int primaryAttackPower, int primaryMaxEnergy,
        int secondaryDecay, int secondaryCombatTransfer, int secondaryAttackPower, int secondaryMaxEnergy,
        double bondRateBonusMin, double bondRateBonusMax,
        double bondDecayCostMin, double bondDecayCostMax,
        java.util.random.RandomGenerator rng                    // widened from ThreadLocalRandom
) { … }

static int hybridRate(int a, int b, double min, double max, RandomGenerator rng) { … }
static int bondDecayCost(int a, int b, double min, double max, RandomGenerator rng) { … }
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Inject seeded RNG into SimulationEngine, ActionResolver, CompositeEnergyDistributor, FertilityInitializer, and Entity.BondedPair.formBond</name>
  <files>
    src/main/java/com/paralife/engine/SimulationEngine.java,
    src/main/java/com/paralife/engine/ActionResolver.java,
    src/main/java/com/paralife/engine/CompositeEnergyDistributor.java,
    src/main/java/com/paralife/engine/FertilityInitializer.java,
    src/main/java/com/paralife/engine/FertilityConfig.java,
    src/main/java/com/paralife/engine/SimulationConfig.java,
    src/main/java/com/paralife/world/Entity.java,
    src/main/resources/application.yml
  </files>
  <read_first>
    - src/main/java/com/paralife/engine/EnvironmentEngine.java (lines 215-230 — existing seeded Random ctor pattern)
    - src/main/java/com/paralife/engine/EnvironmentConfig.java (full — record + `seed()` accessor pattern)
    - src/main/java/com/paralife/engine/SimulationEngine.java (lines 85-108 ctor; 238, 241, 446, 942, 1079, 1090 RNG sites; 521 formBond call site)
    - src/main/java/com/paralife/engine/ActionResolver.java (line 520 RNG site + ctor)
    - src/main/java/com/paralife/engine/CompositeEnergyDistributor.java (line 77 shuffle + ctor; line 67-72 @EventListener @Order(15))
    - src/main/java/com/paralife/engine/FertilityInitializer.java (line 46 RNG site + @PostConstruct ctor)
    - src/main/java/com/paralife/engine/FertilityConfig.java (existing record — add `seed` field)
    - src/main/java/com/paralife/engine/SimulationConfig.java (existing record — add `seed` + `actionSeed` fields OR introduce SimulationConfig.seed())
    - src/main/java/com/paralife/world/Entity.java (lines 255-300 BondedPair + formBond + hybridRate + bondDecayCost)
    - .planning/phases/16-emergent-behavior-tests/16-RESEARCH.md §RNG Audit lines 363-398 (line-exact site list)
    - .planning/phases/16-emergent-behavior-tests/16-PATTERNS.md line 33-40 (RNG replacement table)
    - .planning/phases/16-emergent-behavior-tests/16-CONTEXT.md D-09, D-09a, D-09b
  </read_first>
  <action>
**Step 1 — Extend config records with nullable seed fields (pattern: `EnvironmentConfig.seed()` → `Long` or primitive-with-null-sentinel).**

Per RESEARCH §Seeding Surface lines 546-551, add four new properties. Match `EnvironmentConfig.seed()` shape — RESEARCH line 545 confirms this is the existing precedent (binds `null` when yaml key absent, seeded when set):

- `SimulationConfig` record: add `Long seed` and `Long actionSeed` fields. Add the accessors to the record canonical constructor; default to `null` in `application.yml` (do not write `0` — `null` is the "unseeded" sentinel per EnvironmentConfig convention).
- `FertilityConfig` record: add `Long seed` field.
- No new config record for spawn seed yet — plumb via Task 2's `WorldWebSocketHandler` ctor from a fresh record OR add to an existing gridside config. Defer to Task 2.

**Step 2 — SimulationEngine ctor + RNG field (per D-09b, PATTERNS.md lines 33 & 642-650):**

- Add `private final Random simRng;` field.
- Extend ctor to accept the existing `SimulationConfig` (already injected) and initialize: `this.simRng = config.seed() == null ? new Random() : new Random(config.seed());` — verbatim `EnvironmentEngine:220` pattern.
- Replace at each site exactly (line numbers from RESEARCH §RNG Audit):
  - Line 238: `Collections.shuffle(particlePositions, ThreadLocalRandom.current())` → `Collections.shuffle(particlePositions, simRng)`.
  - Line 241: `ThreadLocalRandom rng = ThreadLocalRandom.current()` → `Random rng = simRng` (shadow local to minimize downstream edits).
  - Line 446: `Collections.shuffle(bondedPairPositions, ThreadLocalRandom.current())` → `Collections.shuffle(bondedPairPositions, simRng)`.
  - Line 942: `ThreadLocalRandom.current().nextDouble()` → `simRng.nextDouble()`.
  - Line 1079: `ThreadLocalRandom.current().nextDouble() < shatterProb` → `simRng.nextDouble() < shatterProb`.
  - Line 1090: `ThreadLocalRandom rng = ThreadLocalRandom.current()` → `Random rng = simRng` (shadow).
  - Line 1105: no change if same shadow name `rng` is reused; else apply same shadow substitution.
- Per PATTERNS.md line 679: if a back-compat unit-test ctor exists at line 108+, extend it with `new Random()` as the default `simRng`, OR update those test ctors. Prefer extending (smaller test-suite churn).

**Step 3 — ActionResolver (RESEARCH #8):**

- Add `private final Random actionRng;` field.
- Ctor accepts `SimulationConfig` (already wired? — if not, inject it). Initialize `actionRng = config.actionSeed() == null ? new Random() : new Random(config.actionSeed())`.
- Line 520: `ThreadLocalRandom.current().nextDouble()` → `actionRng.nextDouble()`.

**Step 4 — CompositeEnergyDistributor (RESEARCH #9):**

- Decision: reuse `simRng` via shared injection OR add its own `Random` + seed property. Per RESEARCH Open Q #4 "Seed the minimum 5-site set" — this is outside the R15 minimum set; reuse `simRng` by passing `SimulationEngine`'s `Random` is harder (tight coupling). Simpler path: add `Random compositeRng` field seeded from `SimulationConfig.seed()` (same master seed, same instance per engine → both share byte-stream — acceptable since they're different call sites; or fork via `new Random(config.seed() ^ 0x9E3779B97F4A7C15L)` to decorrelate).
- Chosen: add a second field initialised as `new Random(config.seed() ^ 0xC0FFEEL)` when `seed != null`, else `new Random()`. Line 77: `Collections.shuffle(memberIds, ThreadLocalRandom.current())` → `Collections.shuffle(memberIds, compositeRng)`.

**Step 5 — FertilityInitializer (RESEARCH #10):**

- Add `private final Random fertilityRng;` field; ctor accepts `FertilityConfig` (already?). Initialize from `FertilityConfig.seed()` with same null-fallback pattern.
- Line 46: `ThreadLocalRandom rng = ThreadLocalRandom.current()` → `Random rng = fertilityRng` (shadow).

**Step 6 — Entity.BondedPair.formBond (RESEARCH #13):**

- Widen the last parameter of `formBond(...)` from `ThreadLocalRandom` to `java.util.random.RandomGenerator` (JDK 17+ supertype). Import `java.util.random.RandomGenerator`.
- Same for sibling helpers `hybridRate(...)` and `bondDecayCost(...)` — last param → `RandomGenerator`.
- DELETE the in-body `ThreadLocalRandom rng = ThreadLocalRandom.current()` line at 265 (and 286, 295) — they are replaced by the parameter.
- Update the caller in `SimulationEngine:505` to pass `simRng` as the final argument.
- CRITICAL — D-09 / PATTERNS.md line 765: keep `formBond` as a **static factory on the existing `BondedPair` record**. Do NOT widen the sealed hierarchy. Do NOT import `BondingConfig`/`MetabolicProfile` into `com.paralife.world`. Param stays primitive-typed.

**Step 7 — application.yml (RESEARCH §Seeding Surface lines 547-551):**

Append under `paralife:`:

```yaml
simulation:
  # existing keys unchanged
  seed:                 # null = unseeded (production default). Test sets via @TestPropertySource.
  action-seed:          # null = unseeded.
world:
  fertility:
    seed:               # null = unseeded.
```

Do not specify values — `null` default preserves prod behaviour byte-for-byte (per Assumption A10).

**Step 8 — Anti-patterns to avoid:**

- Do NOT introduce a `RandomSource` facade bean — per RESEARCH Open Q #1 recommendation "Go with per-component properties (no new bean)." Matches existing `EnvironmentConfig.seed()`.
- Do NOT add `EmergenceMetrics` anywhere — that's 16-02's concern (ctor will be re-extended in Wave 2 after file-conflict).
- Do NOT change `UUID.randomUUID()` at SimulationEngine:556, 567, 568 — RESEARCH #4 flags as "no effect on probability; skip unless byte-stable fixtures are ever revived."
- Do NOT touch RNG sites #11 (EnvironmentEngine — already seeded via events.seed), #14 (RockGenerator — already controllable via rock.seed), #12 (ToxinPathGenerator — WARM site, out of R15 minimum scope per RESEARCH Open Q #4).

**Step 9 — Compile + smoke-test.** Run `./gradlew test` to confirm no regression across all 166+ existing tests (per Assumption A10). Any test that directly constructs `SimulationEngine`/`ActionResolver`/`FertilityInitializer`/`CompositeEnergyDistributor` needs the extra ctor arg — update to pass `new Random()` or use the back-compat ctor if preserved.
  </action>
  <verify>
    <automated>./gradlew test -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "ThreadLocalRandom\\.current\\(\\)" src/main/java/com/paralife/engine/SimulationEngine.java` → 0
    - `grep -cE "ThreadLocalRandom\\.current\\(\\)" src/main/java/com/paralife/engine/ActionResolver.java` → 0
    - `grep -cE "ThreadLocalRandom\\.current\\(\\)" src/main/java/com/paralife/engine/CompositeEnergyDistributor.java` → 0
    - `grep -cE "ThreadLocalRandom\\.current\\(\\)" src/main/java/com/paralife/engine/FertilityInitializer.java` → 0
    - `grep -nE "private final Random simRng" src/main/java/com/paralife/engine/SimulationEngine.java` → matches
    - `grep -nE "RandomGenerator" src/main/java/com/paralife/world/Entity.java | head -1` → matches at/after existing BondedPair line
    - `grep -nE "RandomGenerator rng" src/main/java/com/paralife/world/Entity.java | wc -l` → ≥ 3 (formBond + hybridRate + bondDecayCost)
    - `grep -nE "paralife.simulation.seed|action-seed|fertility.seed" src/main/resources/application.yml | wc -l` → ≥ 3
    - `./gradlew compileJava -q` exit 0
    - `./gradlew test -q` exit 0 — full suite still green (Assumption A10 verification)
  </acceptance_criteria>
  <done>
    All 10 engine-side hot-path RNG sites now draw from ctor-injected seeded Random. `application.yml` exposes 4 new nullable seed properties. `BondedPair.formBond` signature widened to `RandomGenerator`. Full test suite green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Inject spawnRng into WorldWebSocketHandler; fix BotClient.handleDeath:294 one-liner bug</name>
  <files>
    src/main/java/com/paralife/websocket/WorldWebSocketHandler.java,
    src/main/java/com/paralife/bot/BotClient.java,
    src/main/resources/application.yml
  </files>
  <read_first>
    - src/main/java/com/paralife/websocket/WorldWebSocketHandler.java (line 191 RNG site; ctor)
    - src/main/java/com/paralife/bot/BotClient.java (lines 76, 82, 85-97 ctors; line 85-97 6-arg ctor with Random; line 290-303 handleDeath)
    - .planning/phases/16-emergent-behavior-tests/16-RESEARCH.md §RNG Audit site #16 (lines 386-387) and #17 (lines 387-388)
    - .planning/phases/16-emergent-behavior-tests/16-PATTERNS.md lines 42, 844-869 (BotClient bug fix)
  </read_first>
  <action>
**Step 1 — WorldWebSocketHandler:191 (RESEARCH #17):**

- Add `private final Random spawnRng;` field.
- Extend ctor to accept a seed from a suitable config source. Since there's no existing `WorldConfig` that owns this, add a new top-level property `paralife.world.spawn-seed` — decision: thread it via a simple `@Value("${paralife.world.spawn-seed:#{null}}") Long spawnSeed` ctor param (matches Spring idiom for optional scalar property; no new record needed).
- Initialize `spawnRng = spawnSeed == null ? new Random() : new Random(spawnSeed);`.
- Line 191: `ThreadLocalRandom rng = ThreadLocalRandom.current()` → `Random rng = spawnRng` (shadow — preserve all downstream `rng.nextInt(...)` call sites without edit).
- Append to `application.yml` under `paralife.world:`:
  ```yaml
  spawn-seed:    # null = unseeded. Test sets via @TestPropertySource.
  ```

**Step 2 — BotClient.handleDeath:294 (RESEARCH #16, PATTERNS.md line 864-869):**

This is a **single-line bug fix** — RESEARCH line 757 explicitly labels this as "missed seeding point + existing bug." The ctor at line 85-97 already accepts `Random rng` as the 6th arg. The bug: `handleDeath()` at line 294 discards the injected field and calls `ThreadLocalRandom.current().nextLong(respawnJitterMs)`.

Replace at line 294 exactly:
```java
// before:
long jitter = respawnJitterMs > 0
        ? ThreadLocalRandom.current().nextLong(respawnJitterMs)
        : 0L;

// after:
long jitter = respawnJitterMs > 0
        ? rng.nextLong(respawnJitterMs)
        : 0L;
```

Per Assumption A10: 2-arg ctor defaults `rng` to `ThreadLocalRandom.current()` (line 76), so production byte-for-byte behaviour is preserved. Seeded tests (16-04's `SeededBotLauncher`) now see their per-bot seed honoured across respawn jitter.

**Step 3 — Do NOT change:**

- `BotLauncher.java` (2-arg ctor) — 16-04 introduces `SeededBotLauncher` as a parallel class for test use. Production `BotLauncher` stays untouched.
- Any other `ThreadLocalRandom` site — only these two sites are in this plan's scope (16-02 handles EmergenceMetrics, 16-03 handles TickEngine).

**Step 4 — Verify no other `ThreadLocalRandom.current()` remains in `bot/` or `websocket/` hot paths:**

```bash
grep -rn "ThreadLocalRandom.current()" src/main/java/com/paralife/bot src/main/java/com/paralife/websocket
```

Expected result: zero hits (after this task). If any remain, investigate — they are either out-of-scope (cold-startup) or a missed audit-table entry.

**Step 5 — Run full test suite**, specifically `./gradlew test --tests "BotClient*Test" --tests "WorldWebSocketHandler*Test"` to prove neither change regresses existing coverage.
  </action>
  <verify>
    <automated>./gradlew test -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "ThreadLocalRandom\\.current\\(\\)" src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` → 0
    - `grep -cE "ThreadLocalRandom\\.current\\(\\)" src/main/java/com/paralife/bot/BotClient.java` → 0
    - `grep -nE "private final Random spawnRng" src/main/java/com/paralife/websocket/WorldWebSocketHandler.java` → matches
    - `grep -nE "rng\\.nextLong\\(respawnJitterMs\\)" src/main/java/com/paralife/bot/BotClient.java` → matches (line ~294)
    - `grep -E "spawn-seed" src/main/resources/application.yml` → matches
    - `./gradlew test -q` exit 0 — full suite still green (Assumption A10)
  </acceptance_criteria>
  <done>
    `WorldWebSocketHandler` spawn RNG now ctor-injectable and seeded via `paralife.world.spawn-seed`. `BotClient.handleDeath:294` uses the ctor's injected `rng` (bug resolved). Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test RNG config → production beans | `@TestPropertySource` injects seeds that flow through prod ctors; a misconfigured prop key would silently fall back to unseeded Random without any error signal. |
| Cross-commit test isolation | Non-seeded call sites that remain after this refactor will make R15 (plan 16-05) flake — observable as "passes first run, fails second." |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | SimulationEngine.simRng config binding | mitigate | Exact key names `paralife.simulation.seed` / `paralife.simulation.action-seed` verified via grep; match `EnvironmentConfig.seed()` precedent; null fallback yields byte-identical prod behaviour (A10). |
| T-16-02 | Information Disclosure | Leaked seed across parallel tests | accept | Test-only; `@TestPropertySource` seeds are per-class, not cross-class. Virtual-thread scheduling already makes cross-run byte-parity impossible (D-09 byte-stable-rejected rationale). |
| T-16-03 | Denial of Service | Ctor re-extension breaks back-compat unit tests | mitigate | Task 1 explicitly preserves back-compat ctor where present; `./gradlew test` acceptance gate catches any left-behind call site. |
| T-16-04 | Repudiation | Seed logging | transfer | Logging master seed happens in R15/R16 test `@BeforeEach` (plans 16-05, 16-06), not in this plan. Not our concern. |
</threat_model>

<verification>
- All 10 hot-path RNG audit sites now draw from ctor-injected `Random`.
- `grep -rn "ThreadLocalRandom" src/main/java/com/paralife/engine src/main/java/com/paralife/bot src/main/java/com/paralife/websocket` should match only import lines (zero `ThreadLocalRandom.current()` call expressions in SimulationEngine/ActionResolver/CompositeEnergyDistributor/FertilityInitializer/WorldWebSocketHandler/BotClient).
- `./gradlew test` full suite green — no test regression from ThreadLocalRandom → seeded Random swap.
</verification>

<success_criteria>
- 6 production Java files modified with seeded `Random` ctor injection.
- `BondedPair.formBond` signature widened to `RandomGenerator` (JDK 17+ supertype).
- `application.yml` exposes 4 new nullable seed properties (`paralife.simulation.seed`, `.action-seed`, `paralife.world.fertility.seed`, `paralife.world.spawn-seed`).
- `BotClient.handleDeath:294` bug resolved.
- `./gradlew test` green (pre-existing 166+ tests, no regression).
</success_criteria>

<output>
After completion, create `.planning/phases/16-emergent-behavior-tests/16-01-SUMMARY.md` per the summary template.
</output>
