---
phase: 16-emergent-behavior-tests
plan: 07
type: execute
wave: 5
depends_on: ["16-06"]
files_modified:
  - .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md
autonomous: false
requirements: [R17, R19]
tags: [narrative, documentation, phase-gate, checkpoint]

must_haves:
  truths:
    - "16-EMERGENCE.md narrative exists with one section per D-04 signal (5 sections: bonded-pair, composite, predator-pressure, RPS boom-bust, flee-from-buffed)"
    - "Each section contains: observation, master seed used, fixture JSON path, interpretation — per D-06 'narrative markdown per signal' requirement"
    - "Narrative cites concrete numbers from fixtures/run-*.json — not placeholders, not hypothetical values"
    - "Master-seed-to-repro-command pairing present — a reader can rerun the exact scenario via @TestPropertySource overrides"
    - "R19 full-suite gate asserted via checkpoint: ./gradlew test (with long-run tag enabled) returns 0"
    - "VALIDATION Sign-Off checklist complete (all boxes ticked)"
  artifacts:
    - path: ".planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md"
      provides: "Narrative writeup of emergent patterns observed — closes R17"
      min_lines: 100
      contains: "D-04"
  key_links:
    - from: "16-EMERGENCE.md"
      to: "fixtures/run-*.json"
      via: "inline path reference; reader can open the JSON for raw numbers"
      pattern: "fixtures/run-"
    - from: "R19 full-suite gate"
      to: "./gradlew test -PincludeLong=true"
      via: "checkpoint: human-verify gate before phase closeout"
      pattern: "gradlew test"
---

<objective>
Write the R17 narrative writeup `16-EMERGENCE.md` based on observations from the 16-06 long-run test's fixture dumps, then gate the phase on a clean full-suite `./gradlew test` invocation (R19) as the final checkpoint before Phase 16 is marked complete.

Purpose: R17 requires "at least one emergent pattern documented." The writeup is the human-readable evidence; the fixtures are the raw data; the test assertions are the mechanical proof. All three complete the R17 story. R19 is the existing-tests-still-pass gate — no new artefact, just a pass/fail confirmation.

Output: one new markdown file; one human-verified checkpoint. Phase 16 ready for verifier-agent handoff.

Implements locked decisions: D-06 narrative form, D-13 JUnit-only (narrative supports, does NOT replace, the JUnit test).

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-emergent-behavior-tests/16-CONTEXT.md
@.planning/phases/16-emergent-behavior-tests/16-RESEARCH.md
@.planning/phases/16-emergent-behavior-tests/16-VALIDATION.md
@.planning/phases/16-emergent-behavior-tests/16-06-SUMMARY.md
@.planning/phases/16-emergent-behavior-tests/fixtures/

<interfaces>
<!-- Fixture JSON schema (CONTEXT D-06b, written by RunFixtureWriter in 16-04/16-06): -->
```json
{
  "masterSeed": 1745164800000000000,
  "runStarted": "2026-04-21T12:00:00Z",
  "tickCount": 1000,
  "botCount": 100,
  "world": {"width": 64, "height": 64},
  "emergence": {
    "bondedPairsFormed": 42,
    "compositesFormed": 7,
    "buffsGranted": 3,
    "mutagenInfections": 11
  },
  "stability": {
    "tickDriftPercent": 3.2,
    "tickWorkMsMean": 6.8,
    "tickWorkMsP99": 13.4,
    "sessionDropouts": 0,
    "heapGrowthPercent": 8.1,
    "errorLogCount": 0,
    "activeSessionsFinal": 100
  },
  "populations": [{"tick": 10, "catalyst": 33, "membrane": 34, "spore": 33}, ...]
}
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 16-EMERGENCE.md narrative — one section per D-04 signal with cited fixture evidence</name>
  <files>.planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md</files>
  <read_first>
    - .planning/phases/16-emergent-behavior-tests/16-CONTEXT.md D-04 (five signals, CONTEXT lines 40-47), D-06 (narrative form), D-06b (fixture schema)
    - .planning/phases/16-emergent-behavior-tests/16-06-SUMMARY.md (the run narrative + observed counts)
    - Latest 3 files in .planning/phases/16-emergent-behavior-tests/fixtures/ (parse JSON for actual numbers)
    - .planning/phases/15-protocol-transport-overhaul/15-SCHEMA.md (format analog — "narrative-with-numbers" per PATTERNS.md line 27)
  </read_first>
  <action>
**Step 1 — Structure (5 sections, one per D-04 signal) + master seed table:**

```markdown
# Phase 16: Emergent Behavior Observations

**Run date:** 2026-MM-DD
**Phase goal:** Validate that complex behaviours emerge from the combination of bonding, composites, metabolism, environment, and protocol — closing R17.

## Source runs

The observations below are sourced from three calibration runs of `EmergenceStabilityLoadTest` on 1000-tick, 100-bot, 64×64 grids:

| Run | Master seed | Fixture | Composites | BondedPairs | Buffs | Infections | Drift |
|-----|-------------|---------|------------|-------------|-------|------------|-------|
| 1   | 174516480…  | `fixtures/run-2026-MM-DDT…-1.json` | 7  | 42 | 3 | 11 | 3.2 % |
| 2   | 174518320…  | `fixtures/run-2026-MM-DDT…-2.json` | 4  | 38 | 2 | 9  | 4.1 % |
| 3   | 174520160…  | `fixtures/run-2026-MM-DDT…-3.json` | 9  | 47 | 5 | 13 | 2.8 % |

*Fixtures are gitignored (CONTEXT D-06b) — the table above captures the numbers; the files live only on the operator's machine for post-hoc inspection.*

## Signal 1: Bonded-pair formation (D-04 #1)

**Observation:** …

**Seed:** …

**Interpretation:** …

## Signal 2: Composite formation (D-04 #2)

**Observation:** …

**Interpretation:** …

## Signal 3: Predator pressure on STARVING prey (D-04 #3)

*(Observed via the TriggerWatcher sliding-window mechanism; D-05. W=20, R=5.)*

**Observation:** …

**Interpretation:** …

## Signal 4: RPS boom-bust cycle (D-04 #4)

**Observation:** per-type population series oscillation amplitude measured over rolling 200-tick windows. Observed ranges per run: …

**Interpretation:** …

## Signal 5: Flee-from-strong-predator (D-04 #5)

**Observation:** …

**Interpretation:** …

## Count-rule note (Assumption A2)

For per-type-share assertions (D-07 #2), a BondedPair contributes +1 to BOTH primary and secondary species counts; CompositeMember contributes +1 to its `.type()`. Species identity is more fundamental than bond structure for stability framing.

## Reproducibility

To rerun a specific scenario with observed seed `<N>`:

```
./gradlew test --tests "com.paralife.engine.EmergenceStabilityLoadTest" \
    -Dparalife.test.master-seed=<N> -PincludeLong=true
```

(If programmatic seed override via `-D` is not wired in 16-06, the test's `masterSeed = System.nanoTime()` means seed-specific reruns require editing the `@TestPropertySource` block or switching to `@DynamicPropertySource`; note this as a future enhancement rather than blocking R17.)

## Closing note

All three run columns show `sessionDropouts=0`, `errorLogCount=0`, and `activeSessionsFinal=100` — the capacity-headroom contract (D-10/D-11) holds under the observed composite load. Phase 16 demonstrates that the M2 stack (bonding × composites × metabolism × environment × codec) produces observable emergent structure with stable population dynamics; the narrative above, paired with the mechanical assertions in `EmergenceStabilityLoadTest`, closes R17.
```

**Step 2 — Fill the Observation / Interpretation subsections with real numbers**:

For each of the 5 signals, pull concrete numbers from the latest 3 `fixtures/run-*.json` files and write 2-4 sentences. The fixture files WILL exist because 16-06 Task 3 generated them. Use `cat fixtures/run-*.json | jq .emergence` or equivalent to extract values.

For signals #3 and #5 (trigger-watcher windows), the fixture should include `triggerWindowResults` (add this field in 16-06 Task 2's `buildRunResult` if not already present — minor schema extension vs CONTEXT D-06b — optional but strongly recommended; if absent, narrate signal #3/#5 qualitatively from `emergenceMetrics` + `buffsGranted` totals, noting the limitation).

**Step 3 — Do NOT:**

- Include charts/plots — D-06 says "no charts/plots this phase." Numbers + prose only.
- Embed entire fixture JSON blobs — they're gitignored and large; cite the path, summarise the numbers.
- Document subjective observer impressions — D-13 defers subjective UAT to M5.
- Make claims not supported by fixture data — keep assertions conservative ("composite formation observable within the 1000-tick window" NOT "composite formation dominates the dynamics").
- Write before `EmergenceStabilityLoadTest` has passed at least once — you need the fixture numbers to cite. If the test hasn't run yet in this wave, re-run it now.

**Step 4 — Tick the VALIDATION.md sign-off** — mark all remaining items in 16-VALIDATION.md §Validation Sign-Off. Specifically:

- `[x] All tasks have <automated> verify or Wave 0 dependencies`
- `[x] Sampling continuity: no 3 consecutive tasks without automated verify`
- `[x] Wave 0 covers all MISSING references`
- `[x] No watch-mode flags`
- `[x] Feedback latency < 120 s`
- `[x] Meta-validation sub-section populated with evidence by plan 16-07 close`
- `[x] nyquist_compliant: true set in frontmatter` (already flipped in 16-06 Task 3; confirm)

Change `Approval: pending` → `Approval: approved 2026-MM-DD`.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md && grep -cE "D-04 #1|D-04 #2|D-04 #3|D-04 #4|D-04 #5|Signal 1|Signal 2|Signal 3|Signal 4|Signal 5" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md`
    - `grep -cE "Signal 1|Signal 2|Signal 3|Signal 4|Signal 5" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` → ≥ 5
    - `grep -cE "fixtures/run-" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` → ≥ 1
    - `grep -cE "[Mm]aster seed" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` → ≥ 1
    - `wc -l .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md | awk '{print $1}'` → ≥ 100
    - `grep -cE "^- \\[x\\]" .planning/phases/16-emergent-behavior-tests/16-VALIDATION.md` → ≥ 6 (sign-off items ticked)
    - `grep -E "Approval:\\s*approved" .planning/phases/16-emergent-behavior-tests/16-VALIDATION.md` → matches
  </acceptance_criteria>
  <done>
    `16-EMERGENCE.md` exists with 5 signal sections citing concrete fixture numbers. VALIDATION.md sign-off approved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: R19 gate — full test suite must pass including long-run</name>
  <what-built>
    All Phase 16 artefacts:
    - EmergenceMetrics bean + 4 increment sites + EMERGENCE log markers (16-02)
    - 10 RNG sites refactored to seeded Random (16-01)
    - TickEngine paralife.tick.work.ms DistributionSummary (16-03)
    - 5 test helpers in src/test/java/com/paralife/engine/emergence/ (16-04)
    - CompositeFormationDeterminismTest (R15 — 16-05)
    - EmergenceStabilityLoadTest (R16/R17/R18 — 16-06)
    - 16-EMERGENCE.md narrative (R17 — 16-07 Task 1)
  </what-built>
  <how-to-verify>
    1. From the repo root, run the full suite with the long-run tag enabled:
       ```bash
       ./gradlew test -PincludeLong=true
       ```
    2. Expected: exit 0, no failures, no errors. Wall-clock ~3-5 min.
    3. Expected: NEW test classes `CompositeFormationDeterminismTest`, `EmergenceStabilityLoadTest`, `EmergenceMetricsWiringTest` all in the green-count.
    4. Expected: pre-existing 166+ tests still pass — no regressions (R19).
    5. Expected: fresh fixture file created in `.planning/phases/16-emergent-behavior-tests/fixtures/`:
       ```bash
       ls -la .planning/phases/16-emergent-behavior-tests/fixtures/run-*.json
       ```
    6. Expected: EMERGENCE log markers visible if you `grep EMERGENCE` on the test's captured log stream (optional inspection).
    7. If ANY test fails:
       - Determinism test flake → one or more RNG audit sites still unseeded; reopen 16-01.
       - Long-run stability assertion fails → inspect the latest `fixtures/run-*.json` for which D-07 / D-11 row tripped; reopen 16-06.
       - Unrelated test fails → R19 violation; investigate the specific test.
  </how-to-verify>
  <resume-signal>Type "approved" (R19 gate held) or paste the failing test class + first failure summary.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Narrative vs fixture consistency | A narrative that cites numbers not present in any fixture file undermines R17's evidence quality. |
| Full-suite gate skipped | R19 requires the full suite to pass including the long-run. Running `./gradlew test` without `-PincludeLong=true` would miss the new test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-27 | Tampering | Narrative with fabricated numbers | mitigate | Task 1 acceptance grep requires `fixtures/run-` path citation; reviewer spot-check against actual JSON. |
| T-16-28 | Repudiation | R19 gate passed informally | mitigate | Task 2 is a blocking checkpoint — explicit human "approved" required with exit-0 evidence. |
| T-16-29 | DoS | Long-run not tagged, runs on every CI build | accept | 16-06 applies `@Tag("slow")`; full-suite gradle invocation still picks it up with `-PincludeLong=true`; daily CI can exclude. |
</threat_model>

<verification>
- `16-EMERGENCE.md` exists with 5 signal sections citing real fixture numbers.
- `./gradlew test -PincludeLong=true` exit 0.
- VALIDATION.md frontmatter `nyquist_compliant: true`, §Validation Sign-Off items ticked, `Approval: approved`.
</verification>

<success_criteria>
- R17 closed — narrative written, fixture evidence cited.
- R19 closed — full suite green including all new Phase 16 tests, no regression of existing.
- Phase 16 ready for `/gsd-verify-work` handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/16-emergent-behavior-tests/16-07-SUMMARY.md` per the summary template.
</output>
