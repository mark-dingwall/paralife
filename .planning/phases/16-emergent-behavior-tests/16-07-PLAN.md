---
phase: 16-emergent-behavior-tests
plan: 07
type: execute
wave: 5
depends_on: ["16-06"]
files_modified:
  - .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md
autonomous: false
requirements: [R17, R19]
tags: [narrative, documentation, phase-gate, checkpoint]

must_haves:
  truths:
    - "16-EMERGENCE.md narrative exists with one section per D-04 signal (5 sections)"
    - "Each section cites concrete numbers from fixtures/run-*.json — not placeholders"
    - "Master-seed-to-repro-command pairing present — reader can rerun via -Dparalife.test.master-seed=<N>"
    - "Reproducibility section shows the single-property override pattern — not @TestPropertySource editing"
    - "R19 full-suite gate asserted via checkpoint: ./gradlew test -PincludeLong=true returns 0"
    - "VALIDATION Sign-Off checklist complete with every item tied to automated verification (addresses REVIEWS LOW — 16-07 sign-off traceability)"
  artifacts:
    - path: ".planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md"
      provides: "Narrative writeup of emergent patterns — closes R17"
      min_lines: 100
      contains: "D-04"
  key_links:
    - from: "16-EMERGENCE.md"
      to: "fixtures/run-*.json"
      via: "path citations; reader opens JSON for raw numbers"
      pattern: "fixtures/run-"
    - from: "R19 full-suite gate"
      to: "./gradlew test -PincludeLong=true"
      via: "checkpoint:human-verify"
      pattern: "gradlew test"
---

<objective>
Author the R17 narrative `16-EMERGENCE.md` from `EmergenceStabilityLoadTest` fixture dumps, then gate the phase on a clean full-suite `./gradlew test -PincludeLong=true` invocation (R19) as the final checkpoint before Phase 16 is marked complete.

Output: one new markdown file; one human-verified checkpoint. Phase 16 ready for verifier-agent handoff.

Implements: D-06 (narrative form), D-13 (JUnit-only UAT, narrative supports rather than replaces test).

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-emergent-behavior-tests/16-CONTEXT.md
@.planning/phases/16-emergent-behavior-tests/16-RESEARCH.md
@.planning/phases/16-emergent-behavior-tests/16-VALIDATION.md
@.planning/phases/16-emergent-behavior-tests/16-06-SUMMARY.md
@.planning/phases/16-emergent-behavior-tests/fixtures/

<interfaces>
<!-- Fixture JSON schema (16-04 extended — includes trigger-window lists): -->
```json
{
  "masterSeed": 1745164800000000000,
  "runStarted": "2026-04-21T12:00:00Z",
  "tickCount": 1000,
  "botCount": 100,
  "world": {"width": 64, "height": 64},
  "emergence": {
    "bondedPairsFormed": 42,
    "compositesFormed": 7,
    "buffsGranted": 3,
    "mutagenInfections": 11
  },
  "stability": {
    "tickDriftPercent": 3.2,
    "tickWorkMsMean": 6.8,
    "tickWorkMsP99": 11.8,
    "sessionDropouts": 0,
    "heapGrowthPercent": 8.1,
    "errorLogCount": 0,
    "activeSessionsFinal": 100,
    "autocorrelationWinningType": "CATALYST",
    "autocorrelationWinningLag": 42,
    "autocorrelationWinningValue": 0.38
  },
  "populations": [{"tick": 10, "catalyst": 33, "membrane": 34, "spore": 33}, ...],
  "starvingPreyWindows": [{"triggerEntityId": "c-...", "triggerType": "CATALYST", "startTick": 237, "sampleCount": 20, "meanObserverDensity": 1.8, "baselineDensity": 0.9, "signalHeld": true}, ...],
  "fleeWindows": [{"triggerEntityId": "bp-...", "triggerType": "MEMBRANE", "startTick": 541, "sampleCount": 20, "meanObserverDensity": 0.4, "baselineDensity": 1.2, "signalHeld": true}, ...]
}
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 16-EMERGENCE.md narrative citing fixture evidence</name>
  <files>.planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md</files>
  <read_first>
    - .planning/phases/16-emergent-behavior-tests/16-CONTEXT.md D-04 (five signals), D-06, D-06b, D-20 addendum (master-seed override)
    - .planning/phases/16-emergent-behavior-tests/16-06-SUMMARY.md
    - Latest 3 files in .planning/phases/16-emergent-behavior-tests/fixtures/ (parse JSON for numbers)
  </read_first>
  <action>
**Step 1 — Structure (5 sections — one per D-04 signal — plus source runs and reproducibility):**

```markdown
# Phase 16: Emergent Behavior Observations

**Run date:** 2026-MM-DD
**Phase goal:** Validate that complex behaviours emerge from the combination of bonding, composites, metabolism, environment, and protocol — closing R17.

## Source runs

Observations below are from three calibration runs of `EmergenceStabilityLoadTest` on 1000-tick, 100-bot, 64×64 grids at 20 ms tick interval:

| Run | Master seed | Fixture | Composites | BondedPairs | Buffs | Infections | Drift | p99 (ms) |
|-----|-------------|---------|------------|-------------|-------|------------|-------|----------|
| 1   | …           | `fixtures/run-…-1.json` | …   | …  | … | …  | …% | …  |
| 2   | …           | `fixtures/run-…-2.json` | …   | …  | … | …  | …% | …  |
| 3   | …           | `fixtures/run-…-3.json` | …   | …  | … | …  | …% | …  |

*Fixtures are gitignored (CONTEXT D-06b). The table captures numbers; files live on the operator's machine.*

## Signal 1: Bonded-pair formation (D-04 #1)
**Observation:** …
**Interpretation:** …

## Signal 2: Composite formation (D-04 #2)
**Observation:** …
**Interpretation:** …

## Signal 3: Predator pressure on STARVING prey (D-04 #3)
*(TriggerWatcher sliding-window; W=20, R=5.)*
**Observation (from fixture `starvingPreyWindows`):** …
**Interpretation:** …

## Signal 4: RPS boom-bust cycle (D-04 #4)
**Observation (from fixture `stability.autocorrelationWinning*`):**
Across runs, peak lag-k autocorrelation over the scan range [20,100] fell on lag=… type=… value=….
**Interpretation:** …

## Signal 5: Flee-from-strong-predator (D-04 #5)
**Observation (from fixture `fleeWindows`):** …
**Interpretation:** …

## Count-rule note

For per-type-share assertions (D-07 #2), `BondedPair` contributes +1 to BOTH primary and secondary species counts; `CompositeMember` contributes +1 to its `.type()`; plain `Particle` contributes +1 to its type.

## Reproducibility

To rerun a specific scenario with observed master seed `<N>`:

```
./gradlew test --tests "com.paralife.engine.EmergenceStabilityLoadTest" \
    -Dparalife.test.master-seed=<N> -PincludeLong=true
```

The D-20 addendum wires `@Value("${paralife.test.master-seed:#{null}}") Long` into the test class (captured in `MASTER_SEED_HOLDER`), and `@DynamicPropertySource` derives all per-component seeds from that value via `SplittableRandom.split()`. A single CLI flag reproduces the entire run — no `@TestPropertySource` editing required. If the flag is omitted, `masterSeed = System.nanoTime()` is used and logged.

## Closing note

All three run columns show `sessionDropouts=0`, `errorLogCount=0`, and `activeSessionsFinal=100` — the capacity-headroom contract (D-10/D-11) holds under observed composite load. Phase 16 demonstrates that the M2 stack (bonding × composites × metabolism × environment × codec) produces observable emergent structure with stable population dynamics. The narrative above, paired with the mechanical assertions in `EmergenceStabilityLoadTest`, closes R17.
```

**Step 2 — Fill Observation / Interpretation with real numbers.**

Read the 3 most recent fixture files. Use `jq` or manual parsing to extract the relevant fields. Each signal's Observation subsection cites at least 2 concrete numeric facts from fixture data (e.g., "Run 1 reported 42 bonded pairs formed across 1000 ticks; Run 2 reported 38 across 1000 ticks"). The Interpretation subsection is 2-4 sentences of plain-English reasoning.

For Signal #3 and Signal #5 the fixture now includes `starvingPreyWindows` / `fleeWindows` lists (REVIEWS MEDIUM fix in 16-04 / 16-06). Cite at least one concrete window — its triggerEntityId, startTick, meanObserverDensity, baselineDensity, and signalHeld.

**Step 3 — Do NOT:**

- Include charts/plots (D-06: narrative prose only).
- Embed entire fixture JSON blobs.
- Document subjective observer impressions (D-13: deferred to M5).
- Make claims not grounded in fixture numbers.
- Write before `EmergenceStabilityLoadTest` has passed — you need fixture numbers. If fixtures are absent, rerun 16-06 first.

**Step 4 — Tick VALIDATION.md sign-off with automated-verification traceability (REVIEWS LOW — 16-07 sign-off):**

In `16-VALIDATION.md` §Validation Sign-Off, each ticked item MUST have a grep-checkable or command-checkable backing. Example:

```markdown
- [x] All tasks have <automated> verify or Wave 0 dependencies
      ← grep-verified: `grep -c "<automated>" .planning/phases/16-emergent-behavior-tests/16-*-PLAN.md`
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
      ← manual audit of per-task <verify> blocks
- [x] Wave 0 covers all MISSING references  ← N/A — no Wave 0 tasks
- [x] No watch-mode flags
      ← grep-verified: `grep -c "watch" .planning/phases/16-emergent-behavior-tests/16-*-PLAN.md` → 0
- [x] Feedback latency < 120 s
      ← Task-level `./gradlew test --tests "<class>" -q` timings sampled during 16-05 / 16-06 runs
- [x] Meta-validation sub-section populated with evidence by plan 16-07 close
- [x] nyquist_compliant: true set in frontmatter
      ← grep-verified: `grep -E "^nyquist_compliant: true" .planning/phases/16-emergent-behavior-tests/16-VALIDATION.md`
```

Change `Approval: pending` → `Approval: approved 2026-MM-DD`.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md && grep -cE "Signal 1|Signal 2|Signal 3|Signal 4|Signal 5" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md`
    - `grep -cE "Signal 1|Signal 2|Signal 3|Signal 4|Signal 5" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` → ≥ 5
    - `grep -cE "fixtures/run-" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` → ≥ 1
    - `grep -cE "[Mm]aster seed" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` → ≥ 1
    - `grep -cE "paralife\\.test\\.master-seed" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` → ≥ 1 (reproducibility command)
    - `grep -cE "starvingPreyWindows|fleeWindows" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` → ≥ 1 (cites extended fixture schema — REVIEWS MEDIUM)
    - `wc -l .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md | awk '{print $1}'` → ≥ 100
    - `grep -cE "^- \\[x\\]" .planning/phases/16-emergent-behavior-tests/16-VALIDATION.md` → ≥ 6 (sign-off items ticked)
    - `grep -E "Approval:\\s*approved" .planning/phases/16-emergent-behavior-tests/16-VALIDATION.md` → matches
    - `grep -cE "← grep-verified|← manual audit|← N/A" .planning/phases/16-emergent-behavior-tests/16-VALIDATION.md` → ≥ 3 (REVIEWS LOW — traceability)
  </acceptance_criteria>
  <done>
    16-EMERGENCE.md exists with 5 signal sections citing concrete fixture numbers + reproducibility section. VALIDATION.md sign-off items ticked with traceability notes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: R19 gate — full test suite must pass including long-run</name>
  <what-built>
    All Phase 16 artefacts:
    - RNG seeding + resetSeed() hooks across 6 production files (16-01)
    - EmergenceMetrics bean + 4 increment sites (bond/composite in SimulationEngine; buff/infection in EnvironmentEngine) + EMERGENCE log markers (16-02)
    - TickEngine paralife.tick.work.ms DistributionSummary + .gitignore entry + build.gradle.kts -PincludeLong wiring (16-03)
    - 5 test helpers in src/test/java/com/paralife/engine/emergence/ (16-04)
    - CompositeFormationDeterminismTest (R15 — 16-05)
    - EmergenceStabilityLoadTest (R16/R17/R18 — 16-06)
    - 16-EMERGENCE.md narrative (R17 — 16-07 Task 1)
  </what-built>
  <how-to-verify>
    1. From repo root, run the full suite with long-run tag enabled:
       ```bash
       ./gradlew test -PincludeLong=true
       ```
    2. Expected: exit 0, no failures, no errors. Wall-clock ~3-5 min.
    3. Expected: new test classes `CompositeFormationDeterminismTest`, `EmergenceStabilityLoadTest`, `EmergenceMetricsWiringTest` all in green-count.
    4. Expected: all pre-existing tests still pass — no regressions (R19).
    5. Expected: fresh fixture file created:
       ```bash
       ls -la .planning/phases/16-emergent-behavior-tests/fixtures/run-*.json
       ```
    6. Optional: `grep EMERGENCE` on captured logs shows markers from all 4 counters.
    7. If any test fails:
       - Determinism flake → RNG audit site still unseeded or resetSeed missing a component; reopen 16-01.
       - Long-run stability fail → inspect latest `fixtures/run-*.json`; identify which D-07 / D-04 / D-11 row tripped; reopen 16-06.
       - Buff counter double-counting → REVIEWS HIGH #3 regression; check that BuffRegistry.grant has no counter increment.
       - Unrelated test failure → R19 violation; investigate specific test.
  </how-to-verify>
  <resume-signal>Type "approved" (R19 gate held) or paste the failing test class + first failure summary.</resume-signal>
</task>

</tasks>

<threat_model>
Documentation + gate check only. No code change. No new attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-27 | Tampering | Narrative cites fabricated numbers | mitigate | Acceptance grep requires `fixtures/run-` citation; reviewer spot-checks against JSON. |
| T-16-28 | Repudiation | R19 gate passed informally | mitigate | Task 2 is blocking checkpoint — explicit human "approved" with exit-0 evidence. |
| T-16-29 | DoS | Long-run in every CI build | accept | @Tag("slow") + -PincludeLong=true opt-in (16-03). |
</threat_model>

<verification>
- 16-EMERGENCE.md with 5 signal sections citing real fixture numbers.
- `./gradlew test -PincludeLong=true` exit 0.
- VALIDATION.md nyquist_compliant=true; §Validation Sign-Off ticked with traceability; Approval: approved.
</verification>

<success_criteria>
- R17 closed — narrative written, fixture evidence cited.
- R19 closed — full suite green including new Phase 16 tests; no regressions.
- Phase 16 ready for /gsd-verify-work handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/16-emergent-behavior-tests/16-07-SUMMARY.md`.
</output>
