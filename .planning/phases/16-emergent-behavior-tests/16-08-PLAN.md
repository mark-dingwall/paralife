---
phase: 16-emergent-behavior-tests
plan: 08
type: execute
wave: 6
depends_on: [16-05, 16-07]
files_modified:
  - .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md
autonomous: false
requirements: [R17, R19]
tags: [documentation, emergence, narrative, r17, r19-gate]

must_haves:
  truths:
    - "16-EMERGENCE.md exists with YAML frontmatter (phase, plan, tags, dependency_graph)"
    - "All 5 D-04 signals have a dedicated narrative section (bonded-pair formation, composite formation, predator-pressure-on-STARVING, RPS boom-bust, flee-from-strong-predator)"
    - "Each signal section contains: master seed, observed values (from 16-05 and 16-07 summaries), qualitative interpretation paragraph"
    - "Document references the fixture JSON path for forensic reproduction"
    - "R19 regression gate recorded — full `./gradlew test` result captured in a closing section"
  artifacts:
    - path: ".planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md"
      provides: "R17 evidence writeup per D-06"
      contains: "EMERGENCE"
  key_links:
    - from: "16-EMERGENCE.md"
      to: "Plan 16-05 SUMMARY (R15 counters) + Plan 16-07 SUMMARY (R16/R17/R18 counters + TriggerWatcher results)"
      via: "narrative prose citing values"
      pattern: "seed=42|MASTER_SEED"
    - from: "16-EMERGENCE.md closing section"
      to: "./gradlew test (full suite)"
      via: "recorded test summary output"
      pattern: "BUILD SUCCESSFUL|tests completed"
---

<objective>
Write `16-EMERGENCE.md` — R17 narrative evidence per D-06. Consume the fixture JSON written by Plan 16-07 + the counter values recorded in Plan 16-05 and 16-07 summaries. Close R19 by running the full `./gradlew test` suite and recording the outcome in the final section.

This plan directly closes **R17** (narrative evidence writeup) and **R19** (full-suite regression gate). R15, R16, R18 are closed by Plans 16-05 / 16-06 / 16-07 respectively and are only cross-referenced (not re-asserted) here. Per-plan requirement attribution (checker-revision INFO 4): this plan's `closes: [R17, R19]` YAML reflects only the requirements this plan itself lands.

Once 16-EMERGENCE.md lands AND the full test suite is green, combined with the earlier plan closeouts, all 5 Phase 16 requirements (R15–R19) are satisfied and v2.0 (pending M5 subjective visualiser UAT per D-13) is done.

Purpose: Turn raw counter values + TriggerWatcher outputs into a human-readable narrative that documents the emergent patterns observed. Document-only work — the actual emergent behaviour is already proven by the assertions in Plans 16-05 and 16-07.

Output: 1 markdown file (~200–400 lines), recorded `./gradlew test` output in the closing section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-emergent-behavior-tests/16-CONTEXT.md
@.planning/phases/16-emergent-behavior-tests/16-RESEARCH.md
@.planning/phases/16-emergent-behavior-tests/16-PATTERNS.md
@.planning/phases/16-emergent-behavior-tests/16-05-SUMMARY.md
@.planning/phases/16-emergent-behavior-tests/16-07-SUMMARY.md
@.planning/phases/15-protocol-transport-overhaul/15-10-SUMMARY.md

<interfaces>
<!-- 15-10-SUMMARY.md frontmatter style (Plan 16-08 mirrors this) -->
```yaml
---
phase: 15-protocol-transport-overhaul
plan: 10
subsystem: observability
tags: [metrics, micrometer, actuator, observability]
dependency_graph:
  requires: [15-08]
  provides: [...]
  affects: [...]
---
```

<!-- Fixture JSON shape (Plan 16-07 writes) -->
```json
{
  "masterSeed": 42,
  "startedAt": "2026-04-21T10:15:00Z",
  "totalTicks": 1000,
  "config": { "width": 64, "height": 64, "botCount": 100, "intervalMs": 20 },
  "counters": {
    "bondedPairsFormed": <N>,
    "compositesFormed": <N>,
    "buffsGranted": <N>,
    "mutagenInfections": <N>
  },
  "ticks": [ {"tick": 10, "CATALYST": 33, "MEMBRANE": 34, "SPORE": 33}, ... ]
}
```
</interfaces>
</context>

<threat_model>
No new trust boundary. 16-EMERGENCE.md is committed docs. No sensitive data (only per-type aggregate counts, seed value, counter values — all reproducibility-level).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | I | Committed seed + counter values | accept | Seed is a reproducibility aid, not a secret (D-09). Counter values are aggregate-only. |

Block severity: LOW.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and capture output for the R19 gate</name>
  <files>.planning/phases/16-emergent-behavior-tests/16-gradle-test-output.txt</files>
  <read_first>
    - .planning/phases/16-emergent-behavior-tests/16-CONTEXT.md D-03 (R19 covered by ./gradlew test)
    - .planning/phases/16-emergent-behavior-tests/16-VALIDATION.md (R19 validation spec)
  </read_first>
  <action>
    Run `./gradlew test 2>&1 | tail -80 > .planning/phases/16-emergent-behavior-tests/16-gradle-test-output.txt` from the repo root. Capture exit status in a variable. If the test suite fails (exit != 0), STOP — R19 regression must be resolved before 16-EMERGENCE.md writeup can be finalized. Surface the failing test class to the user with a checkpoint (see Task 2 fallback).

    If exit == 0, confirm `grep -c "BUILD SUCCESSFUL" 16-gradle-test-output.txt` returns `1` and `grep -E "[0-9]+ tests completed" 16-gradle-test-output.txt` shows the total. Keep this file for Task 3 reference.

    Note: the `16-gradle-test-output.txt` file is not gitignored — it's small (tail -80) and serves as a committed phase closeout receipt per R19 evidence.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && test -f .planning/phases/16-emergent-behavior-tests/16-gradle-test-output.txt && grep -q "BUILD SUCCESSFUL" .planning/phases/16-emergent-behavior-tests/16-gradle-test-output.txt && echo "R19 gate: OK"'</automated>
  </verify>
  <done>
    - 16-gradle-test-output.txt exists
    - File contains `BUILD SUCCESSFUL` (R19 gate satisfied)
    - Total test count visible in the captured output
  </done>
  <acceptance_criteria>
    - File `.planning/phases/16-emergent-behavior-tests/16-gradle-test-output.txt` exists
    - `grep -c "BUILD SUCCESSFUL" 16-gradle-test-output.txt` returns `1`
    - `grep -E "[0-9]+ tests completed" 16-gradle-test-output.txt` returns at least one line
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 runs the full `./gradlew test` suite and writes the result to `16-gradle-test-output.txt`. If the build succeeds, R19 is satisfied.

    If the build FAILS, this is a regression that must be fixed before Phase 16 closes. The failure surface will be one of:
    - A test class newly authored in this phase (16-05 or 16-07) failing its own assertion
    - An existing test broken by the Plan 16-01 / 16-02 ctor-signature changes (despite CompositeFormationTest being explicitly fixed up)
    - A flake (RESEARCH Pitfall 2 — MetabolismIntegrationTest virtual-thread leak)

    For a genuine regression: create a new plan (16-09) to fix it.
    For a flake: run the suite a second time; if it passes, note the flake in SUMMARY and proceed.
  </what-built>
  <how-to-verify>
    1. Review `.planning/phases/16-emergent-behavior-tests/16-gradle-test-output.txt` — confirm `BUILD SUCCESSFUL`.
    2. If failing — surface the failing test class name to the user so a remediation plan can be drafted.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the writeup; type "fix first" if remediation is required before closing.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author 16-EMERGENCE.md narrative writeup</name>
  <files>.planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md</files>
  <read_first>
    - .planning/phases/16-emergent-behavior-tests/16-05-SUMMARY.md (R15 counter values + master seed)
    - .planning/phases/16-emergent-behavior-tests/16-07-SUMMARY.md (R16/R17/R18 counter values, TriggerWatcher results, oscillation amplitudes, tick-work timings, heap delta)
    - Most recent `.planning/phases/16-emergent-behavior-tests/fixtures/run-*.json` — for per-tick series data
    - .planning/phases/16-emergent-behavior-tests/16-gradle-test-output.txt (R19 gate receipt)
    - .planning/phases/15-protocol-transport-overhaul/15-10-SUMMARY.md (frontmatter reference)
    - .planning/phases/16-emergent-behavior-tests/16-CONTEXT.md D-04 (5 signals), D-06 (narrative form — no charts)
  </read_first>
  <action>
    Create `.planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` with this structure (exact section headings required):

    ```markdown
    ---
    phase: 16-emergent-behavior-tests
    plan: 08
    subsystem: emergence
    tags: [r17, emergence, narrative, seeded-stats]
    dependency_graph:
      requires: [16-05, 16-07]
      provides: [phase-16-closure, m3-closeout-pending-m5-uat]
      affects: [M3 v2.0 milestone]
    closes: [R17, R19]
    ---

    # Phase 16 — Emergent Behavior Evidence

    R17 narrative evidence per CONTEXT D-06. Documents the five emergent signals
    observed during the seeded long-run `EmergenceStabilityLoadTest` (1000 ticks,
    100 bots, grid 64×64, master seed 42).

    Cross-references earlier plan closeouts:
    - **R15** closed by Plan 16-05 (`CompositeFormationDeterminismTest`) — cited below.
    - **R16** closed by Plan 16-06 scaffold + Plan 16-07 D-07 stability assertions — cited below.
    - **R18** closed by Plan 16-07 D-11 load-stability table — cited below.

    This plan's own closeouts are **R17** (narrative evidence, below) and **R19**
    (full-suite regression gate, recorded at the foot of the document).

    ## Run Configuration

    | Parameter | Value |
    |-----------|-------|
    | Master seed | <VALUE from 16-07 SUMMARY> |
    | Grid | 64 × 64 toroidal |
    | Bots | 100 seeded (Random(master ^ botIndex)) |
    | Tick interval | 20 ms |
    | Total ticks | 1000 |
    | Warmup | 200 ticks |
    | Wall-clock | <VALUE from 16-07 SUMMARY> |
    | Forced-formation knobs | bond-energy-threshold=15, bonding-probability=0.5, nutrient-spawn=0.01, dissolution-chance=0.005 |
    | Fixture dump | `.planning/phases/16-emergent-behavior-tests/fixtures/run-<ts>.json` |

    ## Signal 1 — Bonded-Pair Formation

    **Assertion:** bonded-pair counter > 0 by end of run (D-04 #1, hard).

    **Observed:** `paralife.emergence.bonded.pairs.formed = <VALUE from 16-07 SUMMARY>`

    **Interpretation:** <1–2 paragraph prose. Describe the spatial/temporal pattern
    observed (e.g., "bonds form most heavily in the first 200 ticks as nutrient
    density is high and starvation is absent; the rate stabilises around tick
    400 once dissolution + combat recycle bond-eligible prey"). Cite what the
    fixture JSON per-tick series shows. Use the master seed to mark the run
    reproducible.>

    ## Signal 2 — Composite Formation

    **Assertion:** composite counter > 0 (D-04 #2, soft — WARN if zero, not FAIL).

    **Observed:** `paralife.emergence.composites.formed = <VALUE>`

    **Interpretation:** <If > 0: prose describing when composites emerged —
    typically after a bond-rich early phase when adjacent BondedPairs align.
    If 0: note the soft-warning, note that CONTEXT D-12 allows this outcome,
    and document the knob-tuning recommendation for a future run.>

    ## Signal 3 — Predator Pressure on STARVING Prey

    **Assertion (D-04 #3, trigger-watcher):** mean predator density within radius 5
    of a triggered STARVING-prey position, over a 30-tick window, exceeds the
    baseline density of predators across the grid.

    **Observed:**
    - Windows triggered / closed: <VALUE>
    - Observed mean density: <VALUE>
    - Baseline density: <VALUE>
    - Ratio (observed / baseline): <VALUE>

    **Interpretation:** <Prose describing whether STARVING prey actually attract
    their predators — i.e., does weakness emit a scent? A ratio noticeably above
    1.0 supports the emergent-predation hypothesis; ratio close to 1.0 suggests
    the heuristic brain's flag-weighting (see HeuristicBrain Phase 15 decide()
    cascade) doesn't strongly prioritise STARVING prey. Cite what this tells us
    about the bot cascade's working assumptions.>

    ## Signal 4 — RPS Boom-Bust Cycle

    **Assertion (D-04 #4, weak):** at least one of the 3 types shows ≥2 direction
    changes in its population delta across the run (rules out frozen equilibrium).

    **Observed (oscillation amplitudes over 200-tick rolling windows):**
    - CATALYST: best (max-min)/mean = <VALUE>
    - MEMBRANE: best (max-min)/mean = <VALUE>
    - SPORE: best (max-min)/mean = <VALUE>

    **Interpretation:** <Prose. Describe the observed periodicity — if visible.
    Note whether the cycles look like classical Lotka-Volterra sine waves, more
    chaotic, or damped. Cross-reference what the fixture JSON's per-tick series
    shows. A run where CATALYST collapses first then recovers is the most
    textbook-perfect RPS signature — note if it was observed.>

    ## Signal 5 — Flee from Strong Predator

    **Assertion (D-04 #5, trigger-watcher):** weaker-type-prey density within
    radius 5 of a buffed or bonded predator DECLINES over the 30-tick window.
    Majority (>50%) of closed windows must show decline.

    **Observed:**
    - Total closed windows: <VALUE>
    - Declining windows: <VALUE> (<PCT>%)

    **Interpretation:** <Prose. If prey densities decline, the HeuristicBrain's
    "flee from predator" priority is being exercised emergently — the bot's flee
    cascade converts buff/bond awareness into spatial avoidance. If it stays
    steady, the bots may be sensing but not fleeing (perception works, action
    doesn't). Cross-check against the D-11 tick-work budget — if mean tick time
    was near budget, bots may be starving for decision latency.>

    ## Stability (R16, D-07) — closed by Plan 16-07 assertions, cited here

    | Assertion | Threshold | Observed |
    |-----------|-----------|----------|
    | No extinction | all 3 types > 0 at every sample | <OBSERVED> |
    | Per-type floor coverage | each type ≥5% for ≥80% of ticks | <OBSERVED per type> |
    | Oscillation amplitude | ≥0.15 over 200-tick window for at least one type | <BEST type + value> |

    ## Load Stability (R18, D-11) — closed by Plan 16-07 assertions, cited here

    | Metric | Assertion | Observed |
    |--------|-----------|----------|
    | Tick drift | <10% | <VALUE> |
    | Mean tick-work | ≤50% of 20ms | <VALUE> ms |
    | p99 tick-work | ≤90% of 20ms | <VALUE> ms |
    | Session dropouts (D-11 #4: min of sampled history post-warmup) | ==100 | <VALUE> |
    | Heap delta (last200 vs first200) | <20% | <VALUE>% |
    | ERROR log entries | 0 | <VALUE> |
    | Active-session end-state (D-11 #7: point-in-time at end-of-run) | ==100 | <VALUE> |

    ## Regression Gate (R19, D-03)

    Full test suite outcome — captured in `16-gradle-test-output.txt`:

    ```
    <paste last 20 lines of 16-gradle-test-output.txt — specifically the
    "tests completed" / "BUILD SUCCESSFUL" block>
    ```

    ## Deferred to M5

    Per CONTEXT D-13: subjective human-observer UAT ("is this pleasing to watch")
    is deferred to the M5 Observability & Operations visualiser phase. R17 narrative
    evidence (this document) is the JUnit-backed substitute for now.

    ## Reproducibility

    To reproduce the run whose values are cited above:

    ```bash
    # Engine-direct R15 determinism
    ./gradlew test --tests 'com.paralife.engine.CompositeFormationDeterminismTest'

    # Full-stack R16/R17/R18
    ./gradlew test --tests 'com.paralife.engine.EmergenceStabilityLoadTest'
    ```

    Master seed `42` is hardcoded in both test files' `@TestPropertySource`.
    Override via `-Dparalife.seed.master=<N>` on the JVM command line for
    forensic variation.
    ```

    **Instructions for filling in `<VALUE>` placeholders:**
    1. Read 16-05-SUMMARY.md and 16-07-SUMMARY.md — both plans' summaries were instructed to record exact observed values.
    2. Open the most recent `.planning/phases/16-emergent-behavior-tests/fixtures/run-*.json` and transcribe the `counters` block into Signals 1-2 and the closing D-11 table.
    3. Paste the last 20 lines of `16-gradle-test-output.txt` verbatim into the "Regression Gate" code block.
    4. For interpretation paragraphs — write prose. Be specific about what the numbers show. Do NOT invent numbers; if a value is `0` or `NaN`, say so and explain.

    **Keep the file under 450 lines total.** If it grows larger, tighten the interpretation prose.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && test -f .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md && grep -q "Signal 1 — Bonded-Pair Formation" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md && grep -q "Signal 2 — Composite Formation" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md && grep -q "Signal 3 — Predator Pressure" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md && grep -q "Signal 4 — RPS Boom-Bust" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md && grep -q "Signal 5 — Flee from Strong Predator" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md && grep -q "Regression Gate" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md && grep -q "Master seed" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md && echo "16-EMERGENCE.md structural check: OK"'</automated>
  </verify>
  <done>
    - 16-EMERGENCE.md exists
    - All 5 signal sections present
    - Stability + Load Stability tables filled with observed values
    - Regression Gate section quotes the gradle test output
    - Master seed explicitly logged for reproducibility
    - Document under 450 lines
    - Frontmatter `closes: [R17, R19]` (per-plan attribution; R15/R16/R18 closed by their own plans)
  </done>
  <acceptance_criteria>
    - File exists at `.planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md`
    - YAML frontmatter has `phase`, `plan: 08`, `closes: [R17, R19]` (narrowed per checker INFO 4)
    - `grep -c "^## Signal [1-5]" .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` returns `5`
    - `grep -c "^## " .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` returns `>=9` (Run Config, 5 signals, Stability, Load Stability, Regression Gate, Deferred, Reproducibility)
    - Load Stability table distinguishes D-11 #4 (min-of-history) from D-11 #7 (end-state) as distinct rows
    - No `<VALUE>` placeholder tokens remain — all replaced with concrete numbers from SUMMARY files / fixture JSON / gradle output
    - `wc -l .planning/phases/16-emergent-behavior-tests/16-EMERGENCE.md` returns `<= 450`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew test` full suite green (captured in 16-gradle-test-output.txt; R19 satisfied)
- 16-EMERGENCE.md exists with all required sections
- All observed values traceable to 16-05-SUMMARY / 16-07-SUMMARY / fixture JSON
- Checkpoint (Task 2) reached and approved by human reviewer
- Frontmatter `closes` field attributes only R17 + R19 to this plan (per-plan attribution); earlier plans (16-05, 16-06, 16-07) remain the definitive closeout for R15, R16, R18 respectively
</verification>

<success_criteria>
- R17 narrative evidence committed per D-06 (16-EMERGENCE.md)
- R19 regression gate recorded (16-gradle-test-output.txt + full-suite summary in doc)
- Phase 16 closes — all 5 requirements satisfied (R15 via 16-05, R16 via 16-06+16-07, R17 via this plan, R18 via 16-07, R19 via this plan)
- v2.0 milestone ready for close (pending M5 subjective UAT per D-13)
- Master seed documented for forensic reproducibility
</success_criteria>

<output>
After completion, create `.planning/phases/16-emergent-behavior-tests/16-08-SUMMARY.md` + update `.planning/ROADMAP.md` Phase 16 status to `✅ Complete` + update `.planning/STATE.md` to reflect phase 16 complete.
</output>
