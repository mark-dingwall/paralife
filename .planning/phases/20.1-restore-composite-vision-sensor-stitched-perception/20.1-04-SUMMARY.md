## Task-0 V-action source note

V-action source: **(B) fixed-frame-injected** — evidence:

- `GoldenTraceWithActionsTest.buildDeterministicAction` (lines 447–456): the `'V'` case returns `new Frame.ActionFrame('V', Optional.of(numpadBallot(seed)))` directly, where `seed = entityId.hashCode() ^ tick`.
- `numpadBallot(seed)` (lines 466–472) computes a deterministic 3-char numpad ballot from the hash. No HeuristicBrain, no `.decide(`, no brain call anywhere in the V-action path.
- `runTicks()` (lines 410–440) calls `actionResolver.queueAction(sessionId, buildDeterministicAction(verb, entry.entityId(), t))` on each live entity each tick — same pattern for R as for V.

**Consequence for Task 2:** Plan 03's `rankDirections` change has **zero effect** on this baseline. The with-actions baseline shifts only from D-01/D-04 frame reshape + D-02 REPRODUCER auto-place. If Task 2's independent re-run still mismatches, suspect frame shape or REPRODUCER auto-place, NOT V voting.

## Task-1 attemptPlacementForTest finding

`attemptPlacementForTest` (WorldWebSocketHandler.java lines 271–287) retries up to `LOST_RACE_MAX_RETRIES = 3` times (line 104, line 276). Each attempt samples an unoccupied position from `eligibleCellIndex.sample(spawnRng)` and calls `worldGrid.trySetEntity(...)`. If `trySetEntity` returns false (race — cell just became occupied), it increments the lost-race counter and retries. If all 3 attempts fail, it returns `Optional.empty()` and the test assertion at the call site fires.

**Consequence for Task 1:** The 26-bot loop can be left UNMODIFIED. `attemptPlacementForTest` will naturally avoid the 2 composite-seeded cells (each retry uses a fresh `eligibleCellIndex.sample`). A manual `if(occupied) continue;` skip guard is NOT needed and would silently drop a bot (26→25) if the sampled position happened to match a seeder cell that was not yet marked occupied in the index.
