# Phase 20.1: Restore Composite Vision (SENSOR-stitched perception) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-11
**Phase:** 20.1-restore-composite-vision-sensor-stitched-perception
**Areas discussed:** Stitch consumers, Stitched-cell fidelity, OVERCROWDED handling, Golden-trace strategy, Per-role vision model, REPRODUCER mechanics, Brain-consumption scope

---

## Stitch consumers / per-role vision

Initial question: who receives the stitched union (LOCOMOTOR only vs LOCOMOTOR + AUTHORITY_LITE). User redirected — wanted to re-specify vision per role from scratch, suspecting the P15 authority-tier model was wrong. Went role by role.

| Role | Options offered | Selected |
|------|-----------------|----------|
| FEEDER | union / own 3×3 / own 5×5 / passive | "8 spaces immediately surrounding it — just enough to know when adjacent to food" (= radius-1) |
| ATTACKER | union / own 3×3 / own 5×5 / passive | "8 spaces immediately surrounding — adjacent to a target it can attack" (= radius-1) |
| REPRODUCER | union / own 3×3 / own 5×5 / passive | Minimal / passive (then refined — see below) |
| DEFENDER | union / own 3×3 / own 5×5 / passive (current) | Minimal / passive (current) |
| SENSOR (receive) | passive / own 5×5 / full union | Minimal / passive (roadmap-locked) |
| SENSOR (contribute radius) | 5×5 / 7×7 / you decide | 5×5 (radius-2) |
| LOCOMOTOR | (corrected mid-flow) | own 8-cell ∪ stitched union — **"a foot is not an eye"**; no own 5×5 |

**Notes:** User explicitly corrected the LOCOMOTOR after the SENSOR questions — it gets only the same 8-cell adjacency as FEEDER/ATTACKER plus the SENSOR union; the wide field comes only from SENSORs. This replaces the P15 FULL/AUTHORITY_LITE/PASSIVE tier model with a sensory-organ model and supersedes roadmap criterion #2.

## Stitched-cell fidelity

| Option | Description | Selected |
|--------|-------------|----------|
| Full bitmask | kind + entityStatus + cellStatus identical to own cells | ✓ |
| Presence+kind only | drop env/entity status bits on extended ring | |
| You decide | | |

**User's choice:** Full bitmask (later narrowed by the OVERCROWDED decision).

## REPRODUCER mechanics

User paused to reconsider REPRODUCER: "does it need vision to decide when to reproduce, or just wait until the composite has healthy energy?" Verified mechanics: timing is server-gated on the shared pool; only bud *placement direction* is bot-chosen.

| Option | Description | Selected |
|--------|-------------|----------|
| Passive — blind bud | bud in fixed/voted direction, server gates pool + empty-cell; fails if occupied | |
| 8-cell vision | aim bud at known-empty neighbour | |
| Server auto-place | drop bot direction; server scans nearest free cell | ✓ |

**User's choice:** Server auto-place. **Notes:** "REPRODUCERs should live within the composite behind DEFENDERs and still fulfil their role. Placement = nearest available free space (Euclidean dist)." → action contract change; nearest-free-cell from REPRODUCER's own cell, torus-wrapped.

## OVERCROWDED handling

First framed as "whose Moore count sets the bit on SENSOR-contributed cells." User flagged possible conflation; asked whether overcrowding affects composites at all. Verified: CompositeMembers are exempt from the overcrowding penalty (`SimulationEngine` ≈:1064) and budding doesn't check the flag — so it's informational-only for composites.

| Option | Description | Selected |
|--------|-------------|----------|
| SENSOR computes it | contributing SENSOR sets bit from its own count | |
| Omit (bit 0 = 0) | don't compute for contributed cells | (first pass) |
| True global flag | pass Cell.flags through | |
| **Generalize:** omit for ALL composite members | OVERCROWDED becomes solo-only | ✓ |

**User's choice:** Omit entirely — and generalized to all composite members. **Notes:** User clarified LOCOMOTORs shouldn't compute it at all — "seeing cells as overcrowded is a second-order source of information derived from visible entities, which LOCOMOTORs already have." Solo Particle/BondedPair wire unchanged.

## Golden-trace strategy

| Option | Description | Selected |
|--------|-------------|----------|
| New dedicated scenario | separate composite-with-SENSOR scenario, own digest | ✓ |
| Extend existing scenario | re-pin all digests | |

**User's choice:** New dedicated scenario. **Notes:** Claude flagged existing composite-bearing baselines will also shift (LOCOMOTOR frame changed) and must be re-pinned in the same plan.

## Brain-consumption scope

| Option | Description | Selected |
|--------|-------------|----------|
| Server-side only | wire + tests; brain unchanged | |
| Include brain consumption | update HeuristicBrain to act on SENSOR cells + behavioural test | ✓ |

**User's choice:** Include brain consumption.

## Lite-FOV confirmation

| Option | Description | Selected |
|--------|-------------|----------|
| Confirmed — 8-cell only | union is LOCOMOTOR-exclusive | ✓ |
| Give them the union too | | |

**User's choice:** Confirmed 8-cell only — deliberate deviation from the goal's "AUTHORITY_LITE tactical decisions" prose.

---

## Claude's Discretion

- Union ordering algorithm (deterministic, pinned by golden test).
- Relative-coord clamp/assert for the non-practical >±63 case.
- REPRODUCER nearest-free-cell tie-break + search bound.

## Deferred Ideas

- Genetic/learning brains exploiting the richer FOV — future intelligence phase.
- Giving FEEDER/ATTACKER the stitched union — reopenable later if emergent play warrants.
