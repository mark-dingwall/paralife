---
phase: 20.1-restore-composite-vision-sensor-stitched-perception
plan: 04
type: execute
wave: 3
depends_on: ["20.1-01", "20.1-02", "20.1-03"]
files_modified:
  - src/test/java/com/paralife/engine/GoldenTraceEquivalenceTest.java
  - src/test/resources/golden-trace-phase19.json
  - src/test/resources/golden-trace-with-actions-phase19.json
autonomous: true
requirements: []
tags: [golden-trace, rebaseline, determinism, equivalence, sensor]

must_haves:
  truths:
    - "GoldenTraceEquivalenceTest contains a new dedicated scenario that places a formed composite (>= 1 SENSOR + 1 LOCOMOTOR) directly on the grid and captures its SENSOR-stitched LOCOMOTOR frames."
    - "seedCompositeWithSensorAndLocomotor places its members and nutrient on KNOWN-EMPTY coordinates reserved away from the 26 randomly-seeded bots, and ASSERTS placement success (trySetEntity / registration returned true) — a collision fails loudly, it does not silently perturb emergence."
    - "A bot session is registered for the SENSOR member too, so the SENSOR's minimal-frame digest (absence of s/p/r/e blocks at sensorRadius=0) is captured under the equivalence gate — not only the LOCOMOTOR's frame."
    - "Both golden-trace baselines (golden-trace-phase19.json and golden-trace-with-actions-phase19.json) are regenerated and re-pinned to reflect the D-01/D-04 frame-shape change and the D-02 REPRODUCER passive/auto-place change."
    - "Each golden test passes its OWN dual-run digest equivalence after re-baselining (mapA==mapB on the pinning run) — explicitly including GoldenTraceWithActionsTest, not just GoldenTraceEquivalenceTest."
  artifacts:
    - path: "src/test/java/com/paralife/engine/GoldenTraceEquivalenceTest.java"
      provides: "New SENSOR-stitched composite scenario (reserved coords + asserted placement + SENSOR session capture) + re-pinned equivalence assertions"
      contains: "seedCompositeWithSensorAndLocomotor"
    - path: "src/test/resources/golden-trace-phase19.json"
      provides: "Re-pinned no-action equivalence baseline"
    - path: "src/test/resources/golden-trace-with-actions-phase19.json"
      provides: "Re-pinned with-actions baseline (REPRODUCER auto-place + LOCOMOTOR V voting shifts)"
  key_links:
    - from: "GoldenTraceEquivalenceTest.driveScenario"
      to: "seedCompositeWithSensorAndLocomotor"
      via: "direct CompositeMember placement on reserved coords + LOCOMOTOR & SENSOR bot session registration"
      pattern: "seedCompositeWithSensorAndLocomotor"
    - from: "re-baseline procedure"
      to: "both golden-trace JSON resources"
      via: "delete → run (writes baseline) → re-run (passes) → confirm own dual-run mapA==mapB"
      pattern: "golden-trace.*\\.json"
---

<objective>
Implement D-07: re-baseline the golden traces after all Phase 20.1 frame-shape and action changes have landed. Add a new dedicated SENSOR-stitched scenario to GoldenTraceEquivalenceTest, then regenerate and re-pin BOTH golden-trace baselines.

Purpose: The D-10 byte-for-byte equivalence gate hashes the wire. The D-01 LOCOMOTOR frame reshape, D-04 OVERCROWDED omission, D-02 REPRODUCER passive→minimal + auto-place, and D-06 LOCOMOTOR V voting all shift composite-bearing digests. This plan intentionally re-pins those digests and adds a SENSOR-specific scenario so the new perception is captured under the gate. It MUST run last — re-baselining before the frame changes settle would churn the digests mid-phase.

Output: Extended GoldenTraceEquivalenceTest, regenerated golden-trace-phase19.json AND golden-trace-with-actions-phase19.json. The with-actions baseline also shifts because GoldenTraceWithActionsTest forms composites (bonding-probability=1.0) and drives R (REPRODUCER) and V (LOCOMOTOR) actions — both affected by this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-RESEARCH.md
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-PATTERNS.md
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-01-SUMMARY.md
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-02-SUMMARY.md
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-03-SUMMARY.md

<interfaces>
<!-- Extracted from codebase. Use directly — no exploration needed. -->

GoldenTraceEquivalenceTest mechanism (src/test/java/com/paralife/engine/GoldenTraceEquivalenceTest.java):
  - line 67: @TestPropertySource paralife.bonding.bonding-probability=1.0
  - driveScenario() (line 257): seeds 2 adjacent bonding pairs (lines 261/273 seedAdjacentBondingPair) + 26 more bots, drives 200 ticks, accumulates SHA-256 per-session digests
  - dual-run: runs driveScenario twice; asserts mapA == mapB (byte-identical) AND formationsA == formationsB
  - loadExpectedDigests() / writeBaseline(mapA): if golden-trace-phase19.json absent → writeBaseline + fail("BASELINE_PINNED"); else assertThat(mapA).isEqualTo(expected)
  - resetAll() (lines 205-247): at line 242 calls tickBroadcaster.clearStateForTest() + actionResolver.clearStateForTest() — extend if Plan 01 added new TickBroadcaster state
  - seedAdjacentBondingPair (lines 316-333): Particle.spawn + trySetEntity + liveEntityRegistry.register + eligibleCellIndex.notifyChanged — model for the new SENSOR seeder. NOTE: trySetEntity RETURNS A BOOLEAN — the new seeder must ASSERT it returned true (placement success) on each member/nutrient.
  - the 26-bot random seeding (within driveScenario) uses a SEEDED RNG — its occupied coordinates are deterministic; reserve a contiguous block the random seeder cannot reach (or seed the composite FIRST and have the random loop skip already-occupied cells / assert non-collision) so the composite's reserved coords are known-empty.
  - per-bot session registration block (lines 294-303): mock WebSocketSession (isOpen=true, getId), sessionRegistry.register, outboundSender.attachSession, botRegistry.register, liveEntityRegistry.register, registeredSessionIds.add — apply this block to BOTH the LOCOMOTOR and the SENSOR member.

GoldenTraceWithActionsTest (src/test/java/com/paralife/engine/GoldenTraceWithActionsTest.java):
  - SAME re-baseline mechanism (loadExpectedDigests/writeBaseline/BASELINE_PINNED at lines 191-199)
  - RESOURCE_PATH = /golden-trace-with-actions-phase19.json (line 80), SOURCE_PATH line 82
  - bonding-probability=1.0 (line 72) → composites form with REPRODUCER members; drives R and V actions
  - dual-run guard: it runs its scenario twice and asserts its own mapA==mapB before comparing to baseline (same shape as the equivalence test) — this OWN dual-run equality MUST pass on the pinning run, not just the equivalence test's.
  - This test is NOT edited (no new scenario) — but its BASELINE JSON must be regenerated because P20.1 shifts its composite frames.

CompositeMember constructor (src/main/java/com/paralife/world/Entity.java:320):
  new CompositeMember(String id, String compositeId, ParticleType type, Role role, int energy, int maxEnergy)

CompositeRegistry.register(compositeId, List<memberIds>, Map<memberId,Position>, poolEnergy, poolMax) — see CompositePerceptionTest line 92-104 usage.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SENSOR-stitched composite scenario to GoldenTraceEquivalenceTest (reserved coords, asserted placement, SENSOR session)</name>
  <files>src/test/java/com/paralife/engine/GoldenTraceEquivalenceTest.java</files>
  <read_first>
    - src/test/java/com/paralife/engine/GoldenTraceEquivalenceTest.java FULL (driveScenario 257+, seedAdjacentBondingPair 316-333 incl. the trySetEntity boolean return, per-bot registration 285-303, resetAll 205-247, re-baseline 166-175, the 26-bot random-seed loop)
    - src/test/java/com/paralife/websocket/CompositePerceptionTest.java lines 92-104 (direct CompositeMember placement + compositeRegistry.register pattern)
    - 20.1-RESEARCH.md Q5 (which scenarios shift, new SENSOR scenario, re-baseline procedure) and §"Open Questions (RESOLVED)" #2 (RESOLVED: direct-placement) and Pitfall 2 / Pitfall 5
    - 20.1-PATTERNS.md §GoldenTraceEquivalenceTest (seedCompositeWithSensorAndLocomotor pattern, resetAll extension, re-baseline)
    - 20.1-01-SUMMARY.md (confirm whether Plan 01 added any TickBroadcaster persistent state needing a clearStateForTest extension)
  </read_first>
  <action>
    Add a private seedCompositeWithSensorAndLocomotor(...) helper modeled on seedAdjacentBondingPair, but placing CompositeMember entities directly (not Particle.spawn): a LOCOMOTOR member and >= 1 SENSOR member positioned within radius-2 of the LOCOMOTOR.
    RESERVE KNOWN-EMPTY COORDS: choose a contiguous coordinate block that the deterministic 26-bot random seeder does not occupy — either by seeding the composite BEFORE the random-bot loop and having that loop skip occupied cells (and asserting the composite block is collision-free), or by hardcoding a region the seeded RNG provably never reaches. Document the reserved region in a comment.
    ASSERT PLACEMENT SUCCESS: for each member and the nutrient, assert the placement call (trySetEntity / grid set) returned true — a collision must fail the test loudly, never silently perturb emergence.
    Register via compositeRegistry.register(compositeId, memberIds, positionMap, poolEnergy, poolMax), liveEntityRegistry.register, and eligibleCellIndex.notifyChanged for each cell. Place a nutrient (or distinct kind) inside the SENSOR's 5x5 but outside the LOCOMOTOR's radius-1 so the stitched union is non-trivially captured.
    REGISTER A BOT SESSION FOR BOTH the LOCOMOTOR member AND the SENSOR member following the per-bot block (lines 294-303) — so the LOCOMOTOR's SENSOR-stitched frame AND the SENSOR's minimal-form (sensorRadius=0, no s/p/r/e blocks) digest are both captured under the equivalence gate.
    Invoke this seeder inside driveScenario (alongside the existing bonding pairs) so the new scenario's frames flow into the digest capture. If 20.1-01-SUMMARY reports new TickBroadcaster persistent state, extend resetAll()/clearStateForTest accordingly; otherwise leave resetAll unchanged. Do NOT yet regenerate the baseline JSON in this task — only the test code changes here.
  </action>
  <verify>
    <automated>./gradlew compileTestJava 2>&1 | tail -5; ./gradlew test --tests "com.paralife.engine.GoldenTraceEquivalenceTest" 2>&1 | tee /tmp/20.1-04-t1.log | tail -30; grep -Eq "BASELINE_PINNED|expected:|but was:|digest" /tmp/20.1-04-t1.log && ! grep -Eq "NullPointerException|placement|trySetEntity|at com.paralife.engine.GoldenTraceEquivalenceTest.seedCompositeWithSensorAndLocomotor" /tmp/20.1-04-t1.log && echo SEEDER_REACHED_BASELINE_FORK</automated>
  </verify>
  <acceptance_criteria>
    - GoldenTraceEquivalenceTest.java contains `seedCompositeWithSensorAndLocomotor` and it is invoked from driveScenario (grep -c >= 2).
    - The seeder ASSERTS placement success: `grep -nA60 "seedCompositeWithSensorAndLocomotor" src/test/java/com/paralife/engine/GoldenTraceEquivalenceTest.java | grep -Ec "assert.*[Tt]rue|isTrue\\(\\)|trySetEntity"` >= 1 — the placement boolean is asserted, not discarded.
    - The seeder registers a bot session for BOTH members: the per-bot registration block (sessionRegistry.register / botRegistry.register / outboundSender.attachSession) is applied to the SENSOR member as well as the LOCOMOTOR (grep the seeder region shows two registration invocations, one per member id).
    - compileTestJava exits 0.
    - The new seeder uses direct CompositeMember placement + compositeRegistry.register (not Particle.spawn).
    - Runtime reaches the re-baseline fork, not an early crash: the verify run prints `SEEDER_REACHED_BASELINE_FORK` — the test executed `seedCompositeWithSensorAndLocomotor` and reached the digest-comparison / BASELINE_PINNED path WITHOUT throwing (no NPE, no placement-assertion failure) from inside the seeder. Because the JSON baseline still exists at this task (deletion is Task 2), the expected run outcome is a digest mismatch (`expected:`/`but was:`) or `BASELINE_PINNED`; either is fine.
    - Test will FAIL on run with BASELINE_PINNED or digest mismatch — that is expected and handled in Task 2.
  </acceptance_criteria>
  <done>New SENSOR scenario wired into driveScenario on reserved/asserted coords with BOTH LOCOMOTOR and SENSOR sessions registered; test compiles; the seeder runs to the baseline fork without crashing. Baseline regeneration is Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and re-pin BOTH golden-trace baselines (each gated on its own dual-run equality)</name>
  <files>src/test/resources/golden-trace-phase19.json, src/test/resources/golden-trace-with-actions-phase19.json</files>
  <read_first>
    - 20.1-RESEARCH.md Q5 §"Re-baseline procedure" (delete → run writes baseline + BASELINE_PINNED fail → re-run passes) and Pitfall 2 (verify dual-run equivalence before accepting)
    - src/test/java/com/paralife/engine/GoldenTraceEquivalenceTest.java lines 166-175 (writeBaseline/loadExpectedDigests path) and its dual-run mapA==mapB guard
    - src/test/java/com/paralife/engine/GoldenTraceWithActionsTest.java lines 191-199 (same mechanism, with-actions resource) and its OWN dual-run mapA==mapB guard
  </read_first>
  <action>
    Re-baseline BOTH golden traces using the test-driven write procedure (do NOT hand-edit the JSON).
    For golden-trace-phase19.json: delete src/test/resources/golden-trace-phase19.json, run `./gradlew test --tests "com.paralife.engine.GoldenTraceEquivalenceTest"` (expected fail: BASELINE_PINNED — the test writes the new baseline), then re-run the same command (expected pass).
    For golden-trace-with-actions-phase19.json: delete it, run `./gradlew test --tests "com.paralife.engine.GoldenTraceWithActionsTest"` (expected fail: BASELINE_PINNED — writes baseline), then re-run (expected pass).
    For EACH test, the passing re-run must satisfy that test's OWN dual-run guard (mapA==mapB / formationsA==formationsB) — i.e. GoldenTraceWithActionsTest must pass ITS OWN mapA==mapB on the pinning run, exactly as GoldenTraceEquivalenceTest does; do not treat the with-actions baseline as accepted merely because the equivalence test is deterministic. If the second run still fails with a digest mismatch (not BASELINE_PINNED), or either test's own dual-run mapA==mapB fails, STOP: that indicates non-determinism introduced by Plans 01-03 (e.g. an unsorted union, a signed-shift dedup-key collision, or a per-run state leak), which must be fixed in the originating plan, not papered over by re-pinning. Both regenerated JSON files are committed as the new pinned baselines.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.paralife.engine.GoldenTraceEquivalenceTest" --tests "com.paralife.engine.GoldenTraceWithActionsTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both golden-trace-phase19.json and golden-trace-with-actions-phase19.json exist and differ from their pre-phase content: `git status --short src/test/resources/golden-trace-phase19.json src/test/resources/golden-trace-with-actions-phase19.json` shows both modified.
    - GoldenTraceEquivalenceTest passes (exit 0) — NOT BASELINE_PINNED — on a clean re-run, with its own dual-run mapA==mapB satisfied.
    - GoldenTraceWithActionsTest passes (exit 0) — NOT BASELINE_PINNED — on a clean re-run, with ITS OWN dual-run mapA==mapB satisfied (explicit gate, not inherited from the equivalence test).
    - Neither JSON was hand-edited (both produced by the test's writeBaseline path).
  </acceptance_criteria>
  <done>Both baselines regenerated via test-driven writeBaseline; each test's own dual-run equivalence holds on the pinning run; both tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite phase gate</name>
  <files>src/test/resources/golden-trace-phase19.json, src/test/resources/golden-trace-with-actions-phase19.json</files>
  <read_first>
    - 20.1-RESEARCH.md §"Validation Architecture" (phase gate: full suite green before verify-work)
    - .planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-VALIDATION.md
  </read_first>
  <action>
    Run the full test suite as the phase gate. Confirm the existing baseline suite (166+ tests) plus all four Phase 20.1 plans' new/updated tests pass together: CompositePerceptionTest, SensorStitchedPerceptionTest, VisionScopedOvercrowdingTest, ReproducerAutoPlaceTest, HeuristicBrainLocomotorTest, GoldenTraceEquivalenceTest, GoldenTraceWithActionsTest. Both re-pinned baselines from Task 2 (golden-trace-phase19.json AND golden-trace-with-actions-phase19.json) must hold under the full-suite run. If any pre-existing composite-bearing test (beyond the two golden traces) fails due to the D-01/D-04 frame reshape, identify it and update its assertions to the new expected shape (do not leave red).
  </action>
  <verify>
    <automated>./gradlew test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test` exits 0 (full suite green).
    - Both golden-trace-phase19.json and golden-trace-with-actions-phase19.json remain unchanged by this task (the Task-2 re-pin holds under full-suite): `git status --short src/test/resources/golden-trace-phase19.json src/test/resources/golden-trace-with-actions-phase19.json` shows no NEW modification introduced by Task 3 beyond Task 2's re-pin.
    - No composite-perception or golden-trace test left red.
    - If any assertion was updated, the change is limited to expected-shape updates consistent with D-01/D-04 (no behavior weakening).
  </acceptance_criteria>
  <done>Full suite green; both re-pinned baselines hold; phase ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → committed baseline | regenerated digests become the pinned equivalence gate; must be test-produced, not hand-forged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-08 | Tampering (false baseline) | golden-trace JSON regeneration | mitigate | Baselines are produced only by the test's writeBaseline path, never hand-edited; each test's OWN dual-run equivalence (mapA==mapB) must hold before its baseline is accepted, so a non-deterministic (forged-looking) digest set is rejected at Task 2. |
| T-20.1-09 | Repudiation (masked non-determinism) | re-baseline accepting a churning digest | mitigate | Task 2 explicitly halts if the second run mismatches OR either test's own dual-run mapA==mapB fails (rather than re-pins), forcing the non-determinism back to its originating plan (unsorted union / signed-shift dedup-key collision / state leak) instead of laundering it through a re-baseline. The new SENSOR scenario seeds on reserved coords with asserted placement so a collision fails loudly rather than perturbing the digest. |
</threat_model>

<verification>
- Both golden traces regenerated via test-driven writeBaseline (not hand-edited) and green on clean re-run, each satisfying its own dual-run equality.
- `./gradlew test` full suite green.
- Dual-run equivalence holds for both traces (each gated independently).
</verification>

<success_criteria>
- New SENSOR-stitched scenario captured under the equivalence gate, on reserved coords with asserted placement, with BOTH LOCOMOTOR and SENSOR sessions registered.
- golden-trace-phase19.json AND golden-trace-with-actions-phase19.json re-pinned, each passing its own dual-run mapA==mapB.
- Full suite green; no composite test left red.
</success_criteria>

<output>
Create `.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-04-SUMMARY.md` when done.
</output>
</content>
