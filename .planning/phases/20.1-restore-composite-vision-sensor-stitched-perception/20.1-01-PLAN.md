---
phase: 20.1-restore-composite-vision-sensor-stitched-perception
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/paralife/websocket/TickBroadcaster.java
  - src/test/java/com/paralife/websocket/CompositePerceptionTest.java
  - src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java
autonomous: true
requirements: []
tags: [perception, composite, sensor, tickbroadcaster, vision]

must_haves:
  truths:
    - "A LOCOMOTOR composite member's tick frame carries its own 8-cell (radius-1) adjacency PLUS a deduplicated, deterministically-ordered union of every SENSOR member's 5x5 cells, all expressed relative to the LOCOMOTOR's position."
    - "The union's cell ORDER is stable: building the same world state's LOCOMOTOR frame twice yields list-equal cells() (order + contents), not merely an equal cell COUNT."
    - "A SENSOR window straddling NEGATIVE relative coords (SENSOR north/west of the LOCOMOTOR) is stitched correctly: a cell at LOCOMOTOR-relative (dx<0, dy<0) survives dedup and appears at the asserted numeric dx/dy — a signed-shift dedup-key collision would drop or merge it and fail."
    - "Adding a SENSOR to a composite strictly widens (never shrinks) the LOCOMOTOR's visible cell set; a LOCOMOTOR in a composite with ZERO SENSORs gets at most its own 8-cell adjacency (no 5x5)."
    - "FEEDER and ATTACKER receive only their own 8-cell adjacency and do NOT receive any SENSOR-only-visible cell; REPRODUCER, DEFENDER and SENSOR receive the passive minimal form (sensorRadius=0, no s block)."
    - "The OVERCROWDED bit (cellStatus bit 0) is always 0 in every composite-member frame — including union cells contributed by a SENSOR whose neighbourhood is overcrowded; solo Particle / BondedPair frames keep the D-40 per-bot recompute unchanged."
  artifacts:
    - path: "src/main/java/com/paralife/websocket/TickBroadcaster.java"
      provides: "Per-role D-01 frame routing, buildLocomotorCells SENSOR union, D-04 OVERCROWDED omission for composite members"
      contains: "buildLocomotorCells"
    - path: "src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java"
      provides: "D-03 monotonic-widening + near-blind baseline + deterministic union ORDER-stability + negative-coord dedup-key test + FEEDER/ATTACKER no-leak test"
      contains: "class SensorStitchedPerceptionTest"
  key_links:
    - from: "TickBroadcaster.buildTickFrame"
      to: "CompositeRegistry.CompositeState.getMemberIds / getPositionForMember"
      via: "SENSOR enumeration in buildLocomotorCells"
      pattern: "getMemberIds|getPositionForMember"
    - from: "TickBroadcaster.envStateFor"
      to: "composite-member OVERCROWDED omission"
      via: "isCompositeMember branch returning cached & ~BIT_OVERCROWDED"
      pattern: "BIT_OVERCROWDED"
---

<objective>
Restore the sensory-organ perception model (D-01/D-03/D-04) inside TickBroadcaster. Replace the flat `tierOf()` authority-tier dispatch with per-role frame routing: LOCOMOTOR gets its own 8-cell adjacency stitched with the union of all SENSOR members' 5x5 windows; FEEDER/ATTACKER get 8-cell adjacency only; REPRODUCER/DEFENDER/SENSOR are passive. Omit OVERCROWDED from all composite-member frames.

Purpose: SENSOR members must produce tangible composite value (extended FOV feeding LOCOMOTOR voting) instead of acting passively. This is the foundational wire-shape change the rest of the phase builds on.

Output: Modified TickBroadcaster, updated CompositePerceptionTest, new SensorStitchedPerceptionTest. No wire/codec protocol change (MAX_S_ENTRIES=256, relative-coord ±63 are sufficient).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-RESEARCH.md
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. Use directly — no exploration needed. -->

Entity.Role enum (src/main/java/com/paralife/world/Entity.java:29):
  enum Role { LOCOMOTOR, FEEDER, ATTACKER, DEFENDER, REPRODUCER, SENSOR }

Frame.TickFrame (src/main/java/com/paralife/codec/Frame.java:70):
  record TickFrame(long tickId, int x, int y, int energy, int maxEnergy,
                   int sensorRadius, List<CellEntry> cells, ChangeCode change,
                   List<Effect> effects, List<Event> events,
                   Optional<PoolSnapshot> pool, List<RosterMember> roster)
  - sensorRadius validated 0..3 (line 88). 0 = minimal form (cells/change/effects/pool/roster MUST be empty).
  - isMinimal() returns sensorRadius == 0.
  - The constructor does NOT validate that cell entries lie within sensorRadius of self — sensorRadius is only a signal field. LOCOMOTOR sets it to 1; union cells extend the s block beyond radius 1 legally.
  - cells() is a List — list equality (order + contents) is the order-stability check.

CompositeRegistry (src/main/java/com/paralife/engine/CompositeRegistry.java):
  Optional<CompositeState> getComposite(String compositeId)
  CompositeState.getMemberIds() : List<String>  (unmodifiable view of CopyOnWriteArrayList — read-safe, no lock)
  CompositeState.getPositionForMember(String memberId) : Position (or null)

TickBroadcaster current seams (src/main/java/com/paralife/websocket/TickBroadcaster.java):
  - line 353: AuthorityTier tier = tierOf(occupant, bot.entityId());
  - line 375: if (tier == AuthorityTier.PASSIVE) { ...minimal frame... }
  - line 383: int radius = sensorRadiusFor(tier, bot.entityId());
  - line 386: List<CellEntry> cells = buildCellEntries(pos, radius);
  - line 414: enum AuthorityTier { FULL, AUTHORITY_LITE, PASSIVE }
  - lines 422-431: tierOf() — current mapping LOCOMOTOR→FULL, FEEDER/ATTACKER/REPRODUCER→AUTHORITY_LITE, SENSOR/DEFENDER→PASSIVE
  - line 458: buildCellEntries(Position botPos, int radius) — row-major emission, dy then dx
  - line 467: `if (dx == 0 && dy == 0) continue;` — VERIFIED: the center/self cell is SKIPPED, so buildCellEntries(pos, 1) emits EXACTLY the 8-cell Moore ring (no center). D-01 "8-cell adjacency" is already correct as implemented; Task 1 pins this with an explicit cardinality assertion rather than changing it.
  - line 475: int envState = envStateFor(cellPos, botPos, radius) & 0xFF;
  - lines 695-702: envStateFor — D-40 expression cellStatus = (cached & ~BIT_OVERCROWDED) | perBotOvercrowdedBit
  - lines 829-837: relativeTo(Position from, Position to) — toroidal shortest-path dx/dy
  - lines 853-894: buildRosterIfChanged — iteration skeleton to model SENSOR enumeration on (getMemberIds, getPositionForMember). VERIFIED: it clamps to ±63 SILENTLY (lines 877-878: `Math.max(-63, Math.min(63, ...))`), it does NOT emit log.warn. buildLocomotorCells must clamp the same silent way (no per-cell log) — a 5x5 SENSOR window could emit up to 25 clamps/SENSOR/tick; logging them would be tick-drift log-spam.
  - lines 873-874: VERIFIED occupant-identity guard pattern — `Cell memberCell = worldGrid.getCell(mp.x(), mp.y()); if (!(memberCell.occupant() instanceof CompositeMember mem)) continue;`. buildLocomotorCells reuses this shape but additionally narrows to `mem.role() == Role.SENSOR`, which guards a dead/foreign occupant from being stitched (a different entity walking onto sensorPos is not a SENSOR CompositeMember of THIS composite).
  - line 184: clearStateForTest()
  - PERCEPTION_RADIUS constant = 2

Cell free-cell predicates (src/main/java/com/paralife/world/Cell.java:31-37) — VERIFIED:
  - isEmpty() returns `occupant == null`; hasOccupant() returns `occupant != null`. They are exact logical inverses. There is NO environmental-obstacle (rock/toxin) guard in either — obstacles live in `flags`, not occupant. So `!hasOccupant()` and `isEmpty()` are interchangeable for occupancy; no obstacle-bypass risk exists in this codebase. (Recorded here because Plan 02 raised the same predicate question.)

PerceptionCodec V-arg validation (src/main/java/com/paralife/codec/PerceptionCodec.java:833-843) — VERIFIED:
  - A V arg must be EXACTLY 3 chars, each in '1'..'9'. Distinctness is NOT enforced (duplicate digits are codec-legal). The hard constraint the brain must satisfy by construction (Plan 03) is "3 chars, each numpad '1'..'9'".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold — SensorStitchedPerceptionTest + CompositePerceptionTest D-01/D-04 assertions</name>
  <files>src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java, src/test/java/com/paralife/websocket/CompositePerceptionTest.java</files>
  <read_first>
    - src/test/java/com/paralife/websocket/CompositePerceptionTest.java (full — copy @BeforeEach setUp lines 69-87 and CompositeMember placement lines 92-104; UPDATE locomotorReceivesPoolAndEnergy line 137 which currently asserts sensorRadius == PERCEPTION_RADIUS; ALSO locate any existing REPRODUCER assertion — e.g. a `reproducerMemberReceives*` test asserting REPRODUCER gets AUTHORITY_LITE / sensorRadius==1 — it WILL break when REPRODUCER moves to PASSIVE and MUST be updated, not just superseded)
    - 20.1-PATTERNS.md §"SensorStitchedPerceptionTest" and §"CompositePerceptionTest (extend)" for exact assertion shapes
    - src/main/java/com/paralife/codec/Frame.java lines 70-106 (TickFrame record, isMinimal, sensorRadius validation; cells() is a List → use List equality for order-stability)
    - src/main/java/com/paralife/codec/Frame.java CellEntry + Coord.Relative constructors (to assert numeric dx/dy of a SENSOR-contributed union cell)
  </read_first>
  <behavior>
    - SensorStitchedPerceptionTest ADJACENCY-CARDINALITY (pins C1/codex-HIGH): a LOCOMOTOR with NO SENSOR members and an empty surrounding neighbourhood has frame.cells() containing EXACTLY its 8-cell Moore ring — assert no cell entry has Coord.Relative(0,0) (the center/self is excluded) and cells().size() <= 8. This pins D-01 "8-cell adjacency means the 8-ring, center excluded" against the verified line-467 skip.
    - SensorStitchedPerceptionTest NEAR-BLIND BASELINE: a LOCOMOTOR in a composite with ZERO SENSOR members gets frame.cells().size() <= 8 (radius-1 adjacency only, never a 5x5) — the explicit baseline the monotonic test widens from.
    - SensorStitchedPerceptionTest D-03-MONOTONIC: build a LOCOMOTOR-only (no-SENSOR) frame (cellCountWithoutSensor = frame.cells().size()); then add a SENSOR member within radius-2 of the LOCOMOTOR and a nutrient inside the SENSOR's 5x5 but outside the LOCOMOTOR's radius-1; assert frameWithSensor.cells().size() > frameWithoutSensor.cells().size().
    - SensorStitchedPerceptionTest NUMERIC-ORIGIN (closes the D-06 wrong-origin pitfall numerically): place a nutrient ONLY inside the SENSOR's 5x5 (not in the LOCOMOTOR's radius-1). Assert the LOCOMOTOR frame.cells() contains a Coord.Relative entry with the exact LOCOMOTOR-relative dx/dy of that nutrient (computed by hand from the LOCOMOTOR and nutrient positions, e.g. nutrient 2 north of LOCO → Coord.Relative(0,-2) or (0,2) per the codebase's dy sign convention — read relativeTo to fix the sign), proving the union re-expresses SENSOR cells relative to the LOCOMOTOR, not the SENSOR.
    - SensorStitchedPerceptionTest NEGATIVE-COORD DEDUP (HIGHEST-LEVERAGE — the only gate that breaks the signed-shift dedup-key trap): register a composite whose SENSOR is positioned NORTH-WEST of the LOCOMOTOR so its 5x5 window straddles NEGATIVE LOCOMOTOR-relative coords (dx<0 AND dy<0). Place a distinguishable cell (nutrient) at a known negative-relative coordinate inside that window. Assert that cell appears EXACTLY ONCE in frame.cells() at the asserted negative Coord.Relative(dx,dy). A dedup key of the form `(dy<<16)|dx` on signed ints sign-extends the negative dx across dy's bits and either drops this cell or collides it with another — this test fails on that bug; the order-stability test and the golden gate do NOT (deterministically-wrong output is equal across runs and freezes as "truth").
    - SensorStitchedPerceptionTest ORDER-STABILITY: with a multi-SENSOR composite (>= 2 SENSORs whose 5x5 windows overlap so dedup + sort are exercised), build the LOCOMOTOR tick frame TWICE against the identical world state and assert `frame1.cells().equals(frame2.cells())` — List.equals is order + contents, so a HashMap-vs-LinkedHashMap or unsorted-stream bug fails HERE at Wave-1.
    - SensorStitchedPerceptionTest FEEDER/ATTACKER NO-LEAK (C5 — mandatory): with a composite containing a LOCOMOTOR, a SENSOR, a FEEDER and an ATTACKER, place a nutrient visible ONLY inside the SENSOR's 5x5 (outside every member's radius-1). Assert the FEEDER frame and the ATTACKER frame do NOT contain that cell's Coord.Relative coordinate (their cells() are 8-cell adjacency only; the SENSOR union is LOCOMOTOR-exclusive per D-05).
    - SensorStitchedPerceptionTest: SENSOR-contributed cells use Coord.Relative (dx/dy relative to LOCOMOTOR), never Coord.Absolute.
    - CompositePerceptionTest D-04 (place dense neighbourhood INSIDE a SENSOR window): build a LOCOMOTOR + SENSOR composite where the SENSOR's 5x5 contains an overcrowded/dense cell that is ALSO overcrowded from the LOCOMOTOR's own perspective. Iterate frame.cells() and assert every entry — INCLUDING the SENSOR-contributed union cell — has (envState & 0x01) == 0. Placing the density inside the SENSOR window (not only around the LOCOMOTOR) is what exercises the union-call OVERCROWDED-omission path (C1/H3.1), not just the adjacency path.
    - CompositePerceptionTest D-01: LOCOMOTOR frame asserts sensorRadius == 1 (REPLACES the existing == PERCEPTION_RADIUS assertion at line 137); REPRODUCER frame asserts isMinimal() == true; any pre-existing REPRODUCER-AUTHORITY_LITE/sensorRadius==1 assertion is UPDATED to assert isMinimal()==true (REPRODUCER is now PASSIVE per D-01).
  </behavior>
  <action>
    Create SensorStitchedPerceptionTest.java copying CompositePerceptionTest's @BeforeEach verbatim (WorldGrid 16x16, mocked EnvironmentEngine returning byte 0 for getCellStatus/getEntityStatus, mocked OutboundSender.offer→true). Add: the ADJACENCY-CARDINALITY test, the NEAR-BLIND BASELINE test (cells().size() <= 8 for a zero-SENSOR LOCOMOTOR composite), the D-03-MONOTONIC test, the NUMERIC-ORIGIN test (assert the exact LOCOMOTOR-relative Coord.Relative dx/dy of a SENSOR-only nutrient — read relativeTo lines 829-837 to fix the dy sign convention before asserting), the NEGATIVE-COORD DEDUP test (SENSOR NW of LOCOMOTOR, distinguishable cell at a negative Coord.Relative, asserted present exactly once at that coord), the ORDER-STABILITY test (>= 2 overlapping SENSORs, two independent builds, `assertThat(frame1.cells()).isEqualTo(frame2.cells())`), the FEEDER/ATTACKER NO-LEAK test (SENSOR-only nutrient absent from FEEDER and ATTACKER frames), and a Coord.Relative-type assertion on SENSOR-contributed cells. In CompositePerceptionTest: UPDATE locomotorReceivesPoolAndEnergy so the LOCOMOTOR sensorRadius assertion expects 1 not PERCEPTION_RADIUS; add the D-04 OVERCROWDED-always-0 test with the dense neighbourhood placed INSIDE a SENSOR's 5x5 window so the assertion exercises a union cell; add a REPRODUCER-passive test asserting isMinimal()==true; and UPDATE (not just add alongside) any existing REPRODUCER assertion that expected AUTHORITY_LITE / sensorRadius==1 so it now expects isMinimal()==true. These tests are EXPECTED RED until Task 2 lands — they pin the D-01/D-03/D-04 + dedup-key + no-leak contracts first.
  </action>
  <verify>
    <automated>./gradlew compileTestJava 2>&1 | tail -5; ./gradlew test --tests "com.paralife.websocket.SensorStitchedPerceptionTest" --tests "com.paralife.websocket.CompositePerceptionTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SensorStitchedPerceptionTest.java compiles and contains class SensorStitchedPerceptionTest with: a monotonic-widening assertion (`isGreaterThan`); a near-blind baseline assertion (a `<= 8` or `isLessThanOrEqualTo(8)` on a zero-SENSOR LOCOMOTOR frame's cells().size()); an order-stability assertion comparing two independently-built frames' cells() via List equality (grep: `grep -E "cells\\(\\)\\)\\.isEqualTo\\(.*cells\\(\\)" src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java` matches).
    - SensorStitchedPerceptionTest contains a NEGATIVE-COORD DEDUP test: a SENSOR positioned NW of the LOCOMOTOR and an assertion that a distinguishable cell appears exactly once at a Coord.Relative with a NEGATIVE dx AND negative dy (grep for a `Coord.Relative(-` literal with a second negative arg, or an explicit comment-tagged `// NEGATIVE-COORD DEDUP` method that asserts the cell's presence-and-coord). This test must reference a SENSOR placed at negative relative coords.
    - SensorStitchedPerceptionTest contains a FEEDER/ATTACKER no-leak assertion: a SENSOR-only nutrient's Coord is asserted ABSENT from both a FEEDER frame and an ATTACKER frame (`doesNotContain` / `noneMatch` over their cells()).
    - SensorStitchedPerceptionTest contains a NUMERIC-ORIGIN assertion pinning the exact LOCOMOTOR-relative dx/dy of a SENSOR-only nutrient.
    - CompositePerceptionTest no longer asserts `sensorRadius() ... isEqualTo(TickBroadcaster.PERCEPTION_RADIUS)` for the LOCOMOTOR pool test (grep: `grep -c "isEqualTo(TickBroadcaster.PERCEPTION_RADIUS)" src/test/java/com/paralife/websocket/CompositePerceptionTest.java` returns only the SENSOR_PLUS_1 buff test count, not the plain LOCOMOTOR test), and its REPRODUCER assertion expects isMinimal()==true (no surviving AUTHORITY_LITE/sensorRadius==1 REPRODUCER assertion).
    - The D-04 OVERCROWDED test asserts bit 0 unset on a SENSOR-contributed union cell (the dense neighbourhood is placed inside a SENSOR's 5x5, not only around the LOCOMOTOR).
    - Tests compile (compileTestJava exits 0). They MAY be red at this task — Task 2 makes them green.
  </acceptance_criteria>
  <done>Wave-0 scaffolds exist, compile, and pin D-01/D-03/D-04, the deterministic union ORDER-stability contract, the negative-coord dedup-key correctness contract, the FEEDER/ATTACKER no-leak contract, and the near-blind baseline. Red is acceptable here.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TickBroadcaster per-role routing + SENSOR union + D-04 OVERCROWDED omission</name>
  <files>src/main/java/com/paralife/websocket/TickBroadcaster.java</files>
  <read_first>
    - src/main/java/com/paralife/websocket/TickBroadcaster.java lines 349-431 (buildTickFrame dispatch + tierOf + AuthorityTier enum), 458-490 (buildCellEntries row-major; line 467 self-skip), 695-702 (envStateFor D-40 expression), 829-837 (relativeTo), 853-894 (buildRosterIfChanged iteration skeleton incl. the SILENT ±63 clamp at 877-878 and the occupant-identity guard at 873-874), 184-186 (clearStateForTest)
    - 20.1-RESEARCH.md Q2 (tierOf replacement + OVERCROWDED omission), Q3 (SENSOR enumeration algorithm + APIs), Q7 (sensorRadius=1 for LOCOMOTOR, MAX_S_ENTRIES budget)
    - 20.1-PATTERNS.md §"SENSOR union construction" and §"D-04 OVERCROWDED omission — exact site"
    - CLAUDE.md §"Env state projection — three layers" (D-40 per-bot OVERCROWDED redaction is the load-bearing site; composite branch zeroes bit 0)
  </read_first>
  <behavior>
    - LOCOMOTOR composite member: frame.sensorRadius()==1; cells = own radius-1 adjacency ∪ each SENSOR member's radius-2 (PERCEPTION_RADIUS) window, deduplicated by relative (dx,dy) from the LOCOMOTOR with adjacency winning ties (putIfAbsent), sorted by (wrappedDy, wrappedDx).
    - Dedup-key correctness: the dedup map key is value-equal on the (dx,dy) PAIR with no sign corruption. Acceptable keys are (a) the `Coord.Relative` record itself (value equality, `coordFor` already produces it) or (b) an explicitly axis-masked `((dy & 0xFFFF) << 16) | (dx & 0xFFFF)`. A bare `(dy << 16) | dx` on signed ints is FORBIDDEN — negative dx sign-extends and corrupts/collides the key (the Task-1 NEGATIVE-COORD DEDUP test fails on it).
    - Union ORDER is deterministic and stable across rebuilds: dedup uses an insertion-ordered map (LinkedHashMap) and the result is explicitly sorted by (wrappedDy, wrappedDx) before emission — no HashMap or unordered stream may leak into the cell ordering.
    - D-04 union-cell coverage (C1/H3.1): the OVERCROWDED-omission flag (`isCompositeMember=true`) is threaded into BOTH buildCellEntries call sites inside buildLocomotorCells — the LOCOMOTOR's own radius-1 adjacency call AND each SENSOR's 5x5 window call. A union cell that is overcrowded from the SENSOR's perspective MUST still emit bit 0 unset.
    - FEEDER / ATTACKER composite member: frame.sensorRadius()==1; cells = own radius-1 adjacency only; NOT the SENSOR union (D-05 LOCOMOTOR-exclusive).
    - REPRODUCER / DEFENDER / SENSOR composite member: isMinimal()==true (sensorRadius=0, no s block).
    - Solo Particle / BondedPair: unchanged (sensorRadius 2, or 3 with SENSOR_PLUS_1).
    - Every composite-member cell entry has OVERCROWDED bit 0 unset; solo/bonded entities keep the D-40 perBotOvercrowdedBit recompute.
    - SENSOR-identity guard: a SENSOR member whose grid cell no longer holds that SENSOR CompositeMember (member died / a foreign entity walked in) is skipped — no vision is stitched from it.
  </behavior>
  <action>
    Change tierOf()'s mapping so REPRODUCER joins SENSOR/DEFENDER on the PASSIVE branch (D-01: only LOCOMOTOR/FEEDER/ATTACKER are active members). Add a LOCOMOTOR-specific branch in buildTickFrame after the PASSIVE check (line 375) and before the generic buildCellEntries call (line 386): when occupant is a CompositeMember with role()==LOCOMOTOR, set radius=1 and build cells via a new buildLocomotorCells(locoPos, compositeId).

    buildLocomotorCells: start with buildCellEntries(locoPos, 1, /*isCompositeMember=*/true) — the FIRST call site. For each memberId in getComposite(compositeId).get().getMemberIds(): resolve getPositionForMember; skip null; resolve `Cell c = worldGrid.getCell(sensorPos.x(), sensorPos.y());` and SKIP unless `c.occupant() instanceof CompositeMember cm && cm.role() == Role.SENSOR` (this is the occupant-identity guard from line 873-874 narrowed to SENSOR — it rejects a dead member or a foreign occupant); then build buildCellEntries(sensorPos, PERCEPTION_RADIUS, /*isCompositeMember=*/true) — the SECOND (union) call site, which MUST also pass true so SENSOR-contributed cells do not re-leak OVERCROWDED. Re-express each SENSOR entry's coord relative to locoPos by adding the relativeTo(locoPos, sensorPos) offset (wrapped); clamp dx/dy to ±63 SILENTLY exactly as buildRosterIfChanged does at lines 877-878 (`Math.max(-63, Math.min(63, ...))`) — do NOT add a per-cell log.warn (a 5x5 window can produce up to 25 clamps per SENSOR per tick; logging is tick-drift spam).

    Dedup into an insertion-ordered LinkedHashMap whose KEY is either the `Coord.Relative` record itself OR an axis-masked `((wrappedDy & 0xFFFF) << 16) | (wrappedDx & 0xFFFF)` long/int — NEVER a bare `(wrappedDy << 16) | wrappedDx` on signed ints (that sign-extends negative dx and corrupts the key; the NEGATIVE-COORD DEDUP test exists to fail on exactly this). Insert adjacency entries first, SENSOR entries via putIfAbsent (adjacency wins ties). Return values sorted by Comparator.comparingInt(wrappedDy).thenComparingInt(wrappedDx). The LinkedHashMap-insertion + explicit final sort is the load-bearing determinism guarantee the Task-1 order-stability test pins; do NOT substitute a HashMap or an unsorted stream collect.

    Add a code comment at the SENSOR-radius site documenting that SENSOR_PLUS_1 widening of contribution radius is intentionally out of scope per D-03 (future extension point). For FEEDER/ATTACKER keep radius=1 via the existing path (no union).

    For D-04: thread an isCompositeMember boolean from buildTickFrame down through buildCellEntries into envStateFor; in envStateFor when isCompositeMember is true return (byte)(cached & ~BIT_OVERCROWDED) directly (skip perBotOvercrowdedBit); preserve the literal `cached & ~BIT_OVERCROWDED` expression and the full D-40 path for non-composite callers. Update ALL existing buildCellEntries callers to pass the new arg (the compiler enumerates them; the main path at line 386 passes false for solo/bonded, true for composite members).

    If buildLocomotorCells introduces any per-session/per-composite persistent cache, clear it in clearStateForTest() — prefer pure stateless inline computation (D-41 pattern) so no extension is needed.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.paralife.websocket.SensorStitchedPerceptionTest" --tests "com.paralife.websocket.CompositePerceptionTest" --tests "com.paralife.websocket.VisionScopedOvercrowdingTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TickBroadcaster.java contains a method `buildLocomotorCells` (grep -c "buildLocomotorCells" >= 2: definition + call).
    - BOTH buildCellEntries call sites inside buildLocomotorCells pass the composite flag true: the union-cell D-04 test (dense neighbourhood inside a SENSOR window) is green, proving SENSOR-contributed cells have bit 0 unset.
    - Dedup key is NOT a bare signed `<< 16 |`: `grep -nE "<<[[:space:]]*16[[:space:]]*\\|" src/main/java/com/paralife/websocket/TickBroadcaster.java` either returns NOTHING in the buildLocomotorCells region OR every hit is preceded by an explicit `& 0xFFFF` mask on both axes (verify by inspection). The dedup map key is a `Coord.Relative` record OR an axis-masked int/long — assert by inspection that no unmasked signed shift forms the key. The Task-1 NEGATIVE-COORD DEDUP test passing is the behavioural proof.
    - The union dedup uses LinkedHashMap and an explicit sort (determinism): `grep -c "LinkedHashMap" src/main/java/com/paralife/websocket/TickBroadcaster.java` >= 1 in the buildLocomotorCells region and a `.sorted(` or `Comparator.comparingInt` sort is applied before the cells are returned.
    - The SENSOR-window stitch is guarded by an occupant-identity-and-role check: `grep -nE "role\\(\\)[[:space:]]*==[[:space:]]*Role.SENSOR|role\\(\\) == Entity.Role.SENSOR" src/main/java/com/paralife/websocket/TickBroadcaster.java` shows the SENSOR narrowing inside buildLocomotorCells.
    - No per-cell log.warn is emitted on clamp inside buildLocomotorCells (silent clamp like buildRosterIfChanged): `grep -nA40 "private .*buildLocomotorCells" src/main/java/com/paralife/websocket/TickBroadcaster.java | grep -c "log.warn"` returns 0.
    - `grep -v '^[[:space:]]*\*' src/main/java/com/paralife/websocket/TickBroadcaster.java | grep -c "cached & ~BIT_OVERCROWDED"` >= 1 (D-40 literal preserved; D-04 branch may add a second).
    - tierOf REPRODUCER now maps to PASSIVE: `grep -n "REPRODUCER" src/main/java/com/paralife/websocket/TickBroadcaster.java` shows REPRODUCER on the SENSOR/DEFENDER passive arm, not the FEEDER/ATTACKER arm.
    - SensorStitchedPerceptionTest (incl. order-stability, negative-coord dedup, FEEDER/ATTACKER no-leak, numeric-origin, near-blind baseline) and CompositePerceptionTest pass (exit 0).
    - VisionScopedOvercrowdingTest still passes (D-40 non-composite path intact) — regression guard.
  </acceptance_criteria>
  <done>Per-role routing, deterministically-ordered SENSOR union with a sign-safe dedup key, dual-call-site D-04 omission, and the SENSOR-identity guard implemented; all three named test classes green incl. union order-stability, negative-coord dedup, and FEEDER/ATTACKER no-leak.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-internal → wire (per-bot frame) | TickBroadcaster emits vision cells to each bot; zero-trust scoping must hold |
| SENSOR window → LOCOMOTOR s block | server-derived projection; no external input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-01 | Information disclosure | buildLocomotorCells / per-role routing | mitigate | SENSOR union is LOCOMOTOR-exclusive (D-05). FEEDER/ATTACKER frames built via the radius-1-only path — Task 1 adds a MANDATORY direct test that a SENSOR-only-visible cell is ABSENT from both FEEDER and ATTACKER frames; a bot in a non-LOCOMOTOR role cannot read cells outside its own 8-cell entitlement. |
| T-20.1-02 | Information disclosure | envStateFor composite branch | mitigate | OVERCROWDED omission is additive — the composite branch only zeroes bit 0 and is threaded into BOTH buildCellEntries call sites (adjacency + SENSOR union); it cannot widen a non-composite entity's vision. VisionScopedOvercrowdingTest regression guard pins the unchanged D-40 path; the D-04 union-cell test pins the SENSOR-window omission. |
| T-20.1-03 | Tampering / overflow | union relative-coord re-expression | mitigate | dx/dy clamped SILENTLY to ±63 before Coord creation (Coord.Relative enforces); dedup key is sign-safe (Coord.Relative record or axis-masked int — never a bare signed shift), guarded by the negative-coord dedup test. Wire encoding cannot overflow. |
| T-20.1-10 | Spoofing (ghost SENSOR) | buildLocomotorCells SENSOR enumeration | mitigate | Before stitching a SENSOR window, the grid occupant at sensorPos must be a `CompositeMember` whose `role() == SENSOR`; a dead member or a foreign entity that walked onto the cell is skipped, so no vision is stitched from a non-SENSOR occupant. |
</threat_model>

<verification>
- `./gradlew test --tests "com.paralife.websocket.*"` green (perception suite, incl. union order-stability, negative-coord dedup, FEEDER/ATTACKER no-leak).
- `./gradlew test --tests "com.paralife.websocket.VisionScopedOvercrowdingTest"` green (D-40 regression).
- No new persistent state in TickBroadcaster, OR clearStateForTest() clears it.
</verification>

<success_criteria>
- LOCOMOTOR frame = radius-1 adjacency ∪ deduplicated (sign-safe key), deterministically-sorted SENSOR 5x5 union, expressed LOCOMOTOR-relative, with stable cell ORDER across rebuilds.
- FEEDER/ATTACKER = own 8-cell only and provably do NOT receive SENSOR-only cells; REPRODUCER/DEFENDER/SENSOR = minimal form.
- OVERCROWDED bit 0 unset in every composite-member frame including SENSOR union cells; solo/bonded unchanged.
- SensorStitchedPerceptionTest (incl. order-stability, negative-coord dedup, no-leak, numeric-origin, near-blind baseline), CompositePerceptionTest, VisionScopedOvercrowdingTest all green.
</success_criteria>

<output>
Create `.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-01-SUMMARY.md` when done.
</output>
</content>
