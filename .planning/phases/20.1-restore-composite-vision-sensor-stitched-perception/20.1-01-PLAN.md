---
phase: 20.1-restore-composite-vision-sensor-stitched-perception
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/paralife/websocket/TickBroadcaster.java
  - src/test/java/com/paralife/websocket/CompositePerceptionTest.java
  - src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java
autonomous: true
requirements: []
tags: [perception, composite, sensor, tickbroadcaster, vision]

must_haves:
  truths:
    - "A LOCOMOTOR composite member's tick frame carries its own 8-cell (radius-1) adjacency PLUS a deduplicated, deterministically-ordered union of every SENSOR member's 5x5 cells, all expressed relative to the LOCOMOTOR's position."
    - "The union's cell ORDER is stable: building the same world state's LOCOMOTOR frame twice yields list-equal cells() (order + contents), not merely an equal cell COUNT. The final explicit sort (by wrappedDy, wrappedDx) is the load-bearing ordering guarantee, and it INTENTIONALLY supersedes the prior row-major adjacency order — this is acknowledged golden-trace churn (Plan 04 re-baselines it), not an accident."
    - "Every SENSOR CellEntry (whatever Coord subtype buildCellEntries emits — radius-1 may be Coord.Numpad, radius-2 Coord.Relative) is re-expressed into a single uniform LOCOMOTOR-relative (dx,dy) by computing the cell's ABSOLUTE position (SENSOR abs pos + SENSOR-relative offset) and then taking the DIRECT relativeTo(locoPos, absoluteCellPos) toroidal-shortest offset — NOT by composing two wrapped offsets. The re-expressed coord is wrapped in a NEW CellEntry as the union value; the dedup KEY is that coord's canonical toroidal-shortest form so the same absolute cell from two SENSORs on opposite sides of the seam dedups."
    - "The LOCOMOTOR's OWN cell (relative (0,0)) never appears in the union: a SENSOR adjacent to the LOCOMOTOR includes the LOCOMOTOR's own cell in its 5x5; re-expressed that is (0,0), which radius-1 adjacency never contains (the center is skipped), so without a filter the union would insert a spurious 'LOCOMOTOR sees itself' entry. A (0,0)-continue filter removes it."
    - "A SENSOR window straddling NEGATIVE relative coords (SENSOR north/west of the LOCOMOTOR) is stitched correctly: a cell at LOCOMOTOR-relative (dx<0, dy<0) survives dedup and appears at the asserted numeric dx/dy — a signed-shift dedup-key collision would drop or merge it and fail. A cross-`dim/2` seam-wrap cell (LOCOMOTOR at edge, SENSOR window wrapping the torus) is re-expressed via the DIRECT relativeTo path to its toroidal-shortest coord, not an offset-sum."
    - "Adding a SENSOR to a composite strictly widens (never shrinks) the LOCOMOTOR's visible cell set; a LOCOMOTOR in a composite with ZERO SENSORs gets at most its own 8-cell adjacency (no 5x5). No LOCOMOTOR frame ever exceeds PerceptionCodec.MAX_S_ENTRIES cells — enforced at RUNTIME by a clamp in buildLocomotorCells, not only asserted in tests."
    - "A SENSOR enumerated from getComposite(compositeId) is looked up via an Optional whose emptiness is GUARDED — a missing composite falls back to the LOCOMOTOR's own radius-1 adjacency, never a blind .get() that would throw NoSuchElementException and kill the tick loop for all bots."
    - "A SENSOR at a position is stitched ONLY if its occupant is a CompositeMember with role()==SENSOR AND compositeId().equals(thisCompositeId): a foreign composite's SENSOR at an overlapping position is EXCLUDED from this composite's vision (cross-composite information-disclosure boundary closed in this phase)."
    - "FEEDER and ATTACKER receive only their own 8-cell adjacency and do NOT receive any SENSOR-only-visible cell; their OWN adjacency cells also carry OVERCROWDED=0 (D-04 applies to LOCOMOTOR, FEEDER, ATTACKER alike). REPRODUCER, DEFENDER and SENSOR receive the passive minimal form (sensorRadius=0, no s block)."
    - "The OVERCROWDED bit (cellStatus bit 0) is always 0 in every composite-member frame — including union cells contributed by a SENSOR whose neighbourhood is overcrowded; solo Particle / BondedPair frames keep the D-40 per-bot recompute unchanged."
  artifacts:
    - path: "src/main/java/com/paralife/websocket/TickBroadcaster.java"
      provides: "Per-role D-01 frame routing, buildLocomotorCells SENSOR union (direct-relativeTo re-expression into a new CellEntry, (0,0) filter, sorted, runtime MAX_S_ENTRIES clamp, Optional-guarded composite lookup, compositeId-equality SENSOR guard), D-04 OVERCROWDED omission for composite members"
      contains: "buildLocomotorCells"
    - path: "src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java"
      provides: "D-03 monotonic-widening + near-blind baseline + deterministic union ORDER-stability + negative-coord dedup-key test + cross-dim/2 seam-wrap test + self-cell (0,0) filter test + MAX_S_ENTRIES many-SENSOR clamp test + cross-composite SENSOR exclusion test + FEEDER/ATTACKER no-leak + FEEDER/ATTACKER own-cell D-04 test"
      contains: "class SensorStitchedPerceptionTest"
  key_links:
    - from: "TickBroadcaster.buildTickFrame"
      to: "CompositeRegistry.CompositeState.getMemberIds / getPositionForMember"
      via: "Optional-guarded SENSOR enumeration in buildLocomotorCells"
      pattern: "getMemberIds|getPositionForMember"
    - from: "TickBroadcaster.envStateFor"
      to: "composite-member OVERCROWDED omission"
      via: "isCompositeMember branch returning cached & ~BIT_OVERCROWDED"
      pattern: "BIT_OVERCROWDED"
---

<objective>
Restore the sensory-organ perception model (D-01/D-03/D-04) inside TickBroadcaster. Replace the flat `tierOf()` authority-tier dispatch with per-role frame routing: LOCOMOTOR gets its own 8-cell adjacency stitched with the union of all SENSOR members' 5x5 windows; FEEDER/ATTACKER get 8-cell adjacency only; REPRODUCER/DEFENDER/SENSOR are passive. Omit OVERCROWDED from all composite-member frames.

Purpose: SENSOR members must produce tangible composite value (extended FOV feeding LOCOMOTOR voting) instead of acting passively. This is the foundational wire-shape change the rest of the phase builds on.

Output: Modified TickBroadcaster, updated CompositePerceptionTest, new SensorStitchedPerceptionTest. No wire/codec protocol change (MAX_S_ENTRIES=256, relative-coord ±63 are sufficient).

REVIEW NOTE (round-2 consensus, Plan 01 [MEDIUM]): four tightenings folded in — (a) an explicit any-`Coord`→LOCOMOTOR-relative (dx,dy) helper contract (SENSOR `buildCellEntries` may emit mixed `Coord.Numpad`/`Coord.Relative`), pinned by the NUMERIC-ORIGIN test; (b) a self-cell `(0,0)` filter + assertion; (c) a direct D-04 assertion on a FEEDER/ATTACKER OWN cell; (d) a MAX_S_ENTRIES bound assertion + explicit acknowledgment that the final sort supersedes the prior row-major order (intentional churn).

REVIEW NOTE (round-3 consensus, Plan 01): five fixes folded in below. (C2) the SENSOR re-expression is pinned precisely: re-express via DIRECT `relativeTo(locoPos, absoluteCellPos)` (compute the cell's absolute position first, then one direct toroidal-shortest offset from the LOCOMOTOR — NOT a sum of two wrapped offsets, which is wrong across the seam); wrap the result in a NEW `CellEntry` as the map VALUE (reusing the SENSOR-relative original would serialize SENSOR-relative coords to the wire → broken frames); and key the dedup map on the canonical toroidal-shortest coord so the same absolute cell from two SENSORs on opposite sides of the seam dedups. A NEW cross-`dim/2` seam-wrap test pins direct `relativeTo` over offset-composition. (C3) a RUNTIME `MAX_S_ENTRIES` clamp is added to `buildLocomotorCells` (truncate after sort + single `log.warn`) so a production composite with many non-overlapping SENSORs cannot overflow the codec where no test runs; a many-SENSOR test exceeding 256 without the guard is added. (gemini) the composite Optional lookup is GUARDED with `isEmpty()` → fallback to LOCOMOTOR adjacency-only, never a blind `.get()` that crashes the tick loop. (C-DIVERGENT, operator-confirmed CLOSE-NOW) the SENSOR occupant guard additionally checks `mem.compositeId().equals(compositeId)` so a foreign composite's SENSOR at an overlapping position is NOT stitched — closing the cross-composite information-disclosure boundary; a test asserts exclusion. Content-identity property is stated explicitly: the same absolute cell from multiple sources is content-identical (worldGrid is authoritative), so dedup insertion order affects nothing observable and member-iteration order is NOT load-bearing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-RESEARCH.md
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. Use directly — no exploration needed. -->

Entity.Role enum (src/main/java/com/paralife/world/Entity.java:29):
  enum Role { LOCOMOTOR, FEEDER, ATTACKER, DEFENDER, REPRODUCER, SENSOR }

CompositeMember (src/main/java/com/paralife/world/Entity.java:320):
  - has role() : Role and compositeId() : String accessors. The compositeId() accessor is what the cross-composite SENSOR guard checks against THIS composite's id.

Frame.TickFrame (src/main/java/com/paralife/codec/Frame.java:70):
  record TickFrame(long tickId, int x, int y, int energy, int maxEnergy,
                   int sensorRadius, List<CellEntry> cells, ChangeCode change,
                   List<Effect> effects, List<Event> events,
                   Optional<PoolSnapshot> pool, List<RosterMember> roster)
  - sensorRadius validated 0..3 (line 88). 0 = minimal form (cells/change/effects/pool/roster MUST be empty).
  - isMinimal() returns sensorRadius == 0.
  - The constructor does NOT validate that cell entries lie within sensorRadius of self — sensorRadius is only a signal field. LOCOMOTOR sets it to 1; union cells extend the s block beyond radius 1 legally.
  - cells() is a List — list equality (order + contents) is the order-stability check.

CellEntry (src/main/java/com/paralife/codec/Frame.java):
  record CellEntry(Coord coord, byte envState)  (field names: confirm at read; envState carries the cellStatus byte)
  - The union VALUE must be a NEW CellEntry built with the RE-EXPRESSED LOCOMOTOR-relative coord, e.g. `new CellEntry(new Coord.Relative(wrappedDx, wrappedDy), entry.envState())`. Do NOT reuse the SENSOR's original CellEntry as the value — its coord is SENSOR-relative and the codec would serialize SENSOR-relative coords to the wire (broken frames).

PerceptionCodec.MAX_S_ENTRIES (src/main/java/com/paralife/codec/PerceptionCodec.java) — VERIFIED:
  - MAX_S_ENTRIES = 256. A LOCOMOTOR frame's cells().size() must never exceed it. Worst case is many non-overlapping SENSORs each adding ~24 unique cells — >~10 SENSORs overflows the codec. A RUNTIME clamp in buildLocomotorCells (truncate after sort + one log.warn) enforces the bound in production, not only in tests.

Coord subtypes (src/main/java/com/paralife/codec/Frame.java):
  - Coord.Numpad(char d): radius-1 directional cell (the 8-ring). buildCellEntries MAY emit radius-1 cells as Coord.Numpad.
  - Coord.Relative(int dx, int dy): radius>=2 cell. buildCellEntries emits radius-2 cells as Coord.Relative.
  - Coord.Absolute: not used here.
  → buildLocomotorCells must convert WHATEVER subtype each SENSOR CellEntry carries into the cell's ABSOLUTE position, then re-express via DIRECT relativeTo (see helper note in Task 2). Coord.Numpad → its (dx,dy) via Direction.fromNumpad(d).delta() (or the existing numpad→delta map); Coord.Relative → its (dx,dy) directly. Those give the SENSOR-relative offset; add it to the SENSOR's absolute position to get the cell's absolute position; THEN take relativeTo(locoPos, cellAbsPos). The helper is the contract the NUMERIC-ORIGIN and SEAM-WRAP tests pin.

CompositeRegistry (src/main/java/com/paralife/engine/CompositeRegistry.java):
  Optional<CompositeState> getComposite(String compositeId)  // MUST be isEmpty()-guarded, never blind .get()
  CompositeState.getMemberIds() : List<String>  (unmodifiable view of CopyOnWriteArrayList — read-safe, no lock)
  CompositeState.getPositionForMember(String memberId) : Position (or null)

TickBroadcaster current seams (src/main/java/com/paralife/websocket/TickBroadcaster.java):
  - line 353: AuthorityTier tier = tierOf(occupant, bot.entityId());
  - line 375: if (tier == AuthorityTier.PASSIVE) { ...minimal frame... }
  - line 383: int radius = sensorRadiusFor(tier, bot.entityId());
  - line 386: List<CellEntry> cells = buildCellEntries(pos, radius);
  - line 414: enum AuthorityTier { FULL, AUTHORITY_LITE, PASSIVE }
  - lines 422-431: tierOf() — current mapping LOCOMOTOR→FULL, FEEDER/ATTACKER/REPRODUCER→AUTHORITY_LITE, SENSOR/DEFENDER→PASSIVE
  - line 458: buildCellEntries(Position botPos, int radius) — row-major emission, dy then dx
  - line 467: `if (dx == 0 && dy == 0) continue;` — VERIFIED: the center/self cell is SKIPPED, so buildCellEntries(pos, 1) emits EXACTLY the 8-cell Moore ring (no center). D-01 "8-cell adjacency" is already correct as implemented; Task 1 pins this with an explicit cardinality assertion rather than changing it. NOTE this also means a SENSOR's OWN center is skipped in its window — but the LOCOMOTOR's cell appears as a NON-center cell from the SENSOR's perspective when the SENSOR is adjacent, which is exactly the (0,0) artifact the union must filter (the helper re-expresses it to (0,0); filter on (0,0) after re-expression).
  - line 475: int envState = envStateFor(cellPos, botPos, radius) & 0xFF;
  - lines 695-702: envStateFor — D-40 expression cellStatus = (cached & ~BIT_OVERCROWDED) | perBotOvercrowdedBit
  - lines 829-837: relativeTo(Position from, Position to) — toroidal shortest-path dx/dy. THIS is the direct re-expression call: relativeTo(locoPos, cellAbsolutePos). Read it to fix the dy sign convention.
  - lines 853-894: buildRosterIfChanged — iteration skeleton to model SENSOR enumeration on (getMemberIds, getPositionForMember). VERIFIED: it clamps to ±63 SILENTLY (lines 877-878: `Math.max(-63, Math.min(63, ...))`), it does NOT emit log.warn. buildLocomotorCells must clamp dx/dy the same silent way (no per-cell log).
  - lines 873-874: VERIFIED occupant-identity guard pattern — `Cell memberCell = worldGrid.getCell(mp.x(), mp.y()); if (!(memberCell.occupant() instanceof CompositeMember mem)) continue;`. buildLocomotorCells reuses this shape but additionally narrows to `mem.role() == Role.SENSOR && mem.compositeId().equals(compositeId)`, guarding both a dead/foreign occupant AND a foreign-composite SENSOR from being stitched.
  - line 184: clearStateForTest()
  - PERCEPTION_RADIUS constant = 2

Cell free-cell predicates (src/main/java/com/paralife/world/Cell.java:31-37) — VERIFIED:
  - isEmpty() returns `occupant == null`; hasOccupant() returns `occupant != null`. They are exact logical inverses. No obstacle-bypass risk.

PerceptionCodec V-arg validation (src/main/java/com/paralife/codec/PerceptionCodec.java:833-843) — VERIFIED:
  - A V arg must be EXACTLY 3 chars, each in '1'..'9'. Distinctness is NOT enforced (duplicate digits are codec-legal). The hard constraint the brain must satisfy by construction (Plan 03) is "3 chars, each numpad '1'..'9'".

CONTENT-IDENTITY PROPERTY (stated explicitly so a future reader knows it is relied-on, not luck): the same absolute cell contributed by two SENSORs (or a SENSOR and adjacency) reads identical `worldGrid` content because worldGrid is authoritative. Therefore dedup insertion order affects NOTHING observable, and getMemberIds() member-iteration order is NOT load-bearing for content. The final explicit sort is the only ordering guarantee; tie-resolution between two sources is cosmetic (putIfAbsent / adjacency-first is fine but the value is content-identical either way).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold — SensorStitchedPerceptionTest + CompositePerceptionTest D-01/D-04 assertions</name>
  <files>src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java, src/test/java/com/paralife/websocket/CompositePerceptionTest.java</files>
  <read_first>
    - src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java (the file being created)
    - src/test/java/com/paralife/websocket/CompositePerceptionTest.java (full — copy @BeforeEach setUp lines 69-87 and CompositeMember placement lines 92-104; UPDATE locomotorReceivesPoolAndEnergy line 137 which currently asserts sensorRadius == PERCEPTION_RADIUS; ALSO locate any existing REPRODUCER assertion — e.g. a `reproducerMemberReceives*` test asserting REPRODUCER gets AUTHORITY_LITE / sensorRadius==1 — it WILL break when REPRODUCER moves to PASSIVE and MUST be updated, not just superseded)
    - 20.1-PATTERNS.md §"SensorStitchedPerceptionTest" and §"CompositePerceptionTest (extend)" for exact assertion shapes
    - src/main/java/com/paralife/codec/Frame.java lines 70-106 (TickFrame record, isMinimal, sensorRadius validation; cells() is a List → use List equality for order-stability)
    - src/main/java/com/paralife/codec/Frame.java CellEntry + Coord.Relative + Coord.Numpad constructors (to assert numeric dx/dy of a SENSOR-contributed union cell, and to construct the self-cell + seam-wrap + cross-composite scenarios)
    - src/main/java/com/paralife/codec/PerceptionCodec.java (MAX_S_ENTRIES = 256, for the cells().size() bound assertion)
    - src/main/java/com/paralife/world/Entity.java line 320 (CompositeMember role() + compositeId() accessors — to construct a FOREIGN-composite SENSOR for the cross-composite exclusion test)
  </read_first>
  <behavior>
    - SensorStitchedPerceptionTest ADJACENCY-CARDINALITY: a LOCOMOTOR with NO SENSOR members and an empty surrounding neighbourhood has frame.cells() containing EXACTLY its 8-cell Moore ring — assert no cell entry has Coord.Relative(0,0) and cells().size() <= 8.
    - SensorStitchedPerceptionTest NEAR-BLIND BASELINE: a LOCOMOTOR in a composite with ZERO SENSOR members gets frame.cells().size() <= 8 (radius-1 adjacency only, never a 5x5).
    - SensorStitchedPerceptionTest D-03-MONOTONIC: build a LOCOMOTOR-only (no-SENSOR) frame; then add a SENSOR member within radius-2 of the LOCOMOTOR and a nutrient inside the SENSOR's 5x5 but outside the LOCOMOTOR's radius-1; assert frameWithSensor.cells().size() > frameWithoutSensor.cells().size().
    - SensorStitchedPerceptionTest MAX-S-ENTRIES MANY-SENSOR CLAMP (C3 — would exceed 256 without the runtime guard): construct a composite with enough non-overlapping SENSOR members (each contributing ~24 unique cells) that the raw union would exceed PerceptionCodec.MAX_S_ENTRIES (e.g. >= 11 SENSORs). Assert the resulting LOCOMOTOR frame.cells().size() <= PerceptionCodec.MAX_S_ENTRIES — proving the RUNTIME clamp truncates rather than overrunning the codec. Also assert the bound holds on the multi-SENSOR ORDER-STABILITY scenario.
    - SensorStitchedPerceptionTest NUMERIC-ORIGIN (pins the direct-relativeTo re-expression): place a nutrient ONLY inside the SENSOR's 5x5 (not in the LOCOMOTOR's radius-1). Assert the LOCOMOTOR frame.cells() contains a Coord.Relative entry with the exact LOCOMOTOR-relative dx/dy of that nutrient (computed by hand from the LOCOMOTOR and nutrient positions — read relativeTo lines 829-837 to fix the dy sign convention), proving the union re-expresses SENSOR cells relative to the LOCOMOTOR, not relative to the SENSOR. Also assert the entry is a NEW CellEntry whose coord is the LOCOMOTOR-relative coord (not the SENSOR-relative one) — i.e. the value carries the re-expressed coord, not the original.
    - SensorStitchedPerceptionTest SEAM-WRAP (C2b — NEW, pins direct relativeTo over offset-composition): LOCOMOTOR at (0,0) on the 16×16 grid, SENSOR at (0,2), a nutrient/target at (15,0) (in the SENSOR's 5x5 when the window wraps the torus). The toroidal-shortest offset from LOCOMOTOR (0,0) to (15,0) is dx=-1. Assert the nutrient appears in the LOCOMOTOR frame at exactly Coord.Relative(-1, 0). A composed-offset (sensorCellRel + sensorFromLoco) implementation would emit a different (wrong) coord across the seam — this test fails on it.
    - SensorStitchedPerceptionTest SELF-CELL (0,0) FILTER: place a SENSOR ADJACENT to the LOCOMOTOR so the SENSOR's 5x5 includes the LOCOMOTOR's own cell as a non-center cell; re-expressed that is (0,0). Assert frame.cells() contains NO entry whose re-expressed coord is (0,0).
    - SensorStitchedPerceptionTest NEGATIVE-COORD DEDUP (HIGHEST-LEVERAGE — breaks the signed-shift dedup-key trap): register a composite whose SENSOR is positioned NORTH-WEST of the LOCOMOTOR so its 5x5 window straddles NEGATIVE LOCOMOTOR-relative coords (dx<0 AND dy<0). Place a distinguishable cell at a known negative-relative coordinate inside that window. Assert it appears EXACTLY ONCE in frame.cells() at the asserted negative Coord.Relative(dx,dy).
    - SensorStitchedPerceptionTest CROSS-COMPOSITE EXCLUSION (C-DIVERGENT — NEW): seed THIS composite's LOCOMOTOR + SENSOR, then place a FOREIGN composite's SENSOR (a CompositeMember whose role()==SENSOR but compositeId() differs from this composite's id) at a position whose 5x5 window would OVERLAP this composite's union (e.g. adjacent to this composite's SENSOR or LOCOMOTOR). Place a distinguishable cell visible ONLY through the foreign SENSOR's window (outside this composite's own SENSOR/adjacency reach). Assert that cell's Coord.Relative is ABSENT from the LOCOMOTOR frame — the foreign SENSOR is NOT stitched.
    - SensorStitchedPerceptionTest ORDER-STABILITY: with a multi-SENSOR composite (>= 2 SENSORs whose 5x5 windows overlap so dedup + sort are exercised), build the LOCOMOTOR tick frame TWICE against the identical world state and assert `frame1.cells().equals(frame2.cells())`.
    - SensorStitchedPerceptionTest FEEDER/ATTACKER NO-LEAK: with a composite containing a LOCOMOTOR, a SENSOR, a FEEDER and an ATTACKER, place a nutrient visible ONLY inside the SENSOR's 5x5. Assert the FEEDER frame and the ATTACKER frame do NOT contain that cell's Coord.Relative coordinate.
    - SensorStitchedPerceptionTest FEEDER/ATTACKER OWN-CELL D-04: in a composite where a FEEDER (or ATTACKER) sits in an OVERCROWDED neighbourhood, iterate that member's frame.cells() and assert every entry has (envState & 0x01) == 0.
    - SensorStitchedPerceptionTest: SENSOR-contributed cells use Coord.Relative (dx/dy relative to LOCOMOTOR), never Coord.Absolute.
    - SensorStitchedPerceptionTest MISSING-COMPOSITE FALLBACK (gemini .get() guard — NEW, optional-but-recommended): if the test harness can register a LOCOMOTOR whose getComposite(compositeId) returns Optional.empty() (e.g. a LOCOMOTOR member whose composite was deregistered), assert buildLocomotorCells returns its own radius-1 adjacency WITHOUT throwing (no NoSuchElementException). If constructing this state is impractical in the unit harness, document in the SUMMARY that the guard is verified by inspection (the isEmpty() branch) and covered only structurally.
    - CompositePerceptionTest D-04 (place dense neighbourhood INSIDE a SENSOR window): build a LOCOMOTOR + SENSOR composite where the SENSOR's 5x5 contains an overcrowded/dense cell that is ALSO overcrowded from the LOCOMOTOR's own perspective. Iterate frame.cells() and assert every entry — INCLUDING the SENSOR-contributed union cell — has (envState & 0x01) == 0.
    - CompositePerceptionTest D-01: LOCOMOTOR frame asserts sensorRadius == 1 (REPLACES the existing == PERCEPTION_RADIUS assertion at line 137); REPRODUCER frame asserts isMinimal() == true; any pre-existing REPRODUCER-AUTHORITY_LITE/sensorRadius==1 assertion is UPDATED to assert isMinimal()==true.
  </behavior>
  <action>
    Create SensorStitchedPerceptionTest.java copying CompositePerceptionTest's @BeforeEach verbatim (WorldGrid 16x16, mocked EnvironmentEngine returning byte 0 for getCellStatus/getEntityStatus, mocked OutboundSender.offer→true). Add: the ADJACENCY-CARDINALITY test, the NEAR-BLIND BASELINE test, the D-03-MONOTONIC test, the MAX-S-ENTRIES MANY-SENSOR CLAMP test (>= 11 non-overlapping SENSORs so the raw union would exceed 256, assert cells().size() <= MAX_S_ENTRIES), the NUMERIC-ORIGIN test (assert the exact LOCOMOTOR-relative Coord.Relative dx/dy of a SENSOR-only nutrient AND that the value is a new CellEntry carrying that re-expressed coord), the SEAM-WRAP test (LOCOMOTOR (0,0), SENSOR (0,2), target (15,0) → assert Coord.Relative(-1,0)), the SELF-CELL (0,0) FILTER test, the NEGATIVE-COORD DEDUP test, the CROSS-COMPOSITE EXCLUSION test (foreign-compositeId SENSOR's cell absent), the ORDER-STABILITY test (>= 2 overlapping SENSORs), the FEEDER/ATTACKER NO-LEAK test, the FEEDER/ATTACKER OWN-CELL D-04 test, a Coord.Relative-type assertion on SENSOR-contributed cells, and (if practical) the MISSING-COMPOSITE FALLBACK test. In CompositePerceptionTest: UPDATE locomotorReceivesPoolAndEnergy so the LOCOMOTOR sensorRadius assertion expects 1 not PERCEPTION_RADIUS; add the D-04 OVERCROWDED-always-0 test with the dense neighbourhood placed INSIDE a SENSOR's 5x5; add a REPRODUCER-passive test asserting isMinimal()==true; and UPDATE any existing REPRODUCER assertion that expected AUTHORITY_LITE / sensorRadius==1 so it now expects isMinimal()==true. These tests are EXPECTED RED until Task 2 lands.
  </action>
  <verify>
    <automated>./gradlew compileTestJava 2>&1 | tail -5; ./gradlew test --tests "com.paralife.websocket.SensorStitchedPerceptionTest" --tests "com.paralife.websocket.CompositePerceptionTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SensorStitchedPerceptionTest.java compiles and contains class SensorStitchedPerceptionTest with: a monotonic-widening assertion (`isGreaterThan`); a near-blind baseline assertion (`<= 8` on a zero-SENSOR LOCOMOTOR frame's cells().size()); an order-stability assertion comparing two independently-built frames' cells() via List equality (grep: `grep -E "cells\\(\\)\\)\\.isEqualTo\\(.*cells\\(\\)" src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java` matches).
    - SensorStitchedPerceptionTest contains a MANY-SENSOR MAX_S_ENTRIES CLAMP test: `grep -Ec "MAX_S_ENTRIES" src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java` >= 1, applied to a cells().size() upper-bound check, AND the test registers enough SENSORs that the raw union would exceed 256 (a comment-tagged `// MANY-SENSOR` / `// CLAMP` method with >= 11 SENSOR registrations).
    - SensorStitchedPerceptionTest contains a SEAM-WRAP test asserting a target at (15,0) viewed by a SENSOR at (0,2) with LOCOMOTOR at (0,0) appears at `Coord.Relative(-1, 0)` (grep for a `Coord.Relative(-1, 0)` literal or a comment-tagged `// SEAM-WRAP` method).
    - SensorStitchedPerceptionTest contains a SELF-CELL (0,0) FILTER test asserting NO entry re-expresses to (0,0).
    - SensorStitchedPerceptionTest contains a NEGATIVE-COORD DEDUP test asserting a cell appears exactly once at a Coord.Relative with NEGATIVE dx AND negative dy.
    - SensorStitchedPerceptionTest contains a CROSS-COMPOSITE EXCLUSION test: a foreign-compositeId SENSOR is placed overlapping this composite's union and its only-via-foreign-SENSOR cell is asserted ABSENT from the LOCOMOTOR frame (grep for a comment-tagged `// CROSS-COMPOSITE` method that constructs a CompositeMember with a different compositeId and asserts `doesNotContain`/`noneMatch`).
    - SensorStitchedPerceptionTest contains a FEEDER/ATTACKER no-leak assertion and a FEEDER/ATTACKER OWN-CELL D-04 assertion (`(envState & 0x01) == 0`).
    - SensorStitchedPerceptionTest contains a NUMERIC-ORIGIN assertion pinning the exact LOCOMOTOR-relative dx/dy of a SENSOR-only nutrient.
    - CompositePerceptionTest no longer asserts `sensorRadius() ... isEqualTo(TickBroadcaster.PERCEPTION_RADIUS)` for the LOCOMOTOR pool test, and its REPRODUCER assertion expects isMinimal()==true.
    - The D-04 OVERCROWDED test asserts bit 0 unset on a SENSOR-contributed union cell.
    - Tests compile (compileTestJava exits 0). They MAY be red at this task.
  </acceptance_criteria>
  <done>Wave-0 scaffolds exist, compile, and pin D-01/D-03/D-04, the deterministic union ORDER-stability contract, the self-cell (0,0) filter, the seam-wrap direct-relativeTo contract, the many-SENSOR MAX_S_ENTRIES runtime-clamp bound, the negative-coord dedup-key correctness contract, the cross-composite SENSOR exclusion, the FEEDER/ATTACKER no-leak AND own-cell D-04 contracts, and the near-blind baseline. Red is acceptable here.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TickBroadcaster per-role routing + SENSOR union (direct-relativeTo re-expression into new CellEntry, (0,0) filter, sort, runtime MAX_S_ENTRIES clamp, Optional-guarded lookup, compositeId-equality SENSOR guard) + D-04 OVERCROWDED omission</name>
  <files>src/main/java/com/paralife/websocket/TickBroadcaster.java</files>
  <read_first>
    - src/main/java/com/paralife/websocket/TickBroadcaster.java lines 349-431 (buildTickFrame dispatch + tierOf + AuthorityTier enum), 458-490 (buildCellEntries row-major; line 467 self-skip; note which Coord subtype it emits at radius 1 vs radius 2), 695-702 (envStateFor D-40 expression), 829-837 (relativeTo — the DIRECT re-expression call), 853-894 (buildRosterIfChanged iteration skeleton incl. the SILENT ±63 clamp at 877-878 and the occupant-identity guard at 873-874), 184-186 (clearStateForTest)
    - src/main/java/com/paralife/codec/Frame.java (Coord.Numpad vs Coord.Relative — the helper must convert both; CellEntry constructor — for the NEW value), src/main/java/com/paralife/world/Direction.java (numpad char → (dx,dy) delta, for the Coord.Numpad branch of the helper)
    - src/main/java/com/paralife/world/Entity.java line 320 (CompositeMember role() + compositeId() — the cross-composite guard reads compositeId())
    - src/main/java/com/paralife/codec/PerceptionCodec.java (MAX_S_ENTRIES = 256)
    - 20.1-RESEARCH.md Q2 (tierOf replacement + OVERCROWDED omission), Q3 (SENSOR enumeration algorithm + APIs), Q7 (sensorRadius=1 for LOCOMOTOR, MAX_S_ENTRIES budget)
    - 20.1-PATTERNS.md §"SENSOR union construction" and §"D-04 OVERCROWDED omission — exact site"
    - CLAUDE.md §"Env state projection — three layers" (D-40 per-bot OVERCROWDED redaction is the load-bearing site; composite branch zeroes bit 0)
  </read_first>
  <behavior>
    - LOCOMOTOR composite member: frame.sensorRadius()==1; cells = own radius-1 adjacency ∪ each SENSOR member's radius-2 (PERCEPTION_RADIUS) window, deduplicated by the canonical LOCOMOTOR-relative coord (content-identical across sources — adjacency-first insertion is cosmetic only), sorted by (wrappedDy, wrappedDx), clamped to MAX_S_ENTRIES.
    - DIRECT-RELATIVETO RE-EXPRESSION HELPER (C2b): a single helper re-expresses each SENSOR CellEntry into a LOCOMOTOR-relative (dx,dy). Steps: (1) get the SENSOR-relative offset from the entry's Coord — Coord.Numpad → its numpad delta; Coord.Relative → its (dx,dy); (2) compute the cell's ABSOLUTE position by toroidally adding that offset to the SENSOR's absolute position (Math.floorMod on each axis with grid w/h); (3) take the DIRECT relativeTo(locoPos, cellAbsPos) to get the LOCOMOTOR-relative (dx,dy). Do NOT compose two wrapped offsets (sensorCellRel + relativeTo(locoPos, sensorPos)) — offset-addition is wrong across the torus seam (the SEAM-WRAP test fails on it). The result is the canonical toroidal-shortest offset by construction.
    - NEW CELLENTRY VALUE (C2a): the union map VALUE is a NEW `new CellEntry(new Coord.Relative(wrappedDx, wrappedDy), entry.envState())`. Do NOT reuse the SENSOR's original CellEntry — its coord is SENSOR-relative and would serialize SENSOR-relative coords to the wire (broken frames).
    - DEDUP KEY (C2c): the dedup map key is the canonical toroidal-shortest LOCOMOTOR-relative coord (the (wrappedDx, wrappedDy) PAIR produced by relativeTo above) — value-equal with no sign corruption. Acceptable keys: (a) the `Coord.Relative` record itself (value equality) or (b) an explicitly axis-masked `((wrappedDy & 0xFFFF) << 16) | (wrappedDx & 0xFFFF)`. A bare `(wrappedDy << 16) | wrappedDx` on signed ints is FORBIDDEN (the NEGATIVE-COORD DEDUP test fails on it). Because the key is the canonical relativeTo form (NOT a bare floorMod), the same absolute cell contributed by two SENSORs on opposite sides of the seam produces the SAME key and dedups.
    - SELF-CELL FILTER: after re-expression, a cell whose LOCOMOTOR-relative coord is (0,0) is SKIPPED (`continue`).
    - SENSOR-IDENTITY + CROSS-COMPOSITE GUARD (C-DIVERGENT): the SENSOR-window stitch is guarded so the grid occupant at sensorPos must be a `CompositeMember mem` with `mem.role() == Role.SENSOR && mem.compositeId().equals(compositeId)`. A dead member, a foreign entity that walked in, OR a foreign composite's SENSOR at the same/overlapping position is skipped — no vision is stitched from it. The CROSS-COMPOSITE EXCLUSION test pins this.
    - OPTIONAL GUARD (gemini): the composite lookup `getComposite(compositeId)` is GUARDED with `isEmpty()`. If the composite is absent (registries momentarily out of sync), buildLocomotorCells returns the LOCOMOTOR's own radius-1 adjacency only (no SENSOR enumeration) and does NOT throw — never a blind `.get()` that would crash the tick loop for all bots.
    - Union ORDER deterministic and stable: dedup uses an insertion-ordered map (LinkedHashMap) and the result is explicitly sorted by (wrappedDy, wrappedDx) before emission. This final sort INTENTIONALLY supersedes the prior row-major adjacency order (re-baselined in Plan 04).
    - RUNTIME MAX_S_ENTRIES CLAMP (C3): AFTER the sort, if the cells list exceeds PerceptionCodec.MAX_S_ENTRIES, truncate it to MAX_S_ENTRIES and emit a SINGLE `log.warn` (not per-cell). This is the production guard — the codec can never be overrun by a many-SENSOR composite even where no test runs. (In typical composites the union is far under 256; the clamp is defensive.)
    - D-04 union-cell coverage: the OVERCROWDED-omission flag (`isCompositeMember=true`) is threaded into BOTH buildCellEntries call sites inside buildLocomotorCells — the LOCOMOTOR's own radius-1 adjacency call AND each SENSOR's 5x5 window call.
    - FEEDER / ATTACKER composite member: frame.sensorRadius()==1; cells = own radius-1 adjacency only (via the line-386 generic path with isCompositeMember=true); NOT the SENSOR union.
    - REPRODUCER / DEFENDER / SENSOR composite member: isMinimal()==true (sensorRadius=0, no s block).
    - Solo Particle / BondedPair: unchanged (sensorRadius 2, or 3 with SENSOR_PLUS_1).
    - Every composite-member cell entry has OVERCROWDED bit 0 unset; solo/bonded entities keep the D-40 perBotOvercrowdedBit recompute.
  </behavior>
  <action>
    Change tierOf()'s mapping so REPRODUCER joins SENSOR/DEFENDER on the PASSIVE branch (D-01: only LOCOMOTOR/FEEDER/ATTACKER are active members). Add a LOCOMOTOR-specific branch in buildTickFrame after the PASSIVE check (line 375) and before the generic buildCellEntries call (line 386): when occupant is a CompositeMember with role()==LOCOMOTOR, set radius=1 and build cells via a new buildLocomotorCells(locoPos, compositeId).

    Add a private helper to re-express any SENSOR CellEntry coord into a LOCOMOTOR-relative (dx,dy) via the DIRECT-RELATIVETO path: branch on the Coord subtype — Coord.Numpad → its numpad delta (via Direction.fromNumpad(d).delta() or the existing numpad→delta map); Coord.Relative → its (dx,dy) — to get the SENSOR-relative offset; compute the cell's ABSOLUTE position by `Math.floorMod(sensorPos.x()+offDx, w)` / `Math.floorMod(sensorPos.y()+offDy, h)`; then return relativeTo(locoPos, cellAbsPos). Do NOT add the relativeTo(locoPos, sensorPos) offset to the cell offset (offset-composition is wrong across the seam — the SEAM-WRAP and NUMERIC-ORIGIN tests pin the direct path). This is the single re-expression contract.

    buildLocomotorCells: start with buildCellEntries(locoPos, 1, /*isCompositeMember=*/true) — the FIRST call site. Look up the composite with `Optional<CompositeState> co = getComposite(compositeId); if (co.isEmpty()) return <the adjacency-only list>;` — NEVER `.get()` directly (gemini crash-guard). For each memberId in co.get().getMemberIds(): resolve getPositionForMember; skip null; resolve `Cell c = worldGrid.getCell(sensorPos.x(), sensorPos.y());` and SKIP unless `c.occupant() instanceof CompositeMember cm && cm.role() == Role.SENSOR && cm.compositeId().equals(compositeId)` (occupant-identity + role + CROSS-COMPOSITE guard — a foreign composite's SENSOR is excluded); then build buildCellEntries(sensorPos, PERCEPTION_RADIUS, /*isCompositeMember=*/true) — the SECOND (union) call site. Re-express each SENSOR entry's coord via the DIRECT helper above; clamp dx/dy to ±63 SILENTLY exactly as buildRosterIfChanged does at lines 877-878 (no per-cell log.warn). After re-expression, if the relative coord is (0,0) `continue` (SELF-CELL filter). Build the union VALUE as a NEW `CellEntry(new Coord.Relative(wrappedDx, wrappedDy), entry.envState())` — never the original SENSOR CellEntry.

    Dedup into an insertion-ordered LinkedHashMap whose KEY is either the `Coord.Relative` record itself OR an axis-masked `((wrappedDy & 0xFFFF) << 16) | (wrappedDx & 0xFFFF)` — NEVER a bare `(wrappedDy << 16) | wrappedDx` on signed ints. Insert adjacency entries first, SENSOR entries via putIfAbsent. (Tie-resolution is cosmetic: the same absolute cell is content-identical across sources — worldGrid is authoritative — so putIfAbsent vs put changes nothing observable.) Collect the values, sort by Comparator.comparingInt(wrappedDy).thenComparingInt(wrappedDx). The LinkedHashMap-insertion + explicit final sort is the load-bearing determinism guarantee. NOTE the final sort intentionally supersedes the prior row-major adjacency order (re-baselined in Plan 04).

    AFTER the sort, apply the RUNTIME MAX_S_ENTRIES CLAMP: if the sorted list size > PerceptionCodec.MAX_S_ENTRIES, truncate to MAX_S_ENTRIES (e.g. subList(0, MAX_S_ENTRIES)) and emit ONE log.warn naming the composite id and the dropped count — a single warn, not per-cell. This is the production overflow guard the MANY-SENSOR test exercises.

    Add a code comment at the SENSOR-radius site documenting that SENSOR_PLUS_1 widening of contribution radius is intentionally out of scope per D-03 (future extension point). For FEEDER/ATTACKER keep radius=1 via the existing line-386 generic path WITH isCompositeMember=true.

    For D-04: thread an isCompositeMember boolean from buildTickFrame down through buildCellEntries into envStateFor; in envStateFor when isCompositeMember is true return (byte)(cached & ~BIT_OVERCROWDED) directly (skip perBotOvercrowdedBit); preserve the literal `cached & ~BIT_OVERCROWDED` expression and the full D-40 path for non-composite callers. Update ALL existing buildCellEntries callers to pass the new arg (the main path at line 386 passes false for solo/bonded, true for composite members).

    If buildLocomotorCells introduces any per-session/per-composite persistent cache, clear it in clearStateForTest() — prefer pure stateless inline computation (D-41 pattern) so no extension is needed.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.paralife.websocket.SensorStitchedPerceptionTest" --tests "com.paralife.websocket.CompositePerceptionTest" --tests "com.paralife.websocket.VisionScopedOvercrowdingTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TickBroadcaster.java contains a method `buildLocomotorCells` (grep -c "buildLocomotorCells" >= 2: definition + call).
    - The composite Optional lookup is GUARDED, not blind: `grep -nA60 "private .*buildLocomotorCells" src/main/java/com/paralife/websocket/TickBroadcaster.java | grep -Ec "isEmpty\\(\\)|isPresent\\(\\)|orElse"` >= 1, AND a blind `getComposite\\([^)]*\\)\\.get\\(\\)` does NOT appear inside buildLocomotorCells (grep returns 0 for the unguarded `.get()` in that region) — the MISSING-COMPOSITE path falls back to adjacency-only.
    - The re-expression uses DIRECT relativeTo (not offset-composition): by inspection the helper computes the cell's absolute position then calls relativeTo(locoPos, cellAbsPos) — it does NOT add relativeTo(locoPos, sensorPos) to the cell offset. The SEAM-WRAP and NUMERIC-ORIGIN tests passing are the behavioural proof.
    - The union VALUE is a NEW CellEntry with the re-expressed coord: `grep -nA60 "private .*buildLocomotorCells" src/main/java/com/paralife/websocket/TickBroadcaster.java | grep -Ec "new CellEntry\\("` >= 1, and by inspection the original SENSOR CellEntry is not used as the map value.
    - The union applies a self-cell (0,0) filter (the Task-1 SELF-CELL test passing is the behavioural proof).
    - BOTH buildCellEntries call sites inside buildLocomotorCells pass the composite flag true (the union-cell D-04 test is green).
    - FEEDER/ATTACKER own-cell D-04 holds (the Task-1 FEEDER/ATTACKER OWN-CELL test is green).
    - Dedup key is NOT a bare signed `<< 16 |`: `grep -nE "<<[[:space:]]*16[[:space:]]*\\|" src/main/java/com/paralife/websocket/TickBroadcaster.java` either returns NOTHING in the buildLocomotorCells region OR every hit is preceded by an explicit `& 0xFFFF` mask on both axes. The NEGATIVE-COORD DEDUP test passing is the behavioural proof.
    - The union dedup uses LinkedHashMap and an explicit sort: `grep -c "LinkedHashMap" src/main/java/com/paralife/websocket/TickBroadcaster.java` >= 1 in the buildLocomotorCells region and a `.sorted(` or `Comparator.comparingInt` sort is applied before the cells are returned.
    - A RUNTIME MAX_S_ENTRIES clamp exists AFTER the sort: `grep -nA80 "private .*buildLocomotorCells" src/main/java/com/paralife/websocket/TickBroadcaster.java | grep -Ec "MAX_S_ENTRIES"` >= 1 used in a size-truncation (subList / limit / size check), and a single `log.warn` accompanies the truncation. The MANY-SENSOR Task-1 test passing (cells().size() <= MAX_S_ENTRIES with >= 11 SENSORs) is the behavioural proof.
    - The SENSOR-window stitch is guarded by occupant-identity + role + compositeId equality: `grep -nE "compositeId\\(\\)\\.equals\\(|\\.equals\\(compositeId\\)" src/main/java/com/paralife/websocket/TickBroadcaster.java` shows the cross-composite equality check inside buildLocomotorCells, alongside the `role() == Role.SENSOR` narrowing. The CROSS-COMPOSITE EXCLUSION test passing is the behavioural proof.
    - No per-cell log.warn on clamp inside the SENSOR loop (only the single MAX_S_ENTRIES truncation warn is permitted): the ±63 coordinate clamp emits no log (silent like buildRosterIfChanged).
    - `grep -v '^[[:space:]]*\*' src/main/java/com/paralife/websocket/TickBroadcaster.java | grep -c "cached & ~BIT_OVERCROWDED"` >= 1 (D-40 literal preserved; D-04 branch may add a second).
    - tierOf REPRODUCER now maps to PASSIVE: `grep -n "REPRODUCER" src/main/java/com/paralife/websocket/TickBroadcaster.java` shows REPRODUCER on the SENSOR/DEFENDER passive arm, not the FEEDER/ATTACKER arm.
    - SensorStitchedPerceptionTest (incl. order-stability, self-cell (0,0), seam-wrap, many-SENSOR MAX_S_ENTRIES clamp, negative-coord dedup, cross-composite exclusion, FEEDER/ATTACKER no-leak AND own-cell D-04, numeric-origin, near-blind baseline) and CompositePerceptionTest pass (exit 0).
    - VisionScopedOvercrowdingTest still passes (D-40 non-composite path intact) — regression guard.
  </acceptance_criteria>
  <done>Per-role routing, deterministically-ordered SENSOR union with a direct-relativeTo re-expression helper into a NEW CellEntry, self-cell (0,0) filter, a sign-safe canonical dedup key, dual-call-site D-04 omission (incl. FEEDER/ATTACKER own cells), a RUNTIME MAX_S_ENTRIES clamp, an Optional-guarded composite lookup, and the occupant-identity + compositeId-equality SENSOR guard implemented; all three named test classes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-internal → wire (per-bot frame) | TickBroadcaster emits vision cells to each bot; zero-trust scoping must hold |
| SENSOR window → LOCOMOTOR s block | server-derived projection; no external input crosses here |
| foreign composite SENSOR → this composite's LOCOMOTOR s block | cross-composite boundary; a foreign SENSOR must NOT leak its vision into another composite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-01 | Information disclosure | buildLocomotorCells / per-role routing | mitigate | SENSOR union is LOCOMOTOR-exclusive (D-05). FEEDER/ATTACKER frames built via the radius-1-only path — Task 1 adds a MANDATORY direct test that a SENSOR-only-visible cell is ABSENT from both FEEDER and ATTACKER frames; a bot in a non-LOCOMOTOR role cannot read cells outside its own 8-cell entitlement. |
| T-20.1-02 | Information disclosure | envStateFor composite branch | mitigate | OVERCROWDED omission is additive — the composite branch only zeroes bit 0 and is threaded into BOTH buildCellEntries call sites (adjacency + SENSOR union) AND the FEEDER/ATTACKER own-adjacency path; it cannot widen a non-composite entity's vision. VisionScopedOvercrowdingTest regression guard pins the unchanged D-40 path; the D-04 union-cell test pins the SENSOR-window omission; the FEEDER/ATTACKER own-cell D-04 test pins their adjacency omission. |
| T-20.1-03 | Tampering / overflow | union relative-coord re-expression | mitigate | dx/dy clamped SILENTLY to ±63 before Coord creation; the self-cell (0,0) entry is filtered out; dedup key is the canonical toroidal-shortest form (Coord.Relative record or axis-masked int — never a bare signed shift), guarded by the negative-coord dedup test; cells().size() is bounded by a RUNTIME MAX_S_ENTRIES clamp (truncate + single log.warn) AND the Task-1 many-SENSOR assertion. Wire encoding cannot overflow even for an unbounded-SENSOR composite in production. |
| T-20.1-10 | Spoofing (ghost / foreign SENSOR) | buildLocomotorCells SENSOR enumeration | mitigate | Before stitching a SENSOR window, the grid occupant at sensorPos must be a `CompositeMember` whose `role() == SENSOR` AND `compositeId().equals(thisCompositeId)`; a dead member, a foreign entity that walked onto the cell, OR a foreign composite's SENSOR at an overlapping position is skipped, so no vision is stitched from a non-member SENSOR. CLOSED this phase (round-3, operator-confirmed): the cross-composite information-disclosure edge — a foreign SENSOR from a DIFFERENT composite leaking its window into this composite's LOCOMOTOR — is now mitigated by the compositeId-equality check, pinned by the CROSS-COMPOSITE EXCLUSION test. (Previously documented as deferred TD; now closed.) |
| T-20.1-11 | Denial of service (tick-loop crash) | buildLocomotorCells composite lookup | mitigate | getComposite(compositeId) is isEmpty()-guarded; a momentarily-out-of-sync registry yields a fallback to LOCOMOTOR adjacency-only rather than a blind .get() throwing NoSuchElementException that would kill the tick loop for ALL bots. |
</threat_model>

<verification>
- `./gradlew test --tests "com.paralife.websocket.*"` green (perception suite, incl. union order-stability, self-cell (0,0), seam-wrap, many-SENSOR MAX_S_ENTRIES clamp, negative-coord dedup, cross-composite exclusion, FEEDER/ATTACKER no-leak + own-cell D-04).
- `./gradlew test --tests "com.paralife.websocket.VisionScopedOvercrowdingTest"` green (D-40 regression).
- No blind `.get()` on the composite Optional in buildLocomotorCells; runtime MAX_S_ENTRIES clamp present.
- No new persistent state in TickBroadcaster, OR clearStateForTest() clears it.
</verification>

<success_criteria>
- LOCOMOTOR frame = radius-1 adjacency ∪ deduplicated (canonical sign-safe key, direct-relativeTo re-expression into a NEW CellEntry, (0,0) filtered, foreign-composite SENSORs excluded), deterministically-sorted SENSOR 5x5 union, expressed LOCOMOTOR-relative, with stable cell ORDER across rebuilds, runtime-clamped to MAX_S_ENTRIES.
- FEEDER/ATTACKER = own 8-cell only (OVERCROWDED-omitted) and provably do NOT receive SENSOR-only cells; REPRODUCER/DEFENDER/SENSOR = minimal form.
- OVERCROWDED bit 0 unset in every composite-member frame including SENSOR union cells AND FEEDER/ATTACKER own cells; solo/bonded unchanged.
- A missing composite Optional falls back to adjacency-only without crashing; a foreign-composite SENSOR is excluded from the union.
- SensorStitchedPerceptionTest (incl. order-stability, self-cell (0,0), seam-wrap, many-SENSOR MAX_S_ENTRIES, negative-coord dedup, cross-composite exclusion, no-leak + own-cell D-04, numeric-origin, near-blind baseline), CompositePerceptionTest, VisionScopedOvercrowdingTest all green.
</success_criteria>

<output>
Create `.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-01-SUMMARY.md` when done.
</output>
</content>
</output>
