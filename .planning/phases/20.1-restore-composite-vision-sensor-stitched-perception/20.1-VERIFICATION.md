---
phase: 20.1-restore-composite-vision-sensor-stitched-perception
verified: 2026-06-18T07:25:00Z
status: passed
score: 6/6
overrides_applied: 0
---

# Phase 20.1: Restore Composite Vision (SENSOR-stitched perception) — Verification Report

**Phase Goal:** Restore the SENSOR-based stitched perception that Phase 12 designed and Phase 15's
protocol overhaul dropped, so SENSOR role members produce tangible value to their composite — an
extended field of view, assembled from the union of SENSOR 5×5 windows, that informs LOCOMOTOR
movement voting (the colony's only wide sight), rather than rest-acting passively. Adopts a
sensory-organ model: only SENSORs are eyes; movement/action members see only their own 8-cell
adjacency.

**Verified:** 2026-06-18T07:25:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| SC-1 | LOCOMOTOR tick frame carries a stitched neighbourhood from SENSOR 5×5 union, deduplicated and deterministically ordered; union is LOCOMOTOR-exclusive | VERIFIED | `TickBroadcaster.buildLocomotorCells` builds SENSOR union via direct-relativeTo re-expression into a new `CellEntry`, deduped by `Coord.Relative` (sign-safe), sorted by `(dy, dx)` via `sortAndClamp`. FEEDER/ATTACKER explicitly excluded. 15 `SensorStitchedPerceptionTest` tests pass, including `orderStability`, `feederAttackerNoLeak`, `d03Monotonic`. |
| SC-2 | Per-role sensory-organ model: LOCOMOTOR = 8-cell ∪ SENSOR union; FEEDER/ATTACKER = 8-cell only; REPRODUCER/DEFENDER/SENSOR = passive (no vision block) | VERIFIED | `TickBroadcaster.tierOf()`: LOCOMOTOR → AUTHORITY_LITE-with-SENSOR-union branch, FEEDER/ATTACKER → radius-1 only, SENSOR/DEFENDER/REPRODUCER → `AuthorityTier.PASSIVE` (minimal frame). Verified at `TickBroadcaster.java:444-446`. `CompositePerceptionTest.reproducerMemberReceivesMinimalForm` and SENSOR `isMinimal()` assertions pass. |
| SC-3 | D-10 byte-for-byte equivalence re-baselined: new SENSOR scenario pinned in `GoldenTraceEquivalenceTest`; both existing baselines re-pinned | VERIFIED | `GoldenTraceEquivalenceTest.seedCompositeWithSensorAndLocomotor` seeds LOCOMOTOR at (1,1) + SENSOR at (1,3) + nutrient at (1,5); `golden-trace-phase19.json` contains `comp-sess-loco-1` and `comp-sess-sensor-1` digests (28 sessions total). Both baselines regenerated via documented write-then-independent-rerun cycle. GoldenTraceEquivalenceTest (1/1) passes. |
| SC-4 | Tests verify: adding a SENSOR strictly widens LOCOMOTOR visible cells; SENSORs remain PASSIVE on wire; HeuristicBrain demonstrably votes using SENSOR-derived cells | VERIFIED | `d03Monotonic` asserts `frameWithSensor.cells().size() > frameWithoutSensor.cells().size()`. `CompositePerceptionTest` asserts SENSOR `isMinimal() == true`. `HeuristicBrainLocomotorTest.sensorFoodSteer_topRankTowardNorthNutrient` places nutrient at `Coord.Relative(0,-2)` (outside radius-1) and asserts top ballot rank is `'8'` (North). 8/8 brain tests pass. |
| SC-5 | REPRODUCER buds server-auto-placed at nearest free cell (toroidal Euclidean); no bot-chosen direction; OVERCROWDED omitted from all composite-member frames | VERIFIED | `ActionResolver.resolveReproducerBud` calls `nearestFreeCell(rca.bot.position(), claimedCells)` — `directionOf` absent from this method. Pool energy only deducted AFTER successful target find (`return` guards before `drainEnergy`). `ReproducerAutoPlaceTest` (6/6) tests pass including `boundedSkip_noSpawn_poolEnergyUnchanged`. D-04: `TickBroadcaster.envStateFor` returns `(byte)(cached & ~BIT_OVERCROWDED)` when `isCompositeMember=true`, threaded into both `buildCellEntries` call sites. |
| SC-6 | Must land before M5 (phase completed before M5 starts) | VERIFIED | Phase 20.1 complete, all 4 plans executed, full test suite green. M5 not yet started. Phase 20.1 is checked off in ROADMAP.md. |

**Score: 6/6 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/main/java/com/paralife/websocket/TickBroadcaster.java` | Per-role D-01 routing, `buildLocomotorCells`, D-04 OVERCROWDED omission | VERIFIED | `buildLocomotorCells` present (4 occurrences incl. definition + calls). `REPRODUCER → PASSIVE` at line 446. `isCompositeMember` bool threaded into `envStateFor`. |
| `src/test/java/com/paralife/websocket/SensorStitchedPerceptionTest.java` | 15-test pin suite for D-01/D-03/D-04 | VERIFIED | 695 lines, 15 `@Test` methods. All 15 pass: adjacencyCardinality, nearBlindBaseline, d03Monotonic, maxSEntriesBound, numericOrigin, seamWrap, selfCellFilter, negativeCoordDedup, crossCompositeExclusion, adjacencySensorOverlapDedup, orderStability, feederAttackerNoLeak, feederOwnCellD04, sensorContributedCells, missingCompositeFallback. |
| `src/main/java/com/paralife/engine/ActionResolver.java` | `nearestFreeCell` D-02 nearest-free-cell auto-place, `MAX_REPRODUCER_SEARCH_RADIUS` | VERIFIED | `nearestFreeCell` at line 728, `MAX_REPRODUCER_SEARCH_RADIUS = 5` at line 901. No `static` offset cache. No `directionOf` inside `resolveReproducerBud`. |
| `src/test/java/com/paralife/engine/ReproducerAutoPlaceTest.java` | D-02 determinism + bounded-skip tests (6 tests) | VERIFIED | 6/6 tests pass. |
| `src/main/java/com/paralife/bot/HeuristicBrain.java` | D-06 `decideLocomotor` with `rankDirections` helper | VERIFIED | `rankDirections` at line 308; called from `decideLocomotor` at line 256. Scans `frame.cells()` including SENSOR-derived `Coord.Relative` entries via `toDirectionInfo`. `LinkedHashSet` dedup. No `new Random`. |
| `src/test/java/com/paralife/bot/HeuristicBrainLocomotorTest.java` | D-06 behavioural tests (8 tests) | VERIFIED | 8/8 tests pass. |
| `src/test/java/com/paralife/engine/GoldenTraceEquivalenceTest.java` | New `seedCompositeWithSensorAndLocomotor` + re-pinned equivalence | VERIFIED | Method added at line 377, called in `driveScenario` at line 294. Post-loop collision-free assertions at lines 326-343. SENSOR session `comp-sess-sensor-1` registered. |
| `src/test/resources/golden-trace-phase19.json` | Re-pinned to include SENSOR-stitched composite session digests | VERIFIED | 28 sessions including `comp-sess-loco-1` and `comp-sess-sensor-1`. Dual-run `mapA==mapB` passes. |
| `src/test/resources/golden-trace-with-actions-phase19.json` | Unchanged from pre-phase (no LOCOMOTOR/SENSOR composite entities in that test) | VERIFIED | Content identical to pre-phase — expected deviation documented in 20.1-04-SUMMARY.md: test seeds `BondedPair` entities, not `CompositeMember`; D-01/D-04/D-02 don't affect BondedPair wire output. Independent rerun passes. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `TickBroadcaster.buildTickFrame` | `buildLocomotorCells` | `occupant instanceof CompositeMember cm && cm.role() == LOCOMOTOR` branch | WIRED | Lines 389-398: LOCOMOTOR composite branch calls `buildLocomotorCells(pos, cm.compositeId())` |
| `buildLocomotorCells` | `CompositeRegistry.getComposite()` | Optional-guarded `isEmpty()` check | WIRED | Lines 602-605: `if (coOpt.isEmpty()) return sortAndClamp(...)` fallback. No blind `.get()`. |
| `buildLocomotorCells` SENSOR stitch | `compositeId().equals(compositeId)` cross-composite guard | occupant-identity + role + compositeId check | WIRED | Lines 615-617: `mem.role() != Role.SENSOR` → continue; `!mem.compositeId().equals(compositeId)` → continue. |
| `buildLocomotorCells` | `PerceptionCodec.MAX_S_ENTRIES` runtime clamp | `sortAndClamp` subList truncation + `log.warn` | WIRED | Lines 671-674: `if (cells.size() > MAX_S_ENTRIES) { log.warn(...); return cells.subList(0, MAX_S_ENTRIES); }` |
| `ActionResolver.resolveReproducerBud` | `nearestFreeCell` | `directionOf` removed; `nearestFreeCell` replaces direction-derived target | WIRED | Line 930: `Position target = nearestFreeCell(rca.bot.position(), claimedCells)`. |
| `HeuristicBrain.decideLocomotor` | `rankDirections` | named helper call over `frame.cells()` | WIRED | Line 256: `List<Direction> ranked = rankDirections(frame, state, rng)`. |
| `rankDirections` | `frame.cells()` (including SENSOR union entries) | `toDirectionInfo` handles both `Coord.Numpad` and `Coord.Relative` | WIRED | Lines 320-344: `for (CellEntry cell : frame.cells())` — `toDirectionInfo` returns non-null for `Coord.Relative` entries, enabling SENSOR-derived food/threat detection. |
| `GoldenTraceEquivalenceTest.driveScenario` | `seedCompositeWithSensorAndLocomotor` | called before 26-bot loop | WIRED | Line 294: `seedCompositeWithSensorAndLocomotor()` called first, followed by 26-bot loop and post-loop collision assertion. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `TickBroadcaster.buildLocomotorCells` | union `CellEntry` list | `worldGrid.getCell(sensorPos.x(), sensorPos.y())` + `buildCellEntries(sensorPos, PERCEPTION_RADIUS, true)` | Yes — live worldGrid query on every tick frame build (D-41 stateless inline pattern) | FLOWING |
| `HeuristicBrain.decideLocomotor` | `frame.cells()` | `TickBroadcaster.buildLocomotorCells` — SENSOR union delivered on the tick frame | Yes — if composite has SENSOR, union cells include SENSOR-only-visible content | FLOWING |
| `ActionResolver.nearestFreeCell` | `target Position` | `worldGrid.getCell(tx, ty).hasOccupant()` + `claimedCells.contains()` | Yes — live occupancy query per candidate cell | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Verification | Result | Status |
|----------|--------------|--------|--------|
| SENSOR-stitched LOCOMOTOR frame carries cells beyond radius-1 | `SensorStitchedPerceptionTest.d03Monotonic` (`frameWithSensor.cells().size() > frameWithoutSensor.cells().size()`) | PASS | PASS |
| SENSOR-only nutrient steers LOCOMOTOR top ballot rank | `HeuristicBrainLocomotorTest.sensorFoodSteer_topRankTowardNorthNutrient` — nutrient at `(0,-2)` → top rank `'8'` | PASS | PASS |
| FEEDER does not receive SENSOR-only cells | `SensorStitchedPerceptionTest.feederAttackerNoLeak` — no `|dx|>1 || |dy|>1` cell in FEEDER frame | PASS | PASS |
| REPRODUCER bud placed at nearest free cell ignoring bot direction | `ReproducerAutoPlaceTest.directionArgIgnored_placesAtNearestFree_notNoOp` | PASS | PASS |
| Bounded skip leaves pool energy unchanged | `ReproducerAutoPlaceTest.boundedSkip_noSpawn_poolEnergyUnchanged` | PASS | PASS |
| Seam-wrap target re-expressed via direct relativeTo (not composed offset) | `seamWrap_directRelativeToNotOffsetComposition` — target at (9,0) appears at `Coord.Relative(-7,0)` | PASS | PASS |
| D-10 equivalence gate re-baselined with SENSOR scenario | `GoldenTraceEquivalenceTest` (1/1) — dual-run `mapA==mapB` passes with 28 sessions | PASS | PASS |

---

### Requirements Coverage

No `REQUIREMENTS.md` in this project (GSD1 migration; requirements tracked as ROADMAP.md success criteria). All 6 ROADMAP success criteria verified above.

---

### Anti-Patterns Found

No blocking debt markers (`TBD`, `FIXME`, `XXX`) found in any phase-modified file.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `HeuristicBrain.java` | rankDirections javadoc | Known accepted TD: flee gate only acts on dist-1 predators; SENSOR-visible dist-2 predator not a flee trigger | INFO | Documented in SUMMARY, PLAN, and javadoc; ROADMAP SC does not require threat-via-SENSOR |
| `ActionResolver.java` | resolveReproducerBud comment | Known accepted TD: bot still sends `a|R|<numpad>`, direction arg server-ignored (contract cleanup deferred) | INFO | Wire contract unchanged; no user-observable regression |
| `TickBroadcaster.java` | buildLocomotorCells RockRun note | Known accepted TD: RockRun mid-run coord re-expression has narrow edge cases; rocks non-actionable for brain | INFO | Documented in MULTIREVIEW-backlog.md; does not affect perception correctness for nutrient/prey/predator cells |

---

### Human Verification Required

None. All phase success criteria are verifiable through code inspection, grep analysis, and automated tests. No visual, real-time, or external-service behaviors were introduced.

---

### Gaps Summary

No gaps. All 6 ROADMAP success criteria are VERIFIED against the codebase:

- `buildLocomotorCells` in `TickBroadcaster` implements the full SENSOR union with direct-relativeTo re-expression, sign-safe dedup key, self-cell filter, cross-composite guard, Optional guard, OVERCROWDED omission, and MAX_S_ENTRIES runtime clamp.
- `ActionResolver.resolveReproducerBud` uses `nearestFreeCell` with correct toroidal spawn wrap, raw Euclidean sort key, no static cache, and pool-energy-only-on-success semantics.
- `HeuristicBrain.decideLocomotor` delegates to `rankDirections` which scans `frame.cells()` (including SENSOR-derived `Coord.Relative` entries), produces a deduplicated direction ranking, and emits a deterministic well-formed V ballot.
- `GoldenTraceEquivalenceTest` adds a SENSOR-stitched scenario with LOCOMOTOR and SENSOR sessions registered; both golden-trace baselines re-pinned via write-then-independent-rerun.
- Full test suite: 15 `SensorStitchedPerceptionTest` + 8 `HeuristicBrainLocomotorTest` + 6 `ReproducerAutoPlaceTest` + 1 `GoldenTraceEquivalenceTest` — all pass.

---

_Verified: 2026-06-18T07:25:00Z_
_Verifier: Claude (gsd-verifier)_
