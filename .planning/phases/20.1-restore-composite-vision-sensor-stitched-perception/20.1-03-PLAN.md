---
phase: 20.1-restore-composite-vision-sensor-stitched-perception
plan: 03
type: execute
wave: 2
depends_on: ["20.1-01"]
files_modified:
  - src/main/java/com/paralife/bot/HeuristicBrain.java
  - src/test/java/com/paralife/bot/HeuristicBrainLocomotorTest.java
autonomous: true
requirements: []
tags: [heuristicbrain, locomotor, voting, sensor, brain]

must_haves:
  truths:
    - "The LOCOMOTOR's IRV ballot is derived from frame.cells() (its own adjacency PLUS SENSOR-derived union cells) instead of a random shuffle."
    - "When a nutrient is visible only via a SENSOR's 5x5 window (relative distance >= 2, outside the LOCOMOTOR's own 8-cell adjacency), the LOCOMOTOR's top ballot rank points toward it."
    - "The LOCOMOTOR still emits a well-formed `a|V|<3-char numpad ballot>` action frame."
  artifacts:
    - path: "src/main/java/com/paralife/bot/HeuristicBrain.java"
      provides: "D-06 LOCOMOTOR voting driven by frame.cells() scan (reuses decideFullAuthority scoring)"
      contains: "decideLocomotor"
    - path: "src/test/java/com/paralife/bot/HeuristicBrainLocomotorTest.java"
      provides: "D-06 behavioural test: LOCOMOTOR steers toward SENSOR-only-visible food"
      contains: "class HeuristicBrainLocomotorTest"
  key_links:
    - from: "HeuristicBrain.decideLocomotor"
      to: "frame.cells() scan / toDirectionInfo"
      via: "reuse of decideFullAuthority priority cascade, emit V verb"
      pattern: "frame.cells\\(\\)|toDirectionInfo"
---

<objective>
Implement D-06: replace `HeuristicBrain.decideLocomotor`'s random-shuffle ballot with a real food/threat steering heuristic that reads `frame.cells()` — including the SENSOR-derived union cells now delivered by Plan 01. The LOCOMOTOR must demonstrably vote toward food (and away from threats) that are only visible through a SENSOR.

Purpose: Close the loop — SENSOR-extended FOV is worthless unless the brain consumes it. This is the behavioural payoff of the sensory-organ model.

Output: Modified HeuristicBrain, new HeuristicBrainLocomotorTest. Depends on Plan 01 because the LOCOMOTOR frame must already carry SENSOR union cells as Coord.Relative entries for the brain to steer by them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-RESEARCH.md
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-PATTERNS.md
@.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase. Use directly — no exploration needed. -->

HeuristicBrain current decideLocomotor (src/main/java/com/paralife/bot/HeuristicBrain.java lines 251-259):
  private Frame.ActionFrame decideLocomotor(Frame.TickFrame frame, BotState state, Random rng) {
      char[] ranks = topThreeDirections(rng);   // random shuffle — REPLACE
      return new Frame.ActionFrame('V',
              Optional.of(String.valueOf(ranks[0]) + ranks[1] + ranks[2]));
  }

decideFullAuthority cell-scan cascade (lines 116-245) — the scoring logic to reuse:
  - lines 129-174: build predators / prey / nutrients / emptyCells lists from frame.cells() via toDirectionInfo(cell)
  - priority cascade: flee predators → consume adjacent nutrient → chase close prey → move toward distant nutrient → wander

toDirectionInfo (lines 299-318) — already handles Coord.Relative (SENSOR union cells):
  - Coord.Numpad → DirectionInfo(dir, dist=1)
  - Coord.Relative r → dir = fromDxDy(signum(dx), signum(dy)); dist = Chebyshev max(|dx|,|dy|); returns null for (0,0)
  - Coord.Absolute → null
  SENSOR-derived cells arrive as Coord.Relative with dist >= 2.

Output format (PRESERVE): new Frame.ActionFrame('V', Optional.of(ranks[0]+ranks[1]+ranks[2]))
  where ranks[i] are numpad digit chars ('1'..'9').

BotState: embodiment COMPOSITE_MEMBER, compositeRole ordinal 0 = LOCOMOTOR (Role enum: LOCOMOTOR=0).
HeuristicBrain is a plain class (no @Component / @Autowired) — construct directly: new HeuristicBrain(70).
Frame.TickFrame is a record — construct directly with List.of(cellEntry) for unit tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 behavioural test — HeuristicBrainLocomotorTest</name>
  <files>src/test/java/com/paralife/bot/HeuristicBrainLocomotorTest.java</files>
  <read_first>
    - src/main/java/com/paralife/bot/HeuristicBrain.java lines 95-115 (decide dispatch), 251-259 (decideLocomotor), 299-318 (toDirectionInfo), and the decideFullAuthority cascade 129-245 for expected priority semantics
    - src/main/java/com/paralife/bot/BotState.java (construct a COMPOSITE_MEMBER / LOCOMOTOR state directly)
    - src/main/java/com/paralife/codec/Frame.java (TickFrame + ActionFrame + CellEntry + Coord.Relative constructors)
    - 20.1-PATTERNS.md §HeuristicBrainLocomotorTest (construction + scenario + assertion shape)
    - 20.1-RESEARCH.md Q6 (behavioural-test requirement: nutrient at SENSOR-only distance → ballot top rank toward it)
  </read_first>
  <behavior>
    - Given a LOCOMOTOR TickFrame whose cells() contains a NUTRIENT at Coord.Relative(0, 2) (north-2, outside radius-1 adjacency, inside a SENSOR 5x5), brain.decide returns a V-verb ActionFrame.
    - The ballot's top rank (ranks[0]) is the numpad direction toward (0, 2) — north — i.e. '8'.
    - Given a LOCOMOTOR frame with a PREDATOR cell visible only via a SENSOR, the top rank steers away from it (negative control on threat-avoidance), OR (if that is over-constrained for a 3-rank ballot) the predator's direction is NOT ranked first.
    - The returned ballot is a valid 3-char numpad string (each char in '1'..'9').
  </behavior>
  <action>
    Create HeuristicBrainLocomotorTest.java. Instantiate `new HeuristicBrain(70)` directly. Build a Frame.TickFrame for a LOCOMOTOR (sensorRadius=1) whose cells() = List.of(a nutrient CellEntry at Coord.Relative(0,2)). Build a BotState with embodiment COMPOSITE_MEMBER and compositeRole 0 (LOCOMOTOR), matching the species used for the nutrient/prey kinds. Call brain.decide(frame, state, new Random(42)). Assert: verb=='V'; arg present and length 3; arg.charAt(0)=='8' (north toward the SENSOR-visible nutrient). Add a second scenario placing a predator-kind cell at a SENSOR distance and assert the top rank does not point at it. These tests are EXPECTED RED until Task 2.
  </action>
  <verify>
    <automated>./gradlew compileTestJava 2>&1 | tail -5; ./gradlew test --tests "com.paralife.bot.HeuristicBrainLocomotorTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - HeuristicBrainLocomotorTest.java compiles (compileTestJava exits 0) and contains class HeuristicBrainLocomotorTest.
    - Contains an assertion that a SENSOR-distance nutrient (Coord.Relative dist>=2) produces a V ballot whose top rank points toward it (`charAt(0)` assertion).
    - Tests MAY be red at this task — Task 2 makes them green.
  </acceptance_criteria>
  <done>Behavioural scaffold exists, compiles, pins the D-06 steering contract. Red acceptable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HeuristicBrain LOCOMOTOR voting from frame.cells()</name>
  <files>src/main/java/com/paralife/bot/HeuristicBrain.java</files>
  <read_first>
    - src/main/java/com/paralife/bot/HeuristicBrain.java lines 116-245 (decideFullAuthority scoring cascade to reuse), 251-259 (decideLocomotor current random shuffle), 261-298 (topThreeDirections helper), 299-318 (toDirectionInfo)
    - 20.1-RESEARCH.md Q6 (minimal viable change: reuse decideFullAuthority's frame.cells() scanner, emit V) and 20.1-PATTERNS.md §HeuristicBrain (decideFullAuthority analog, V-verb output format)
  </read_first>
  <behavior>
    - decideLocomotor scans frame.cells() (own adjacency + SENSOR union) using the same prey/predator/nutrient classification + distance scoring as decideFullAuthority.
    - The top-ranked direction (ranks[0]) follows the priority cascade: flee predator > consume/chase prey > move toward nutrient > wander; SENSOR-distance (dist>=2) nutrients participate in the toward-nutrient step.
    - ranks[1] and ranks[2] are filled from the remaining directions (next-best by the same scoring, or deterministic fallback) so the ballot is always 3 valid numpad chars.
    - Output stays `a|V|<3-char ballot>` — verb unchanged, only the ranking source changes.
  </behavior>
  <action>
    Refactor decideLocomotor to derive its ranking from frame.cells() rather than topThreeDirections(rng). Reuse the decideFullAuthority classification + scoring (extract the shared scan into a private helper returning a priority-ordered list of Directions, or call the cascade directly and capture the ordered direction preference). Map the top preferred direction to ranks[0], then fill ranks[1]/ranks[2] from the remaining scored directions; if fewer than 3 meaningful directions exist, fall back to the existing topThreeDirections(rng) ordering for the unfilled slots to keep determinism and a valid 3-char ballot. Emit new Frame.ActionFrame('V', Optional.of(String.valueOf(ranks[0]) + ranks[1] + ranks[2])). Keep the rng parameter (used for tie-break / fallback) so the signature is unchanged. Do not alter decideFullAuthority or toDirectionInfo.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.paralife.bot.HeuristicBrainLocomotorTest" --tests "com.paralife.bot.HeuristicBrain*" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - HeuristicBrain.decideLocomotor references frame.cells() (grep: `grep -n "frame.cells()" src/main/java/com/paralife/bot/HeuristicBrain.java` shows a hit inside the decideLocomotor region, ~lines 251+).
    - The V-verb output format is preserved: `grep -c "new Frame.ActionFrame('V'" src/main/java/com/paralife/bot/HeuristicBrain.java` >= 1.
    - HeuristicBrainLocomotorTest passes (exit 0).
    - Existing HeuristicBrain tests still pass (no regression to decideFullAuthority).
  </acceptance_criteria>
  <done>LOCOMOTOR votes from frame.cells() and steers toward SENSOR-only-visible food; behavioural test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server frame → bot brain | brain consumes server-derived cells; client-side only, no privilege boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-07 | (n/a) | HeuristicBrain.decideLocomotor | accept | This is client-side bot decision logic operating on server-sent frames. It introduces no new wire input, no privilege boundary, and no state mutation server-side. No realistic STRIDE threat for an internal heuristic consumer; no mitigation required. |
</threat_model>

<verification>
- `./gradlew test --tests "com.paralife.bot.*"` green (brain suite).
- decideLocomotor consumes frame.cells(); V-verb output preserved.
</verification>

<success_criteria>
- LOCOMOTOR ballot derived from frame.cells() including SENSOR union cells.
- Nutrient visible only via a SENSOR → top ballot rank points toward it.
- `a|V|<3-char>` output format unchanged; brain suite green.
</success_criteria>

<output>
Create `.planning/phases/20.1-restore-composite-vision-sensor-stitched-perception/20.1-03-SUMMARY.md` when done.
</output>
