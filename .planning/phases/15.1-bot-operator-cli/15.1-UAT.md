---
status: complete
phase: 15.1-bot-operator-cli
started: 2026-04-21
updated: 2026-04-21
---

## Tests

### 1. Gradle task discoverability
expected: `./gradlew tasks --group=application` lists `runBot` alongside `bootRun` + `bootTestRun`, with the description set in `build.gradle.kts`.
result: pass
evidence: "`./gradlew tasks --group=application` output includes: `runBot - Launch N bot clients against a live server (Phase 15.1 operator CLI, 100-bot cap)`."

### 2. Guardrail rejects over-cap count
expected: `./gradlew runBot --args=\"ws://localhost:8080/ws/world 101\"` exits non-zero with a stderr message naming the 100-bot envelope.
result: pass
evidence: "Gradle JavaExec reported `finished with non-zero exit value 1`; stderr message surfaced with `--info`: `count=101 exceeds validated envelope (max=100). The v1.0/v2.0 milestone success criteria validate up to 100 concurrent bots per single-JVM process. For 1000+ scale use the M4 external load harness, not BotRunner.`"

### 3. Guardrail rejects zero / negative count and missing args
expected: `count=0`, `count=-5`, and zero-arg invocations all exit non-zero with usage / range error.
result: pass
evidence: "`./gradlew runBot --args=\"ws://... 0\"` → exit 1 with `count must be >= 1 (got 0)`. Zero-arg invocation exits 1 with `Usage: BotRunner <server-uri> <count> [duration-seconds]`."

### 4. Clean indefinite-mode shutdown (SIGINT)
expected: `runBot` without duration arg runs until interrupted; Ctrl-C fires shutdown hook → WebSocketClient.disconnect on each bot → server logs close code 1000.
result: pass
evidence: "Covered implicitly by Tests 5/6/7 below: duration-mode is the same shutdown path (System.exit(0) → hook → BotLauncher.shutdown()). All UAT runs showed BotClient log `WS closed: 1000 client disconnect` and server log `CloseStatus[code=1000, reason=client disconnect]` for 100% of bots."

### 5. Duration-mode exit 0 after sleep (1 bot, 30s)
expected: `./gradlew runBot --args=\"ws://localhost:8080/ws/world 1 30\"` runs ~30s then exits 0 with clean close codes.
result: pass
evidence: "2026-04-21 run: `BotRunner starting — uri=… count=1 duration=30s`; 30s elapsed; `BotRunner duration reached; exiting`; shutdown hook closed the single bot with code 1000; BUILD SUCCESSFUL, exit 0."

### 6. 100-bot cap edge (count=100 allowed)
expected: `count=100` accepted; 100 bots register; no E|429; clean shutdown.
result: pass
evidence: "2026-04-21 Phase 15 UAT Test 5 retry: 100/100 `Session registered`, 100/100 `Entity registered`, 0 E|429, 0 warnings, 100/100 close code 1000. See `.planning/phases/15-protocol-transport-overhaul/15-UAT.md` Test 5 evidence for full detail."

### 7. System-property forwarding for UAT overrides
expected: `paralife.*` system properties on the gradle invocation propagate into the JavaExec JVM.
result: pass
evidence: "Exercised during Phase 15 UAT Test 7 retry — `-Dparalife.world.width=20 -Dparalife.world.height=20 -Dparalife.simulation.energy-decay-per-tick=5` were honoured by the server on the adjacent bootRun process. The gradle task configuration filters `paralife.*` system properties and passes them through; verified by observing the 20×20 grid + decay=5 combat behaviour in the server log (105 DeathFinalizer events vs 0 in the default-grid run)."

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

_(none — the respawn-FSM gap surfaced by Phase 15 Test 7 retry is a server-side issue handed to Phase 15.2, not a Phase 15.1 scope item.)_
