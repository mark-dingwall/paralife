---
phase: 12-composite-entities
plan: 06
type: execute
wave: 4
depends_on: ["12-02", "12-03", "12-04", "12-05"]
files_modified:
  - src/test/java/com/paralife/engine/CompositeIntegrationTest.java
autonomous: true
requirements:
  - R04
  - R05
  - R06
  - R07

must_haves:
  truths:
    - "Full pipeline runs without errors: formation → energy → actions → perception → broadcast"
    - "Composites form from BondedPairs, move as units, take damage, and dissolve — all in one simulation"
    - "Existing v1.0 behavior (Particle combat, bonding, nutrients) still works alongside composites"
    - "All 202+ existing tests still pass (no regressions)"
  artifacts:
    - path: "src/test/java/com/paralife/engine/CompositeIntegrationTest.java"
      provides: "End-to-end integration test for composite lifecycle"
  key_links:
    - from: "CompositeIntegrationTest"
      to: "SimulationEngine, ActionResolver, PerceptionBroadcaster, TickBroadcaster"
      via: "@SpringBootTest wiring all components"
      pattern: "@SpringBootTest"
---

<objective>
Create an integration test that exercises the complete composite lifecycle: formation from BondedPairs, energy distribution, movement, combat, and dissolution — all running through the real tick pipeline.

Purpose: Validates that all 5 preceding plans wire together correctly. Individual unit tests prove components work in isolation; this test proves they work together. Also serves as a regression gate ensuring v1.0 behaviors are unaffected.

Output: 1 new integration test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-composite-entities/12-CONTEXT.md
@.planning/phases/12-composite-entities/12-RESEARCH.md
@.planning/phases/12-composite-entities/12-01-SUMMARY.md
@.planning/phases/12-composite-entities/12-02-SUMMARY.md
@.planning/phases/12-composite-entities/12-03-SUMMARY.md
@.planning/phases/12-composite-entities/12-04-SUMMARY.md
@.planning/phases/12-composite-entities/12-05-SUMMARY.md

<interfaces>
From CompositeRegistry.java:
```java
public int size();
public Optional<CompositeState> getComposite(String compositeId);
public Collection<CompositeState> getAll();
```

From SimulationEngine.java:
```java
public void processTick(long tickNumber);
```

From WorldGrid.java:
```java
public Cell getCell(int x, int y);
public void setEntity(int x, int y, Entity entity);
public GridSnapshot snapshot();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full composite lifecycle integration test</name>
  <files>
    src/test/java/com/paralife/engine/CompositeIntegrationTest.java
  </files>
  <read_first>
    src/test/java/com/paralife/engine/SimulationIntegrationTest.java
    src/test/java/com/paralife/engine/SimulationEngineTest.java
    src/main/java/com/paralife/engine/SimulationEngine.java
    src/main/java/com/paralife/engine/CompositeRegistry.java
    src/main/java/com/paralife/engine/ActionResolver.java
    src/main/java/com/paralife/engine/CompositeEnergyDistributor.java
    src/main/java/com/paralife/engine/PerceptionBroadcaster.java
    src/main/java/com/paralife/world/Entity.java
    src/main/resources/application.yml
  </read_first>
  <action>
    Create `src/test/java/com/paralife/engine/CompositeIntegrationTest.java`:

    Use `@SpringBootTest` to wire all real components. Use the test application.yml (or override with `@TestPropertySource`) to set fast tick interval, enable simulation, and configure composite parameters.

    **Test 1: `compositeLifecycleFormToDissolve`**
    End-to-end lifecycle:
    1. Place 4 Particles on the grid (2 predator-prey pairs in adjacent cells)
    2. Run enough ticks for bonding to occur (both pairs form BondedPairs)
    3. Verify 2 BondedPairs exist on grid
    4. Position BondedPairs adjacent to each other (may need manual placement if bonding moved them)
    5. Run more ticks for composite formation
    6. Assert: CompositeRegistry has 1 composite with 2 members
    7. Assert: Grid contains CompositeMember entities at expected positions
    8. Assert: Shared pool energy > 0
    9. Run more ticks to observe energy decay
    10. Assert: Member individual energy decreased (passive drain active)
    11. Drain all energy from composite members (set energy to 0 directly or via many ticks)
    12. Run processDeaths
    13. Assert: Composite dissolved or degraded (CompositeRegistry size decreased)

    **Test 2: `compositeDoesNotBreakExistingBehavior`**
    Regression test:
    1. Place Particles of all 3 types on grid (no composites)
    2. Place nutrients
    3. Run 50 ticks
    4. Assert: RPS combat still occurs (some entities died)
    5. Assert: Energy decay still works
    6. Assert: Nutrient spawning still works
    7. Assert: No composites formed (no adjacent BondedPairs arranged for formation)

    **Test 3: `compositeMovesAsUnit`**
    Movement integration:
    1. Manually create a 2-member composite with one LOCOMOTOR
    2. Register the composite in CompositeRegistry
    3. Place CompositeMember entities on grid
    4. Register bot sessions for both members in BotRegistry
    5. Queue a LOCOMOTOR action (CompositeAction with direction "N")
    6. Run the tick pipeline (simulationEngine.processTick + actionResolver.onTick)
    7. Assert: Both members shifted 1 cell north
    8. Assert: BotRegistry positions updated

    **Test 4: `fullSuiteRegressionCheck`**
    Simply run `./gradlew test` and assert all 200+ tests pass. This is verified by the automated command but having it as an explicit test assertion makes the intent clear.

    Use `SimulationConfig.defaults()` with simulation enabled, `BondingConfig.defaults()`, and `CompositeConfig.defaults()` for realistic parameter values.

    For deterministic tests, seed the random via test configuration or use `@RepeatedTest` with seed control where possible.
  </action>
  <verify>
    <automated>cd /home/mark/kramtime/paralife && ./gradlew test --tests "com.paralife.engine.CompositeIntegrationTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CompositeIntegrationTest.java exists with `@SpringBootTest` annotation
    - CompositeIntegrationTest.java contains at least 3 test methods: lifecycle, regression, movement
    - Lifecycle test verifies: formation → energy decay → dissolution
    - Regression test verifies: existing Particle/BondedPair behavior unaffected by composite code
    - Movement test verifies: rigid body translation works through real tick pipeline
    - `./gradlew test --tests "com.paralife.engine.CompositeIntegrationTest"` exits 0
    - `./gradlew test` exits 0 (ALL tests, including existing 202+ tests)
  </acceptance_criteria>
  <done>Integration test validates complete composite lifecycle through real tick pipeline. Regression test confirms v1.0 behavior unaffected. Full test suite passes with zero regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A (test code) | Integration test runs in-process with no network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | — | CompositeIntegrationTest | accept | Test code only. No production surface. |
</threat_model>

<verification>
1. `./gradlew test --tests "com.paralife.engine.CompositeIntegrationTest"` — integration test passes
2. `./gradlew test` — full suite green (ALL 200+ tests)
3. No flaky tests — integration test uses deterministic setup where possible
</verification>

<success_criteria>
- Complete composite lifecycle tested end-to-end: formation → energy → movement → dissolution
- Existing v1.0 behavior verified as unregressed
- Full test suite (200+ tests) passes
- No flaky tests introduced
</success_criteria>

<output>
After completion, create `.planning/phases/12-composite-entities/12-06-SUMMARY.md`
</output>
