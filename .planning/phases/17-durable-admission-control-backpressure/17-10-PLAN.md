---
phase: 17
plan: 10
type: execute
wave: 4
depends_on: [17-07, 17-08, 17-09]
files_modified:
  - src/main/resources/application.yml
  - src/main/java/com/paralife/websocket/PopulationCapConfig.java
  - src/test/java/com/paralife/websocket/WorldWebSocketHandlerPopulationCapTest.java
  - src/test/java/com/paralife/engine/LoadTest.java
  - src/main/java/com/paralife/engine/ActionResolver.java
  - CLAUDE.md
autonomous: true
requirements: [SCALE-01, SCALE-02]
tags: [migration, cleanup, application-yml, claude-md, test-migration, ingress-counter]

must_haves:
  truths:
    - "PopulationCapConfig.java is deleted from the codebase"
    - "application.yml contains paralife.admission section with cap, maintenance, tick-overload, backpressure sub-sections"
    - "application.yml does NOT contain paralife.websocket.max-active-entities"
    - "WorldWebSocketHandlerPopulationCapTest.java is deleted (rewrite is AdmissionGateTest from Plan 03)"
    - "LoadTest @TestPropertySource references paralife.admission.cap"
    - "CLAUDE.md gains an Outbound concurrency sub-section per D-10"
    - "ActionResolver ingress-overwrite counter wired to AdmissionMetrics.incIngressOverwrite per D-09"
  artifacts:
    - path: "src/main/resources/application.yml"
      provides: "Updated config with paralife.admission namespace"
      contains: "paralife.admission"
    - path: "CLAUDE.md"
      provides: "Outbound concurrency section documenting D-10 VT-per-session rationale"
      contains: "Outbound concurrency"
  key_links:
    - from: "application.yml paralife.admission.cap"
      to: "AdmissionConfig binding"
      via: "@ConfigurationProperties"
      pattern: "paralife\\.admission"
    - from: "ActionResolver pendingActions overwrite site"
      to: "AdmissionMetrics.incIngressOverwrite"
      via: "D-09 counter"
      pattern: "incIngressOverwrite"
---

<objective>
Final migration sweep that supersedes the 999.1 stopgap (D-04). Delete `PopulationCapConfig.java`, migrate `application.yml` from `paralife.websocket.max-active-entities` to the `paralife.admission` namespace, delete `WorldWebSocketHandlerPopulationCapTest` (superseded by `AdmissionGateTest` in Plan 03), migrate `LoadTest` `@TestPropertySource`, wire the D-09 ingress-overwrite counter into `ActionResolver`, and add the D-10 "Outbound concurrency" sub-section to `CLAUDE.md`.

Purpose: Without this, `application.yml` still binds to a deleted config and the build breaks. Plan 10 closes the migration so the project compiles and runs cleanly with only the new admission namespace.

Output: `application.yml` updated, `PopulationCapConfig.java` deleted, `WorldWebSocketHandlerPopulationCapTest.java` deleted, `LoadTest.java` migrated, `ActionResolver.java` instrumented with the ingress-overwrite counter, `CLAUDE.md` updated with the Outbound concurrency sub-section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-durable-admission-control-backpressure/17-CONTEXT.md
@.planning/phases/17-durable-admission-control-backpressure/17-RESEARCH.md
@.planning/phases/17-durable-admission-control-backpressure/17-PATTERNS.md
@.planning/phases/17-durable-admission-control-backpressure/17-ADMISSION.md
@CLAUDE.md

<interfaces>
Existing application.yml shape (PATTERNS.md cites lines 48-52):
  paralife.websocket.max-active-entities: 256       (DELETE)
  paralife.websocket.max-respawns-per-session: 5    (KEEP)

WorldWebSocketHandlerPopulationCapTest line 29 references the to-be-deleted property and is replaced by AdmissionGateTest (Plan 03). Plan 10 deletes the old file.

LoadTest line 35 currently:
  paralife.websocket.max-active-entities=1000000
must become:
  paralife.admission.cap=1000000

ActionResolver pendingActions sites (RESEARCH cites lines 125, 262, 299): every put returns the previous value; non-null previous = D-09 overwrite event.

CLAUDE.md needs a new sub-section. Heading depth: `###` since it sits inside `## Architecture`. Content per D-10 rationale (see action body).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate application.yml + delete PopulationCapConfig + delete old test + migrate LoadTest</name>
  <files>
    src/main/resources/application.yml,
    src/main/java/com/paralife/websocket/PopulationCapConfig.java,
    src/test/java/com/paralife/websocket/WorldWebSocketHandlerPopulationCapTest.java,
    src/test/java/com/paralife/engine/LoadTest.java
  </files>
  <read_first>
    src/main/resources/application.yml,
    src/main/java/com/paralife/websocket/PopulationCapConfig.java,
    src/test/java/com/paralife/websocket/WorldWebSocketHandlerPopulationCapTest.java,
    src/test/java/com/paralife/engine/LoadTest.java,
    src/main/java/com/paralife/admission/AdmissionConfig.java
  </read_first>
  <action>
Step 1: Edit `src/main/resources/application.yml`. Find the `paralife.websocket` section. DELETE the `max-active-entities` line. KEEP `max-respawns-per-session`. Add a new `paralife.admission` section with the Phase 17 keys (defaults from Plan 01):

    paralife:
      websocket:
        max-respawns-per-session: 5
      admission:
        cap: 256
        maintenance: false
        tick-overload:
          high-water-pct: 80
          low-water-pct: 60
          window-ticks: 10
        backpressure:
          outbound-queue-size: 16
          grace-window-ticks: 10

Preserve all other sections verbatim. Use a single edit operation; do not reorder unrelated keys. Do NOT leave a YAML comment placeholder for the deleted key — just remove it cleanly.

Step 2: Delete `src/main/java/com/paralife/websocket/PopulationCapConfig.java`.

Before deleting, run `grep -r "PopulationCapConfig" src/` and confirm 0 matches (Plan 07 already removed all references). If any references remain, fix them first — they will likely be in test classes that Plan 07 explicitly left for Plan 10 (search `// PHASE-17 PLAN-10 MIGRATION PENDING` comments).

Use the `rm` Bash command:

    rm src/main/java/com/paralife/websocket/PopulationCapConfig.java

Step 3: Delete `src/test/java/com/paralife/websocket/WorldWebSocketHandlerPopulationCapTest.java`. Plan 03 created `src/test/java/com/paralife/admission/AdmissionGateTest.java` as the rewrite (D-04). The old file is superseded:

    rm src/test/java/com/paralife/websocket/WorldWebSocketHandlerPopulationCapTest.java

Step 4: Migrate `src/test/java/com/paralife/engine/LoadTest.java` `@TestPropertySource`. Locate the property literal `paralife.websocket.max-active-entities=1000000` and replace with `paralife.admission.cap=1000000`. Leave all other test properties unchanged. If `LoadTest` references `PopulationCapConfig` directly anywhere (constructor injection or field access), remove those references. RESEARCH §Standard Stack confirms LoadTest only uses the property literal.

Step 5: Search for any remaining references and fix. Run:

    grep -r "max-active-entities" src/ build.gradle.kts CLAUDE.md
    grep -r "PopulationCapConfig" src/

Both should return 0 matches after Steps 1-4. If matches exist, hunt them down and migrate. Common stragglers: `BotRunner` config defaults, README files (do not modify CLAUDE.md other than per Task 2).

Step 6: Verify compile + targeted test run:

    ./gradlew compileJava compileTestJava
    ./gradlew test --tests "com.paralife.admission.*" --tests "com.paralife.codec.*"

`LoadTest` itself is a slow-tagged test; do not run it here. Plan 11 verifies LoadTest separately.
  </action>
  <verify>
    <automated>./gradlew compileJava compileTestJava 2>&1 | tee /tmp/p17-10a-build.log; grep -q "BUILD SUCCESSFUL" /tmp/p17-10a-build.log &amp;&amp; ! test -f src/main/java/com/paralife/websocket/PopulationCapConfig.java &amp;&amp; ! test -f src/test/java/com/paralife/websocket/WorldWebSocketHandlerPopulationCapTest.java &amp;&amp; grep -q "paralife.admission" src/main/resources/application.yml &amp;&amp; ! grep -q "max-active-entities" src/main/resources/application.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `src/main/resources/application.yml` contains literal `paralife:` and nested `admission:` block
    - `application.yml` contains keys: `cap: 256`, `maintenance: false`, `tick-overload:`, `high-water-pct: 80`, `low-water-pct: 60`, `window-ticks: 10`, `backpressure:`, `outbound-queue-size: 16`, `grace-window-ticks: 10`
    - `application.yml` does NOT contain literal `max-active-entities`
    - File `src/main/java/com/paralife/websocket/PopulationCapConfig.java` does NOT exist
    - File `src/test/java/com/paralife/websocket/WorldWebSocketHandlerPopulationCapTest.java` does NOT exist
    - File `src/test/java/com/paralife/engine/LoadTest.java` contains literal `paralife.admission.cap=1000000`
    - `LoadTest.java` does NOT contain literal `paralife.websocket.max-active-entities`
    - `grep -r "PopulationCapConfig" src/` returns no matches
    - `grep -r "max-active-entities" src/` returns no matches
    - `./gradlew compileJava compileTestJava` exits 0
  </acceptance_criteria>
  <done>
    Migration complete. `PopulationCapConfig` and the old test file are deleted. `application.yml` and `LoadTest` use the `paralife.admission` namespace. Project compiles. The 999.1 stopgap is superseded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ActionResolver D-09 ingress-overwrite counter + add CLAUDE.md Outbound concurrency section</name>
  <files>
    src/main/java/com/paralife/engine/ActionResolver.java,
    CLAUDE.md
  </files>
  <read_first>
    src/main/java/com/paralife/engine/ActionResolver.java,
    src/main/java/com/paralife/admission/AdmissionMetrics.java,
    CLAUDE.md
  </read_first>
  <action>
Step 1: Wire D-09 ingress-overwrite counter in `ActionResolver`.

Read `ActionResolver.java`. Locate every `pendingActions.put(sessionId, frame)` site (RESEARCH cites lines 125, 262, 299). The `put` returns the previous value; if non-null, the new frame overwrote a pending one — that is the D-09 event to count.

Inject `AdmissionMetrics` into `ActionResolver` constructor. Replace each `pendingActions.put(...)` site with:

    ActionFrame previous = pendingActions.put(sessionId, frame);
    if (previous != null) {
        admissionMetrics.incIngressOverwrite();
    }

If there are multiple put sites, every one of them needs this guard. **No auto-disconnect** per D-09 — purely observational.

If `ActionResolver` already has a constructor taking dependencies, append `AdmissionMetrics admissionMetrics` to the parameter list and store it as a final field. Spring will inject it.

Step 2: Add Outbound concurrency sub-section to `CLAUDE.md`.

Read the existing `CLAUDE.md` to find the right insertion point. The Architecture section has a 4-layer table and a tick pipeline description. Insert the new sub-section AFTER the existing Architecture content but BEFORE other sections (e.g. before `## Project Skills` or at the end of `## Architecture`).

Use this exact content (heading depth `###` since it sits inside `## Architecture`):

    ### Outbound concurrency (Phase 17, D-10)

    Each connected WebSocket session is paired with one virtual thread that loops
    `queue.take(); session.sendMessage(...)` over a per-session bounded
    `ArrayBlockingQueue<Frame>` (capacity from `paralife.admission.backpressure.outbound-queue-size`).

    **Why VT-per-session and not Jetty native async write:**
    - Matches Paralife's stated philosophy — simple blocking code, virtual threads do concurrency.
    - Per-session isolation is structural — one slow socket cannot block the tick thread or any other session.
    - `queue.size()` is the explicit backpressure signal — observable as
      `paralife.backpressure.stalled.sessions` gauge and per-session via `OutboundSender.queueDepth(sessionId)`.
    - Java 21 VTs scheduled on shared carriers; per-VT cost is a few KB heap. 1000+ VTs is acceptable.
    - Slow-client detection becomes implicit with Jetty native async (write-Future latency / Jetty internals);
      the API surface differs across Jetty 12 minor versions.

    When the queue overflows, the session transitions to STALLED:
    - `OutboundSender.offer` invokes the overflow callback registered by `WorldWebSocketHandler`.
    - `WorldWebSocketHandler.markStalled` removes `ATTR_ENTITY_ID`, sets `ATTR_STALL_TICK`,
      issues a resume token via `ResumeTokenRegistry.issue`, and detaches the sender VT.
    - The next inbound frame from the stalled session receives `E|408|reconnect-required` and the WS is closed.
    - The entity is held on the grid for `paralife.admission.backpressure.grace-window-ticks` ticks.
    - If the client reconnects with `r|<species>|<resumeToken>` within the grace window, `AdmissionGate` consults
      `ResumeTokenRegistry.tryRebind` and re-binds the new session to the preserved entityId.

    Single-writer invariant: only the per-session VT calls `session.sendMessage`. Error frames built on the
    inbound Jetty thread are routed through `OutboundSender.offer` rather than direct `sendMessage`. The
    fallback path in `WorldWebSocketHandler.sendFrame` (post-detach) uses a `synchronized(session)` guard.

Step 3: Verify:

    ./gradlew compileJava compileTestJava
    ./gradlew test --tests "com.paralife.engine.*" --tests "com.paralife.admission.*"
  </action>
  <verify>
    <automated>./gradlew compileJava compileTestJava 2>&1 | tee /tmp/p17-10b-build.log; grep -q "BUILD SUCCESSFUL" /tmp/p17-10b-build.log &amp;&amp; grep -q "Outbound concurrency" CLAUDE.md &amp;&amp; grep -q "incIngressOverwrite" src/main/java/com/paralife/engine/ActionResolver.java</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/paralife/engine/ActionResolver.java` contains literal `incIngressOverwrite`
    - `ActionResolver.java` injects `AdmissionMetrics` (grep `AdmissionMetrics` matches in field declaration and ctor parameter)
    - Every `pendingActions.put` site in `ActionResolver.java` is paired with the `if (previous != null)` overwrite guard (grep `if \(previous != null\)` matches the same number of times as `pendingActions\.put` calls, OR a verifiable equivalent pattern)
    - `CLAUDE.md` contains literal `Outbound concurrency`
    - `CLAUDE.md` contains literal `STALLED`
    - `CLAUDE.md` contains literal `paralife.admission.backpressure.outbound-queue-size`
    - `CLAUDE.md` contains literal `paralife.admission.backpressure.grace-window-ticks`
    - `CLAUDE.md` contains literal `ResumeTokenRegistry`
    - `CLAUDE.md` contains literal `VT-per-session` OR `virtual thread that loops`
    - `./gradlew compileJava compileTestJava` exits 0
    - `./gradlew test --tests "com.paralife.engine.*"` exits 0
  </acceptance_criteria>
  <done>
    `ActionResolver` increments the D-09 aggregate ingress-overwrite counter on every collapsed action. `CLAUDE.md` documents the D-10 VT-per-session rationale + STALLED transition flow. Project compiles; engine + admission tests stay green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `application.yml` | Operator-controlled; `@ConstructorBinding` validates values |
| `pendingActions.put(...)` previous value | Server-internal; trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | D — Memory exhaustion via flood inbound | ActionResolver pendingActions | mitigate | `pendingActions` is a `ConcurrentHashMap<sessionId, ActionFrame>` — last-write-wins collapse already protects sim correctness (one action per tick per session, regardless of ingress rate). The new D-09 counter is purely observational; the existing collapse is the real defence. Counter increment is O(1). |
| T-17-misc | T — Stale `application.yml` config caching | AdmissionConfig binding | accept | Spring binds at startup; restart required to flip values. Same lifecycle invariant as every other `@ConfigurationProperties` record in this project. |
</threat_model>

<verification>
1. `./gradlew compileJava compileTestJava` exits 0.
2. `./gradlew test --tests "com.paralife.admission.*" --tests "com.paralife.codec.*" --tests "com.paralife.engine.*"` exits 0.
3. `! test -f src/main/java/com/paralife/websocket/PopulationCapConfig.java` (file is gone).
4. `! test -f src/test/java/com/paralife/websocket/WorldWebSocketHandlerPopulationCapTest.java` (file is gone).
5. `grep -c "Outbound concurrency" CLAUDE.md` returns ≥ 1.
6. `grep -c "incIngressOverwrite" src/main/java/com/paralife/engine/ActionResolver.java` returns ≥ 1.
</verification>

<success_criteria>
- `999.1` stopgap superseded — `PopulationCapConfig` deleted; `application.yml` uses the new namespace.
- `LoadTest` runs against `paralife.admission.cap`.
- D-09 ingress overwrite counter wired.
- D-10 rationale present in both source Javadoc (Plan 06) and `CLAUDE.md` (this plan).
- All targeted tests still green.
</success_criteria>

<output>
After completion, create `.planning/phases/17-durable-admission-control-backpressure/17-10-SUMMARY.md`. Confirm:
- `application.yml` + `LoadTest` migration done.
- `PopulationCapConfig` and old test file deleted.
- `ActionResolver` instrumented at every `pendingActions.put` site.
- `CLAUDE.md` updated.
</output>
