---
title: Phase 17 execution — pause point before Wave 4
created: 2026-04-27
last_updated: 2026-04-27
status: active
---

# Phase 17 — Resume Handoff

## Where we are

- Branch: `master`
- Last commit: `aa76192 docs(phase-17): update tracking after wave 3`
- Worktrees: clean (none active).
- Wave 1 complete: 17-01 (AdmissionConfig + RejectionToken + 17-ADMISSION.md spec), 17-02 (codec `r:`-sentinel)
- Wave 2 complete: 17-03 (AdmissionGate + AdmissionMetrics canonical), 17-04 (TickHealthMonitor + TickEngine), 17-05 (ResumeTokenRegistry full lifecycle), 17-06 (OutboundSender VT-per-session)
- Wave 3 complete: 17-07 (WorldWebSocketHandler refactor + STALLED FSM), 17-08 (TickBroadcaster non-blocking), 17-09 (BotClient resume-reconnect)

## Known test failures (expected, will resolve in Wave 4)

Three failing tests after Wave 3 merge — flagged by 17-07 SUMMARY as expected pending Plan 10 migration:

- `WorldWebSocketHandlerPopulationCapTest.populationCapEnforcedWithError429`
- `WorldWebSocketHandlerTest.respawnCapEnforced`
- `WorldWebSocketHandlerTest.malformedFrameProducesError400`

Root cause: mock sessions not attached to OutboundSender queue before `sendFrame` is called. Plan 10 deletes `WorldWebSocketHandlerPopulationCapTest` (superseded by `AdmissionGateTest`) and migrates the other two; failures will clear naturally.

## What's next

### Wave 4 — final wave (parallelization-safe, no file overlap)

**17-10 — Migration sweep** (the mass-deletion / yml migration):
- Delete `PopulationCapConfig.java`
- Migrate `application.yml` from `paralife.websocket.max-active-entities` to `paralife.admission` namespace (matches the `@DefaultValue` defaults already on AdmissionConfig)
- Delete `WorldWebSocketHandlerPopulationCapTest`
- Migrate `LoadTest` `@TestPropertySource`
- Wire D-09 ingress-overwrite counter into `ActionResolver` (`AdmissionMetrics.incIngressOverwrite()`)
- Add D-10 "Outbound concurrency" sub-section to `CLAUDE.md`
- Files: `application.yml`, `PopulationCapConfig.java`, `WorldWebSocketHandlerPopulationCapTest.java`, `LoadTest.java`, `ActionResolver.java`, `CLAUDE.md`

**17-11 — Integration tests** (the SCALE-01/SCALE-02 verification gate):
- `BlockingWebSocketClient.java` (test helper)
- `StallRecoveryIntegrationTest.java`
- `TickHealthGateIntegrationTest.java`
- `AdmissionLogMarkersIntegrationTest.java`
- `EdgeCasesIntegrationTest.java`

### After Wave 4

Then the standard phase-completion flow:
- Code review gate (`/gsd-code-review` skill, advisory)
- Regression gate (run prior phases' tests)
- Schema drift gate (no schema in this project, will skip)
- gsd-verifier — VERIFICATION.md against phase goal + REQUIREMENTS.md SCALE-01/SCALE-02
- Roadmap completion update via `gsd-sdk query phase.complete 17`
- PROJECT.md evolve

## Critical contracts honored across Wave 1–3

- Resume tokens MUST use `String.format("r:%016x", n)` — the literal `r:` prefix is the codec disambiguator.
- `AdmissionMetrics` is a strict superset across the 4 deviations; HEAD canonical version (17-03) was kept on every conflict.
- `WorldWebSocketHandler` was canonical-owned by 17-07; 17-08's compile-gate deviation was overridden in favor of HEAD.
- `OutboundSender.recordFrameSize` is called inside the drain loop only; `TickBroadcaster` does NOT call it (codex MEDIUM finding addressed in 17-06).
- `TickHealthMonitor` uses strict watermark comparators (>/<) and a window-fill warmup guard — Plan 11 integration tests must use mean values strictly outside watermark boundaries.

## Files to read on resume (for next agent or post-compact orchestrator)

1. `.planning/phases/17-durable-admission-control-backpressure/17-10-PLAN.md`
2. `.planning/phases/17-durable-admission-control-backpressure/17-11-PLAN.md`
3. `.planning/phases/17-durable-admission-control-backpressure/17-CONTEXT.md`
4. `.planning/phases/17-durable-admission-control-backpressure/17-RESEARCH.md`
5. The Wave 1–3 SUMMARY.md files (17-01..17-09) — these record API shapes and deviations
6. `.planning/STATE.md` — `executing` status, `9/11` plans complete

## Resume commands

```bash
# Confirm clean state
git log --oneline -5
git worktree list   # should show only main
./gradlew test 2>&1 | tail -5  # should still show ~3 failures from Plan 10 migration backlog

# Capture wave-4 base for executor agents
git rev-parse HEAD
```

Then dispatch 17-10 and 17-11 as parallel worktree executor agents (sequential dispatch, run_in_background:true). After both return, merge worktrees, run post-merge test gate (should now be fully green — Plan 10 deletes the failing test class and migrates the others), update tracking, then proceed to verification.

## Critical Anti-Patterns

| Severity | Anti-pattern | How to avoid |
|---|---|---|
| advisory | Removing the `@DefaultValue` annotations on `AdmissionConfig` record components | Plan 10's yml migration adds the explicit `paralife.admission` block. Keep `@DefaultValue` — it lets `@TestPropertySource` overrides remain partial and protects against forgotten yml entries breaking 100+ tests. |
| advisory | Calling `AdmissionMetrics.recordFrameSize` from anywhere other than `OutboundSender.drainLoop` | The drain loop is the sole measurement point. Adding callers re-introduces the codex MEDIUM operational regression. |
| advisory | Resurrecting `synchronized(session) { session.sendMessage(...) }` in `TickBroadcaster` | The whole point of Wave 3 was removing that pattern. Always go through `OutboundSender.offer(...)`. |
