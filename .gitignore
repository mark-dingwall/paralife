# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
*.swp
bin/

# OS
.DS_Store
Thumbs.db

# Spring Boot
*.log

# GSD runtime (STATE.md is engine-managed)
.gsd/STATE.md

# GSD2 SQLite store — defunct after migration to GSD1 .planning/ on 2026-04-11
# (file is created empty by tooling on session start; archived M001 data lives in .gsd/milestones/)
.gsd/gsd.db

# GSD fixtures — Phase 16 long-run test per-run dump (D-06b; rollover N=5)
/.planning/phases/16-emergent-behavior-tests/fixtures/*.json

# Claude
.claude/

# Worktrees
.worktrees/

# Codex
.codex/
.codex

# Multi-review transient prompts, sessions, and raw model output. Curated review records live in docs/history/reviews/.
/.multi-review/

# Harness runtime reports (transient capture artifacts — committed sidecars go under profiles/)
harness-*-report.json

# Benchmark sweep raw output — timestamped, non-reproducible (Phase 21 D4/B3). Curated
# per-tier fixtures the doc cites live under docs/benchmarks/, committed separately.
/reports/
