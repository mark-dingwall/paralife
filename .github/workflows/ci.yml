name: CI

# Gate workflow: runs the full JUnit suite on every push to main and every PR.
# The suite runs under the build's default forkEvery=0 (one shared JVM) — fast,
# and leak-sensitive by design (see build.gradle.kts). This job reflects realistic
# runner conditions (no core pinning); the flake-hunting stress sweep lives in
# stress.yml (manual workflow_dispatch).
on:
  push:
    branches: [ main ]
  pull_request:

# Cancel superseded runs on the same ref to save runner minutes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history so Spotless ratchetFrom(origin/main) can resolve the base

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Set up Node 22
        # The observer renderer's pure modules are tested by Node's built-in
        # runner (no npm dependencies) — see the jsTest task in build.gradle.kts.
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Lint (Spotless) + Java and JavaScript test suites
        # One Gradle invocation: pays JVM startup once and runs spotlessCheck
        # first, so a formatting failure fails fast before the long test suite.
        # jsTest is named explicitly: this job never invokes `check`, so binding
        # jsTest to check alone would leave it out of the merge gate entirely.
        run: ./gradlew spotlessCheck test jsTest --no-daemon --console=plain

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            build/reports/tests/test/
            build/test-results/test/
          if-no-files-found: ignore
